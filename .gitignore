node_modules
.next*/
.cache/
.cache-cleanup-trash/
.artifacts/
.env
.env.local
.env.prod
.env.backup
uploads/
*.tsbuildinfo
.DS_Store
coverage/
drizzle/meta/_journal.json.bak
.data/
# .claude/ 默认忽略（launch.json 等本地设置因人而异），但下面三样必须入库：
# skills/ 是团队/云端会话共享的工作教义；settings.json + hooks/ 是 advisory 提醒层。
# 真正门禁仍由测试、发布脚本和权限边界承担。
.claude/*
!.claude/skills/
!.claude/settings.json
!.claude/hooks/
.writer.lock
backups/
