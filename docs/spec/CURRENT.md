# CURRENT · 项目现状唯一索引

> **看这一页就知道现在的真相。** 更新纪律：任何改判/决议先改本页，再改宿主文档。
> 最后更新：2026-07-27（数据血缘、导入放行、流程智能、计划版本与 194 项能力证据收口）

## 一句话故事（五句版）

为 8 品牌、约 751 成品 SKU 的消费品公司，单人用 Claude Code 自研 Web 系统（Next.js+PostgreSQL）替代 Excel 手工账。第一期只做**委外生产闭环**：备货申请→委外工单→采购/加工通知→发料→收货检验→入库→逐物料结算，库存实时记账、错账红字冲销、每日与聚水潭对账。真实历史数据（BOM/库存/销量/效期）经 staging+人工裁决管道导入。此后 1.1 接快照仓与批次效期，P1 接财务/供应商门户/NPD 外围，P2 上看板。**为什么做**：月度对账 5 人×2 周，委外结算公式在 Excel 里算不对（红队实证），价格无管控。

## 阅读顺序与文档角色

| 文档 | 角色 |
|---|---|
| **CURRENT.md（本页）** | 现状索引+决策登记簿——一切入口 |
| **15-194项能力审计台账.md** | 去重后的逐项执行证据；只有 ✅ 计入完成 |
| 01-系统完整规格 + 02-MVP构建规格 | **现行规格**（叠加 04 §9 改判补丁后为准；过期处已打 ⚠ 锚点） |
| 04-数据整合架构 v2 | 数据/维度/DW 阶段的现行规格 + 改判登记（§9） |
| 03 / 05 / 06 | 审计档案（历史，只读不改） |
| 供应链系统 PRD.pdf + 会议纪要 + 14+4 份业务文件 | 原始输入（在 /Users/yj/Downloads/，不在 repo——见"源文件清单"） |

## 当前状态（2026-07-27 · 数据血缘、放行边界与能力证据收口，UAT-ready）

> 下面 07-24 的记录保持原样（历史快照）；本段是 07-25/26 两轮工作的增量，**以代码实证为准**。

- **194 项能力证据（07-27）**：把多轮审计、会议需求与现有实现去重为恰好 194 项的机检台账；
  当前 177 项完成、5 项部分、5 项待做、7 项受外部依赖阻塞，部分完成不计完成。每项必须绑定
  当前代码、测试、运行证据或明确出口，避免把 schema、占位 UI 或重复表述误算成实现。
- **情景管理增量（07-27）**：单 SKU 沙盘可命名保存，并选择最多两个历史情景响应式并排比较；
  保存的是当时基准、假设与推演输出双快照，不按今日库存回算。证据表由数据库强制 append-only，
  PMC 写、登录用户读，幂等并发与审计同事务；提交为业务参数/草稿仍保留为需确认的后续扩展。
- **中文检索增量（07-27）**：顶栏搜索在编码、中文和单号的数据库直查之外，增加连续全拼、
  首字母与多音字匹配；只对罗马字母输入启用 60 秒轻量目录，保持 25 项总上限，并取消过期请求
  防止旧响应覆盖新输入。真实中文夹具覆盖全拼、缩写、分隔符、供应商与乱序拒绝。
- **计划版本增量（07-27）**：PMC 可把例会当下的全量补货建议保存为不可变、幂等且事务审计的
  版本；采购可只读复核。差异读模型只比较保存事实，区分新增告急、解除、恶化、改善、混合变化
  与稳定，并展示前后建议量/短缺日证据；浏览器已实测筛选、排序、URL 状态和三档响应式布局。
- **操作闭环增量（07-27）**：导入拒收明细已形成按模板角色隔离的受控 CSV 下载；收货/盘点
  已支持扫码枪回车流；工作台下一步引擎以六类审计事件为触发，返回前复核当前状态与下游事实，
  只给责任角色建议和直达入口，不自动执行业务写入。
- **流程智能增量（07-27）**：唯一审计写路径现在持久化 `event-v1` canonical/domain/state-change
  身份，旧事件保持不可变并只读兼容分类。角色隔离的流程效率页展示阶段中位/P90、样本可靠性、
  路径变体与单据时间线，同时诚实披露事件映射、版本和案例覆盖率；未知 system fallback 不计映射。
- **导入/成本写边界（07-27）**：同模板、同范围版本放行新增逐行指纹 diff 与控制总量/分桶
  30% 异动闸；人工覆盖必须携带输入绑定 token、原因并追加审计。SKU 成本上传不再直接写正式表，
  改为 staging→预演→finance/admin 原子放行；同一 SKU 矛盾成本整项阻塞，PMC 无法旁路覆盖。
- **报废处置闭环（07-27）**：报废评审登记可预填精确绑定的报废出库单；审批、库存扣减、
  处置自动完成与审计同一事务，普通出库不会误关。若报废出库被红字冲销，处置自动重开。

- **身份会话硬化（07-26）**：迁移 0021 为 users 增 `session_version`。改密/重置、停用、
  角色或审批权变化在同一事务内递增版本并写审计；服务端会话刷新与全部 fresh 写守卫比对版本，
  旧 JWT 立即失效。部署后旧版无版本 token 一次性退出重登，属于预期安全行为。
- **逐 SKU 临期口径贯通（07-26）**：风险数量此前已按 `nearExpiryDays` 聚合，但建议动作、
  风险页文案/导出与 SKU 速览仍写死 90 天；现统一透传逐 SKU 阈值（未维护才回落 90），
  低于/高于 90 天的阈值均有回归测试。
- **发布证据增强（07-26）**：CI/本地/生产容器统一 Node 24；CI 生产构建改验完整 Docker 镜像，
  新增真实 PostgreSQL 16 的迁移契约任务；`scm doctor` 对超过 1 GiB 的构建缓存显式报警。
- **跨平台发布契约实证（07-26）**：首轮 GitHub Actions 准确抓出 macOS lockfile 缺 Linux/WASM
  嵌套 optional 包；现改用 Linux 生成的 canonical lock，并以架构测试防回归。真实 Linux 完整门禁为
  133 文件通过/5 文件跳过、852 例通过/21 例跳过，生产构建通过；Vitest 并发收口为 4，
  消除 PGlite 多实例内存争抢与 `ERR_IPC_CHANNEL_CLOSED`。PostgreSQL 16 已实跑 22 个迁移，
  最终生产镜像已连该库启动并通过 `/api/health`。
- **恢复与监控闭环（07-26）**：生产备份从两个松散压缩包升级为 DB+附件原子备份集，
  以双文件 SHA-256 manifest 作为唯一完成标志；附件经 compose 只读工具服务导出，不再猜实际卷名，
  也不依赖 app 镜像可启动。迁移前强制异地备份；每日调度 + 每小时配对/校验和/压缩结构/新鲜度监控。
  `db:restore-drill:prod` 已在隔离 PostgreSQL 16 实跑通过：22 迁移、5 关键表、1 用户、1 附件，2 秒完成，
  一次性容器/卷已精确清理。部署/备份现显式读取 `.env.prod`，修复原文档写该文件但 Compose 未加载的问题。
- **依赖安全收口（07-26）**：Drizzle ORM 升至 0.45.2，Next.js/ESLint config 升至 15.5.22，
  并固定已修复的 PostCSS、Sharp、UUID 与 Drizzle 工具链 esbuild。唯一保留的审计项来自
  ExcelJS→archiver 的 glob 工具链；应用无 glob 输入/API，自动强修会降级 ExcelJS，
  故按不可达风险临时接受并登记在 `ops/SECURITY-NOTES.md`，不以不兼容 override 伪造“0 漏洞”。
- **上线阻断项 4 条已清零（07-26）**：
  ① 三处落盘硬编码 `process.cwd()/uploads`，而 compose 设的是 `FILE_STORAGE_DIR=/data/uploads`
  且该变量零读取方——容器写可写层，**每次 deploy 抹掉全部附件与导入原件，备份备的是空卷**。
  已收口到 `core/storage.ts` + 扫描式护栏。
  ② 缺 `.dockerignore`，`.env`（含 AUTH_SECRET）经 standalone 烘进运行镜像——已补。
  **⚠ 业务侧待办：轮换 AUTH_SECRET**（旧密钥可能已在既有镜像层里）。
  ③ `drizzle/0018` 只有 .sql 未登记 `_journal.json`，prod 的 drizzle-kit migrate 永不执行——已补登记 + snapshot。
  ④ `/api/health` 缺探活，DB 连不上仍返回 ok:true——已加 `SELECT 1`。
- **口径收口（07-26）**：`fuseCover` 不再把 legacy_fg 与 on_order 相加（实测重复计入约 129 万件）；
  可销天数 5 处内联收口到 `coverDays`，并写明**全系统存在两个同名口径**（在库可销 vs 在库+PO在途）；
  仓库 360 改为逐 (仓,SKU) 最新期；库存曲线到货源收口到 `core/supply`（行级交期优先）。
- **首屏可信度（07-26）**：「待我审批」原是与登录人无关的常量（七个角色一律 2），改走 `getInbox`；
  「未读通知」原不带收件人条件（恒 8、读完仍卡 4 且无法归零），改走新的 `core/notify-audience`；
  critical「断货风险 111」拆为 **55 可行动 + 56 已抑制**（同源自补货页，抑制项明示不吞）。
- **告警治理（07-26）**：侦测三阈值收编 `sys_params`（原硬编码、无 D 号）；
  命中率分母修正（1026→441，真实命中率 78%，原显示 33%）；
  通知改内容指纹去重 + medium 按周节流 + 已读 30 天保留期（原每天必推、无保留期、列表截断 100 条）。
- **入口补齐（07-26）**：PO 供应商确认链接（confirm-token 原零调用方，导致行级交期至今全空）；
  分域参数覆盖 API（sku/brand/segment 三层原无任何写入口，全体 SKU 共用一个安全库存兜底天数），
  含 `clearScopedParam`——设得上必须撤得掉。
- **能力接线（07-26）**：E2-09 齐套 ATP 接进委外自动链，从「现在够不够」升级为「几号能齐套」。
  `rules/spc.ts` 本轮**不接**：需 ≥8 期样本而 sales_monthly 仅 6 期，接了对全部序列只返回「样本不足」。
- **E7-01 改判（07-25）**：`rollup_sku_month`/`rollup_warehouse_sku` 两张表已随迁移 0018 删除——
  夜间构建 8 轮、**零读取方**，且其立项理由（60s 缓存撑不住）经核实不成立：
  那个缓存只有读没有写、从未生效，提交信息里的「15×」是同进程 call#1 与 call#2 之差。
  `rollup_supplier_lead` 保留（仍在喂安全库存交期波动项）。
- **护栏（07-25/26）**：新增扫描式架构测试 3 个——客户端/服务端边界、`<Suspense>` 边界、落盘根目录；
  三者均已验证「重新引入缺陷会变红」。另加 PreToolUse/PostToolUse 钩子（git 提交前列未跟踪文件、
  改 schema 提示重启纪律）。
- **独立红队复审（07-26 凌晨）**：对本轮 11 个提交做对抗式审计（54 次攻击 → 17 条确认 → 13 条被推翻），
  **全部 17 条已闭环**，分三批修复（9b0192d / 17baee7 / 09b034f）。其中值得记入的教训：
  - **提交信息曾与事实不符**：b66443c 声称「PO 抽屉已加生成确认链接按钮」，实际按钮从未落到 JSX，
    回调是编译进包却零引用的死代码；根因是「端到端实测」走的是 curl 打 API 而非 UI。已补按钮。
  - **提权口子**：新开的 /api/admin/params/scoped 接受 scope:{kind:"global"}，
    与 admin-only 的 updateParam 落在同一唯一键 —— pmc 实测可改超收容差(0%→20%)、比价硬门、
    让步价率、D33 两个自动链开关。已限定 global 需 admin，并补 4 例回归。
  - **两个部署阻断**：.dockerignore 排除 tests/ 与 scripts/perf-smoke 的 import 相撞使 next build 必挂；
    Dockerfile 未 mkdir/chown /data/uploads 而运行身份是 USER scm，prod 三处写盘全 EACCES。均已修。
  - **齐套 ATP 需求放大 wo.qty 倍**（grossReq 本就是全单量），且我把「永远齐不了」误当成功能生效的证据。
  - **护栏给过虚假保证**：Suspense 护栏只查字符串存在，可被「边界在、组件在界外」绕过；
    加严为检查包含关系后，立刻抓出 admin/audit 页把边界包在了无权限分支上（真 bug）。
- **抑制闸口径改判（07-26，⚠ 需业务确认 D 号）**：抑制基准原用 cover_alert_days(30)，
  而触发基准是 actionWindow(=生产周期 40–68 天)，二者不同源导致闸门对 46% 的建议**结构性打不开**。
  已改为同源，实测 suggestCount 80→75、suppressed 91→96（5 条移入人工核实）。
  抑制≠拦单（heldQty 保留原量、逐行给原因、可人工放行），但**这条改变建议下单量，请登记 D 号确认**。
- **架构护栏现 4 个**（均已验证「重新引入缺陷会变红」）：客户端/服务端边界、Suspense 边界（含包含关系）、
  落盘根目录（三种等价写法）、迁移登记（孤儿 .sql）。
- **代码与现行规格已在同一私有 GitHub 仓**：`yujin7/Expressions-SCM`；本地 `main`
  已关联 `origin/main`，且两套同树异源历史已用无覆盖 merge 接通。R8 已推送；首轮 Actions
  暴露的跨平台 lockfile 问题已在本轮修复，远端四项门禁全绿。Actions 官方组件已升级到
  Node 24 运行时的 `checkout@v7` / `setup-node@v7`；私有仓分支保护受当前 GitHub 套餐限制。
- **测试数字**：当前 Linux 发布证据为 **852 通过 / 21 有意跳过**，lint、三套 typecheck、
  Next 生产构建、PostgreSQL 16 迁移契约与最终容器健康检查均通过。
- **交接给下一会话的待办**见本文件末尾「下一会话接手清单」。

## 当前状态（2026-07-24 · RT5 完成 + 需求合规审计闭环，UAT-ready）

- **需求合规审计（4 源文件三路复核）**：功能清单 80 项=✅27/🟡15/📅32（全部有裁决归属）/❌1（F-011 附件线，D24 即补）/划除5——**P1/P2 零静默丢失**；数据字典 6 项"已裁决未建"schema 全部本轮补落（skuType 5 值/供应商暂停+服务/BOM 双损耗+三列/WO-JG W3 增列批/条码校验列/银行账户+渠道价）；NPD 分期合规、试销报表打标本轮补落；无裁决缺口以 D24–D30 登记。详见 spec/08。

- **代码**：W1–W5 全量建成 + **DW2 放行引擎** + **D20 全仓视图** + **D21 PO 打印页** + **W6 经营驾驶舱**（/report/dashboard：销量/库存/效期/可销天数/委外/数据健康 + 智能洞察，R13/R14/R15 v1 报表口径落地）+ **W6.5 运营环**（文件上传→staging、放行工作台 UI、快照周期刷新 releaseSnapshots——月度数据循环业务自助，端到端实证 7,065 行幂等重放）；**测试数以 `npx vitest run` 为准**；三轮红队（14 路审计）全部整改闭环；ops 层（Dockerfile/compose/备份/恢复演练）就绪。（该阶段快照；当前 HEAD 见 `git log -1`。）
- **真实数据已全量落库（dev 库，2026-07-24 代决放行，含补遗轮）**：SPU 348（336 放行+品牌壳档，124 review 簇代决接受）· SKU 5,376（BOM 成品 749+物料 4,002+段位代决物料 335+壳档 286——壳档已按编码前缀归品牌，744 别名全认领）· BOM 817 放行/**725 生效**（501 歧义块代决=表内最后块；pmc01 两轮批审，SoD 合规）· 加工费参考 693 · 批次效期 3,131 · 月销 7,065 · **期初 2 单 341 行已过账**（warehouse01 制单→finance01 审批）· **快照 1,526 键**（bizDate 2026-07-21）。
- **段位代决规则（补遗轮实证归纳）**：0102=进口内料、0601=膜布/膜材→原料；0301/0302/0501=标贴、0602/0603=袋/衬、0001=配件→包材；ZCLY-*=自供原料（适配器原只认 ZCYL，真实编码 ZCLY）。全部 SKU 打 attrs.needsReview 标。
- **复核清单 1,780+ 条** → `supply-chain/reports/复核清单-2026-07-24.md`（SPU 代决簇/BOM 歧义裁决/段位分类/壳档品牌归位（V→微初为消去法存疑）/生效抽样）——供业务复核，改判走红字/重导。
- **已知余量（人工 own，引擎不代决）**：bom_block 62 行（依赖 3 个不可分类物料+脏值）；费用候选 56（46 行供应商列为空+10 行无产品编码）；无编码物料 646 建档队列；sku_leadtime 1,227 行留 staging（MVP 无落库目标表，1.1 采购提前期启用）。
- **RT5 Top-20 改进已全量落地（commit ee8dc04，6 并行工作包）**：在案复核清单（1,786 项落库可裁决）· BOM 版本对比 · SPU 批量归组 · 加工费参考价页 · BOM 生效动效检查 · 自助改密码+首登强改 · 审计日志页 · 快照数据龄告警 · 异步导出管道 · 抽盘全流程（PD→CA 调整过账）· R11 补货建议页（一键 BH 草稿人工闸）· 临期出库提示 · 工作台角色焦点卡 · SKU 360 全景 · 引擎收口×2 · 登录限速 · 备份定时 · 结构化日志 · 驾驶舱缓存。348 测试（327 通过）。
- **DoD-5 性能实测达标（2026-07-24）**：perf-smoke 100 万行流水（独立 PGlite）——余额 1ms/单SKU流水 11ms/对账聚合 3ms/无筛选流水 85ms（加 ix_ledger_time_desc 降序索引前 662ms，7.8×）；全部 <1s 目标。正式 staging 真 PG 压测仍待环境（部署物就绪）。
- **红队第四轮（2026-07-24 晚）已闭环**：4 路审计（安全/引擎正确性/数据完整性/产品连贯性）→ 1 P0+6 P1+9 P2 全部整改：重导幂等（superseded 机制）、月销同键聚合（修回少记 2,354 件）、**仓库 hex 碰撞修复（5 组 12 仓分立、快照重建 923 键、批次纠仓 1,601 行）**、批次自然幂等键（迁移 0007）、别名裁决优先、税率字段入脱敏、API 401 JSON、角色过滤菜单+页面门（"点了才 403"清零）、驾驶舱口径同源+动态窗口+行动链接、PO 打印水印/时区、借调打印全量明细。268/268 测试。
- **下一步（业务侧）**：项目会追认（D8 会签+D9 人名+D1/D2/D7 校准）→ IT 环境供给（staging/prod+密钥+备份远端）→ 复核清单业务审阅（**新增：5 组分立仓布局确认**）→ UAT（spec/07，41 场景）。

## 当前答案速查（每行=唯一权威）

| 问题 | 当前答案 | 权威 |
|---|---|---|
| SKU 编码规则 | **D10 已代决 2026-07-24**：采用现行商家编码为主码（01-R8 的"品类2位+5位"已过期勿用） | 04 §9 / D10 |
| SPU 编码/派生 | P+5位内部合成（doc_counter 取号）；派生规则=族前缀∩归一品名双证+人工闸 | 04 §4.1 |
| 计划损耗率 | 行级双损耗 incomingLossPct/productionLossPct（旧 lossRatePct 弃用）；毛=净×(1+来料)×(1+生产) | 04 §2 |
| 结算损耗容差 | 品类容差单一来源（包材5%），与计划损耗无关 | 01 R2/R5 |
| 快照仓（保税/E/云） | 记账=1.1；**D20 已代决 2026-07-24：只读全仓视图纳入 MVP**（已建成） | 04 §5 / D20 |
| 期初审批 | 财务（opening 审批域）；盘点同理 | 01 §6 + 05 C2 |
| 批次数量载体（1.0） | batch_stocks 参考层；账本批次列=哨兵 | 04 §2 |
| 在途存量单迁移 | 新旧划断（推荐）；长周期单走迁移补录例外通道 | 04 §6 |
| 报表数量（MVP） | 4 张（含结算汇总表）；01 §2 的"3 张"已过期 | 02 §2.1 |
| 进度标签（11 态） | 只由单据事实推导，禁止手填 | 04 §2.5 |
| NPD（产品开发） | MVP 只取 orderType+lifecycle 两钩子；核心 1.x；打样/备案 P1 | 05 §5 |
| 智能层 R13–R16 | 定义=04 §8.A；R13 v1 仅出总口径报表列。**自动草稿归 D33**（自动链开关 auto_wo_on_bh / auto_jg_on_ready，代码里缺省 0=关；自动只产草稿，审批永远是人工闸）——原文误记为「待 D20」，D20 是全仓视图那条 | 04 §8.A/§7.2 / D33 |

## 决策登记簿（唯一权威；D1–D9 详情见 00 §D，D10 起见 04/05/06）

| # | 事项 | 拍板人 | 状态 | 备注 |
|---|---|---|---|---|
| D1 | KPI 基线与目标值 | 谭莹莹* | **已代决 2026-07-24（提案值）**：①月度对账 5人×2周→**≤2人×3天**；②委外结算公式性差错→**0**（系统计算+审计留痕）；③价格异动拦截率 **100%**（R1 硬门）；④效期风险量（≤6月）环比**下降**；⑤别名/导入积压 **≤50 条**滚动 | 基线自驾驶舱自动采集；UAT 期校准目标值，业务可改判；**UAT 校准清单另含 PRD §1.3 三项待定值：交付周期（WO→入库时差可算）、库存周转率（驾驶舱有原语）、缺货率（暂无数据源，随 JST 日汇总启用）** |
| D2 | 成本核算方法终版 | 司徒春桃 | **已代决 2026-07-24（方向）**：采纳会议方向=**季度盘点均摊法 + 备品/包材/损耗对冲池**（offset_pools 表已建）；P1 成本引擎按此实现，结算扣款价维持 D23 代理口径至切换 | 终版参数（分摊维度/池清算周期）司徒春桃 P1 前定 |
| D3 | 产品定义规则（SPU 模型） | 张杏琳* | **已代决 2026-07-24**：SPU/SKU 双层 + §4.1 派生规则 | 业主授权代决；歧义簇自动归组并打 needsReview 标待业务复核 |
| D4 | 价格异动容差/阈值 | 采购负责人 | **已代决 2026-07-24**：±3% 起步，UAT 按真实 PC 量校准 | |
| D5 | 各品类正常损耗率 | PMC+采购 | **已代决 2026-07-24**：包材5%（会议定死）/原料2%（缺省，UAT 校准） | |
| D6 | 让步接收结算单价 | 财务 | **已代决 2026-07-24**：默认全价（ratio=100），单单可调需财务审批 | |
| D7 | 供应商评分权重/频率 | 采购+品控 | **已代决 2026-07-24**：v1=质量40/交付40/价格20，**季度**评分；数据源=QC 记录+PO 履约+R1 异动记录（系统已留痕） | **上会校准三点（会议纪要复核发现）**：①纪要为品控**月度**打分，代决改季度——请确认；②纪要的红/黄/蓝三级生产异常分级仅红线（黑名单硬门）有落点，黄线提案对应 supplierStatus=paused、蓝线记 P1 参数——请确认；③01 R6 旧提案「准交40/质量40/配合20」已被本裁决取代（01 已打过期锚） |
| D8 | 自研架构追认 | 项目会 | **已代决 2026-07-24（事实追认待会签）**：建设侧已完成至 UAT 前（261+ 测试/三轮红队/ops 就绪），继续自研路线；项目会补正式签认 | 03 称"最大单点前提"——若会签否决，退出成本=数据可全量导出（CSV 基建已备） |
| D9 | 并行中止阈值/签字人 | 项目会 | **已代决 2026-07-24**：阈值按 spec/07 §3；签字人=财务+PMC+运营各一 | 具体人名上会补 |
| D10 | SKU 编码=商家编码 | 项目会 | **已代决 2026-07-24**：采用现行商家编码为主码 | 业主授权代决；变更集按 04 §9 执行 |
| D11 | 功能清单 P0 冲突 6 项 | 产品 owner | **已代决 2026-07-24**：全部按 04 §8 裁决提案执行（冲突项以 04/CURRENT 现行规格为准，功能清单表述让位） | 产品 owner 可逐项改判，走 §9 登记 |
| D12 | 期初裁决规则 | 业务追认 | **已代决 2026-07-24**：按 04 §5 执行；1仓2仓渠道子桶合并为「自有仓(1仓2仓)」实时仓 | 拆分留 1.1 |
| D13 | 保质期例外品类 | 业务 | **已代决 2026-07-24**：SKU 级字段默认 1095；效期文件实测 730/1090/1092 天的品按实测落库 | |
| D14 | 包材颜色语义规则化 | 采购 | **已代决 2026-07-24**：按 15/7/3 天规则化预警替代手工涂色 | |
| D15 | 时间线 | 管理层 | **已代决 2026-07-24**：方案 C（A 的 W10 基线 + D20 全仓视图） | 建设侧已全部完成，实际剩余=环境/评审/UAT——日期风险已消化 |
| D16 | 委外仓期初 / 新旧划断 | PMC+采购 | **已代决 2026-07-24**：新旧划断；存量单在旧流程收尾，仅登记外部单号 | 长周期单例外通道保留 |
| D17 | 变体后缀 -a/-X 的 SPU 归属 | 张杏琳* | **已代决 2026-07-24**：同 SPU（升级版/小规格=同款） | 服装类比（00-A7） |
| D18 | 读路径 8h JWT 窗口 | IT/项目会 | **已代决 2026-07-24**：接受（写路径已实时回查） | |
| D19 | NPD 分期（05 §5） | 产品+研发 | **已代决 2026-07-24**：按 05 §5 分期执行 | |
| D20 | 只读快照导入+全仓视图进 MVP | 管理层 | **已代决 2026-07-24**：采纳（只读+数据龄标注，不入账本） | 随本轮数据填充一并落地 |
| D21 | 采购日 1 价值包 | 采购+项目会 | **已代决 2026-07-24**：PO 打印页进 1.0；R1 维持硬门（容差 3% 起步，UAT 期间可调 D4） | |
| D22 | PRD 保真度补漏 6 项 | 业务 | **已代决 2026-07-24**：按下节裁决提案执行 | |
| D23 | 结算扣款价=价格表代理口径 | 司徒春桃/财务 | **已代决 2026-07-24**：接受代理口径至 P1 成本引擎 | DTO 全程标注；换引擎单点 |
| D24 | 附件/图片线（货品图/QC照片/证照） | 业务追认 | **已代决 2026-07-24**：file_metas 接线落地（上传/预览/删除，SKU/供应商/QC 三挂点） | 合规审计 F-011 唯一无裁决 P0 缺口，即补 |
| D25 | 单位主档表 | PMC | **已代决 2026-07-24**：1.1 建 units 主档；1.0 维持自由文本+uom_convs 换算 | 字典实体3；枚举文件无单位组佐证低风险 |
| D26 | 供应商联系/资质明细字段 | 采购 | **已代决 2026-07-24**：phone/email/address/bankAccount/paymentTerm 本轮建列（银行账户入脱敏）；证照影像走 D24 附件 | D11 承诺字段兑现 |
| D27 | 渠道价字段 | 运营 | **已代决 2026-07-24**：price_lists.channelId 建列（空=默认价）；管理界面 1.x | D11 承诺字段兑现 |
| D28 | 产品分级 S–D（category_l4） | 运营+PMC | **已代决 2026-07-24**：P1 随 D7 评分体系一并落（复用其参数框架） | 字典字段，1.0 无消费方 |
| D29 | PO 商务字段（合同号/付款条件/送货信息）与邮件发送 | 采购 | **已代决 2026-07-24**：P1（供应商门户/电子回签一并设计）；1.0 打印页+线下送达 | F-034 缺口登记 |
| D30 | 销售金额域（单价/金额/毛利）与售后品质回流（NPD d.2） | 运营+财务 | **已代决 2026-07-24**：P1 销售域（金额随 JST API，毛利依 D2）；售后回流 P1 登记 | 数量口径 R13 现行 |
| D31 | 编码划断细则（0724会议） | 供应链+财务 | **会议已决**：旧码全留；新码仅用于后续淘汰替换品 | 系统无码制约束天然兼容；新码规则定稿后可加建档校验 |
| D32 | 仓库树状层级（保税分中转/发货） | PMC+物流 | **会议已决方向**：树状非平级 | Wave B：parentId+树 UI+汇总口径（见 09） |
| D33 | 分批齐套自动触发链（BH→WO→PO→齐套→分批JG→分批入库） | PMC | **会议已决方向** | 旗舰改造：解除一WO一JG、齐套引擎、自动草稿+人工审批闸；入库时退/补料建议随行（09 §3） |
| D34 | 加工费分类（OEM填充/保税加工/保税仓操作费） | 财务+采购 | **会议已决方向** | feeType 维度，Wave B 小项 |
| D35 | 调拨不计销售达成（R17 口径） | 运营+财务 | **会议已决**：调拨仅数量；达成率剔除调拨出库 | 现状天然合规（销量=导入口径）；R17 入规格防未来聚合误算 |
| D36 | 共用包材关联展示 | PMC | **会议已决** | BOM 已含共用关系；Wave A：档案展示+备货时包材可用量 |
| D37 | 退货申请（新品/旧品两类+标准审批） | 运营 | **会议已决方向**；待标准表单（会议待办） | 归 P1 销售域扩容 |
| D38 | 供应商全生命周期（准入→池→绩效→淘汰） | 采购+品控 | **会议已决方向**（现档案阶段被评过糙） | Wave A 出设计稿；P1 实现；现状已有四态+黑名单硬门+资质预警 |
| D39 | 六看板体系+动销率滞销警戒阈值 | 运营+PMC | **会议已决方向** | Wave A：阈值参数化（sys_params 接 R14 0.35/0.11）；P2 完整看板 |
| D40 | 对账差异扩维（应付vs支出、保税出库vs聚水潭） | 财务 | **会议已决方向**；规则待补 | P1 对账 v2；依赖财务支出源+保税出库源 |

*人名为会议参会人，任命属提案待确认。**2026-07-24 业主（会话内明确授权"make all the best decisions for us"）委托代决——上表"已代决"项均已执行/生效；项目会可追认或改判，改判走红字/重导，不丢数据。D1–D23 已全部代决（2026-07-24 第二轮授权“do all of what's best for us”）；项目会仅需：追认 D8 会签、为 D9 签字人补人名、并对 D1 目标值/D2 参数/D7 权重做校准性确认。**

## PRD 保真度补漏（第三轮发现的静默丢失项 + 裁决提案 = D22 材料）

| 原 PRD 诉求 | 现状 | 裁决提案 |
|---|---|---|
| 锁定库存 | 全文档消失 | P1：销售订单/备货锁定随 B2B 销售域实现；MVP 在途/可用视图已覆盖主要用途 |
| 原料仓（PRD 原文划线删除） | 规格/种子静默恢复 | 保留并请业务追认：委外发料必须有原料源仓（技术必需） |
| 盘点"永续" | 改名"抽盘"无说明 | 澄清：抽盘=永续盘点的落地形式（循环抽点），非删减 |
| 批次扫码查询 | 入口消失 | 1.1 批次开启时以"批号搜索"实现；扫码枪硬件对接 P1 |
| 销售分析维度（销售员/区域/毛利） | 一笔带过 | P1 销售分析范围明细化时纳入；毛利依赖成本口径（D2） |
| 财务收款侧（应收/收款核销） | 只剩应付 | P1 财务集成范围补：应收凭证+收款核销与应付对称 |

## 给业务同事的词汇表

- **SPU**（你们说的"款式/产品族"）：同一产品的不同规格/版本归为一个 SPU 看报表。例：N006 系列 3 个规格 = 3 个 SKU，1 个 SPU。`-a`(升级版)/`-X`(小规格) 归属同一 SPU（**D17 已代决 2026-07-24**）。
- **快照仓**：系统只存"每日照片"（盘点/RPA 数据）、不记流水账的仓——保税仓、云仓、平台仓（E 仓=电商仓）。自有仓才逐笔记账。
- **红字冲销**：错账不改不删，开一张负数单据冲掉，两笔都留痕（≠作废：作废是单据从未生效）。
- **让步接收**：检验不合格但决定收下入库的货，单独打标、单独定结算价。
- **抽盘**：即原 PRD 的"永续盘点"——不停业，按循环计划抽点部分货位。
- **lifecycle 四态**：在售/试销/停售/淘汰（替代"启用/停用"开关）；试销=新品观察期，报表打标。

## 源文件清单（权威输入，均在 /Users/yj/Downloads/）
PRD+数据字典+功能清单（PDF 3 份）· 会议纪要 PDF · BOM×3 · 字段枚举 · 总库存明细(主文件，.download 副本已废) · 电商部库存明细 · 效期占比 · 在途进度表(损坏，需修复通道) · 销量汇总 · 需求计划达成 · 货盘-PMC · NPD 4 份（流程图 PDF+节点说明×3）。

### 2026-07-24 第五批文件（电商部/总库存/NPD三件套）
- 电商部库存明细26-7-21：MD5 与既有 import_jobs #4（inventory_long_721/done）一致——即期初/快照权威源本体，**不重复入账**。
- 总库存明细2026-7-21 → transit_refs kind=stock_summary（789 行，775 解析 SKU / 14 未建档）：只作核对参考。**核对结论（重要口径发现）**：775 可比 SKU 中 301 一致、474 差异且几乎全部 文件>系统（TOP：E054-000 差 176,216）——总库存=全公司口径（含海外/其他部门仓），系统快照源仅覆盖电商部文件口径。属**覆盖缺口非记账错误**；每行差异已烘焙进 extra.差异_系统减文件（核对时点 2026-07-24），全量见 supply-chain/reports/总库存核对-2026-07-21.json。扩源方案：取得海外仓/其他部门库存明细后按同模板导入快照即可闭合。
- NPD 各节点核心说明三件套 → kind=npd_node（69 节点，节点名称合并键：基表⊕模拟起止⊕角色文件）+ npd_role（19 角色）。D19 口径不变：NPD 核心=1.x，本登记为只读配置底稿。UI：/report/npd；总库存核对=需求达成与货盘页第三 Tab。

---

## 下一会话接手清单（2026-07-26 交接）

### 07-26 收尾补记
- **主数据审计已原子化**：master/{sku,supplier,warehouse,category} 的 create/update
  改为「写入 + writeAudit 同事务」，8 个路由不再补记（原 auditFromRoute 用新连接、
  在提交后才跑，进程挂在中间会留下有数据无审计的行）。护栏 3 例。
- **订正一条曾被列为 CRITICAL 的错误结论**：我曾报「7 个模块写库无审计、主数据改动不可追溯」。
  核实后四个 master 模块本就有路由层审计、四个 helper 由调用方审计，
  该判据（grep 文件里有无 writeAudit 字样）对被调用方审计的 helper 必然误报。
  真实问题只是原子性，已修。**教训：grep 缺失 ≠ 能力缺失，报缺陷前先查调用方。**

### 必须由业务/你本人处理（我做不了）
1. **轮换 AUTH_SECRET**。.dockerignore 已阻止密钥进入**将来**的镜像，
   但**此前从本树构建过的镜像里，/app/.env 仍含 JWT 签名密钥**。凭据类操作不由 AI 代劳。
2. **确认抑制闸口径改判**（见上）——它改变建议下单量，需要一个 D 号。
3. **决定是否升级 GitHub 套餐**：代码、历史、upstream、R8 推送及四项 Actions 门禁均已完成。
   GitHub API 对私有仓分支保护 / rulesets 返回 403，明确要求升级 GitHub Pro 或公开仓库。
   本项目含业务资料，应保持私有；只有你决定升级套餐后，才能设置 required checks、禁 force-push
   与 PR 合并保护。

### 解锁即可用（每项差一个决定或一份数据）
| 缺什么 | 解锁什么 |
|---|---|
| 售价数据源 | 毛利报表全列点亮（现留白，绝不臆造） |
| 飞书 app 凭据 | 通知/摘要出圈；OAuth 端点与字段契约已静态复核并测试，凭据到位后做真实回调握手 |
| 各平台临期合同条款 | 逐 SKU 临期阈值（写入路径已通，值需业务核准） |
| 哪些品牌/分层该用不同安全库存 | 分域参数（API 已通，含 DELETE 撤销） |
| 有人真的把供应商确认链接发出去 | po_lines.expected_date 才会有数据，行级交期相关能力才有输入 |

### 已知但判定为「不是缺陷」的（别重复排查）
- rules/spc.ts 零调用方：需 ≥8 期样本而 sales_monthly 仅 6 期，接了对全部序列只返回「样本不足」。
- rollup_supplier_lead 0 行：公式已接通，等 PO→收货历史自然积累。
- 多层 BOM 未实现：实测 725 父件/3441 子件互不相交，当前 0 例；data-health 已设哨兵，出现即示警。
- /api/workbench ~260ms：用延迟换首屏正确性，**刻意不加缓存**，取舍与实测数字已写进代码。
