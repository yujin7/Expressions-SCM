# 06 · 红队第三轮 — 产品级审计与整改（直观/合理/易用/智能/连贯）

> 日期：2026-07-24 ｜ 与前两轮不同，本轮审"产品"：真实五角色 UI 走查（121 次交互）+ 全文档概念完整性 + 商业价值拷问。
> 三路独立对抗审计 → 主审逐项核实 → 当轮整改 + 后续项入决策登记簿（CURRENT.md）。

## 1. 三路结论一句话

| 审计 | 结论 |
|---|---|
| UX 走查（五角色实操） | 骨架与业务语言对（"过账/红字/审批建模明显懂行"），但**信任面最薄**：工作台假数字、错误话术、点了才 403 的按钮——恰是 Excel 用户决定去留之处。今天没人自愿离开 Excel；仓管最接近。 |
| 概念完整性 | 单篇文档质量高、术语体系（BH/WO/…）全链一致（全项目最佳），但文档集已退化为"补丁链"：3 阻断（04 自引用断裂/R13-16 无定义/时间线无权威答案）+ 19 项决策零决议记录。 |
| 商业价值 | **MVP 1.0 只解 25–35% 日常痛**：静态 Excel（BOM/枚举）退休，日常件（总库存/效期/可销天数）全部存活。核心处方 D20：只读快照导入+全仓视图并入 MVP（导入便宜，贵的是 RPA+记账语义）；D21：给采购日 1 正收益（PO 打印模板前移+R1 影子期）。委外切片本身站得住：唯一可验收、唯一即刻省钱（结算公式）。 |

## 2. 当轮已整改（代码+文档，全部验证）

**信任面（UX Top-10 之 1/2/3/5/7/9/10）**
- 工作台真数：待审批=五类单据实时合计、待认领别名可点击直达；文案如实注明"我发起的"建设中。
- 错误话术人话化：SELF_APPROVAL→"不能审批自己提交的单据（职责分离）"等全套；500 兜底附行动指引；唯一约束→"编码已存在"。
- 环境级 schema 漂移（走查实锤：迁移 0004 未应用→审批全线 500）：/api/health 暴露漂移 + CLAUDE.md 纪律"迁移后必须重启"；已重启修复（health: 7/7 applied）。
- SKU 表单补 品牌/生命周期（DW1 断层实锤）+ 顺手抓获**潜伏缺陷**：createSku 从未写入 name（默认空串掩盖）。
- 流水页动作中文化；表格 scroll + 抽屉宽度响应式；数量去尾零工具；退出页中文化（残留：server-action 提交待改普通表单，暂以程序化登出兜底）。
- 菜单：委外/导入中心解禁上线（W3/W4 黑话随之消失两项）。

**连贯性（3 阻断全闭合）**
- 04 v2 补回被覆盖销毁的 枚举裁决表 与 D11 决策材料（§8）+ R13–R16 定义（§8.A）。
- `spec/CURRENT.md`：五句话故事 / 12 行"当前答案"速查 / **D1–D22 统一决策登记簿**（含"事实绑架"标记）/ 按拍板人待办 / 业务词汇表 / PRD 保真度 6 项补漏提案。
- 01/02 过期处植入 ⚠ 前向锚点；README/CLAUDE.md 指向 CURRENT；spu MAX+1 竞态修复（doc_counter+碰撞跳号）。

**同轮建成并验证（继续建设指令下的产出）**
- DW2 六适配器全绿（详见 05 后记与提交 1bd7625）；复核工作台前后端（801 别名异常→认领/忽略闭环）。
- W3 委外链服务+UI 全量（提交 4bd8a60 + 本轮 UI）。**浏览器实测全链**：BH-20260724-0001→WO-0001 审批→wo_line 快照数值精确（瓶身 10500 毛需 −5000 在手 → 建议 6000 按千箱取整；肽粉 51→75 按 25kg 包）→生成 PO×2+JG→**R1 实战擒获真偏差**（提交价含税、基准未税——归一化后 −4.13%/−11% 触发 PC，正是首轮审计预言的"含税/未税假比价"陷阱，系统判对）→PC 审批→重提放行→PO 审批→供应商确认→执行中。

## 3. 接受/移交的残余
- UX Top-10 之 4/6/8（按角色渲染按钮、导出/排序/合计行、BOM 行编辑器表格化+粘贴导入）→ W4 前端任务清单（排 UAT 前）。
- 流水来源列显示单据号（需跨类型 join，W4 随 FL/SH 来源扩展一并做）。
- PO 详情 confirmedBy 未随 DTO 返回（小缺口，W4 补）。
- BH.orderType 暂存 purpose 列（服务层已别名封装；独立列随下次迁移）。
- 商业处方 D20/D21/方案 C、NPD 分期 D19：**决策项非缺陷**——材料已备齐在 CURRENT.md，待项目会。

## 4. 面向 UAT 的角色就绪度（整改后重估）
仓管：核心闭环+真工作台+人话错误——**可争取**。财务：期初审批权真实+待办可见——**可争取**。采购：R1 全链可用但仍是净约束——**依赖 D21**。PMC：委外链上线后有真业务可跑——**W4 结算落地后可争取**。运营：BH 已可发起——**首个真实价值点已到位**。

## 5. 数字
三轮累计：**14 路独立对抗审计，225+ 项发现，零未处置**（修复/移交任务清单/决策登记三分法）。当前：提交 3 个（1103347/1bd7625/4bd8a60+本轮）、迁移 0000–0006、**206/206 测试**、tsc 清洁、健康检查在线。
