# 05 · 红队审计第二轮 — 报告与整改记录

> 日期：2026-07-23 ｜ 对象：**运行中的代码（W1/W2+DW1-core）+ 三路数据解析结论 + 《04》v1 方案**
> 方法：4 个独立对抗性审计（引擎 PoC 实测 / 数据事实核查 / 方案设计 / 系统一致性与安全）+ 主审逐项验证 + 当轮整改
> 与第一轮（《03》，审文档）不同，本轮审**实物**：可执行 PoC、原始文件复测、真实路由行为。

---

## 1. 总览

| 审计 | 发现 | 结果 |
|---|---|---|
| 引擎 PoC（57 个对抗测试） | 2 MAJOR + 3 MINOR **确认**；10 项高危猜想 **证伪**（并发双过账/回滚原子性/原型污染等均守住） | 全部修复 |
| 系统一致性/安全 | 3 BLOCKER + 3 MAJOR + 3 MINOR；10 项怀疑排除 | BLOCKER/MAJOR 全修，1 项部分整改（见 §4） |
| 数据事实核查 | 12 项断言复测：**2 项计数被驳**（NING 探查工具静默丢片）+ 4 项重要修正 | 《04》v2 已按复核值订正 |
| 《04》方案设计 | 4 BLOCKER + 9 MAJOR + 5 MINOR | 《04》重写为 v2（含 §9 改判登记簿） |

**整改后闸门：tsc 清洁 ｜ 160/160 测试全绿（85 原有 + 17 维度 + 57 红队 PoC + 1 新专项）｜ 生产构建通过 ｜ 浏览器实测双 BLOCKER 修复生效。**

---

## 2. 已修复（代码）

| # | 缺陷 | 修复 | 验证 |
|---|---|---|---|
| C1 (BLOCKER) | audit_logs 有表零写入——全部写路径无审计 | 新增 `core/audit.ts` writeAudit；接入库存单 create/submit/approve/reject/complete/红字 + 13 条主数据路由 + BOM 生效 | 代码路径全覆盖；单据时间线补齐中间态（原体检 #7 一并解决） |
| C2 (BLOCKER) | 期初审批被 stock_doc 统一配置劫持为仓管自审（财务被配置+路由双重排除） | approveStockDoc 按子类型映射审批域（opening→finance / count→finance / 其余→仓管）；审批路由授权下沉至 approval_configs | 浏览器实测：仓管审期初 → 403 需要角色 finance；vitest 专项（财务通过、仓管拒绝） |
| C3 (BLOCKER) | maskSensitive 定义后零调用，运营/仓管可读期初成本价 | 单据详情路由接入 maskSensitive 序列化边界；price 字段仅期初可写（zod superRefine） | 浏览器实测：仓管读单据详情 price=(masked) |
| C4 (MAJOR) | BOM 单人自建自激活（且 createdBy 从未写入） | createBom 记录创建人；activateBom 强制 is_approver + 非本人 + 写 approvals 留痕 | 红队 BOM 测试更新后通过 |
| C5 (MAJOR) | JWT 30 天陈旧窗口（停用/降权不生效） | session 8h/滑动1h；**全部写守卫回查 DB 新鲜身份**（停用立即拦截） | guardWrite/guardWarehouseWrite 走 getFreshSessionUser |
| C6 (MAJOR) | 仓管审批人单点（admin 例行盖章） | seed 增 warehouse02 审批人 | 拓扑修复 |
| M1 (MAJOR PoC) | 驳回→重提→再驳回被幂等键短路，单据永卡 pending 且二次驳回意见丢失 | approvals 增 cycle=单据版本，幂等键含轮次（迁移 0003） | PoC 由 FAIL 转 PASS：二次驳回生效、两条驳回留痕 |
| M2 (MAJOR PoC) | 基准价=0 → 裸除零 500 | 基准 0 → 数据异常强制走 PC；uomFactor≤0 → 类型化业务错误 | PoC 转 PASS |
| m4 (MINOR PoC) | dMul/dDiv 先截断后舍入，系统性向下偏差 | 有符号半进位除法，全链半进位 | PoC 转 PASS（双重舍入特性以特征化测试记录） |
| m5 (MINOR PoC) | dCeilToMultiple 负数取整错误 | floorDiv/ceil 数学正确实现 | PoC 转 PASS |
| — (MINOR PoC) | 引擎层无双冲防线（模块层 409 是唯一防线） | reverse() action 编码原单身份 + 先查已冲即拒 | 特征化测试翻转为防线断言 |
| C8/C9 (MINOR) | compose 全网卡绑定；非期初行可携带 price | 127.0.0.1 绑定；superRefine 限制 | — |

## 3. 事实核查修正（已入《04》v2）
NING 成品 ≈490（非 457），总计 ~751 SKU / ~9,400 BOM 行 / 815 加工费行；**exceljs 在 3 个最高价值文件上崩溃** → 适配器强制双通道解析 + 行数±0.5% 校验；D13 保质期=SKU 级字段默认 1095 天（独立数据 99.6%，存在真实 730 天品）；BOM 编码脏值走显式拒收道；功能清单 P0 自身算术差 1 项（需文档 owner 确认）。

## 4. 接受的残余（显式记录，不再隐式）
- **maskSensitive 不穿透 Map/class 容器**（PoC 特征化）：约定 DTO 禁用非平凡容器（CLAUDE.md 补则），不改引擎。
- **两步半进位的双重舍入**（≤1 分、方向一致）：契约统一优先，特征化测试留档。
- **admin 双账号可绕部门制衡**：机制正确（SoD 按用户 id），属账号治理——上线制度：admin 账号唯一。
- **读路径 JWT 8h 窗口**：写路径已回查 DB；读路径残余 8h 视为可接受（D 类追认）。
- 后台任务（对账/预警/备份）按计划 W4 落地，非缺失。

## 5. 新增域：产品开发（NPD）——四份新文件的整合裁决
71 节点全流程（需求确认 9d → 需求开发 77d 双线并行 → 投产 65d → 上线）已完整解析：
- **MVP 仅取两个钩子**（本就在关键路径上）：woDocs/jgDocs 的 `orderType` 枚举定义（含 新品首单/N月备货）+ skus `lifecycle` 四态（NPD 是"试销"的天然写入方）。
- **1.x：NPD 核心**（npd_projects 立项 + 节点实例化 + 关卡复用 approvals + BOM 授权链 + c 阶段"发起 BH/WO 带项目溯源"桥）。
- **P1：研发外围**（打样轮次/测试记录/海关备案实体、动态角色指派、meego/OA 深度集成）——打样轮次记录即在途 11 态中"中试样/产前样"的事实源，恪守"进度标签只由单据事实推导"。
- 硬依赖提示：NPD 的 BOM 授权必须走《04》§4.3 人工闸；c.2.2.0（PMC 按 BOM 拆采购）就是 W3 委外链本体，NPD 不重复建设。

## 6. 决策项汇总（待项目会）
D10 编码策略（含完整变更集）· D11 P0 冲突 · D12 期初裁决规则追认 · D13 保质期例外品类清单 · D14 颜色规则化 · D15 时间线 A(W10)/B(保W8砍项) · D16 委外仓期初/新旧划断 · D17 变体后缀 SPU 归属 · **D18 读路径 8h JWT 窗口追认** · **D19 NPD 分期（§5）追认**
