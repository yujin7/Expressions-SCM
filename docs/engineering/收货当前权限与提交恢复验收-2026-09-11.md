# 收货当前权限与提交恢复验收

## 锚点与范围

应用候选 `60af220625883639807dd9e3fd128d77588fe3e3`，分支 `agent/scm-workbench-evidence-20260907`。只验证收货创建、提交、审批、检验与入库当前身份/源事实和失败恢复；CT复用同一身份守卫，无新增业务规则、迁移或正式数据改写。

身份行共享锁→本单锁→来源事实/写入/审计。已提供的会话版本过期、停用或撤权拒绝；原制单人提交例外与审批职责分离保留。创建草稿不预占、不自动重发写入。同JG的跨SH累计并发、同SKU多PO行归属和全站读取生命周期不是本次已完成项。

## 准确候选证据

- 完整门：`.artifacts/verification/2026-09-10T19-31-34-652Z.json`，READY 9/9，起止同一干净SHA；668测试文件/6068项，生产构建、全量lint、三套类型、HTTP 34/34零跳过。
- 冻结包3414，隔离合成PostgreSQL `qc_boundary_20260911`，65迁移。非正式3100，非真实业务UAT。
- 外部证据目录：宿主 `交付证据/2026-09-09-申报产能/`。`sh-authority-exact-60af2206.json` 14项通过：四请求真实等待SH锁后一成功三冲突、一审计；指定审计异常回滚后原版本重试；审批实际等待账号撤权提交后拒绝；PO关闭/仓停用实际锁等待后拒绝且无额外收货单。
- 开发首脚本 `sh-authority-dev-bededf80.json` 因函数关键字拼写错误失败；修正r2通过但明确dirty，不充当准确提交验收。

## 浏览器恢复全过程

`browser-sh-authority-60af2206.json` 首5项：登录后用真实认证API创建合成SH33（不是表单建单），实际UI提交遇指定审计触发器异常；独立读取确认仍draft/version1且无提交审计。脚本误等待已不可见的退场loading图标被移出DOM而停，失败保留。

`browser-sh-authority-recovery-60af2206.json` 接续4项：确认同单未变与触发器已移除，人工重试成功pending、提交审计一次，旧版本重放409，另一仓管登录；审批确认框尚未就绪使脚本中断，没有审批生效，失败保留。

`browser-sh-authority-finish-60af2206.json` 接续9项通过：确认同单pending/无审批→实际审批确认→QC完整判合格0.1→确认入库→重复入库409→两账号实际退出后详情401。SH33 completed/version4，PO18 completed，PO行18 receivedQty=0.1000；一条sh_purchase_in流水0.1000，创建/提交/审批/QC/入库审计各一次，审批事件一次。三段证据不能称为一个脚本全绿。

390宽截图 `sh-submit-failure-390-60af2206.png`、`sh-completed-390-60af2206.png` 已目视：分别显示草稿错误提示和完成/检验结果，物料列保留、其余列在表内滚动。完成截图仍有连续成功提示遮住短时顶部内容，本次无UI修改，不据此声称所有手机视觉问题消除。

## 正式环境与剩余范围

验收runner结束时正式容器/镜像/启动时点未变；随后03:54登录排查已观察守护重建正式容器，镜像仍 `9ec914e49423…`，未部署本候选，不将旧时点事实说成永久不变。本地Chrome localhost实际登录成功，Safari HTTP Secure Cookie问题仍在；公网隧道/DNS与守护重建另需处理。异地备份发布前置、101条真实业务验收、三方授权/消费资格及D69–D71均未完成。

下一写边界：多个SH审批同一JG的累计判定必须独立并发取证；QC改变不合格分母的并发也需纳入分析。不能用单SH锁代替整个JG聚合的保护。
