# 发退料当前操作资格与恢复入口

日期：2026-09-13（上海）。已验应用：`9ad135b23f7849ef2b6db667ac2bd008afd45d20`；分支 `agent/scm-material-actions-20260913`，工作树 `worktrees/material-source-20260913`。候选通过不等于正式部署或真实业务验收；当前发布状态只看 [NOW](../NOW.md)。

## 本轮交付

| 缺口 | 修复与边界 |
|---|---|
| 制单人看见自审按钮，前端写死仓管审批 | FL/TL详情由服务端返回当前操作资格；四个列表/详情GET重新核验有效身份。复用审批配置、审批人标记和职责分离；管理员也不能自审，仍须存在审批配置 |
| 冻结或来源失效要点批准才知道不能处理 | 共用写入侧来源规则生成可读原因；关闭JG的FL、冻结JS的TL不能提交或批准。读取故障继续报错，不伪装成权限不足 |
| 超发/超退提示与批准判断可能分叉 | 读提示和写审批共用逐SKU十进制累计判断。FL保留仅检查本单SKU及管理员超发例外；TL保留所有累计退料SKU检查，无管理员超退例外，不跨物料抵扣 |
| 批准被阻止时，驳回入口也消失 | 独立生成并显示驳回资格，冻结/数量阻塞不剥夺合法驳回；驳回后重新加载当前草稿资格，冻结来源仍不能再提交 |
| 旧身份或读取失败仍留下新建按钮 | 列表返回当前新建资格；开始重读、读取失败或缺少资格字段时不开放新建确认。详情读取复用现有身份绑定/取消/重试，不新增缓存或并行权限体系 |

提示只说明当前处理资格，不承诺库存、账期或其他状态不会变化；提交/批准仍在原事务中检查最新身份、版本、来源和库存。当前有效制单人即使不再是仓管，仍保留后端既有草稿提交例外。未修改审批配置、岗位权限、库存或结算业务规则，无新迁移/依赖。

## 精确证据

- **READY 9/9**：714个测试文件、6,662项全部通过；在线HTTP 34/34，零失败、零跳过；全量lint、应用/测试/脚本类型、生产构建、运维与PostgreSQL迁移契约通过。
- 回执：`.artifacts/verification/2026-09-12T19-41-48-974Z.json`；起止均为干净的 `9ad135b2`。后续文档提交不声称另一个SHA重跑了应用全门。
- 真实PostgreSQL **10/10**：多连接实际锁等待，覆盖FL/TL累计数量竞争、FL创建/提交与来源改变、重复提交、退料与JS批准/依据刷新竞争、JS真实核销先完成时TL三种写入均拒绝。最终合成夹具 `f32790cd`，`postgres-final.log`。
- 新规则20项、HTTP资格10项；原服务/结算测试加入当前审批配置、制单人、管理员超发/超退差异、终态、关闭来源、真实冻结后操作资格断言。首轮聚焦65项通过，后续全门涵盖全部新增断言。
- 复查发现提取数量判断会把审计 `overIssue` 从布尔改成对象/空值；最终显式保留布尔并增加真实批准审计断言。不是放任审计格式变化或另建兼容字段。

除单独列出的回执外，本轮日志/截图均在 `.artifacts/material-actions-20260913/`，冻结运行包 `.artifacts/runtime-9ad135b2/`。

## 实际浏览器、接口和数据

隔离本机3456，库 `scm_contract_quickwins_20260911`，66/66迁移零漂移、作业关闭。使用合成仓管 `warehouse01` 实际表单登录；没有修改正式账号、3100环境或业务数据。浏览器验收覆盖1280px桌面与390×844手机，不等于真实Safari正式验收。

1. FL22为该仓管自制的pending单：只读提示“制单人不可自审或自行驳回”，无批准或驳回按钮。`browser-maker.txt`。
2. FL21为他人制单，累计已发120、毛需求100、本单1.2345：仅显示驳回，批准不可用；接口资格与页面一致。`browser-overissue.txt`、`fl-overissue-desktop.png`、`fl-overissue-mobile.png`。
3. JG151的结算已冻结；实际所选TL33对应已完成JS-20260913-0021。冻结TL33 pending v2只显示合法驳回；填意见、确认后draft v3，新建/批准未执行。数据库create/submit/reject各1条，`tl_return`流水0；重新加载无提交按钮，明确要求财务和仓管核对。`browser-frozen.txt`、`browser-rejected-database.txt`、`tl-frozen-rejected-mobile.png`。
4. 手机页面宽与scrollWidth均390；FL表750在342容器内滚动，TL表660在342容器内滚动；提示可换行，物料身份与数量不靠裁切页面隐藏。`browser-fl-mobile.txt`、`browser-rejected-mobile.txt`。
5. 不存在的FL999999实际返回失败；重试后仍显示“单据不存在”与重试入口，无旧业务动作；再打开TL33恢复正确草稿身份。首次立即采样落在加载阶段，等待网络完成后重新取证，不把加载中空白当成失败态验证。`browser-notfound-retry.txt`、`browser-recovered.txt`。
6. 三条已登录详情GET样本为22/13/19ms；这只是隔离数据单次观察，非正式规模基准或性能提升百分比。浏览器未捕获异常缓冲为空。`browser-api-actions.txt`、`browser-errors-final.txt`；`health.json`确认编译身份。

## Git、清理与未完成边界

- 前轮PR53已合入main `47dce71e`。确认其完整文件树与旧基点 `2b0443bb`相同后，仅把本轮未推送提交接到新main；提取前后本轮文件树相同，不接受有冲突的自动合并结果。
- 本轮走普通私有仓库PR/检查流程，未绕过保护、未公开仓库；推送/合并以GitHub和NOW记录为准。
- 已关闭本轮浏览器；按PID/cwd/端口核对后停止3456测试运行及owner明确的隔离PG。保留数据库、冻结包、截图、失败过程、回执与共享依赖，不作大范围删除。
- 本轮沿用工作流体验技能约束真实页面/恢复路径，写路径技能保留锁、精度、过账及审计，发布技能绑定精确版本。只更新NOW/CURRENT和领域报告，不复制CLAUDE/AGENTS/技能或创造第二份状态权威。
- 下一工程工作：冻结后的实际库存/财务差额纠错、工单外物料与代理成本资格、历史审批及歧义采购行恢复、真实角色端到端链；不从本次按钮验证推断这些已完成。
- 正式发布仍需获授权的异地DB+附件备份位置/访问与发布窗口；之后才验证真实Safari本机HTTP及公网HTTPS登录。平台授权与真实数据资格、D69/D70/D71及总监101条真实验收仍未闭合。
