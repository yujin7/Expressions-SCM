# 库存效期参考来源与读取恢复验收 · 2026-09-14

应用候选：`8d80868934553e8b5bbdc482a8d61c96d52451c2`，分支`agent/scm-stock-expiry-context-20260914`。[PR107](https://github.com/yujin7/Expressions-SCM/pull/107)基于[PR106](https://github.com/yujin7/Expressions-SCM/pull/106)，未合并、未部署。续接[出库预览](库存出库预览精度与读取恢复验收-2026-09-14.md)中的关联效期提示，不替代FEFO、审批或过账资格。

## 确认的缺陷与修复

| 原缺陷 | 候选行为 |
|---|---|
| 请求失败清空告警，与没有风险完全同形 | 明确失败/15秒超时、保持未知、可手动重试，不显示旧结果 |
| 只取消500ms定时器，已发请求仍可能覆盖新仓/SKU | 当前仓库+去重SKU集合绑定读取；变化立即撤旧，取消旧请求，晚到响应丢弃 |
| 每次改数量重新读取相同参考 | 读取键不含数量/行顺序/重复SKU；按500ms防抖，输入不改变范围就不重读 |
| 单次服务静默截取200个SKU | 接口每批最多200项、超限明确400；页面分批完整读取，任一批失败整组保持未知；超过1000个SKU明确提示拆单，不伪装全部核对 |
| 非法仓库被当作省略仓库，悄悄查全仓 | 非法SKU/仓库、重复查询参数、不支持参数明确400；有意省略仓库的内部/接口全仓语义保留 |
| 盘点参考当实时风险、所有数量都写“件” | 显示批次盘点参考、非实时库存、真实基本单位、实际盘点日期与计算日；未命中不等于可出库 |
| 未观察到SKU、未知SKU、正库存缺少效期均显示零 | 增加主档存在、当前期参考行数、缺效期正库存行数、日期/年龄；页面将未知与有记录未命中区分 |
| 浮点累计0.1+0.2 | 沿用`core/decimal`定点求和，UI直接显示精确字符串；保留原报表数字字段兼容，缓存键升至`inventory-alerts/v10`使旧聚合失效 |

仍只读取`batch_stocks`参考层，粒度SKU×所选仓（省略时全仓参考合并），按每仓最新盘点期收口，沿用`core/stock-view`最新期权威，不回落旧期。阈值仍为SKU设定或90天；到期日≤计算日计入已过期，近效期含已过期。上海业务日不变。补货净额的共用加载器仍排除无效期/非正数行，原有新鲜度规则不变。

没有新增依赖、迁移、API路径、业务写入口。**R15仍只提示，不单独拦截建单；缺参考不赋予出库许可，不改变FEFO/实际保存/审批的服务端校验。** 不从“各仓最新”推断数据足够新，也不声称全仓覆盖完整；未来盘点日期明确待核对。不同分批请求不是同一数据库事务快照，跨业务日响应拒绝并提示重试。

## 精确候选验收

完整门 **READY9/9**：797文件/8030项；全量lint零警告、应用/测试/脚本类型、运维、PostgreSQL75迁移零漂移、生产构建、HTTP34/34零跳过。回执`.artifacts/verification/2026-09-13T23-40-31-660Z.json`；UTC09-13 23:35:45.376→23:40:31.660，起止干净同SHA。冻结目录`.artifacts/runtime-stock-expiry-8d808689`编译标记为同SHA/git-clean；文档提交不冒称应用门重跑。

新增38项测试，包括：严格范围、鉴权先行、完整201SKU分批、部分失败/超时/晚到/关闭重开、日期及响应身份不一致、真实单位、小数、未来参考、当前期缺行、缺效期及原补货净额兼容。专项29文件/248项先通过，随后跑完整门。

开发首轮测试指出当天年龄为`-0`及新增合成SKU夹具漏填必填SPU。前者改为共享日期函数正向算龄，后者补齐合法夹具；不是绕过数据库约束。修正后专项和完整检查均通过。

## 页面 → HTTP → 数据库

独立3489、合成admin、SKU177（kg）、仓177（有参考）/178（无参考），未改正式用户或库存。可复核文件在`.artifacts/stock-expiry-context-20260914/`。

1. 原生页面选择销售出、仓和SKU；实际GET200返回盘点期2026-09-01，0.1+0.2=0.3000 kg，以及1条正库存无效期。界面明确盘点参考，不与实时库存混同。
2. 数量从0改为0.4，等待后仍只有1次效期GET；数量原字符串不被改成浮点提交。
3. 注入一次仅限效期GET的网络失败；旧详情撤下，持久错误及重试按钮可见，真实重试恢复。不是合成成功响应冒充服务端事实。
4. 扣住旧仓的真实HTTP回复，切换无参考仓；新响应明确1个缺参考。释放旧回复后仍保持新仓缺数，不恢复旧仓风险。
5. 390×844窄屏宽度与页面scrollWidth均390；缺参考、单位与来源说明可读，明细以原生details展开，Enter可以关闭再打开。窄屏失败后原生重试成功，再取消表单，没有建单。
6. 同会话真实接口：有效仓/SKU200；非法仓库或混入非法SKU400，均no-store；健康75/75无漂移且编译SHA相符。浏览器错误捕获为空，桌面/窄屏成功、缺参考、失败截图已查看。

数据库前后129行余额逐字段相同，流水506条、库存单据56张均不变。这里只验只读提醒，不代真实角色验收出库/报废/完整业务链，也不代表全站或正式Safari表现。

## 交审、清理与后续

[云端运行34790430699](https://github.com/yujin7/Expressions-SCM/actions/runs/34790430699)8个job均0步骤；Static checks `103813501205`注释明确账户付款/额度问题。云端尚无通过证据，不能由本地通过代签。应用分支已推送，主线本轮核对仍为`ee1c977e45b1c4e30961d2d6d6a4ea2068fefde2`；PR107→106→…→55依赖未合并，后续文档提交运行另验。

已核对owner/cwd/端口后关闭独立浏览器、3489 PID89549及隔离PG；保留合成库、冻结包、截图和所有验收文件。正式3100未动，不删除数据、备份、迁移或共享依赖。

此轮关联效期读取缺口已补；实际批次来源资格、替代单关系、工单占用/预留、其他单据退出及全链真实角色验收仍待续接。正式备份/发布窗口、实际Safari登录、外部授权和D69/D70/D71继续由[NOW](../NOW.md)及[CURRENT](../spec/CURRENT.md)导航，不从本轮结论推断全项目已完成。
