# 自动链预演紧凑布局与生成回链验收 · 2026-09-13

## 结论与范围

精确候选 **`c9949e87326cdd56fcf98d0fec4e216e0a0188c3`**，分支 `agent/scm-auto-chain-experience-20260913`，完整门 **READY 9/9**：720文件 / 6,742项测试，HTTP34/34零跳过，66/66迁移零漂移。已推送[PR59](https://github.com/yujin7/Expressions-SCM/pull/59)，基于待合并PR58，不自动合并到功能分支。

本轮修复 `/outsource/auto-chain` 的“找到来源→看依据→生成草稿→打开准确新单→返回筛选”体验。**不是正式部署、不是D33全闭环、不是全站或真实业务UAT**。正式入口、密码、业务库及外部接口未改；版本/发布状态只由 [NOW](../NOW.md)维护。

## 已修复的断点

| 原问题 | 当前行为 | 边界 |
|---|---|---|
| 桌面侧栏展开后成品列0px | 所有列明确宽度；成品240px，名称换行；两表固定布局与各自内部横向滚动 | 不靠全局裁剪隐藏溢出；移动端仍需横向查看宽表 |
| 页面缺少找单入口 | 共用紧凑工具栏，按工单/备货单/成品/OEM筛选，网址保存查询 | 在已完整返回的预演结果上筛选，不冒称服务端分页或新增采购排序 |
| hover才能看依据 | 有明确标签的原生按钮打开依据弹窗，保留系统供给预测、缺料和旧台账旁证含义 | 旁证不参与生成数量；未知不是0 |
| 成功toast只说“去审批” | 持续显示本次成功凭据，以返回ID打开准确JG/WO，明确“检查并提交” | 只生成草稿，不自动提交、批准、执行或过账；跳转后实际状态以详情为准 |
| 初始失败时参数假装关、计数假装0 | 读取中/失败显示未知，无旧建议操作；持久错误和显式重试 | 复用`useDocumentRead`的15秒超时、过期响应失效和卸载取消 |
| 双击、失败后盲目重发 | 同步写锁与按钮忙碌态阻止同页并发；失败绑定来源，须先GET刷新核对 | 不自动重试POST，不取消已发出的业务写；服务端新鲜权限/事务/幂等仍是最后保护 |
| 成功响应缺ID仍看似已成功 | 不生成虚假链接，显示未确认结果并阻止继续生成，提示先核对 | 不把合法JSON等同于合法业务凭据 |
| 更换读取实现后旧覆盖门少算一页 | 保留原60项覆盖下限，显式计算已迁移读取；反例拒绝注释、空hook、原始fetch及错误URL | 不降低覆盖门槛；实际行为另由组件测试与共享读取测试验证 |

新增代码复用既有搜索、列表状态、工具栏、失败组件；`page.tsx`增加Suspense以承接URL状态。没有新增依赖、数据库迁移、全局CSS或业务数量口径。

## 证据与失败留档

本地证据目录：`.artifacts/auto-chain-experience-20260913/`，不作为缓存删除；截图不另复制进业务源数据目录。

- `release-final.log`及`.artifacts/verification/2026-09-12T21-54-11-031Z.json`：上海09-13 05:54完成，起止干净、同一c9949e87；全lint、app/test/scripts类型、运维、迁移、全测试、生产构建、HTTP均通过。
- `tests/components/auto-chain-experience.test.ts`：14个回调/生命周期测试，覆盖初次错误、刷新撤旧、超时/迟到结果、异常响应、全列宽、搜索恢复、依据身份、两类生成防双击/精确回链、未确认写入、生成后读失败和卸载。该测试不代替真实AntD布局或焦点证明。
- `quickwin-read-consumers`、`auto-chain-experience`、`document-read-lifecycle`合计3文件108项通过。
- 初版`d0703274`的全门 **NOT READY**，`release.log`及`2026-09-12T21-47-25-520Z.json`完整保留：719文件通过、1文件失败；原因是旧回调扫描从60变59，迁到共享读取的本页未计入。修正测试护栏后重新构建、重新全门；未复用失败候选的READY结论。d0703274→c9949e87仅测试文件变化，应用源完全相同。

## 实际浏览器 → API → 数据库

仅使用已标记owner的隔离PG与合成PMC账号，jobs关闭，冻结运行3462。

1. **桌面1280×633、侧栏展开**：实际成品列240px，表容器950px/表内容1265px；页面scrollWidth=1280。中文成品名称可见。`desktop-product-visible.png`及`final-desktop-geometry.txt`。
2. **实际生成与精确回链**：初版WO124生成JG221（0004），最终c9949e87又以WO123生成JG222（`JG-20260913-0005`），分别50件、draft。最终点击成功凭据进入`/outsource/jg?docId=222`，显示对应WO/PO、草稿/待提交与另一位合格审批人审核提示；没有点击提交。`final-draft-receipt.png`、`final-exact-detail.txt`、`database-outcome-final.txt`。库存流水始终139条，无库存写入。
3. **返回与刷新**：从JG浏览器后退，查询词和结果仍在；刷新仍恢复输入；清空移除URL中的q。初版和最终版均实际验证返回筛选。不是仅直接打开详情URL。
4. **手机390×844**：页面scrollWidth=390；两表容器320px，表内容分别1265/790px，滚动留在表内。`mobile-filter-restored.png`、`final-mobile.png`、`final-mobile-geometry.txt`。
5. **依据交互**：桌面聚焦依据按钮后Enter打开，Escape关闭，动画结束焦点回到同一WO按钮；手机点击打开/关闭，标题和内容不越出屏幕。`mobile-evidence-dialog.png`。不是hover-only，也未将小截图泛化为全站键盘无障碍验收。
6. **读失败恢复**：浏览器拦截预演GET并中止；页面清旧行、计数/开关未知，显示“重试自动链预演”；移除拦截后点击重试恢复。`mobile-read-error.png`。
7. **写结果未确认**：对WO123的生成POST做浏览器中止，显示准确来源及“可能已完成，勿重复提交”，按钮禁用；GET刷新不重发POST。该次数据库没有新JG。随后最终版本的另一次显式点击才创建JG222。`mobile-write-unconfirmed.png`、`browser-writes.txt`及两份数据库结果明确区分这两次操作。
8. 浏览器没有未捕获JS错误，最终没有框架错误覆盖层；网络中止是明确注入的测试故障。最终健康绑定c9949e87、66/66、零漂移。浏览器和两次3462进程均核对PID/cwd后停止；隔离PG停止，数据库/冻结包/证据保留，正式3100未动。

本轮没有真实浏览器生成BH→WO（合成预演没有可用BH建议），该分支只有组件响应/回链测试；没有实际Safari或公网验收、真实长尾数据规模测试，也没有重新跑PR58的20项PG并发矩阵。PR58后端未改，其证据仍按原版本引用。

## 下一步与保留的边界

- 采购SH入库后`hookAfterPoReceipt`仍是best-effort；失败只留审计，仓管与PMC权限交接缺持久待恢复入口。下一步补独立恢复及准确来源，不通过授予仓管PMC角色或重复入库解决。
- 规格11余额+FL−TL、现行WO下PO已收生成量、ATP系统供给预测仍不是同一口径；真实到厂、占用、仓库/来源资格和R18极小数量精度待专项核验。不用页面说明替代口径裁决。
- 其他建单写端、真实角色全链、外部接口资格及D69/D70/D71仍按NOW续接。正式发布需授权异地DB+附件备份、恢复边界、窗口，以及实际Safari HTTP/HTTPS登录和业务UAT。
- 工作流技能决定以完整找单/依据/行动/返回环修复；写路径技能保留草稿与不自动重试边界，React/Next技能核对状态与Suspense，发布/浏览器技能分别绑定精确版本和真实交互。现有CLAUDE/AGENTS/7个canonical技能已覆盖这些约束，本轮不另建重复知识权威。
