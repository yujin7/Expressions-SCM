# 加工执行状态与紧凑展示 · 2026-09-11

范围：JG列表、详情、打印与其单位读取；基线为已合并主线`9129d62f`。应用候选`f1a33fa18bf2c14836d16158d9866fd31c20c3e4`已完成隔离验证；本报告不是正式3100上线或真实业务UAT签认，后续文档提交不冒充重新验证应用。

## 需求与已复现问题

用户截图要求消除「已完成 + 未开始 + 紧急」并改善拥挤交期；总监纪要要求明确采购订单节点与规则，CURRENT的D33要求保留分批来源关系。

在与基线应用代码一致的冻结运行（旧应用`0c6813ef`、隔离3443、Chromium 1280、合成PG、admin）实际复现：已关闭JG仍显示「未开始」，历史紧急/暂停挤在交期旁，日期断成三行，数量显示`10.0000`且无单位。没有用旧正式截图替代当前代码验证。

## 本次改变

- 单据状态优先：completed/closed/void不再由`inProduction=false`推成「未开始」，也不由true推成仍在生产。
- 非终态区分待提交、待审批、待加工确认、已确认加工；状态/确认标记不一致或缺失明确待核对，不补造工厂真实进度。
- 计划加急/暂停与日期拆开；终态保留为中性「历史加急/历史暂停」，不当作当前催单信号；未知状态也不激活红色计划信号。
- 列表「状态与执行」合并一个位置，详情复用同一组件；打印复用同一纯展示解释。沿用`DocStatusTag`、`ContextHelp`，不建立新状态机。
- 帮助用现有原生按钮/弹层；重要异常文字常显，不只悬停可见。解释加工确认只是代录流程事实，不是现场生产、完工或入库量。
- 保留完整交期、单号、真实小数；产品名合理折行，未知名称显式待补录。列表、详情和打印显示SKU基础单位，缺单位不猜测。
- 单位来自原有SKU联查，详情与产能使用同一值，删除一次重复SKU单位查询；仅声称少一次查询，不宣称端到端耗时改善百分比。
- 增加已关闭/已作废筛选，URL仍由既有列表状态管理；去掉「一工单一JG」误导性页面说明（`auto-chain.ts`有分批JG，未改生成规则）。

## 验证边界

- `tests/components/jg-execution.test.ts`：22项，覆盖终态×历史布尔值、非终态、缺失/未知、计划标记，以及真实React/AntD组件和打印的服务端渲染。不是浏览器交互替身证明。
- `tests/outsource/receipt-source-options.test.ts`：新增真实数据库读取断言，列表/详情/产能单位一致，completed+false原始事实未被改写；已有>500条选项范围测试保留。
- 开发检查：上述用例及既有采购加工链合计35项通过；另一次组件/读取/客户端边界98项通过。类型检查与开发构建通过；最终回执不与中途结果混用。
- 最终完整门禁：2026-09-11 12:20:22Z，READY 9/9；701文件/6444项通过，HTTP34/34、失败0、跳过0；起止同SHA且工作树干净。回执：任务目录`.artifacts/verification/2026-09-11T12-20-22-758Z.json`，日志`.artifacts/jg-execution/release-f1a33fa1.log`。包括运维/PG迁移契约、全量lint、全部类型作用域、测试及生产构建。
- 最终冻结候选实际浏览器：1280宽页面无溢出，表格可视/内容均976px，查看按钮右缘1220.64px；日期单行。390宽详情无整页溢出，帮助边界8–366px，Escape返回触发按钮、Enter重新打开。返回/刷新保留`status=closed&docId=2`与详情；打印页面数量`10 支`及历史标记一致，浏览器未记录控制台错误。
- 截图和尺寸记录：`.artifacts/jg-execution/{after-1280,detail-390,help-390,print-1280}.png`与`browser-evidence.json`。仅Chromium合成管理员读取路径；不等于正式Safari、全部角色/设备、物理打印或合同模板定稿验收。
- 合成fixture `JG-1ed5e53d`只在一次性`scm_contract_quickwins_20260911`库设置历史计划标记用于重现；正式数据未改。旧/新冻结包和截图保留在各任务`.artifacts`，不进代码仓库。

## 设计依据与未完成范围

依据[W3C按钮交互](https://www.w3.org/WAI/ARIA/apg/patterns/button/)检查Enter/Space和焦点；复用已安装AntD的表格与帮助实现。查阅[AntD表格文档](https://ant.design/components/table/)时当前网站为6.x，本项目仍用5.x，未据此升级依赖或套用6.x专属接口。

本次不改变确认/审批/收货/结算写规则，不宣称现场生产遥测已接入，不把历史标记纠正为真实生产事实。完整角色链、历史数据资格、三方平台授权、D69–D71与正式备份/发布时间仍由[NOW](../NOW.md)续接；整个目标未完成。
