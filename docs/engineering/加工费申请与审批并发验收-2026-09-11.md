# 加工费申请与审批并发验收 · 2026-09-11

## 范围与缺陷证据

基线为PR46文档提交`31ab26f2`，与其已合并主干`5b77ad8a`树一致。独立工作树`worktrees/pc-write-20260911`，不更改原用户工作仓、生产库或部署。

新行为测试在基线16项中12项失败、4项通过（`.artifacts/pc-write/baseline.log`）：发起/批准/驳回沿用停用账号、失效会话或已撤销角色/审批资格；申请原价与现价不一致仍可批准覆盖。无测试夹具故障。原发起流程还在事务外读取现价和查待审批申请，存在检查后插入竞争。

## 修复契约

- 发起：当前采购/admin；不要求制单人具有审批资格。保持用户共享锁及JG排他锁至提交；待审查询、十进制原价/偏差、统一取号、PC与审计同一事务。
- 审批/驳回：当前账号、会话、角色及审批资格；沿原配置和制审分离，不豁免admin自审。先PC锁再JG锁；发起仅锁JG，不锁其他PC，避免反向等待。并发重放保留同版本既有幂等。
- 批准：锁后比较JG现价与申请原价；历史并行申请依据过期返回409，回滚审批记录/PC状态/审计，不覆盖现价。驳回不受旧价冲突阻断，允许重新发起并取得新原价。
- 单价/偏差继续decimal；数量、单位、库存、应付、既有生效时点不改。不新增迁移、自动重试或新的权限系统。

## 开发与准确候选证据（不是正式上线结论）

- 16项新测试 + 8项原委外链测试：24/24；全TypeScript范围通过。
- 实际PG16隔离库、三连接锁屏障，5/5：重复申请仅一张、审批后申请读取已提交现价、并行审批仅一条效果、历史重复申请不覆盖新价、等待账号停用后拒绝。
- 真实PG留档`.artifacts/pc-write/postgres.log`，fixture `c899be21`；合成数据保留。脚本已接现有CI PG门，生产严禁运行。
- 最终应用`370255a0a583b9dfd6fcdc72032e8bbff776632e`完整门READY9/9：703文件/6485项，全lint/三种类型范围、生产构建、PG66迁移契约、HTTP34/34零跳过。起止同一干净SHA，回执`.artifacts/verification/2026-09-11T13-03-12-677Z.json`（12:59:00Z→13:03:12Z）。旧基线原样保留，当前分支移植到树相同的已合并主干后验证。
- 同SHA再跑真实PG5/5，`postgres-370255a0.log`，fixture `3de1c69f`。无生产迁移或账表写入。

## 实际浏览器/API/数据库故事

冻结同版生产包3445，后台任务关闭，使用仅合成库的admin与purchasing01，不重置任何正式账号。登录页有内容、控件、无框架错误层；实际表单登录到工作台。以下是分段证据，不是假称一个自动脚本全程通过。

1. admin在PC页面选合成JG#17、输入2.95并提交，真实UI生成PC#5（`PC-20260911-0004`），原价2.50、新价2.95、偏差18.00%。同会话重复HTTP申请409明确指出原PC。
2. 采购purchasing01打开精确PC#5，实际点击批准及确认；DB状态approved/version2、JG现价2.95、数量10.1250不变。PC create=用户3、approve=用户5、JG fee_change=用户5，各一次。
3. 打开历史重复夹具PC#4，实际批准被拒，捕捉到页面提示“加工费现价已与申请原价不一致，请核对并驳回旧申请后重新发起”；PC仍pending/version1、JG仍2.80。首次截图错过短暂提示，重试同一被拒操作后用DOM观察器记录提示，不隐瞒这一步。
4. 采购实际UI驳回#4后，admin同会话HTTP重新发起#6：201，原价2.80、新价3.20、偏差14.29%；admin尝试自审#6获403 SELF_APPROVAL。重建是HTTP而非再次填表；不混称全UI。
5. 1280列表与390×844已批准详情截图保留；手机文档宽390、无横向页面溢出、无框架错误层，浏览器errors命令无输出。截图`.artifacts/pc-write/approved-list-1280.png`、`approved-detail-390.png`、`stale-price-refusal.png`。未验全站/Safari/真实业务打印，也未把已知PC角色按钮提示不足说成已修。

验收后仅停止本轮3445测试服务器、两个命名浏览器会话与已确认owner的合成PG容器；冻结包、数据库和审计保留。主干PR46已合并不是正式部署；此候选的远端交付状态另查GitHub。

## 独立未完范围

`createPcForJgFeeSchema`允许`retroactive`；当前`approvePc`仅追加批准时刻分段，结算`js.ts`按SH创建时刻选费率。此次未证明“含已收追溯”真正重算历史金额，应作为下一项独立业务/结算审计；不得通过覆写历史分段或已批准JS私自补齐。PC界面当前角色提示、窄屏与真实角色完整链仍待专项验收。普通PO提交/确认不是本次已验范围。

正式3100未发布。异地备份授权/恢复与窗口、真实三方资格、D69–D71以及全需求UAT继续由NOW保留，不从合成成功推断业务已完成。
