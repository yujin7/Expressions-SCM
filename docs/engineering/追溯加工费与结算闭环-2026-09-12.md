# 追溯加工费与结算闭环 · 2026-09-12

## 2026-09-13：审查发现的同毫秒边界续修

PR48先前8项CI均通过，但因未解决审查讨论保持OPEN，并非已合并。审查指出的同毫秒后续普通改价被过滤，独立负验确实出现3项失败：收货在批准时点/之后时纯规则取旧追溯价，以及真实服务批准后结算35.00而非40.00。原绿门未覆盖此边界，不能据此否认问题。

修复`processingFeeAt`只排除**严格早于**追溯批准时点的段，同时间继续遵守调用方`effectiveFrom, id`升序；追溯段本身恢复基线，后序普通段继续生效。6项新增测试覆盖旧/同点/后续收货、两种批准顺序及服务预览/建JS。无迁移、依赖或权限变更，不改历史数据。

- 最新准确应用：`9e7a012185b5bed5649d42cc69639ebe9ab407ff`，READY9/9，705文件/6511项，HTTP34/34零跳过；回执`.artifacts/verification/2026-09-12T16-15-12-090Z.json`。本段时间为上海09-13，回执UTC日期仍09-12。候选起止干净同SHA，3447冻结包health同SHA/git-clean、66迁移无漂移。
- 聚焦47/47、全TypeScript范围、实际PG8/8（fixture`e0b1c2c0`）；新增测试在修复前失败的日志、终验和截图均在`.artifacts/fee-order-20260913/`，旧失败/旧绿门证据不删除。
- 实际登录后，以admin/purchasing01两会话的已登录HTTP发起/批准合成PC#40，JG#38。在已确认的**隔离夹具**中将PC#40和费率段#37生效时间归一到追溯PC#37的同毫秒，保持段ID先后；先读旧收货得35.00，再将该夹具SH#7设为同点收货，实际预览接口得40.00。时间碰撞是显式构造，不宣称两次人工批准自然发生在同一毫秒；未改不可变审计/库存流水或生产库。
- 实际页面JS#7“更新加工费”及确认后：保存40.00、数量10.0000、version1→2。1280桌面、固定390×844手机均已目视核对。首次即时缩放截图捕捉到过渡裁切，保留原图；新会话固定手机尺寸重新打开、等待加载后截图正常，抽屉x=0/width=390；没有拿documentWidth=390单独当通过证据。页面errors为空。
- 验收后关闭本轮自有浏览器、3447进程及合成PG容器，保留冻结包/数据库/截图。正式3100、真实UAT、下方待办均未改。PR讨论凭本段证据处理；最新推送/合并以GitHub与NOW为准。

## 原问题与边界

基线`5f3766c5`（PR47已合并为`4c894a6f`且树相同）。独立负验工作树`fee-scope-baseline-20260912`仅复制新行为测试、筛选追溯场景：真实批准2→3.50后10件旧收货仍计20，而期望35；不是夹具/编译失败。原段和批准PC均存在，消费者没有读scope。

业务依据《01》PC生效范围、R5分段加工费与结算财务审批；CURRENT记录本次明确语义。作用于未冻结结算，不改历史已批JS，不重写库存/原加工费段，不新增迁移或依赖。

## 完整用户路径

采购申请PC → 另一位合格审批人批准 → 未结算收货按最新已批追溯价取费（后续普通段仍有效） → PMC预览/建JS。

若已有JS草稿：展示已保存金额；PMC显式更新加工费、核对再提交。若已待审批：过期金额批准会整笔拒绝，财务驳回后PMC更新再提交。若已冻结：阻止新PC及已存在PC的后续批准，保留冻结金额，提示联系财务核对差额调整。**财务差额单及已结算追补并未实现，本报告不将其算作完成。**

## 实施与安全依据

- 纯取价规则`processingFeeAt`；服务联结批准PC和准确审批cycle，未批/驳回不生效、缺审批证据拒算；不猜价/时间关联。已有批准PC以`updatedAt`解释批准生效，新PC与费率段写入同一锁后时间。历史库未检查、不批量回填。
- JS创建/提交/审批/费用更新以JG→JS锁顺序；PC以PC→JG锁、读取冻结JS状态。JS读取PC不持PC写锁，因此没有反向锁环；共享`assertJgFeeMutable`守申请和批准。
- 草稿费用更新保留数量、物料扣款、手工调整及单据明细；数量与当前收货不一致则拒绝，不能只改费掩盖数量变化。金额/版本/审计原子，重复旧版本409，不自动重试。
- JS写端及JG收货关闭回查当前账号，关闭/提交原来事务外审计一并修复。库存核销唯一入口、制审分离、参数与旧版代理扣款口径不变。
- PC表单解释真实生效范围；JS详情提示快照与更新路径。新按钮有确认、等待锁与原版本约束；等待不能关闭抽屉。复用Next路由/脱敏、AntD组件，无平行权限体系。

## 准确候选与失败留档

- 最终候选`c2c229f1fdd94e63297b804bfc57efdf23e5ef9e`：READY **9/9**，705文件/**6505项**，全lint、三类TypeScript、生产构建、PG迁移契约、HTTP **34/34零跳过**。起止同一干净SHA；回执`.artifacts/verification/2026-09-12T07-36-12-735Z.json`（07:31:54Z→07:36:12Z）。66迁移，无新增依赖/迁移。
- 初轮`6720f5bb`全量门因新增接口漏登审计台账而失败（6503通过/1失败）；补A714及正文/导航/计数后通过。第二轮c2c229f1虽6505测试及构建通过，但运行目标仍旧冻结包，版本门正确拒绝；**没有改环境变量冒充新版本**，冻结新产物并重启自有3446后重跑全部门。
- 聚焦规则9项 + 新服务10项 + 原结算6项 + 原PC16项：41/41。独立旧基线负验真实失败（10件×旧2=20，而追溯3.50应35），不是夹具导入错误；留档`.artifacts/fee-scope/baseline-negative.log`。
- 实际PG16三连接锁屏障8/8，最终fixture`0bd1c005`，留档`postgres-exact-final.log`；改价vs建JS、改价vs提交、财务审批vs改价及此前5项PC竞争均覆盖，现有CI自动执行。保留合成记录，不在生产运行。

## 实际浏览器、接口及数据库

使用仅合成库的三个独立会话，经真实登录表单进入。先在`6720f5bb`冻结包实际操作完整链；c2c229f1只补台账/导航/测试，应用文件相同。再在**精确c2c229f1**冻结包实际登录、操作费用更新及等待保护，health确认为`git-clean`、66/66零漂移。

1. admin实际PC表单生成PC#18（`PC-20260912-0007`），JG#23，3.50→3.80，含已收追溯。purchasing01实际批准；JG旧收货10件。
2. 已有JS#1仍保留35.00；实际提交被拒，同会话HTTP诊断确认409及“草稿请先更新加工费”指引。不是把旧金额悄悄变掉。
3. admin实际“更新加工费”及确认：JS#1金额38.00、数量10不变、version1→2；提交后finance01实际批准至completed/version5。冻结JS#2仍35.00。
4. 再对JG#23发起PC的已登录HTTP负验为409，提示该JS已冻结及财务差额调整。审计PC create=user3/approve=user5，JS refresh/submit=user3、approve/complete=user10；没有同一账号自审。
5. 1280桌面及390×844手机截图已目视检查；草稿按钮独立换行、不压标题，页面宽度390无整页横溢。三会话页面errors为空；不是全站/所有设备验收。
6. 精确包在独立PG连接锁住JG#28时，JS#3更新按钮真实请求等待；按Esc仍保持抽屉及loading，释放锁后busy=0、version1→2，只更新费用。这是真实锁等待，不是模拟成功响应。

完整分段证据在`.artifacts/fee-scope/browser-evidence.md`及该目录截图/日志。瞬时PC表单截图仍有动画残影，最终scope以DB和批准详情为准；旧金额截图不含已消失toast，409另有HTTP证据。3446仅隔离验收服务，验收后停止自有进程/PG容器、保留冻结包和数据；正式3100未部署或改写。后续文档提交不是新的应用验收锚；GitHub交付状态见NOW及远端。

## 未覆盖或未完

真实数据的历史PC审批证据质量、真实角色及财务接受度；已冻结结算的差额调整闭环；物料数量变化后的整单重建/重算、扣款来源时效及其他费用；全站布局/性能和三方资格均非本次完成证明。正式3100、异地备份授权/窗口及D69–D71仍由NOW保留。
