# 关联单据紧凑分组 · 2026-09-11

## 范围与状态

准确应用候选 `1234aceecd1b947f1c0eccebb5b0bec8347229a5`，分支 `agent/scm-workbench-evidence-20260907`。
主责 design-supply-chain-flows；提交/隔离运行按 parallel-sessions，准确候选门按 release-sweep，真实交互用 agent-browser。
这是隔离工程验收，不是正式部署或101条业务需求全部完成。

共享 ChainStrip 被 BH/WO/PO/JG/FL/TL/SH/CT/JS 九类单据使用。本轮先读 SH、CT 消费方与接口契约，实际浏览器覆盖 CT、SH 与回链 PO；没有声称九类全部完成逐页人工验收。

## 用户闭环与修复

仓管从采购退货详情识别当前单据，按类型展开其他收退货，查看状态并进入精确关联凭据；可直接打开单张采购订单。原平铺箭头不是后端提供的边关系，会把同级单据误画成前后流转。

- 当前单号/类型/状态常驻；其他同类型多张用原生 details/summary，单张保持直接链接。
- 计数只取服务器返回的可见节点，排除当前；不是全公司单据数。所有单号、加工批次标签、状态文字与ID回链保留，无截断或静默删重。
- 去除伪顺序箭头；不按单号猜身份，不改变读取授权、服务、数据库或业务写入。
- 重复身份、当前标记错位、无效目标和 false/0 等异常成功响应显式失败，纯GET重试；沿用取消、超时与切换撤旧。
- 标签允许长文本换行，滚动留在既有宽表；用户明确展开时允许内容自然增高。

采用原生披露控件避免新增交互库；[MDN details 文档](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/details)说明默认折叠、summary与空格切换。本轮实际验证键盘，不以文档推断应用已经可用。未使用较新的互斥 name 属性或升级依赖。

## 开发反例与准确门

新增7场景修前6红、1项单张直达本已成立；修后定向3文件45项通过。三套类型及改动lint通过；类型检查曾指出 unknown payload 的属性窄化不成立，改成本地常量后通过，无强制类型断言绕过。

准确全门：667文件 / 6057项通过，完整lint、三套类型、生产构建、HTTP34/34零跳过，READY9/9。
起止同一干净SHA；回执 `.artifacts/verification/2026-09-10T18-56-20-115Z.json`。
冻结 `3413` 同版65迁移零漂移；合成PG `qc_boundary_20260911`，后台任务关闭。

## 浏览器实测

基线冻结3412/cb7012d8，对比冻结3413/1234acee。真实Chrome表单登录 warehouse01；同一CT18关联12张（当前1、其他11），没有用更少数据制造高度改善。

| 视口宽度 | 原关联区高度 | 新默认高度 | 减少 |
|---|---:|---:|---:|
| 1280 | 124px | 108.84px | 12.2% |
| 390 | 320px | 144.84px | 54.7% |
| 320 | 348px | 167.69px | 51.8% |

实测内容：当前常驻、全部11精确链接唯一保留、收货2/其他退货8分组、空格展开、状态可见、Tab进入首个CT链接、Enter打开CT10且新身份折叠状态重置；SH25直达PO14；链路503撤旧且非无关联，原页面纯GET重试恢复；所有组展开后逐个单号/状态与接口对照；1280/390/320展开无组件越界；无未处理浏览器错误；退出后接口401。

过程记录不抹去：首轮3项后读取到了抽屉缩放动画中间帧而停止；r2等待实际目标宽度后22项通过，再因AntD按钮名实际为“重 试”而定位中断。续验同一SH错误状态、实际重试及展开/窄屏/退出12项PASS。不是一份脚本一次全绿，未改应用迎合选择器。

证据目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`

- `run-release-chain-1234acee.mjs`、`build-1234acee.log`、`full-release-1234acee.log`、`owner-1234acee.json`
- `browser-chain-1234acee.json`（首轮FAIL）、`browser-chain-1234acee-r2.json`（22项后中断）、`browser-chain-tail-1234acee.json`（12项PASS）
- `chain-3412-{1280,390,320}-1234acee.png`、`chain-3413-{1280,390,320}-1234acee.png`
- `chain-expanded-sh-{1280,390,320}-1234acee.png`

已目视新320默认/展开及1280默认截图，键盘焦点可见；展开增长是显式用户选择，不隐藏单据。两个本轮浏览器均已关闭。没有本轮业务数据写入；登录/退出使用合成账号。

## 正式边界与下一步

03:00复核正式容器仍 `643f07055068…` / 镜像 `9ec914e49423…` / 启动 `2026-09-10T09:27:14.19320175Z`；本轮未替换正式3100，异地备份配置仍缺。不能以隔离READY声称用户已看到新界面或本地HTTP登录已修复上线。

继续相邻SH写路径的新鲜权限/状态审计、其余消费页专项体验检查，以及原12组101条真实业务UAT、简道云/聚水潭/用友当前授权与数据消费资格、D69–D71和受控发布。分组属于界面改进，不替代真实完整批次谱系或跨PO行收货归属设计。
