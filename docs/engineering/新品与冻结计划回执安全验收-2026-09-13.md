# 新品与冻结计划回执安全验收 · 2026-09-13

## 结论与准确候选

应用`4b637cbc362614f3ee494c3ff181efc9eaca5141`，分支`agent/scm-source-replay-authority-20260913`，[PR72](https://github.com/yujin7/Expressions-SCM/pull/72)，基于PR71文档锚`737b09a5a74ed9ace88fac5a3b180da0607b1557`。**隔离READY9/9，未合并主线、未正式部署、未签真实业务UAT。** 当前状态只看[NOW](../NOW.md)，意图只看[CURRENT](../spec/CURRENT.md)。

本批续接[备货写端资格](备货写入资格与来源审计验收-2026-09-13.md)留下的上游回执缺口。使用既有写路径、数据迁移及发布技能：当前身份、准确来源、原请求、事务审计、历史未知及发布边界均落实到代码/测试。未新建技能或另一份SSOT；CLAUDE/AGENTS的现有原则无需重复扩写。新增1个迁移，总计69；无新增依赖、API或页面。系统执行审计登记A722。

## 修复内容

| 之前的缺口 | 本批行为 |
|---|---|
| NPD首单回执只信调用者角色，旧会话可能继续取回原单 | 在拥有事务内核对当前账号启用、当前PMC/ops/admin资格、已提供会话版本及DB范围；唯一冲突兼容回读也重新核对 |
| NPD同一用户的同键跨项目竞争只在末端撞唯一约束 | 先按用户+规范化请求键加事务锁，再锁项目；同请求返回原单，不同项目/版本/数量409，仍保留唯一约束兜底 |
| 冻结计划回执只按全局键查询，未绑定发起人和原意 | 仅原发起人且当前有PMC/admin资格、原BH可读时可恢复；保存并核对周期、选中SKU集合、是否含抑制行及备注，异参409、他人403 |
| 冻结状态及来源在BH事务前读取 | 当前身份、全局规范化请求锁、准确周期行锁、冻结摘要/行读取、BH/取号、回执及来源审计处于同一拥有事务；关闭先提交的新请求拒绝 |
| 旧回执没有准确原请求，UUID还可能大小写不同 | 新增可空`request_intent`，旧行不回填；大小写兼容查找，歧义冲突明确拒绝；未知原意给可读原单号和核对指引，不换键静默开单 |
| 回执可以修改或删除 | 数据库拒绝UPDATE/DELETE/TRUNCATE，使用既有仅追加触发器；纠错不重写原请求历史 |
| 重放也提示“新建草稿”，过几秒原单链接消失 | 成功提示改为“已确认备货申请，打开核对当前状态”；本周期记录提供持续可点击的准确BH链接，避免假定原单仍为草稿 |

选中SKU排序/去重、空选择等于缺省全部、缺省抑制选项等于false、空白备注等于未填写；显式选全部SKU与“全部”请求仍是不同原意。新请求键仍可用于有意分批开单，**不按同SKU/同数量自动消除真实需求**。数量保持十进制，正数量判断改用`dCmp`；不改冻结建议量、审批职责分离、库存或金额。

## 自动化与并发证据

- 完整门`.artifacts/verification/2026-09-13T04-42-27-073Z.json`：起止干净同`4b637cbc`，**READY9/9、741文件/7032项、HTTP34/34零跳过**；运维、PG迁移、全量lint、应用/测试/脚本类型、生产构建全部通过。
- 定向4文件65项：NPD写入34、S&OP回执17、共识完整性8、周期6；另验NPD HTTP3和派生审计2。系统审计727项完整纳入全门，不以目录存在替代服务行为。
- 新测试初次缺合成冻结生命周期字段、误用回执schema导出名及SKU字面量类型，65项中20失败；修正夹具后65/65、所有类型通过。未放松业务约束。新回归验证停用/撤角色/会话失效、不同发起人/原意、历史未知/大小写歧义、关闭后原单重放与新单拒绝、精确微量数量及回执仅追加。
- `scripts/verify-postgres-source-replay.ts`精确候选**7/7**，夹具`ce983d01`；每例实际观察第二连接处于PG Lock等待。包含S&OP同键原单恢复、异参拒绝、来源先关闭；NPD同键恢复、同用户同键跨项目；撤PMC角色和停用NPD账号先提交后的旧回执拒绝。计数/原项目版本/库存流水核对，流水414不变。初次实现`c10a2444`及首实现锚`b46b067d`也各7/7，不混作最终SHA证据。
- 隔离PG `scm_contract_quickwins_20260911`，loopback65411，已应用69迁移、journal匹配；冻结运行包`.artifacts/runtime-4b637cbc`在自有3464运行且后台任务关闭。构建器产生的tsconfig差异逐项复核后撤回，不覆盖其他修改。

## 实际浏览器故事

合成源：计划3、周期3（2080-03，避免占用真实业务月份）、SKU93；PMC232、ops233、finance234经真实服务完成三人独立签认再冻结，不声称这是真实负责人签认。

1. 首实现锚`2f821cf7`的PMC浏览器点“按冻结计划开单”。验收脚本在收到服务端201后故意丢弃一次响应，再通过页面重试。两个独立场景各两次请求，分别只返回BH122（`BH-20260913-0018`）和BH123（`BH-20260913-0019`），同一场景保持同一UUID。
2. 数据库只有这两份有意独立请求、两张BH、两条BH创建审计和两条S&OP来源审计；每单SKU93、数量`12.3456`。不是“两次重试生成两张单”。
3. 复查发现瞬时提示不适合之后查找原单，因此最终锚`4b637cbc`增加本周期单据链接；重新完成全门。最终运行下再次请求原UUID返回201/BH123，改备注409；独立PMC227用同UUID返回403，不含原单号/ID。
4. 390×844下选择周期3，**实际点击记录中的BH123链接**，地址进入`/outsource/bh?docId=123`，正确展示草稿、制单人、冻结来源备注、精确明细及既有操作。正文宽390，明细表窗口342/内部内容614，桌面1280×900也核对；长名称未造成整页横向溢出。视口调整动画结束后截图，不把过渡帧当作最终布局。
5. 最终健康回包build SHA准确、69/69零漂移；浏览器未捕获未处理错误或框架错误层。重复请求库存流水414→414。回执反映请求开单事实，不证明后续审批或真实业务闭环完成。

证据保留在`.artifacts/source-replay-authority-20260913/`：`browser-proof.json`、合成夹具及桌面/手机原单截图；全门回执另在verification目录。截图/合成库/运行包不提交GitHub；文档不保存账号口令、Cookie、数据库凭据或外部平台密钥。

## 明确未完成

- 手工/实时BH创建仍需持久请求键与丢响应恢复；本批未用上游原子性冒称全部建单已幂等。
- S&OP页面请求键仍是当前标签生命周期内的ref：刷新/关页后未提供原请求持久恢复，也未冻结失败请求的选择；复用已改请求现在会409，但完整恢复体验仍待续接。关闭周期的历史回执入口也需完善，不能为了查历史重新开放开单。
- 本批只加固NPD首单及S&OP执行写端；其他新品操作、共识/换计划/生命周期写端的事务内身份，及冻结/实时开单模式切换的共同锁协议，不在此证明中。未新增客户端expected cycle version；锁住交易期间来源不等于证明用户看过最新冻结版本。
- 当前DB范围读取不代表所有范围更改都有全局冻结协议；S&OP周期及历史BH列表读取范围仍需独立评审。旧应用并行写入依赖最终唯一约束，须按候选有序发布；历史缺原意不擅自修补。
- 历史BH→WO明细分配/纠错、真实到厂/质量/占用、冻结财务库存差额、总监要求与完整真实角色UAT仍须续接。此次没有重新连接外部平台，不宣称简道云/聚水潭/用友全面上线。
- [CI 34738514778](https://github.com/yujin7/Expressions-SCM/actions/runs/34738514778)的Static checks及PG migration两项注释已直接核验：账户付款/额度导致job未启动；不是代码断言失败，也不是CI通过。需所有者处理后重跑。
- PR72基于PR71，71→70→…→55按序依赖；不能自动合并到功能分支。正式3100未修改/未刷新探针，仍需授权异地DB+附件配对备份、访问方式和发布窗口，再验证实际Safari本机HTTP/公网HTTPS及首次改密。

## 收尾与可恢复性

文档收尾验证：技能治理19项、钩子安全13项、系统执行审计727项，共759项通过；本轮4份文档80个本地Markdown目标全部存在，差异格式检查通过。NOW保持83行/13,977字节；用现有两份authority导航此次证据，没有复制技能、删历史要求或维护另一份状态表。

已关闭本批两个浏览器会话；核对cwd/端口后停止自有3464进程80878（首实现71341已更换停止），确认owner后停止65411隔离PG。保留全部证据、合成数据和冻结包；没有删除共享依赖、原文件、附件、备份或历史回执。文档收尾另行核验，不把后续文档SHA称为重跑应用全门。
