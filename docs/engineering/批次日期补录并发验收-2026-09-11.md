# 批次日期补录并发验收 · 2026-09-11

## 结论

准确候选 `e20d43680ac77fe2815a7d8861cbdf4302bab1ef` 在隔离环境完整门 **READY 9/9**：686 文件 / 6217 测试通过，HTTP 34/34、失败0、跳过0；起止均为同一干净提交。冻结3430、66条迁移无漂移。它是候选验收，不是正式服务发布。

本轮修复批次登记的四个相互关联问题：同请求重复批次丢失后续日期、并发补空覆盖先写日期、唯一键插入竞争丢失补录日期、反序双批补录死锁。未新增迁移，未改数量/批次开关/正式数据，未自动推算到期日。

## 修前证据与处理

| 反例 | 修前实际结果 | 修复 |
| --- | --- | --- |
| 同一请求同批次，首行无日期、后行有生产/到期日 | 日期都留空 | 先按SKU/批次合并每个字段的首次非空值 |
| 已有空日期批次，两连接依次争用锁，先写8月、后写9月 | 先写的2026-08-01 / 2027-08-01被覆盖为2026-09-01 / 2028-09-01 | 在唯一键持锁的原子更新中判断当前字段是否仍为空 |
| 首次空日期登记尚未提交，另一连接携带日期撞唯一键 | 后到者只回查ID，日期仍null | 插入冲突也执行补空并返回真实批次ID |
| 两连接以Z→A与A→Z顺序补录同两个批次 | PostgreSQL报告deadlock detected，第二连接失败 | 按SKU与批次号统一写入/加锁顺序 |

原有规则保持：数据库内已知非空日期、首次来源不覆盖；本请求同字段的后续冲突值不覆盖首次非空值。不将本轮称为冲突裁决/人工纠正流程，也不假装已经验证输入标签的真实性。

采用 `ON CONFLICT DO UPDATE` 和当前值优先的 `coalesce`，依据 [PostgreSQL 官方 INSERT 文档](https://www.postgresql.org/docs/17/sql-insert.html) 的并发原子插入/更新保证。多批锁顺序另行控制，不能只靠upsert消除多行死锁。

## 实际 PostgreSQL 证明

证据根：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

- `verify-batch-date-race-20260911.mts`：只连接已核对完整ID的专用容器与`purchase_race_20260911`合成库；两个独立数据库连接、控制连接、真实`pg_stat_activity`/`pg_blocking_pids`观察后释放锁。
- `batch-date-race-red-8f0ab1e5.json`：修前失败回执保留。数据库日志在`2026-09-11 01:36:12.073 UTC`明确记录PID16422与16421分别等待对方事务5238/5239，两条SQL为批次日期UPDATE，确为死锁而非观察超时。
- `batch-date-race-green-dev-8f0ab1e5.json`：开发修后同脚本通过。
- `batch-date-race-exact-e20d4368.json`：准确提交同脚本通过；回执记录脚本摘要、提交、脏状态与真实锁等待。日期保留/补齐、两连接批次ID一致、反序双批并发全部通过。
- `purchase-line-race-exact-e20d4368.json`：前一轮52个采购行收退货检查点在新提交上全部通过，包括7组锁等待、最终审计故障回滚与重复提交。
- 本轮单元新增2个先红后绿反例；定向4文件30项与三套类型、改动lint通过。完整门回执：`.artifacts/verification/2026-09-11T01-43-54-945Z.json`，另有`full-release-e20d4368.log`、`postgres-e20d4368.log`、`build-e20d4368.log`与`owner-e20d4368.json`。

## 隔离浏览器与实际服务链

冻结3430连接`purchase_line_20260911`，未连接正式库。新增夹具记录`fixture-batch-browser-e20d4368.json`：SKU9 / `QA-BATCH-E20D4368`，仓27，PO22，采购行84/85，批次`QA-LOT-E20D4368`；仅采购前置夹具直接种入。

1. 真实Chrome表单登录warehouse01成功进入工作台。通过该浏览器的同源已认证HTTP调用创建SH57（201）、提交（200）；制单人自审403；另一个真实表单登录的warehouse02审批200。**收货写入用浏览器fetch调用接口，不宣称点击完成了整套SH录入表单UAT。**
2. 两个同批次SH行：第一行无生产日期、0.1合格；第二行生产日期2026-08-03、0.2让步接收。QC创建201、入库200。数据库核对SH completed/v4，采购行84/85分别0.1000/0.2000，实际两条库存流水0.1000/0.2000。
3. 实际进入批次追溯页，填写SKU/批次并点击追溯按钮。生产日期显示2026-08-03，来源SH #57，到期日为“—”；数据库批次唯一、生产日期准确、到期日null。`batch-trace-e20d4368.png`已实际目视，非仅保存截图。
4. 批次过账开关仍0，库存流水batchId为null；页面明确“出库侧覆盖有限”，没有伪造带批次库存。因此这证明日期登记→页面，不证明已完成批次化库存/FEFO上线。
5. 浏览器errors无输出；两个账号实际点击退出，等待回到login后分别请求批次追溯/SH详情，均401，再关闭测试浏览器。没有保存登录会话文件。

首个浏览器eval仅返回函数未执行（输出{}）；随后显式调用函数才发起创建，实际仅一个SH57，不能把工具调用失误算产品故障或重复建单。

## 发布与剩余范围

- 正式镜像仍`sha256:9ec914e49423cd4c61b546e70fccaeb6baa081af8df0ef0bf99cb9f7a338bbf8`，原启动时间未变；`formal-observation-e20d4368.json`证明发布脚本未改正式运行。正式61迁移，本候选66迁移，两者不能混称。
- 正式本地Safari登录修复待发布，数据库与附件异地备份位置待确认；不绕过前置。真实业务UAT、D69–D71、简道云/聚水潭/用友事实资格继续。
- 完整批次化收退货/拆批/效期并发、历史null批次迁移与人工归属纠正尚待。SH目前采集生产日期但未采集到期日，本轮不把缺失到期日当成已具备FEFO资格。
- 追溯页的慢响应身份绑定、切换SKU/批次期间的隔离操作目标、加载失败与无库存空态区分，仍需单独红队验证；当前页面代码尚未证明这些边界。不得以本轮截图或原子登记代替完整页面安全验证。

使用`write-path`落实批次登记与入库审计事务，`parallel-sessions`隔离数据与候选，`release-sweep`核对准确提交完整门，`agent-browser`验证真实登录、追溯显示和退出。
