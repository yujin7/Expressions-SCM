# 平台身份批量确认完整性复核

日期：2026-09-06；基线 `c23568eef4a35bf143e9b6a6469392e3af3d7890`。
独立候选 `agent/scm-studio-compact-20260906`。仅确认展示，不修改写服务、数据库或外部授权。

## 发现与修复

- 三类确认窗口原先只显示前8行，实际却最多提交300个身份或500个条码。新增三个上限反例
  在旧代码均失败（8不等于300/300/500）；修复后整批可分页核对，未纳入本批的候选数明确列出。
- 统一为一个确认窗口、一份不可变候选快照、一份有上限的清单；预览和POST使用同一清单。
  分页只改变显示，不缩小或扩大提交范围。空批次不能确认；提交中仍防重复/取消，权限仍服务端裁决。
- 逐行保留店铺、外部身份、目标SKU和真实匹配依据：天猫四种路径、拼多多同码/成本标准翻译、
  条码财务单来源/聚水潭镜像单来源/两者一致。去掉“一概同码”和“一概两来源一致”的错误说明。
- 小屏合并为一列完整核对项；桌面分身份、目标、依据和有权限的金额。长文本可换行，不用省略号隐藏归属。
  窗口内容可纵向滚动，确认/取消保持在内容区外；桌面和手机均不需要横滑确认表。
- 预览标签、商品名称及金额不进入写请求；拼多多仍带必要的`platform=pdd`，条码仍带镜像来源。

## 证据与限制

- 专项最终2文件28项通过：三类整批上限、预览=提交、手机信息、金额权限、全部来源标签、
  请求字段、不可变快照、防重复及部分失败/未知结果回归。
- 三套类型检查通过，生产构建274/274及standalone安全检查通过。首轮全仓lint发现新增测试
  未使用的解构字段，已修正；无缓存lint后通过。
- 首轮全量4,891项中4,890通过、1失败：本次新增断言遗漏拼多多合法且必需的`platform`字段。
  校正断言并明确验证值为pdd后专项通过；首轮报告保留为`/tmp/expscm-bulk-review-full-20260906.json`，
  不把该失败报告算作全量通过。精确提交需重新完整`check:release`。
- 浏览器使用3317隔离standalone和65364合成PG。真实登录/导航及身份GET通过后，
  仅身份GET/POST被明确合成响应拦截，任何身份POST都未到数据库。
  天猫、拼多多、条码各9项：均翻到第2页看到SKU9，再确认发送完整1–9；
  拼多多请求含pdd、条码请求只有skuId/barcode和来源，无展示标签/金额。
- 手机390×844：页面宽390，弹窗宽374，确认表宽326且scrollWidth同宽；
  桌面1280截图与两种手机截图已目视检查，未见浏览器错误。
  夹具及截图：`/tmp/expscm-bulk-review-browser-20260906.Hpa1Hp/`。
- 300/500边界由真实组件回调测试覆盖，浏览器用9项验证真实分页；未进行真实业务身份批量写入UAT。
- 本次不推送/部署、不处理D69–D71业务决策，不将基线的READY自动继承为新候选READY。

`design-supply-chain-flows`要求完整可核对的人工确认；浏览器技能验证实际分页及手机布局，
`release-sweep`将精确提交的全量/数据库/HTTP证据与真实业务验收分开。
