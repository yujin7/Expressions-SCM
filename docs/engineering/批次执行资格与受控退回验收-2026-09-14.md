# 批次执行资格与受控退回验收

日期：2026-09-14。范围：公共过账、FEFO建议/建单，以及TL/CT/报废的明确批次路径。候选，不是正式部署或真实业务验收。

## 原缺口及修复

- 建单合格不代表执行日合格：保存后跨日到期或补录到期日，正常出库原来仍可执行。注册表明确正常使用、退回、历史调整三类，正常使用在实际扣账前检查保存批次，不自动重分配。
- 批次SKU身份原来未统一核验；FEFO甚至会推荐另一个SKU的批次。现在建议端排除并提示，执行端再拒绝，不用余额充足代替身份。
- 退回与继续使用不是一回事：TL/CT以及同SKU的开放报废评审可明确选择到期批次，数量、身份、定位隔离等原保护不变；自动FEFO仍不推荐过期货。
- 例外只来自受控服务/已批准的源单和当前评审，不接受HTTP自由放行参数。已关闭/改判的评审不能供新的过期出库借用。日期锁与登记共用确定性自然键顺序；发生日保留会计意义，不回溯物理资格。
- 已过账重放、红字纠错、盘点和核销保留原历史语义。null批次/未知效期沿用迁移边界，不冒称已追溯或可用质量合格。

## 开发证据

- 植入回归：最初批次执行9项中8项红；6项效期未拦、2项错SKU只在后续余额不足才失败。修复后11项绿，含实际库存单审批失败整笔回滚、独立驳回、报废及关闭评审不得重复借用。
- FEFO错误身份的新增测试先红：错误批次被分配3；修复后零分配、缺口3。只有1份无批次真实余额时只回落1，仍缺2。
- 相关库存/物料27文件232项通过；追加评审状态保护后专项11项通过。应用/测试/脚本类型检查、lint均通过（完整候选门另留结果）。
- `.artifacts/batch-execution-20260914/`保留隔离验证脚本和结果；不作为缓存删除。合成库只在已核对owner的65411测试实例；正式3100、正式数据及账号未动。

## 精确候选验证

- 应用`c6598ebe2d03549dd0c9984a4d44bdb2d232c5a0`，[PR93](https://github.com/yujin7/Expressions-SCM/pull/93)基于PR92；主线本轮仍为`ee1c977e`。后续文档提交不冒称新SHA重跑全门。
- 完整门`READY` 9/9；772文件/7556项通过，lint/typecheck/构建/运维/PG迁移均过，在线HTTP34/34零跳过、72/72迁移。起止同SHA且工作树干净；回执`.artifacts/verification/2026-09-13T16-20-10-636Z.json`，16:15:13Z→16:20:10Z。
- 精确SHA真实PG复验：`BATCH-LOCK-065a69d5`在日期补录事务尚未提交时，出库被观察到等待该事务锁；提交过期日后返回`EXPIRED_BATCH`，只有原期初流水，余额10不变。`pg-result.json`留证；两个独立连接加第三方锁观察者，不用串行假装竞争。
- 隔离业务夹具先正常FL10，再建FL5待批后补录到期；批次闸门仅在隔离事务内临时启用，完成明确TL5和CT2后恢复原值0，再提交事务。TL177/CT1已完成，自有仓93、加工厂5，PO行268已收由10回到8。正式参数完全未动。
- 实际浏览器使用冻结3483版本：FL381桌面1280×633/手机390×844各一次批准，均409且完整中文提示。拒绝后pending/version2、0本单库存流水/0本单审批、原2条创建提交审计未变；随后实际驳回200→draft/version3，仅新增1审批/1审计。全门后再读，余额93/5、0本单库存流水仍一致；总流水446、审计2055、审批278。
- `final-state.json`、桌面/手机拒绝及驳回截图保留；手机文档宽390、错误提示宽374，无横向页面溢出，无浏览器错误/框架覆盖层。仅证明所验路径，不声称全站UI或正式Safari已验。
- 自有3483/PID35040、专用浏览器与隔离PG已停止；合成库、冻结包和证据保留。未清除原库、附件、备份或共享依赖。
- GitHub应用检查[34767904448](https://github.com/yujin7/Expressions-SCM/actions/runs/34767904448)8个job均0执行步骤，Static checks注释明确账户付款失败/额度限制；需所有者处理后重新验证，不能以本地门替代CI成功。

## 边界与后续

- 未新增迁移/路由/依赖，未打开正式批次闸门，未改TL累计≤FL、CT累计≤已收、超发权限或工单硬预留。
- 批次效期与质量状态不能互代。真实退回后隔离/检验、批次选择体验、历史资格及完整追溯仍需真实角色验证。
- 新发现直接续接：FL当前仅创建/提交/审批/读取，驳回后尚无完整草稿批次修改入口；本次“安全拒绝/驳回”不是“批次纠正已完成”。下一轮优先补制单人受控修改/重新配批，绑定来源、版本、当前身份、仓库与库存依据，保留原审批轨迹，不靠反复重提交或自动替换历史流水。
- 只证明所列日期补录竞争，不声称所有主数据并发和全厂物料归属都已解决。
- 候选发布和CI状态由NOW维护；依赖栈未合并、异地配对备份与窗口未落实时不得称已上线。
