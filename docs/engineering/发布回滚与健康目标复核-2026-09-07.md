# 发布回滚与目标健康门复核

基线：c8133aba8e7f981a68f6a18a061171395032debc。日期：2026-09-07。
角色：获授权的发布/恢复负责人；不改变库存、会计、告警、责任分配或第三方授权。

## 确认的问题

1. 旧回滚块把Compose查询、镜像查询和标签失败全部吞掉；已有应用失去可验证回退点后仍可构建/迁移。
2. 标签使用新源码HEAD，多次从同一源码部署可覆盖不同旧镜像的同一个标签。
3. `SCM_INITIAL_DEPLOY=1`没有拒绝已有应用，因而可跳过正常备份路径。
4. 健康门固定请求宿主机3000，仅字符串匹配ok=true。自定义端口时可能读另一应用，迁移未知亦无独立核对。

## 修正与验证策略

- `ps -a -q app`区分查询失败、明确不存在、唯一目标、多目标；不存在时必须显式首次声明。
- 旧镜像SHA-256摘要合法、标签创建成功且重新读取相符，才允许构建；停止容器不被跳过。
- 标签以旧镜像完整摘要命名。同一旧镜像可重复验证，不依赖正在发布的新源码HEAD。
- 同一Compose服务内运行Node健康探针；5秒AbortSignal、禁止跳转、不缓存；只接受完整就绪字段与正数相同计数。
- HTTP失败、未知/矛盾字段、坏JSON、网络失败均拒绝，不回显上游异常。就绪失败不进入预热；缓存预热仍为尽力而为。

测试直接执行真实deploy.sh构建前脚本，Docker全被隔离假命令拦截，使用合成环境文件。
首次夹具未准确模拟Docker拒绝空container参数，校正后旧代码10失败/1通过；修复后通过。
健康测试执行脚本里的实际JavaScript表达式，并执行实际健康shell段，分别验证容器退出码0/1对预热入口的影响。
原架构测试曾要求`docker tag ... || true`，本轮改为要求失败阻断并核验标签；这是收紧故障规则，不是删除失败断言。

提交前：6文件/765项专项通过（新增部署行为28项），三套TypeScript、针对性lint、运维契约与shell语法通过。
首轮类型检查发现合成进程环境缺项目必需的NODE_ENV字段，已补显式test值，不继承真实凭据环境。

## 保存与交付边界

候选位于项目持久目录`worktrees/release-safety-20260907`，未覆盖临时旧候选或原工作区。
原工作区173改动路径与c8133ab比较：143逐字一致、30不同、0缺失。不同不等于丢失或可覆盖，须保留来源并逐差异整合。

未运行真实部署、迁移、备份调度或通知。假Docker测试是脚本行为证据，不是生产Docker恢复演练。
无容器不能证明目标数据库为空；首次部署仍需人工确认。多实例滚动发布、并行发布互斥、公网连通和UAT不由本轮证明。
本轮须最终精确提交门；c8133ab的既有READY仅作基线证据，不能继承给新增修改。
