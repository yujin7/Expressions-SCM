# 审批依据身份与盘点来源验收 · 2026-09-11

## 结论与范围

最终应用候选 `59db6f33ff8da5687ccd2d806e1bc0b2b5ee14e5` 已通过准确完整门及下述独立合成工作流/浏览器验收，**未部署正式3100**。身份修复提交为 `4dba63a3`，小屏明细续修为 `59db6f33`。无迁移、无历史数据重写；期初/盘点财务审批、其他库存单/红字仓管审批及职责分离不变。

## 缺陷及修正

- 库存详情原以相同数字ID合并 `stock_doc/opening/count`，但前两者属于 `stock_docs`，后者属于独立编号的 `pd_docs`。全新数据库通过实际建单、提交、审批便能产生1/2/3碰撞；不是只能由复用测试库残留触发。
- CA盘点调整由PD审批后生成completed，审批记录属于来源PD，不存在CA独立审批流。原审批写入口借用count域和CA的ID，可误回其他PD的幂等成功。新入口返回409，提示查看来源PD或走红字纠错。
- 期初保留同物理实体的opening/历史stock_doc，其他普通库存单只读stock_doc。CA同时核对来源PD存在、completed及PD明细反向关联唯一且一致，才给来源审批和精确PD链接。来源缺失/冲突明确待核对，空审批不猜同编号；不将关系已核对等同全部历史数据已正确。
- 共享审批时间统一现有上海格式化权威；本库存页制单时间同步。本次未批量改其他页面的制单时间。
- 390宽首轮截图出现中文名称竖排：虽页级无横溢出，视觉仍失败。续修所有库存单详情共用的明细表为740内部横滚、名称180列、长批次允许换行。没有引入另一套表格组件或全局CSS覆盖。

## 反例与开发验证

新身份测试7项，修前6失败/1通过；直接审批反例改用另一财务排除制单人职责分离干扰后，修前确实返回`idempotent:true`。时间测试3项修前均失败。修后定向5文件30项、三套类型、改动lint通过；小屏续修后定向3文件13项及三套类型/lint再通过。

共享读取入口扫描：其他调用为单一审批域，未见第二处跨物理实体并集。`inbox/service.ts`仍保留异常pending CA映射count的遗留分支，正常写流程不会生成pending CA，新审批入口已拒绝；异常历史队列的显式核对体验仍需后续审计，不能把扫描结果称全仓审批完结。

## 准确完整门

| 候选 | 测试文件/项目 | 实际HTTP | 结论 | 回执 |
|---|---:|---:|---|---|
| 4dba63a3 | 691 / 6268 | 34/34，零跳过 | READY 9/9；但首轮手机视觉失败，需续修 | `.artifacts/verification/2026-09-11T07-34-54-980Z.json` |
| 59db6f33 | 691 / 6268 | 34/34，零跳过 | READY 9/9 | `.artifacts/verification/2026-09-11T07-41-29-896Z.json` |

最终门2026-09-11T07:37:30.779Z开始，07:41:29.896Z结束；起止均同一干净SHA。包括全量lint、应用/测试/脚本类型、Vitest、生产构建、真实PG迁移契约与HTTP。3436冻结运行同候选，66/66迁移零漂移；不等同正式服务。

## 全新真实PostgreSQL业务证据

在已核对身份的本任务容器 `exp-scm-capacity-20260909` 内，从空库新建 `approval_identity_20260911` 并执行66迁移；不用上一轮混合夹具库。夹具脚本首次因误引用不存在的`argon2`包在建库前失败，改用项目实际的`@node-rs/argon2`后执行成功；这是测试脚本问题，不算产品失败或业务通过。

四个独立合成账号、一个SKU/仓；业务均经现有service，未直接写库存流水/余额或伪造审批：

1. 期初库存单1：10瓶，另一财务审批“仅期初审批”。
2. PD1：实盘9瓶，财务审批“来源盘点批准”，产生CA2，差异−1。
3. 无关PD2被驳回“其他盘点二驳回”，其ID与CA2相同。
4. 销售库存单3：出1瓶，由另一仓管审批“仅销售单审批”。
5. 无关PD3被驳回“其他盘点三驳回”，其ID与销售单3相同。

此时总账/余额都为8.0000，3条流水、5条审批、20条审计；CA仍version1。读取分别只得到本单/正确来源评论。另一财务对CA2独立驳回被拒绝，控制数及版本完全不变。另插入一条**仅异常读取夹具**CA4（无真实来源、无库存行）验证待核对，不称它是合法业务单。

在最终3437候选上通过浏览器登录后再验：

- 仓管01在CA2抽屉点击红字按钮、填写原因、生成红字5；认证HTTP提交后pending/v2。
- 仓管02另一个浏览器会话登录，通过认证HTTP审批红字5，completed且`idempotent:false`。
- 重复同次审批返回`idempotent:true`；再次生成CA2红字返回409并指出已存在#5。
- 最终PG重读：余额9.0000、流水合计9.0000；4条流水，末条`stock_doc#5 / reverse:count_adjust#2 / +1.0000`；仅1张红字、6条审批、24条审计。五张详情各自审批仍准确，CA2仍只展示来源PD1审批，红字5展示本单审批。

## 浏览器与界面

最终真实Chrome、仓管01/02、`http://127.0.0.1:3437`，运行SHA与健康接口已核对。

- CA2→“查看来源盘点”普通点击到`/inventory/count?docId=1`，打开PD1抽屉，10/9/−1及来源批准内容一致。首候选桌面与最终390宽均实点；没有把仅构造href当导航验收。
- 最终390页宽/scrollWidth均390；明细内部342/740，可读名称，不竖排。截图已目视，来源解释和链接均可见。
- 异常CA4在320宽页宽/scrollWidth均320，零审批条目，明确待核对且无来源链接。首屏截图未覆盖底部警告全文，文案由DOM与API另外核对，不称首屏已完整展示。
- 认证API直接审批CA2返回409及可读中文原因。
- 两个会话均点击“退出”后，再点击确认“退出登录”，各自详情请求均401，再关闭浏览器。未保存认证状态文件。
- 浏览器实际时区为Asia/Shanghai；UTC下不漂移由组件反例验证。本轮未单独保存浏览器console日志，不声称已完成全部浏览器/时区组合测试。

## 可复查证据位置

本轮证据根目录：`交付证据/2026-09-09-申报产能/`（项目根下，Git外）。

- `verify-approval-identity-4dba63a3.mts` / `approval-identity-4dba63a3.json`：新库迁移及实际创建/审批。
- `verify-approval-final-59db6f33.mts` / `approval-final-59db6f33.json`：最终同SHA服务、红字后控制数/逐单历史。
- `approval-identity-4dba63a3-390.png`：保留首轮竖排失败。
- `approval-identity-59db6f33-390.png` / `approval-missing-59db6f33-320.png`：最终已目视截图。
- `full-release-{4dba63a3,59db6f33}.log`、`owner-59db6f33.json`、`owner-approval-identity-59db6f33.json`、`formal-observation-59db6f33.json`：完整门、运行与正式未变证据。

## 正式环境与未完成边界

正式app容器仍`af36cd6581f6…`、镜像`9ec914e49423…`、启动于2026-09-11T02:43:18.62257297Z；本轮未改。正式库此前只读检查未见count/库存同ID审批碰撞，不能据此称全量历史已无异常。本轮只验证独立合成库，不签真实业务UAT。

HTTP3100登录修复上线、正式发布配套异地备份、三方数据资格/真实UAT、101条需求验收及D69–D71仍待。审批历史关联修好，不等于批次化工作流、全部库存写入新鲜身份/并发与异常历史修复均完成。

本轮旧3434/3435测试进程经owner、实时PID、工作目录、监听端口与启动时间一致核对后正常停止；保留数据库、构建和证据文件。最终3436/3437仍用于复查，未停止正式3100。
