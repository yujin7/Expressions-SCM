# 恢复单据导航与工作位置保留验收

日期：2026-09-14。精确应用候选 `edce015297a4c1f2b64d34bc20f5ba17624d6987`，分支 `agent/scm-recovery-navigation-20260914`；[PR111](https://github.com/yujin7/Expressions-SCM/pull/111)依赖PR110。**隔离候选已验，不代表正式3100部署、真实角色UAT或全部系统完成。**

## 本轮闭合的缺口

前轮[库存替代验收](库存错单替代关系与恢复验收-2026-09-14.md)记录：详情内原单/后继按钮保留筛选，但共享恢复区打开单据会丢掉原工作位置。本轮沿用现有单据URL选择，不建第二套路由或状态系统。

- 库存、采购退货、备货申请共享 `RecoveryDocumentLink`。同页正常点击更新准确docId，保留当前URL筛选、页码与锚点，不整页重载。原生前进/后退、复制链接和修饰键行为保留；复制/新标签使用渲染时URL，不承诺尚未同步状态或像素滚动位置。
- 从补货建议、先挪后买、调拨建议、自动补货候选跨页查看，携带有界 `workFrom`。只接受已知同源业务页和各页允许的筛选字段；外部地址、认证/API路径、重复返回参数、嵌套返回、创建/预填命令及凭据字段不进入返回目标。URL提示不是授权，目标API仍按当前身份读取。
- 详情头部显示“返回原工作台”，读取失败仍保留返回入口，隐藏不可用的业务操作。库存及CT创建弹窗打开已保存单据前关闭自身，避免详情被弹窗挡住。
- 文案区分“被替代原单”和“本次创建的单据”；浏览单据不代表确认创建结果，不自动清本机恢复记录、不重发、不提交、不审批、不作废或过账。
- 保存视图剔除临时 `workFrom`，自动补货候选补上必要的Suspense边界。未增加依赖、API、迁移、技能或业务写入口，候选迁移仍77项。

工作流技能要求回到原工作位置及显式下一步；React检查复核hook边界、读取上下文和原生链接语义；发布技能只在干净应用候选形成后启动。复用[Next.js官方支持的原生历史导航](https://nextjs.org/docs/app/getting-started/linking-and-navigating)，没有为此升级框架或引入状态库。7个canonical技能及CLAUDE既有规则足够，本轮不追加重复指南。

## 精确检查

本地完整门 **READY9/9**：804文件、8158测试，全量lint零错误/警告，应用/测试/脚本类型、运维、真实PG迁移合同、生产构建、HTTP34/34零跳过。回执 `.artifacts/verification/2026-09-14T01-31-26-999Z.json`，UTC01:26:39.178→01:31:26.999，起止干净同SHA；冻结 `.artifacts/runtime-recovery-navigation-edce0152` 编译身份相同。后续文档提交不冒称重跑应用门。

新增42项测试覆盖三类同页链接、各来源返回、恶意/重复/超长地址拒绝、命令参数剔除、saved-view隔离、修饰键、最新点击URL、弹窗回调、读取失败及伪造返回。既有恢复生命周期测试同步组件边界，不减少原业务断言。初次开发检查发现测试替身直接赋值window.location的类型错误，已修正替身；最终全量包含修正。

## 原生浏览器与数据库证据

证据目录 `.artifacts/recovery-navigation-20260914/`；独立3489、合成admin id3及合成PMC id232、1280×633和390×844。恢复夹具使用既有回执和已保存单据；BH读取夹具按既有键及原数量恢复，不作为可重发原意证明。没有重置正式账号，也没有新增业务单据。

1. 库存恢复GET找回81（RK-20260914-0029）：`q=RK&status=void&page=1`保留，docId准确，正常点击保留`#rows`；浏览器前退关闭详情、前进重开，同一页面标记仍在，没有整页重载。
2. 库存与CT分别从“修正原请求”打开创建弹窗，GET核对已存在81/48，再原生点恢复链接。弹窗可见数降为0，准确详情出现，原筛选保留；CT还保留page=2。0.0001与0.4准确显示，本机请求仍保留。
3. admin从先挪后买`q=KEPT&page=2`打开库存81，PMC从`q=BHUI&page=2`打开BH149，均带安全返回入口；点击返回后URL及可见搜索值匹配原值。手机页面scrollWidth=390，没有页面横向溢出；明细表自身可横向查看完整列，不把页面宽度合格当作所有表格已优化。
4. 真实不存在的库存ID2147483647返回404，详情明确“单据不存在”，返回先挪后买仍可见，业务操作不显示；不伪造API响应来代替失败场景。
5. 自动补货候选在生产构建中真实加载，核对原BH回执后给出`/outsource/bh?docId=149&workFrom=%2Freport%2Fauto-replenish`，无水合覆盖层；实际浏览器错误捕获为空。注销时一次CDP观察因页面导航中断，下一快照已在登录页，不当成登录失败。合成PMC先用了另一夹具密码被拒，按原夹具记录使用正确密码后登录，未改账号。
6. 记录的恢复/导航流均为GET，没有POST/确认/取消调用。前后8张表逐字段SHA256全部相同：stock_balances 131、stock_ledger 508、stock_docs 80、ct_docs 52、bh_docs 154、stock_create_requests 18、ct_create_requests 14、bh_create_requests 12。余额哈希仍 `ad8dcb6e42fde871c4489119493d8e4af68971dd1029c1b2f9086906ad74bfec`。不声称认证日志或读模型缓存完全不变。

## 发布与未闭合边界

PR111按111→110→…→55依赖交审；本轮远端main仍 `ee1c977e45b1c4e30961d2d6d6a4ea2068fefde2`，仓库私有。应用[云端34796320934](https://github.com/yujin7/Expressions-SCM/actions/runs/34796320934)8任务零步骤，Static checks 103830020481注释明确账户近期付款失败或额度需提高，任务未启动；不是应用测试执行后失败，也不以本地门代签云端。后续文档运行另验，当前状态看[NOW](../NOW.md)。应用候选未部署正式3100。

核对PID38394、冻结目录及3489端口后已停止独立服务；浏览器和有owner的隔离PG已停。保留所有截图、回执、冻结构建与合成数据库，没有删除共享依赖、业务文件或备份。

**仍待续接**：CT/其他来源单替代及退出、跨设备或损坏记录恢复、未保存编辑状态恢复、真实批次来源/工单占用、已过账财务库存纠错与全部真实角色验收。该返回提示不扩展用户权限，不等于任意页面状态完全恢复。正式发布仍需获授权DB+附件异地备份和窗口；原库61→候选77迁移及旧回执兼容边界不能因本轮没有迁移而忽略。当前状态只看[NOW](../NOW.md)，业务意图只看[CURRENT](../spec/CURRENT.md)。
