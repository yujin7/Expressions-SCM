# 决策工作室紧凑展示与读取生命周期复核

日期：2026-09-06。基线 `edbe078`；隔离分支 `agent/scm-studio-compact-20260906`。
本轮不是生产发布，不改数据库结构、金额/销量计算、数据授权或 D69–D71。

## 后续完整门禁发现与补修（17 时段）

`57a9176` 的完整门禁没有通过：4,757 / 4,758 项通过，1 项失败；后续构建及 HTTP 冒烟按门禁规则未运行。
排名错误态在精简时丢失了“窗口完整性未知”的说明。现在保留持续错误 + 重试，并补回未知窗口与不沿用旧排名的说明；原测试没有删改。

独立运营账号打开外部页还发现两个可重复的 403 错误卡，重试不能取得其权限。现按共享
`PRICE_VISIBLE_ROLES` 决定是否挂载这两项分析；未知身份暂不挂载，其他授权分析保持可用。
身份认领的两个导航复用 `ROUTE_REGISTRY.import_exceptions`，不向到不了目标页的角色提供按钮。
这只是用户体验边界，服务端继续回查身份和角色；没有改角色分配、会话权限或扩大 API 可见范围。
外部排名 API 的旧注释“件数全员可见”与原有守卫矛盾，已更正注释，未改守卫。

新增七角色及未知身份的展示反例（旧代码 5 失败）；首次补修测试又暴露了身份标签里的第二个入口，
补齐后 3 文件 / 93 项全部通过，含既有查询绑定、取消、错误重试和客户端边界。类型、lint 通过。
新提交的完整发布门与浏览器证据将保存在 `/tmp/expscm-release-57a9176.zFMDBr/`；
当前段落不把待执行验证预先记为成功。初始失败报告位于候选 `.artifacts/verification/2026-09-06T09-16-28-685Z.json`。

## 问题与修复

1. 外部需求标签前混排内部月度 KPI；来源不同而视觉关系含糊。外部、身份及能力页不再显示无关月度指标。
2. 缺证据时重复铺开 KPI、图表、空表。新增轻量 `AnalysisSection`，仅未就绪且无局部证据的分析默认收起；可展开查看原因和原内容，初次不挂载隐藏图表。
3. 有数据的独立来源不得被天猫缺流连带隐藏；有合法零结果不得按 falsy 判断折叠。由状态和证据判定，不修改服务端 readiness。
4. 窄屏筛选原先四行、说明过重；改为两列与可展开口径。告警按钮在窄屏移到正文下，避免中文被挤成窄条。
5. 平台列过窄使名称/状态逐字换行；明确列宽、单行和固定首列，宽表在自身容器内滚动。
6. 全渠道刷新原先只有 toast，旧值可能留存且迟到请求可覆盖新值。刷新清空旧结果，取消过期请求并校验归属；失败持续展示、提供显式重试，不自动循环。
7. 缺天猫宝贝损益证据时四项金额原先显示 ¥0。现在显示“—”，无证据净利也不再涂成功色。

不引入新依赖、不建立新缓存平台、不自动开启外部信号，不把观察量写进正式库存或补货数量。

## 验证与失败记录

- 先加 5 个反例：旧实现 35 通过 / 5 失败，失败分别对应上述展示与请求生命周期。
- 初次类型检查发现 `gate` 可为 null，而显示组件 reason 类型不接受；已修正可空类型，并完整复跑三套类型。
- 最终专项：2 文件 / 42 项，0 失败、0 pending（组件读取行为与客户端/服务端边界）。
- 全仓无缓存 lint：0 error / 0 warning。`git diff --check` 通过。
- 最终生产构建：274/274，Build ID `t6NSk-wvHaX16sdB93qV7`；standalone 安全检查通过。
  既有 jose CompressionStream / DecompressionStream 两项 Edge 警告仍在，没有隐藏或抑制。
  `tsconfig.json`、`next-env.d.ts` 最终哈希与构建前一致。
- 本輪未重跑全量 556 文件或 `check:release`。基线的 4,753 全绿不能作为本轮代码的完整发布证据。

## 浏览器到数据的证据

独立 Chromium 会话 + loopback standalone + 一次性 PG16 合成库，后台任务显式关闭。
没有读取或改写真实业务数据，没有连接外部账户。

| 验证 | 结果与边界 |
|---|---|
| QA 登录 → 决策工作室 → 外部标签 | 成功，观察 API 200；空库与页面缺流一致，不伪造数据 |
| 390×844 空态布局 | body 高约 9,171→1,633px，scrollWidth=390；7 个可展开分析，初始仅 1 张平台表 |
| 手机告警 | 首轮截图仍有按钮挤压正文，补修后截图确认按钮在正文下 |
| 1280×800 | scrollWidth=1280；平台名及状态高 23px、单行，不逐字折行 |
| 缺损益展开 | 四项金额均“—”；展开后仍无横向页面溢出 |
| 网络中断 → 刷新 | 浏览器主动拦截观察 API；旧平台行清空，持续错误及重试按钮可见 |
| 解除拦截 → 重试 | API 恢复后 3 个平台行出现，错误消失 |
| 独立 ready 零来源 | 浏览器仅替换响应，将流量来源设为 ready 且为 0；该来源保持展开，其他缺数来源继续折叠；没有写库 |
| 客户端异常 | 最终页面无捕获的 JavaScript 错误及框架错误覆盖层 |

局部测得空库观察 API 约 33ms，只用于链路核对，不作为真实容量或全站性能 SLA。
初始页面高度改善不等于 JS 下载减少；没有声称历史 643→539kB 数字仍适用于当前版本。

## 证据位置与交付边界

会话证据目录：`/tmp/expscm-studio-compact-20260906.Rx6t7t/`。
包括 `studio-red.json`、`studio-verified-tests.json`、`lint-final.log`、`build-verified.log`、
`studio-mobile-final.png`、`studio-desktop-final.png`。

基线隔离完整门禁报告：
`/tmp/expscm-main-reconcile-20260906.GhdYaW/candidate/.artifacts/verification/2026-09-06T08-35-44-241Z.json`。
这些临时证据路径属于本机，不是团队持久归档服务。

原项目工作区保持 HEAD `0a56c4b` / 173 个未提交路径；本轮末实际 3100 健康检查仍为 61/61、无漂移。
这不证明实际入口已部署新 UI，也不证明聚水潭、用友授权或真实业务闭环完成。
下一步：按精确提交补齐完整发布门与受控交付；用真实负责人、真实来源事项及真实规模数据完成 UAT。
