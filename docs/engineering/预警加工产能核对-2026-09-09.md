# G04预警加工产能核对 · 开发证据

## 06:22准确门与第二个返回缺陷

9008c0ee5d8c0eebf8f90a6cdc938df943aa1d64于06:18:28准确完整门READY9/9：634文件5764项、HTTP33/33，起止同一干净SHA。原始回执`.artifacts/verification/2026-09-08T22-18-28-754Z.json`，外层完整日志`full-release-9008c0e.log`；冻结3371同SHA64/64。

真实PG正命中脚本首轮等待document全部动画结束，页面无关loading旋转持续，误超时；Select实际80px高且两个可选项已显示。改为只等待Select浮层动画，不改应用或模拟响应。次轮通过源量身份日期/未映射无入口/人工数量/390、320、1280保持，随后可见搜索断言失败。实时DOM确认URL已为`/replenish?q=CAP-UI-SKU`，对应数据一行，搜索placeholder「搜索 SKU 编码/名称」的value为空；浏览器errors为空。原因是replenish-client的SearchInput未传q初值及更新绑定，下一轮需按现有列表模式修复。两次失败原件保留，不将未执行的清空/历史/角色后半段记为通过。

复查合成PG控制仍ledger0/work_items0/audits4/JG1/PO1/suppliers2；正式3100health61/61零漂移，原工作区仍173路径。此准确工程门不等于G04整链完成或正式部署；用户本轮要求的研究结论同步外层《总账独立核验与全局研究-2026-09-09-0600.md》，其中06:00截点保持，续验作为新增说明。

## 06:12正命中整链发现与续修（未部署）

`seed-g04-spike-positive.mts`校验合成容器label/63540回环端口、capacity_qa库、3370的ad1afb8/64迁移及源码无差异后，写入78条日观察和7条身份映射；复用CAP-UI-SKU为普通命中，另有大促、缺日、低基数、平稳、多店缺日和未映射控制。实际服务计算2个已映射/1个未映射，首次开3、重放仅刷新3；audit4/ledger0/work_items0/JG1/PO1/suppliers2前后相同。没有调用外部平台或写正式数据。

同版实际浏览器核对API与源量、日期、身份，未映射行只有认领而无产能；普通命中抽屉数量初始空，手输300.0001后与JG1100比较为1400.0001，390/320/1280保持输入结果。首次脚本没等Select浮层动画，点击被覆盖；原失败保留。修正等待后第二次在返回补货处触发真实失败：URL为`?sku=CAP-UI-SKU`，可见q输入为空。`g04-spike-browser-ad1afb8-attempt2-failure.json`保留整段证据，不算全链通过。

源码唯一错误生产者是sales-spike，库存预警/SKU hover已有q；改该生产者为q。补货服务端页面在挂载客户端前把旧sku转换成q，其他显式参数与重复值保留、显式q（包括空）优先，多值旧sku且无q则拒绝。旧分享/缓存链接因此仍可用；事实/规则未改变，不升读模型事实版本，不删除旧v3缓存。新回归在旧实现6失败17通过，修后5文件70项及三套类型通过。准确候选完整门、新冻结包、旧缓存及新链接的真实返回/清空/历史导航/权限仍待。

## 最新准确验收：ad1afb8（05:52，未部署）

05:51:42完整门READY9/9，633文件5757项、HTTP33/33；`.artifacts/verification/2026-09-08T21-51-42-610Z.json`起止同一干净`ad1afb8cb2267811e5cc62064996e5bea06d957e`。冻结3370返回同SHA、64/64零漂移。此处更新下方96e及开发历史状态，不删除其失败记录。

新冻结包采购登录、迟到旧回复/日期编辑、切厂缺依据、1920/390/320/1280输入结果保持、同SKU返回通过；第一段在已选值Select输入被自身显示标签覆盖时脚本停下，前面的迟到/日期断言已执行，改点可见标签后续验。结果文件明确late为空、关联前段失败，不伪造一次完整输出。

另一个同版脚本完整通过503/15秒超时→显式重试→PG真实结果，重试后activeElement为持久表单→Escape关闭；320px底部返回按钮left24/right154/bottom875.56在900高视口内。仓管新登录API403、页面无入口，重复ID/非法日期400，采购与仓管错误列表为空。原96e失败修复后，测试反例与浏览器事实均有证据；该焦点修复没有改共享错误组件或自动抢焦点。

证据目录`交付证据/2026-09-09-申报产能/`：`capacity-check-frozen-ad1afb8-result.json`、`capacity-check-frozen-ad1afb8-errors-result.json`、同名前缀脚本/截图及原失败；`full-release-ad1afb8-environment-corrected.log`为完整门日志。320表单与结果底部截图已目视检查。PG读回audit_logs仍4，stock_ledger/work_items仍0。旧3369进程按owner与实际cwd核对后停止，冻结文件未删除；3370保留供后续同版检查。

仍未证明：实际爆单正命中整链、真实产能协议、负责人跟进与结果留痕、Linux64及正式交付、101条业务UAT。正式3100仍61/61零漂移，未更改真实密码、数据、角色分配或D69–D71。

## 范围与边界

基于已验应用fc9fc0a/文档1c3e14e；本切片未正式发布，不能继承fc9的READY。角色采购、PMC、运营、管理员从库存预警行或已映射爆单进入；仓管/财务/品控不可读。复用`/api/outsource/sourcing-aid?mode=capacity`，原采购选源模式保留，无新表、迁移或第二套排产平台。

加工厂目录不是SKU准入清单，草稿JG不算已批往来；用户明确输入厂、交期、数量。复用JG未结全单计划量、申报正常/加班情景和历史P90，未知不变零，差额不是可承诺能力。返回`/replenish?q=<SKU>`不带数量，不写业务单据、库存、告警或工作项。完整责任人跟进与核对结果留痕仍是后续工作，不能把这条只读入口称为G04全链完成。

## 修复与防护

- 首轮夹具把两张JG放在同一工单/批次，唯一约束正确拒绝；改成两个独立工单，不弱化约束。
- 新服务真实错误：`text[] @> jsonb`，5条测试失败；改用Drizzle `arrayContains`匹配schema原类型，无数据迁移。
- HTTP拒绝重复/未知情景参数；服务拒绝半填、非法日历、负/零/过精度/越界数量、非成品或非加工厂。
- 数据按当前请求绑定，只有明确点击才计算；编辑原始输入就撤下旧结果，取消旧请求，15秒超时后人工重试；日期采用项目现有中文DatePicker。
- 320px浏览器发现网格被长加工厂名撑到390px。根网格改`minmax(0,1fr)`，内部表单按容器换列；不隐藏横向溢出来伪造通过。
- 抽屉在页签父层持有，不随桌面表格/手机卡片重建；按钮中文aria名称与loading状态分开。
- 原始输入失效首次采用全表单`onInputCapture`，浏览器复现数量输入400.0001后回退100.0001；改冒泡`onInput`在未决请求下仍不足。最终移除全表单输入事件，数量复用InputNumber原onChange；日期开始编辑（onFocus）即撤下结论，提交日期时再更新值。此问题仅靠mock回调单测没有发现，必须以真实输入/请求/结果三者一致复验。

## 开发验证

隔离PostgreSQL容器`exp-scm-capacity-20260909`（63540）、开发3368、64条迁移，SCM_RUN_JOBS=0；合成qa-buyer/qa-warehouse，不使用真实密码或写正式3100。

最终开发定向9文件101项通过；05:31:53研究复核另加历史反冲/期间锁/日期/脱敏/个人已读，14文件130项通过，4.31秒。此前59/93/48项为中间批次，不能相加冒充唯一测试总数。三套类型、改动lint曾通过，提交前再核对最终状态。

实际浏览器按阶段取得：

1. 采购登录→库存预警表SKU行→抽屉→手选厂/日期/数量→真实API/PG结果。300.0001+1100=1400.0001，申报1300、加班1560，差额分别-100.0001/159.9999。
2. 1920/390/320/1280保持输入和结果，抽屉scrollWidth=clientWidth；320结果底部按钮完整。原390检查曾未等断点完成，后续按实际宽度等待纠正；旧溢出截图保留。
3. 模拟503后无旧结果，人工重试读真实数据库；模拟不返回请求触发15秒超时。组件测试另验证编辑取消/迟到回复。浏览器延迟回复补验有独立回执。
4. 实际返回补货显示同SKU，不带情景数量；重新进入不复活旧情景。HTTP重复SKU/非法日期均400。
5. 仓管真实会话API403，库存预警页面无产能核对入口。隔离库audit_logs仍4（既有供应商修改），stock_ledger/work_items仍0。
6. 05:28独立干净浏览器会话在旧请求真实返回并等待稳定后确认新400.0001情景未被覆盖；日期编辑撤下旧结果、十月重新比较、切换缺依据加工厂显示不可比较，错误列表为空。旧会话曾出现rc-select最大更新深度错误，原件保留；不把干净续验说成所有历史会话无错，冻结生产包仍需同版验证。

## 原始证据与限制

### 96e3080准确门与键盘补验

05:42完整门READY9/9，633文件5756项、HTTP33/33；回执`.artifacts/verification/2026-09-08T21-42-26-179Z.json`起止同干净SHA。冻结3369返回96e3080和64/64零漂移，四宽度/迟到旧响应/日期编辑/缺依据/同SKU返回已有生产包证据。503、15秒超时后真实重试与320底部链接也通过，但随后Escape未关抽屉：重试按钮随error消失，activeElement变body，而rc-drawer键盘处理在面板上。新增反例在旧实现1失败25通过；修为重试前将焦点移入持久表单容器，不滚动、不劫持普通成功读取。此修复是新候选，须新准确门及实际键盘复验。

冻结测试还保留三类脚本失败：首次抽屉开启动画阶段未打开选项；同名aria标签匹配外层DIV而不是已展开INPUT；误写重试按钮名为“重新加载”而实际是“重试”。这些不当作应用缺陷，Escape焦点丢失则是已复现的真实缺陷。原回执均在外层同目录`capacity-check-frozen-96e3080*`，不改写成一次全绿。

外层证据目录：`交付证据/2026-09-09-申报产能/`。`capacity-check-browser-failure.json`保留初始窄屏溢出；`capacity-check-browser-failure-2.json`记录已通过的四宽度/重试及脚本误等`/replenish/sop`（实际链接是既有`/replenish`）；`capacity-check-browser-resume-failure.json`保留loading动画名称变化导致的脚本停止；`capacity-check-browser-resume-result.json`为后半程补验。不是一次全绿脚本。

Turbopack因现有node_modules符号链接越根而退出，改用项目已有Webpack方式；没有重装依赖。Next启动生成配置差异需逐项恢复；只控制本次3368进程。

当前真实合成库存行没有销量告警，证明入口和情景链，不冒充爆单正命中或真实业务UAT。已映射爆单入口仍需同版本正命中验收；无映射先认领身份，服务拒绝非成品。正式部署、真实协议、负责人跟进/关案与原101条剩余继续，不改D69–D71。
