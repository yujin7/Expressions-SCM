# 采购操作资格与原子提交 · 2026-09-13

## 结论与版本

应用 `bcd8d6eafbd25c1a00a46df9ee47b3dc4e99ff7e`，分支 `agent/scm-po-workflow-authority-20260913`，[PR67](https://github.com/yujin7/Expressions-SCM/pull/67)，基于PR66文档锚`5b2c8b44`。**隔离候选READY9/9；未正式部署、未签真实业务UAT。** 当前状态只看[NOW](../NOW.md)，业务意图只看[CURRENT](../spec/CURRENT.md)。

按流程体验技能整理操作资格和失败出口，按写路径技能保护当前身份、版本、锁和审计，再按发布技能核验精确候选。复用既有AntD5、审批配置、金额可见权威和读取组件；无新增依赖、迁移或API路径，不复制技能/状态权威。

## 修复内容与不变边界

| 原缺口 | 本批修复 |
|---|---|
| PO提交、内部代录的状态变更和审计分开提交 | 同一事务内当前身份、PO锁、状态/版本及审计一起成功或回滚 |
| R1先建改价单，最后才发现PO版本过期 | 先锁PO并核对版本，再计算逐行R1；同PO并发复用已提交的同价待审PC |
| 价格异动有意返回409，但界面靠中文文本猜分支 | 明确`PO_PRICE_REVIEW_REQUIRED`；PC及审计提交后在事务外返回409，PO保持草稿；错误提示提供PC列表入口 |
| 旧页面/旧调用角色仍尝试提交、审批、撤回、确认或生成链接 | 五类写端事务内回查启用、会话和当前角色；PO串行锁持有到提交 |
| 定制审批角色看不到采购金额仍能批准 | 新批准还需既有金额可见权限；缺权限整笔回滚。合格审批人的驳回和既有准确轮次重放仍保留 |
| 按状态展示按钮，制单人可看到自审或无权限操作 | 详情下发当前配置、制单分离、角色与金额权限计算的资格；UI只显示合格动作。写端仍独立强制校验 |
| 六种动作分散在Drawer按钮、独立弹窗及浏览器prompt | 详情正文统一操作区；确认框绑定单据/版本，忙时锁关闭、同步拦重复点击，失败先读取核对不自动重发 |
| 代录容易误解为供应商已回传交期 | 明确只记录内部备注并进入执行中，不改逐行承诺，不证明供应商回传 |
| 生成链接立即自动复制，旧链接失效缺少说明 | 先说明30天/一次使用/重生成失效，仅展示同源确认路径，用户明确复制；非法ID400，成功响应private/no-store |

R1仍使用原税价归一、容差、采购基准和同价批准PC规则；未改变数量、成本公式、库存过账或会计事实。**只证明同PO竞争的串行决策，不声称跨PO价格基准或全部主数据全局冻结。** 内部确认仍仅approved→in_progress；生成链接仍是每次明确调用产生新令牌并废除旧令牌，不声称幂等生成。

读取资格仅是指导，不是不可变能力凭证。提交/审批/代录成功后GET核对当前状态；当前状态不证明跨设备原请求身份。失败锁只在当前挂载内保留，刷新后重新读取事实；本批未新增通用跨设备持久回执。共享完成/短关原有持久标记继续由[收口恢复](采购与工单收口恢复验收-2026-09-13.md)管理。

## 自动化与真实并发

- 完整门：`.artifacts/verification/2026-09-13T02-27-08-805Z.json`，起止干净同SHA；**736文件/6964项，HTTP34/34零跳过，READY9/9**。运维、PG迁移、全量lint、三类类型检查及生产构建通过，67迁移零漂移。
- 定向7文件/56项通过；新增30项覆盖五类审计失败回滚、停用/过期会话/伪造角色、过期版本无PC、R1竞争和回滚、盲审拒绝及驳回出口、UI双击/换版/撤权/卸载、POST后GET失败、结构化R1及安全链接。
- `verify-postgres-settlement-basis.ts` **49/49**：原45项保留，新增四个实际双连接锁等待场景：R1竞争复用一张PC及一次审计；撤回阻止迟到批准；撤采购角色阻止代录；PO关闭阻止迟到生成链接。夹具`b3e26e11`，日志`.artifacts/po-workflow-postgres-20260913.log`。不是PGlite的并发模拟，也不是正式库测试。
- 构建先提交应用再锚定；独立冻结`.artifacts/runtime-bcd8d6ea`服务于3464，全量门另在`.next`构建，无覆盖正在运行构建。构建自动生成的两处配置差异按已记录基线逐项恢复，最终门起止无脏路径。

## 浏览器 → API → 数据库

只用自有loopback3464、隔离`scm_contract_quickwins_20260911`和合成账号；后台任务关闭，正式3100及真实凭证未动。

1. 合成PMC读取他人草稿PO194时不出现提交按钮；合成admin可以提交。实际提交pending/version2，再由非制单admin批准approved/version3；submit/approve审计各一次。
2. PMC对PO196代录，浏览器只在实际POST200完成后故意丢弃响应。确认框显示“操作结果待核对”、禁止再次确认；点击恢复只GET200，无第二次POST。准确读回in_progress/version2、原备注；confirm审计一次。
3. PO196明确生成链接，POST201、一次gen_confirm_token审计；界面展示可复制的同源链接，无自动导航/自动发送。没有向真实供应商传送链接。
4. PO195实际提交触发结构化R1，采购仍draft/version1，仅一张待审PC51；确认框显示“采购价格需审批”和查看PC入口，不把409误报成成功提交。刷新核对不重新提交。
5. 1280px桌面错误状态截图检查通过；390×844价格异常恢复框宽374、左边距8，pageWidth390，按钮/文字可见，无整页横向溢出。只代表本操作区，不宣称全站美学验收完成。
6. 独立Codex浏览器在PO194打开代录框，Escape取消后焦点返回“确认（代录）”，未执行写入，warning/error日志为空。测试回调不能代替此项实际AntD验证。
7. 浏览器阶段库存流水**374→374**；上述PO194/196状态和审计各一次，PO195保留唯一待审PC。没有补库存或改原已收数量。

证据目录`.artifacts/po-workflow-authority-20260913/`：`build.log`、`release.log`、`server.log`、`database-before.json`、`database-after.json`、`browser-requests.json`、`lost-confirm-desktop.png`、`r1-recovery-mobile.png`、`login.png`。浏览器故障注入和连接包装只存本地验收目录；不提交凭证、令牌或运行数据。原始截图/本地回执不宣称已上传GitHub。

## 交付、清理与下一步

- PR67基于未合并PR66；66→65→64→63→62→61→60→59→58→57→56→55为既有依赖。主线观察仍`ee1c977e`，不自动合并到功能分支。依赖合并后重新锚主线验收。
- PR67应用检查[34733134447](https://github.com/yujin7/Expressions-SCM/actions/runs/34733134447)失败注释已复核：Static checks及Tests(1/4)均明确因账户付款/额度问题未启动；不是测试跑完发现断言失败。须所有者处理，不能用本地READY替代受保护线上检查。
- 文档收尾：技能治理19项、钩子安全13项共32项通过；本批5份文档81个本地Markdown目标均存在。NOW维持单一当前状态，未新增知识或技能副本。
- 自有3464的PID/cwd/端口已核对后停止；两套CLI浏览器、两张Codex测试标签及隔离PG停止。保留数据库、证据及冻结包，不删数据或共享依赖。
- 正式交付仍需获授权的异地DB+附件配对备份位置、访问方式和窗口；之后正式Safari本机HTTP/公网HTTPS、首次改密和真实角色试点另验。没有为本批重置密码或绕过首次改密。
- 继续真实齐套来源/占用资格、冻结财务/库存差额纠错、其他生成写端、跨设备失败对账和完整真实角色链。聚水潭/用友真实接口授权及源资格未由本批解决，不把测试成功当外部业务接入成功。
