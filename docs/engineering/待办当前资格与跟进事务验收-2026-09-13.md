# 待办当前资格与跟进事务验收

2026-09-13。候选工程验收，不是正式部署或真实业务签认。

## 精确候选

| 项目 | 证据 |
|---|---|
| 应用 | `72a0542de58705b932357b06672f8dee117f5fc3`，`agent/scm-todo-current-authority-20260913` |
| 评审 | [PR80](https://github.com/yujin7/Expressions-SCM/pull/80)，基于PR79；80→79→…→55仍待按依赖整合，不自动合并功能分支 |
| 完整门 | READY9/9；752文件/7,274项；HTTP34/34，失败0、跳过0；全lint、全部TS、生产构建与70迁移通过 |
| 稳定性 | 08:32:14.212Z→08:37:53.361Z，起止干净同应用SHA；后续文档不是新SHA全门 |
| 隔离运行 | 3470冻结包`runtime-72a0542d`，health准确revision、70/70零漂移、后台作业关闭 |
| GitHub | [34747970621](https://github.com/yujin7/Expressions-SCM/actions/runs/34747970621)8个job均无步骤；平台注释明确账户付款/额度问题，远端CI未通过 |

本机回执`.artifacts/verification/2026-09-13T08-37-53-361Z.json`，截图`.artifacts/todo-authority-20260913/`。这些忽略目录保留本机，不声称随Git推送。

## 反例与修复

创建、状态/改派、跟进追加和历史读取原先使用事务外传入身份；锁定待办并不意味着保存者仍有资格。新测试在旧应用a400da60上16项均失败：停用/会话失效仍创建或写入、旧管理员角色与旧部门范围继续生效，反向真实管理员被旧仓管角色误拒绝。前期3项范围夹具因未填createdBy先报约束错误，补齐夹具后重新确认全部16项语义失败，未把夹具错误算漏洞。

四入口现于拥有事务开头复用`currentWriteActor`，核对当前启用身份、已提供会话版本和数据库角色；修改/跟进/历史再从数据库加载范围。身份共享锁持有到事务提交，待办原有更新锁/历史共享锁不变；锁行为参考[PostgreSQL显式行锁](https://www.postgresql.org/docs/current/explicit-locking.html)，实际竞争另验。无操作与准确旧跟进重放也须当前授权。仅全员手工创建继续开放；alert/review来源声明必须当前管理员，不依赖早先HTTP归一化时的角色。

现有可见性公式、直接责任关系例外、改派/状态联合事务、审计与通知outbox、来源去重与七日重开政策均保留。来源创建仍是已有系统投影或当前管理员入口，不证明来源本身真实有效。没有新迁移/依赖、自动均分负责人、真实消息发送或库存写入。完成待办与确认/关闭来源告警仍是不同事实。

旧测试同步真实权限来源：部门/渠道范围写入测试DB，另一位管理员冲突测试先真实改角色再恢复；只读历史指派夹具单独建档，来源投影由合成管理员创建，实际计划员仍负责完成。没有删除拒绝、范围、幂等或来源独立性断言来求绿。

## 从并发到页面

相关19文件173项通过，新增16项；原状态机、审计/outbox故障回滚、投影、历史分页及产能历史渠道隔离均继续通过。全部TS与目标lint另通过。

`verify-postgres-todo-authority.ts`仅显式启用的loopback `scm_contract_*`库，三连接/提交屏障/真实Lock等待。精确应用夹具`6b0c9d94`：账号257、待办6；7场景7次实际等待：停用先持锁时创建/修改/跟进/历史四入口等待后403；状态与跟进先持锁时停用等待审计提交；同跟进请求竞争只返回原2038。最终仅create2036/update2037/follow_up2038，任务in_progress，无第二任务。库存414、来源告警4、通知51均不变。开发夹具b634f9db/待办5保留，不混作精确候选。

实际浏览器在冻结3470正常合成管理员登录，经新建表单创建手工待办7，明确本次为合成验收。输入跟进并保存2040，完成待办后准确提示不足10分钟的“可疑”标记，再打开历史：create2039/follow_up2040/complete2041；数据库done且completedAt存在，与页面一致。跟进独立保存时不改任务状态；完成后来源与库存未变，自己指派给自己不额外通知。

已查看`completed-desktop.png`（1280×633）、`history-narrow.png`（390×844），根级宽度分别1280/390，历史正文和状态清晰、浏览器错误/控制台为空。工具按“保存”猜测控件未准确命中，改用当前快照的真实“保 存”按钮后正常点击；命中检测确认按钮未被覆盖，不作为布局缺陷。此证据只覆盖该任务流程，不是全站UI、真实角色或正式Safari验收。

## 真实发现与下一步

新建在旧筛选`#6`中成功后仍停在原列表，新任务7不可见，只能再次搜索标题。这是已观察到的体验缺口：下一直接工作应将手工创建请求持久化、准确回执回链和保留列表上下文一起闭合，而非仅加成功toast。

跟进的pending请求仍在组件内存，刷新可能丢掉未知结果的原键；需要同样独立恢复，不能把产能专用恢复等同于普通跟进已完成。手工创建尚无完整请求幂等；状态更新读回当前状态不证明原请求结果。来源重开竞争、负责人并发变化及全部范围表的冻结快照仍需逐一续接。

核对PID99041的cwd为自有冻结包且监听3470后停止，关闭本轮浏览器、停止已核对owner/65411的隔离PG；保留库/审计/截图/回执/冻结包，不删除证据。未修改正式3100、密码、隧道或外部平台。

正式备份/发布窗口、依赖整合、正式HTTP/HTTPS登录、总监101条和三方真实业务数据验收仍看[NOW](../NOW.md)。意图登记[CURRENT](../spec/CURRENT.md)，脚本保护见[脚本说明](../../scripts/README.md)。不新建平行AGENTS/CLAUDE/技能状态文件；全目标仍未完成。

文档收尾：技能治理/审计台账2文件748项通过，4份本轮文档86个相对链接目标存在，`git diff --check`通过；NOW为83行/13,968字节。不等于全部历史锚点审计，不累加到应用7,274项。
