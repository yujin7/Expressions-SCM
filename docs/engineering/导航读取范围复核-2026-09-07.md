# 导航读取范围一致性

目标：受限用户从搜索、列表、详情及待我审批进入同一组允许读取的备货申请，不因导航绕开D62。
本批不变更审批写权限、派工、数量、库存、价格、数据库schema或业务决议。

## 证据与修复

- 基线5d5f776：BH列表已有本人/共享渠道限制；searchAll不接actor，getBh只按id，collectBh不接范围。
- 复用`core/bh-read-scope.ts`：admin或未登记范围保持现状；显式空数组仅本人；非空范围加共享渠道。
  范围在LIMIT和详情/明细读取之前应用，越界详情用同缺失对象的404，不确认对象存在。
- HTTP列表、搜索和详情的会话传递有行为回归；收件箱沿用新鲜会话和原审批域/SoD分区。
- `inbox/read-access.ts`复用现有路由注册表。搜索和收件箱不暴露角色不可见、或受限用户禁止的目的地摘要；
  它是路由级门，不替代BH行级SQL裁剪，不是新建权限策略平台。
- `CLAUDE.md`补三行读取范围纪律；`AGENTS.md`仍是其符号链接。7技能和两权威入口无需重建。

## 定向与浏览器验证

5文件51项通过，三套类型及改动源码lint通过。范围测试覆盖：本人/共享/外部渠道、隐藏结果超过3条且排序在前、
精确隐藏单号、猜测ID、显式空范围、管理员例外、HTTP传递、审批制单分离、受限PC摘要和JS角色/范围组合。

QA：`exp-scm-qa-edbe078-20260906`，端口65364，独立`scm_read_scope_20260907`库；从既有合成QA模板复制，
不使用实际SCM业务库。夹具新建三个合成账号和两个渠道、六张BH及一张PC，后台任务关闭。
Chrome登录受限计划员，1280×844与390×844，真实API验证：

- 搜索返回OWN/PEER两条，列表total=2；越界id=6返回404，本人详情200。
- pending仅PEER、submitted仅OWN；PC搜索为空，不从收件箱旁路出现。
- 桌面搜索与手机快捷跳转呈同一结果；列表可打开本人详情；截图等待抽屉进入视口后采集。
- 管理员详情读取200、搜索仍取排序前三条；无未捕获pageerror或console error。

使用同一QA库和旧5d5f776生产冻结包在3340执行同一浏览器断言，首个搜索断言失败：返回三个外部渠道单号，
而非OWN/PEER。这是产品反例，原始失败JSON保留。开发候选3339通过。

准备期修正过测试夹具的PC枚举、必需SPU外键、SQL roles数组类型，以及登录按钮正则同时匹配飞书按钮的问题。
它们是测试准备错误，不计作产品缺陷；SQL失败事务未提交。首次详情截图在动画中，后补正向宽度/视口边界等待再采集。

## 交付边界

本批精确提交与完整门结果以外层机器回执为准，开发浏览器和51项回归不代替全量发布门。
证据目录：项目外层`交付证据/2026-09-07-导航读取范围/`，含SQL夹具、浏览器脚本、旧版反例与候选JSON/截图。

尚未部署实际3100，未推送、未改原工作区173路径、真实账号、数据、通知、外部连接或D69–D71。
未宣称全站读写权限审计完成；本批验证限定于上述读取链。单据/NPD精准深链、BI行动与全站紧凑体验继续推进。
