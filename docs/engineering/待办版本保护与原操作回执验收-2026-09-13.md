# 待办版本保护与原操作回执验收 · 2026-09-13

## 结论与边界

应用锚：`1dd1b88e64e6ebe6914e3f499df763218b392ac7`。本轮完成**后台原操作回执、版本冲突保护与界面版本提交**，不是完整的跨刷新恢复界面。正式运行3100、生产库、外部连接器、备份与密码均未改动；没有发布或业务签认。

原缺陷可证伪：完成请求回包丢失，任务被另一次操作重新打开后，旧请求再次提交会再次完成。只比较“当前状态是否等于目标”不足以识别原操作，毫秒时间也不能识别同一毫秒内往返。

## 本轮改动

- `work_items.version`默认1；实际手工状态/改派及系统指纹重开均在原事务内递增。无操作不改版本或更新时间，保留七日重开口径。
- 公开PATCH必须同时提供UUID原请求与所见版本。未知字段、半对参数、非法版本返回400；新键但版本过期返回409并指引核对当前责任人/状态。内部现有包装函数保留原拥有事务契约，仍递增版本，不假装已经具备所有调用方幂等。
- 原请求绑定不可变的版本、目标状态、目标责任人和备注；重放核对原意、当前身份、当前范围及原保存者，不拿当前可变任务替代原意。
- 一次联合改派/状态操作只给其中一条已有审计附加回执，保留原有两类审计；无操作也追加一条非状态流转update回执，防止之后重试变成新的动作。
- 同键重放返回原结果与当前任务两个明确对象，不再次改派/关闭/通知；只读GET等待原请求事务，`private, no-store`。没有原回执时返回未找到；已找到但不完整的回执明确拒绝，不猜测回填。
- `0071_todo_mutation_receipt.sql`只增加版本列、正值约束和审计部分唯一索引；0070→0071快照逐差异核对只有这三项，没有删除手写外键。候选72迁移，A726及台账正文/导航/控制测试同步。
- 界面开始、完成、取消、重开及改派共用操作函数，发送UUID与列表版本；过期页面不会静默覆盖后来任务。未新增依赖、通用请求平台或全站样式。

## 验证证据

### 聚焦与反例

新增`tests/todo/mutation-recovery.test.ts`13项，覆盖完成后重开再重放、同毫秒往返、新键旧版本、无操作回执、联合改派/状态、原负责人停用、四个原意字段冲突、当前账号/版本/权限、不同任务重用键、直接唯一约束违规、审计失败全回滚、系统指纹重开和真实HTTP契约。已有联合写入测试继续验证通知失败全回滚。

现有`todo-mutation-authority`增加4项有界版本门及违规注入；范围是当前两个拥有函数，不宣称能识别任意动态SQL。主/测试/脚本三套类型检查和零警告lint通过。初次两项旧HTTP无操作测试仍断言“无审计”，按新契约更正为“只多一条非状态回执；任务与通知完全不变”，不是放宽原子性。

### 隔离 PostgreSQL

目标容器`expscm-quickwins-pg-20260911`，owner `agent-scm-quickwins-20260911`，仅127.0.0.1:65411的`scm_contract_quickwins_20260911`。迁移前71、后72；没有对正式61迁移库执行。

`scripts/verify-postgres-todo-mutation.ts`三连接+提交屏障，7场景、6次真实Lock等待：

1. 同键同时完成：一次修改、同一不可变回执。
2. 重开先持锁：旧完成重放等到提交，当前保持open/v3，原回执仍done/v2。
3. 只读结果查询等待原写入提交。
4. 两个不同请求针对同一旧版本：仅一个生效，另一个409。
5. 当前账号停用先持锁：结果GET拒绝。
6. 当前账号停用先持锁：重放拒绝。
7. 越过service直接重复写审计（UUID大小写变体）：数据库23505。

合成fixture `a05d4975`，账号263，任务21/22/23，回执2066/2069/2071；保留证据。库存流水414、来源告警4、通知51，前后相同。

### 实际浏览器 → API → DB

自有冻结运行3473，精确上述应用锚，合成管理员；并非正式Safari或公网登录验证。

- 真实登录表单进入工作台，待办页正常水合。API准备合成任务24（创建审计2073），实际点击“完成待办”发出版本1与原键`4eec8cd4-6f21-4b89-aafe-5bd5b59ef412`，200/done/v2，原回执2074，界面显示成功及短时关闭提示。
- 用合成API操作将任务重开为open/v3（2075），重放原完成键仍返回原done/v2回执及当前open/v3；GET同样区分两者。这里是浏览器真实HTTP验证，不冒称已实现恢复按钮。
- 不刷新旧列表，实际点击旧页面“重新打开”：发送所见v2，409；界面明确“待办已被更新，原操作未执行；请刷新并核对当前状态和责任人”，提供准确任务链接。
- 1280×633及390×844截图已人工查看，窄屏根宽390，无横向溢出；没有浏览器脚本错误或框架错误层。不是全站UI审计。
- 独立SQL核对任务24最终open/v3/completed_at=null，仅create/complete/reopen三审计；旧重放、只读GET、过期新请求均未增加审计或通知，库存/告警/通知控制总量仍414/4/51。

证据保留在`.artifacts/todo-mutation-recovery-20260913/`：浏览器JSON、三张截图、数据库断言、构建/类型/lint/完整门日志与隔离包装器。冻结包保留；清理只停止本轮已核对拥有的运行与浏览器，不删除合成库或验收材料。

## 下一步，不要误报完成

1. **首要工程续接：状态/改派原请求的账号级本机持久保存、跨标签串行、刷新不自动重发、独立只读核对与明确确认清理。** 当前页面每次点击产生新键；版本保护已防旧视图覆盖，但它还不能替用户找回关页前丢失的原键。不能把后台GET或此次测试里的开发者核对当成用户可用恢复流程。
2. 恢复界面必须同时标明“原操作已保存”与“当前任务后来已变化”；不以原done回执声称当前仍done，不能因旧回执成功再修改任务。
3. 内部包装函数/上游资格全部竞争、跨设备/损坏记录自助对账、请求取消、真实业务全链仍待后续。
4. 发布仍需授权异地DB+附件备份与窗口，依赖PR按序整合到main后重验；GitHub平台检查未启动不等于代码失败或通过，不改账户付款/额度设置。

## 精确完整门与交审

2026-09-13 10:09:10.978Z→10:14:14.779Z：起止干净同一应用SHA，READY9/9，756文件/7347项、HTTP34/34零跳过。回执：`.artifacts/verification/2026-09-13T10-14-14-779Z.json`。新增18项（13行为、4版本护栏、1迁移目录控制）。后续文档提交不冒称其SHA重新跑过应用全门。

[PR83](https://github.com/yujin7/Expressions-SCM/pull/83)基于[PR82](https://github.com/yujin7/Expressions-SCM/pull/82)，仍按83→82→…→55依赖交审，不自动合并到功能分支。本轮[GitHub应用检查](https://github.com/yujin7/Expressions-SCM/actions/runs/34751355944)已见8个job、0个执行步骤；Static checks注释明确账户付款失败或额度不足，未启动执行。不是代码已通过，也不以本地门代替平台门；由账户所有者处理。

自有3473/PID44804已按cwd/监听端口/启动时刻核对后停止；浏览器关闭，隔离PG停止，冻结包、数据库和证据均保留。正式3100未操作。
