# 知识收尾与清理审计

日期：2026-09-11。起点 `e7c38e76`，分支 `agent/scm-workbench-evidence-20260907`。这是维护审计，不是新的应用READY、生产发布或全项目缺陷清零。

## 已完成

- 补齐[库存提交验收](库存提交身份与事务验收-2026-09-11.md)，将NOW“全门待完成”替换为准确候选回执；仍区分PG提交前测试与干净候选、浏览器与正式部署。
- NOW保留登录未解决：此前Safari HTTPS成功不证明随后用户问题已消失；最新中断检查只到登录表单。未重置密码、绕过改密或发布候选。
- 保留 `AGENTS.md → CLAUDE.md` 和七个 `.agents/skills → .claude/skills` 映射；没有另建agent.md/skills.md/SSOT副本。更新现有技能的收尾路由与安全清理约束。
- 技能参考去掉陈旧的19/33条契约与缓存版本“当前”副本，改读NOW和实际代码；纠正“必须进入supply-chain子目录”的工作树误导航。
- docs总导航不再复制外部平台的时点可用性；新增本报告、验收报告入口。变更史仍用Git，未再创建一份CHANGELOG。
- `clean:cache`原来无预览直接删除，`--all`还包含`.artifacts`和`tmp`。现默认只预览，写操作要求显式目标；检查占用失败即拒绝，目标/隔离区符号链接拒绝；移存可恢复且不自动永久删除。
- 修正`getDb()`弃用说明：实现始终抛错，不存在所谓“PG同步可用”。保留显式错误兼容入口，不改应用行为。
- 核对3438的owner、PID31328、启动时间、cwd和监听后，停止本任务已验收测试服务；保留冻结包及合成库。正式3100镜像/启动时间不变。

## 目录职责与保留决策

| 位置 | 职责/权威 | 本轮处理 |
|---|---|---|
| `docs/NOW.md` / `docs/spec/CURRENT.md` | 当前状态 / 业务意图与裁决 | 状态替换；本轮无新业务裁决，不重复改CURRENT已登记库存规则 |
| `CLAUDE.md` / `AGENTS.md` | 一份操作约定、同目标链接 | 小幅增加清理安全边界 |
| `.claude/skills` / `.agents/skills` | 七份正文 / 发现链接 | 更新现有包；不保证每次自动激活所有技能 |
| `docs/engineering` / `docs/skill-history` / `NOW-history` | 分主题证据 / 非执行历史 | 补回执并导航；历史保留，不批量改日期或完成标签 |
| 外层`supply-chain/` | 原始Git工作仓，存在用户/其他会话改动 | 不覆盖、不合并、不剪依赖 |
| 外层`worktrees/` | 分支隔离开发 | 当前工作在明确分支，其他工作树不动 |
| 外层`spec`、PRD、Images、完整项目包及zip | 旧规格、原输入、归档交付 | 保留原路径；旧包不能用来推断当前部署 |
| 外层`交付证据/` | 日志、回执、冻结构建、恢复证据 | 保留路径以免旧回执失效 |
| `.data`、uploads、backups及SQL语句命名的外层目录 | 可能是数据库/原件/误命名数据目录 | 不从名字判断垃圾，不读取内容到日志、不删除或移动 |
| `.next*` | 23个生成目录，约12GB（du取整观察） | 本轮只预览；多目录归属未逐个证明，不批量回收 |
| `.artifacts` | 验收回执约692KB | 从清理白名单移除，必须保留 |
| `node_modules` | 链到原始工作仓的共享依赖 | 不执行prune/install；不能把npm extraneous当作可删证据 |
| `.cache-cleanup-trash` | 新的本地可恢复移存区，Git忽略 | 测试在隔离夹具验证；真实项目本轮没有移存缓存，不声称释放空间 |

## 代码、依赖与兼容路径审计边界

核对package脚本、清理调用入口、直接依赖清单、Git跟踪入口、弃用标记、技能链接和现有安全门。`npm ls --depth=0 --omit=optional`退出0，但共享依赖链接产生大量extraneous条目；未据此删包。本轮未重建干净依赖树或执行远端漏洞扫描，不能声明依赖无漏洞/全无冗余。

`src`/`scripts`的同步`getDb`搜索仅见拒绝入口；测试中的`mocks.getDb`是别名，不是调用者。保留显式失败入口；若后续删除，需确认动态/外部消费者并补导入/构建证据。旧采购行唯一PO×SKU、认证跨协议cookie和用友响应形状兼容承载历史数据/入口，不能作为“遗留”一删了之。这里是保留决策，不声称全部handler逐个重新验过。

一次性导入脚本的“停dev即可重跑”和“搜索空即无引用”指引已纠正。动态注册、CI/hooks、文档操作、外部消费者和历史数据用途都要检查；迁移文件即使源码无直接import，也属于执行路径，不能删。更深的死代码/依赖替换须单独形成可验证候选。

## 本轮验证

- 缓存安全11项 + 技能治理19项，共2文件30项通过；覆盖只预览、保留数据/证据、移存可恢复、错误参数、占用/检查异常、符号链接拒绝。进程检查使用合成lsof响应验证分支，不宣称消除所有并发启动竞态。
- 扩大架构检查发现新增测试漏登记（80文件通过、1文件失败）；已新增稳定控制A713，同步控制台账分类/合计、CURRENT索引与测试计数。重跑81文件/1116项全部通过；保留原有双向一致性门，没有删断言绕过失败。
- 应用、脚本与测试三个TypeScript作用域通过；全量lint零错误/零告警。第一次测试参数写法导致类型失败，已修正后重跑通过。
- 实际`clean:cache --all`只预览，没有删除或移动真实缓存。使用者必须先停owner并保持停机，lsof不能替代协同锁。
- 相关文档62个链接、外层导航5个链接均解析；技能元数据校验通过。NOW为84行/约11KB。隔离区已被Git/Docker/lint排除，实际ESLint路径判断通过；三份TypeScript配置使用显式源码路径，不纳入隔离区。
- 原应用READY锚仍为e7c38e76，新维护脚本不借用该门冒称新全门。正式3100健康61/61无漂移；未部署新应用。外层导航和用户授权的记忆更新说明位于Git外，不能从仓内提交推断两者已推送。

## 仍需续接

1. 当前用户登录问题与正式HTTP cookie修复的实际交付；需配对异地备份位置/访问方式和发布窗口，不绕发布保护。
2. pending CA异常待办、制单人审批按钮、详情标签布局；其他库存写端身份/并发、完整批次业务链。
3. 聚水潭逐接口权限/IP白名单、用友业务授权/租户组织、简道云逐流核对及真实UAT；token不等于业务可用。09-11运行日志仍有聚水潭110白名单失败，但未重跑全平台探针。
4. 其他生成目录的逐个owner/引用/保留期确认、干净隔离依赖审计；不对未查部分写“全部清理完成”。
5. D69/D70/D71、101条逐项签认和真实任务闭环继续由NOW导航，不因维护整理丢失。
