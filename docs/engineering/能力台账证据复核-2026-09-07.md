# 能力台账证据复核 · 2026-09-07

## 目标与边界

解决后续开发和审计被失效引用误导的问题。主责为工程维护，不涉及库存、身份、财务、派工、
审批或外部授权的业务变更。基于独立候选 f389751，保留真实3100服务与原工作区。

## 证据与修正

| 项目 | 原失效引用 | 已直接核对的真实证据 |
|---|---|---|
| C001 | tests/core/decimal.test.ts | src/server/core/decimal.ts；tests/redteam/decimal.redteam.test.ts：定点精度、正负舍入、大数与安全拒绝 |
| C005 | src/server/posting/reversal.ts | src/server/posting/post.ts 的 reverse、inventory/stock-doc 服务；tests/inventory/count-adjust-reversal.test.ts：盘亏100→90→100、盘盈回原值、二次冲销拒绝 |
| C112 | rules/backtest-fva.ts | src/server/rules/backtest.ts；report/forecast-accuracy 服务消费 fvaLabel；tests/rules/backtest-fva.test.ts：朴素基线、正/负增益、样本不足与全零实际 |

按 Git 已跟踪文件逐一定位其余明确文件简称；唯一匹配才改路径。流程挖掘通配符展开为规则、
服务、架构三份确定测试。未改任何能力编号、名称或状态；不是删除无法证明的能力来凑绿灯。

## 自动检查

复用 tests/architecture/capability-ledger.test.ts，不新建元数据清单或生产依赖。

- 保留原连续编号、独立名称、状态合法及完成数下限检查。
- 新增明确 inline-code 文件引用的精确根路径/规范化/无通配符/文件存在检查，每行多引用逐个检查。
- 标题与 CURRENT 入口总量、完成/部分/待做/阻塞四项汇总从真实行状态核对。
- 负例重新植入历史坏路径、简称、通配符、越界路径、陈旧行号、第二个缺失文件与伪增完成数。
- 路由、函数名和表字段不误当文件。显式文件导航仅覆盖56行65个引用（62个不同文件）；
  其他146行不是机器可验的文件清单。文件可定位不意味着对应测试足够或线上可用。

## 红绿验证与后续

新增门在原台账上失败：1失败/16通过，列出49处不符合新导航契约的引用；其中3处为实际失效文件，
其余46处是需展开的简称或通配符，并非46个产品故障。
修正后能力台账、十进制红队、FVA、盘点反冲合计4文件/40项通过。
再红队检查汇总门时，构造“当前数字错误但历史段落保留正确数字”的反例，初版 includes 检查确实漏报。
现只接受一个当前能力汇总段落，其总量与四类状态必须匹配；历史文字、重复当前段落不能补票。
验证范围为文档相关架构检查、上述行为回归、三套类型与无缓存lint；不把文档改动称为全站发布验收。

最终83文件/1,116项通过，三套类型检查与无缓存lint通过。可重跑命令：

```sh
npx vitest run tests/architecture tests/rules/backtest-fva.test.ts tests/rules/backtest.test.ts tests/redteam/decimal.redteam.test.ts tests/inventory/count-adjust-reversal.test.ts
npm run typecheck:all
npm run lint:full
```

本机JSON证据：`.artifacts/verification/capability-evidence-20260907.json`（忽略的验证产物，不随仓库发布）。
本轮只改文档和测试，未重建/启动候选服务，未重跑全量发布门、不继承旧提交READY。
真实3100只读健康检查61/61无漂移；原工作区173个未提交路径未覆盖。

剩余重点：146行文字/路由/符号证据的风险优先核验、小屏宽表、真实角色处置闭环、
受控部署，以及D69–D71与三方授权/数据覆盖等独立业务门槛。
