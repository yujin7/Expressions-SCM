# 盘点角色操作提示验收 · 2026-09-11

## 准确版本

应用提交 `77fb6bb6231ed94487b5c044e6d82cfa00871269`，分支 `agent/scm-workbench-evidence-20260907`。
冻结服务3422，独立数据库 `qc_boundary_20260911`，65迁移无漂移。
完整门回执 `.artifacts/verification/2026-09-10T22-49-03-064Z.json`：起止干净同SHA，READY9/9；678文件6131测试、三套类型、全量lint、生产构建、同版HTTP34/34零跳过通过。
正式容器/镜像/启动时点在本轮门前后未变，见外部证据 `formal-observation-77fb6bb6.json`；没有部署。

## 实际交互及结果

真实Chrome，初始1280px，已有合成PD2（draft/v4/实盘0.4000）与PD6（pending/v2）。

- 仓管登录后列表API `canCreate=true`；PD2动作edit/submit=true、approve=false，实际单号打开后出现保存/提交/扫码，实盘输入仍0.4000。
- 同一账号查看PD6：API三个动作均false，原因“需要财务审批角色”；实际页面仅保留打印，说明与“刷新操作权限”可见并可点击。不再诱导仓管进行财务审批。
- 仓管实际退出并确认后详情GET401。
- 财务登录后列表API `canCreate=false`；PD2三个动作均false，页面无扫码/实盘输入/提交，数据不隐藏，说明由仓管及制单人处理。
- 财务PD6 approve=true；实际列表回链打开后保留审批通过/驳回，实际打开驳回弹窗、填写合成意见并取消，没有发送驳回写入。
- 财务稳定390px/320px截图已目视，页面宽度分别等于390/320，刷新权限按钮在视口内；宽表保留内部横向滚动。320px说明多行，不宣称全站紧凑性或所有设备已合格。
- 财务退出确认后详情GET401；浏览器错误清单为空，随后关闭本轮自建浏览器。
- 末尾独立SQL：PD2仍draft/v4、PD6仍pending/v2，PD2保留上一轮一条reject审批，PD6没有审批，仓18/SKU2库存仍0.1000。本批UI验收未制造业务变更。

## 失败与证据边界

- 快速打开后关闭抽屉再点下一行，点击被抽屉遮罩拒绝；等待目标按钮的命令随后超时，原截图 `pd-warehouse-actions-77fb.png` 实为仍打开的PD2，不能当PD6验收成功。重新取得关闭按钮后实际点击，核对URL无docId和drawer-open不存在，才重新进入PD6并完成上述验收。没有用API导航掩盖这段失败，首次关闭未生效的准确原因未完全定位。
- 首张390px截图 `pd-warehouse-pending-390-77fb.png` 左边裁切，拍摄紧接视口切换。后续稳定财务截图及DOM证明该次稳定布局不溢出，但不把原图删除或据此排除所有过渡问题。
- 下列成功截图已目视，位于项目根 `交付证据/2026-09-09-申报产能/`：`pd-warehouse-pending-77fb.png`、`pd-finance-draft-390-77fb.png`、`pd-finance-draft-320-77fb.png`、`pd-finance-reject-dialog-77fb.png`。
- 新三个界面反例修前全红；修后加真实配置读取、角色/审批人/管理员自审、接口新鲜身份与既有写边界等，定向5文件36项通过。权限刷新测试初次失败是harness遗漏重渲染，修正后通过，不记为产品缺陷。
- 动作提示只代表读取时当前身份与状态；随后发生撤权/版本改变仍须写事务最终拒绝。本批没有建立新的权限体系，也未放宽仓管HTTP守卫或审批职责分离。

下一步仍需处理共享抽屉过渡交互证据、SH逐PO行分摊等业务缺口，以及正式Safari本地登录修复、异地备份发布前置、101条真实UAT、三方来源资格和D69–D71，不以此验收覆盖。
