# 库存、盘点与补货数字校验验收 · 2026-09-14

续接[物料数量校验](物料数量校验短路与错误恢复验收-2026-09-14.md)留下的库存同类缺陷。应用候选`0b66bd7ae24d5b541e0b27a846d9632e2b5e5d9c`，分支`agent/scm-ct-physical-lots-20260914`、[PR99](https://github.com/yujin7/Expressions-SCM/pull/99)基于PR98。本轮局部验收通过、可进入代码评审；全项目正式发布仍未完成。当前状态只由[NOW](../NOW.md)维护，意图只由[CURRENT](../spec/CURRENT.md)维护。

## 修了什么、没有改什么

- 库存手工期初/领料出/销售出/调拨数量、期初单价、实盘数量，在十进制格式校验成功后才进入`dCmp`。补货建议生成BH草稿的公开请求schema和内部服务亦修复相同问题。
- 非数字、空字符串、指数形式等返回400及原字段路径，不再被当成内部异常500，也不转成0。已有数值输入兼容、首尾空白、正数/非负、期初单价可选/null、零实盘合法等语义不变。
- 不改数量/金额舍入算法、原字段精度政策、库存/审批/版本/审计规则、补货请求原键或冻结政策。不宣称任意输入长度、超字段范围、全部数值精度问题已解决。
- 仅改三个既有模块的校验组合，没有新框架、依赖、迁移、历史回填或第二套数量计算。

## 自动化与反向证据

新增`tests/inventory/decimal-validation.test.ts`10项、`decimal-validation-route.test.ts`22项。修复前同组**13失败/19通过**，修复后32项全过；结合盘点输入、写入、库存单及S&OP资格既有检查，针对性89项通过。测试明确合法值会到达业务持久化边界，真正的后台异常仍500；缺当前身份仍401，错误字段不能在认证前泄露。

自动完整门回执`.artifacts/verification/2026-09-13T19-33-33-746Z.json`：

- 2026-09-13 19:28:57.079Z → 19:33:33.746Z，起止干净同应用SHA。
- **READY9/9，785测试文件/7761项，HTTP34/34，失败0、跳过0；72迁移。**
- 运维契约、PG迁移契约、完整lint、应用/测试/脚本类型及生产构建通过。后续文档提交不冒称重新执行应用全门。

## 真实浏览器与数据验证

仅独立3489冻结包`.artifacts/runtime-inventory-decimal-0b66bd7a`、既有合成PG库。未改正式3100、真实用户密码或业务库存。

1. 浏览器真实表单登录后，为自有测试仓110、SKU134通过实际API创建抽盘PD1，两条批次行分别账面3及1.8766。这是合成测试任务，不代真实业务盘点。
2. 3条公开写路由、7个字段/子类型组合，各发送6个非法值，共**42个真实认证请求均400**，含准确字段路径，没有错误编号。错误请求不是每个页面原生表单逐一点击证据。
3. 上述请求前后，以只读一致性快照核对**12张表完整行摘要及行数均不变**：库存单头行、PD头行、BH头行及请求回执、取号、库存流水/余额、审计、错误日志。该比较在完整回归启动前完成，避免并行测试写入污染证据。
4. 原生盘点页面将旧批次实盘改为1.5。测试只把第一次出站`countedQty`故意改为`abc`，不伪造响应：实际400，页面持久错误提示含具体字段，录入1.5000保留、未保存时不能提交。再次点击发送原合法数据得到200，错误消失，保存按钮回无修改状态。
5. 数据库PD1仍草稿，版本1→2，只有一条`update_counts`审计；旧批次账面1.8766、实盘1.5000，新批次仍3。没有生成盘点调整单，库存两批次余额仍1.8766/3；保存实盘不等于调库存。
6. 实际认证API用合法`qty=0.0001, price=1.23`创建期初草稿stockDoc39，字段原样落库，本单opening流水0；未提交/审批。不以此代替原生期初页面全流程验收。
7. 实际桌面盘点错误/恢复和390×844手机详情已截图查看，手机页面宽390、表格内部横滚；错误恢复后浏览器未捕获异常。一次只读浏览器取值脚本因引号错误失败，修正后继续读取原回执，未重复首次错误提交。

证据根`.artifacts/inventory-decimal-20260914/`：`before-fix.log`、`http-results.json`、`db-before.json`/`db-after.json`、`fixture.json`、`ui-recovery.json`、`stock-create.json`、`recovery-db.json`、`release.log`。复用助手的本轮截图位于`.artifacts/ct-physical-lots-20260914/inventory-*.png`。

## 评审、清理与下一步

- 本轮已闭合前一报告明确列出的三个库存格式抛错，并补齐补货同类点；窄模式源码搜索未再命中原`decStr.refine(...dCmp)`写法，但这不是全系统任意校验错误已清零的证明。
- PR99可评审，不等于可合并或已上线。[GitHub应用运行34777927889](https://github.com/yujin7/Expressions-SCM/actions/runs/34777927889)8个job均0步骤，Static checks检查103779340208注释明确账户付款/额度问题；云端没有通过证据。
- 本报告当时发现库存创建先在事务外查询仓库/SKU资格；后续已由[引用资格与并发验收](库存建单引用资格与并发验收-2026-09-14.md)收口并补12项真实PG竞争，此处保留原问题来源，不用后续证据改写本报告应用SHA。继续CT原单纠正、采购批次来源证明、真实工单占用、字段精度/持续错误反馈及真实角色链，不把仓库余额当来源证明或硬预留。
- 正式部署仍要求获授权的异地DB+附件配对备份、恢复边界和发布窗口，再验实际Safari本机HTTP、实际公网HTTPS及真实角色。按`release-sweep`，此处不签全系统生产READY，也不代签101条需求/外部集成UAT。
- 已核对PID47176、冻结cwd与3489监听后停止本轮运行，关闭独立浏览器；核对owner和127.0.0.1:65411绑定后停止隔离PG。保留测试数据库、草稿、冻结包及所有证据；未删除数据、备份或共享依赖。

文档收尾：19项知识治理检查通过；四份相关文档113个本地链接无缺失，NOW为84行/14318字节。保留历史失败与后续修复指针，不另建平行状态或技能副本。
