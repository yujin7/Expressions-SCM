# 工单操作资格、失败核对与来源分页 · 2026-09-13

## 结论

应用候选`63791e83b57aa15898983216af79a0769d501540`：**READY 9/9，仅隔离候选**。[PR64](https://github.com/yujin7/Expressions-SCM/pull/64)基于PR63，工作分支`agent/scm-wo-action-experience-20260913`；正式部署/真实UAT仍未完成。当前交付状态看[NOW](../NOW.md)，业务边界看[CURRENT](../spec/CURRENT.md)。

本轮复用流程设计技能中的现有组件/失败恢复原则；写路径安全门补当前身份和事务保护，最后由发布检查技能验精确候选。没有新增依赖、迁移、库存入口或平行技能/状态台账。

## 改了什么

| 缺口 | 实施及保留边界 |
|---|---|
| WO只凭pending状态向所有人显示审批/撤回 | 详情按当前数据库身份、审批人标记、既有审批配置及制单人分离下发提示。制单人仅可撤回，合格独立审批人可批准/驳回；草稿按原制单人/admin提交，生成及收口沿用原角色。缺资格不显示旧按钮，服务写时仍独立复核 |
| 提交/批准/驳回/撤回的网络失败只短暂报错 | 同步防重、卸载后不更新其他页面；不确定结果保留当前单据提示并停止重发，先刷新GET核对状态与审批记录，再按新资格操作。此处不等于创建幂等或所有收口按钮都已统一恢复 |
| 撤回/手工收口service信调用者旧身份 | 同事务重新核对启用、角色/会话版本，持有WO锁后调用既有流转引擎；状态及审计同成同败，合法重放保留。没有改完成/短关/作废/重开业务含义 |
| BH选择读取前999条，失败静默空白 | 复用RemoteSelect，每页50条、服务端搜索、显式加载更多/失败重试；已选项按准确ID/单号回读，仍与渠道范围、已审批、日期和搜索条件相交，不绕权、不全表扫描 |
| 换来源后残留手工订单类型 | 切换/清除BH时清空旧手工类型，由建单服务继承新来源；标签展示来源类型，不在客户端猜首单/返单。共享选择器支持`{rows,total}`，删除“不兼容”的旧注释与重复加载逻辑 |

## 精确验证

- 完整门回执：`.artifacts/verification/2026-09-13T00-20-04-177Z.json`；起止干净、同应用SHA，上海08:15:56→08:20:04，约248秒。**730测试文件/6864项**、lint、全类型检查、生产构建、运维/PG迁移、HTTP **34/34零跳过**全部通过。
- 定向四文件72/72：`wo-generation-experience`、`wo-upstream-safety`、`short-close-supply`、`search-scope`。本轮新增12项，覆盖身份变化、审计回滚、重复确认、卸载、来源分页/精确回显及越权/非法参数。
- 真实PG多连接39/39：`scripts/verify-postgres-settlement-basis.ts`；日志`.artifacts/wo-action-experience-20260913/postgres.log`，夹具`2afa84b8`。新增撤回vs批准、停用账号vs撤回、撤销管理角色vs短关，断言实际PG锁等待与结果；不是延时猜测。首次未提供显式变更开关时脚本安全拒绝、未变更数据；核对自有隔离库后显式启用并通过。
- 冻结运行包`.artifacts/runtime-63791e83`，loopback3464，后台任务关闭，66/66迁移零漂移；不是正式3100。
- 后续仅文档收尾：治理/钩子32项通过，5份文档76个本地Markdown目标存在；NOW保持83行/约14KB。该文档检查不冒称文档提交SHA重跑了应用全门。

## 浏览器 → 服务 → 数据库（仅合成场景）

Chromium独立PMC与管理员会话；桌面1280×633、手机390×844。仅在`scm_contract_quickwins_20260911`使用合成账号及数据，未重置正式口令。

1. 服务创建/提交合成WO366 `WO-20260913-0010`，关联`BH-UI-ACTIONS-000`，数量3.0001、费率1.25、类型继承为repeat。
2. PMC准确链接进入：只看到撤回、没有自审/驳回；实际撤回回draft。注入一次本地fetch失败后，提交提示结果待核对且不再显示提交按钮；刷新GET确认草稿后重新提交。
3. 独立admin进入可批准/独立驳回/管理员撤回。填写原因并实际驳回回draft/version5。为复核手机弹窗再提交到pending/version6，打开驳回表单后取消会话，无第二次驳回写入。
4. 来源弹窗实际读取50/61→加载更多61/61；输入旧单号、Enter选中，发生准确`selectedValues=[1]`回读。一次源读取失败显示“选项加载失败，已保留现有选择”，手动重试恢复，不冒充无匹配。
5. 手机页面宽390、document宽390；驳回弹窗稳定布局left8/right382/width374，未被drawer遮住。打开桌面弹窗后立即改视窗的一张过渡帧不作为布局结论；窄屏重新打开并等待稳定后复核截图。两会话无未捕获脚本错误。
6. 浏览器窗口库存流水**314→314**；WO366审计create一次、submit三次（不同合法轮次）、withdraw一次、reject一次；没有新增采购/加工/库存动作。最终读回在`browser-final.json`。

证据目录`.artifacts/wo-action-experience-20260913/`保留：`maker-pending-desktop.png`、`submit-network-recovery.png`、`source-selected-mobile.png`、`checker-reject-mobile.png`、`rejected-draft-mobile.png`、`browser-final.json`及构建/发布/PG日志。这些是指定路径的验证，不是全站美学、所有浏览器或真实业务闭环证明。

## 剩余事项与收尾

- 本轮当时未实现WO创建持久回执及建单幂等；后续已由[手工创建恢复验收](工单创建幂等与持久恢复验收-2026-09-13.md)续接。保留本轮历史边界，不以动作防重冒充建单幂等。
- 共享PO/WO完成/短关组件的同步防重及持久失败恢复仍需独立覆盖两处消费者，不能从本轮提交/审批入口推断已修。
- 历史子BOM快照时效、真实齐套来源/占用资格、完整真实角色链、总监101条及平台真实授权/UAT依NOW继续。
- 正式部署仍需获授权的异地备份位置/访问及发布窗口；GitHub付款/额度导致前序检查未启动的阻塞保留，不绕过保护或自动合并到功能分支。
- 验证后核对owner、PID41383/cwd/3464端口，停止两个测试浏览器、该隔离运行及自有PG；保留数据库、审计、冻结包和证据。不把证据当缓存，也未动正式3100。
