# 通知阅读与投递状态：准确候选验收

## 结论与边界

应用提交 `21d7012c4b054673010ecf928cbbfa2bba0e337c` 在隔离生产构建 3377 与真实合成 PostgreSQL 63540 验证通过，未部署正式 3100。本记录是后续文档更新；完整门绑定上述应用 SHA，不声称文档提交重新运行应用门。

没有改发送日程、目标人、派工、业务数量、库存、财务或 D69–D71。本人已读不等于完成任务；飞书发送成功不等于真人阅读。正式持久日程为上海 11:55/17:55，早晨的 102 条 pending 尚未到下次发送时间，不能单凭它们推断调度故障。

## 修复范围

- 受众列表与未读数纳入 sending，保留原收件人权限；区分站内可读和外部投递状态。
- GET 使用新鲜身份、拒绝非法或重复筛选、private/no-store。客户端取消旧请求、拒绝迟到回包；失败撤下旧事实、未读数保持未知，提供手动重试。
- 本人已读关系与其个人通知保留期字段在同一事务更新；冲突幂等，返回真实新增数。歧义写请求拒绝，不接受调用方指定他人身份。
- 写入超时或回包丢失不自动重试，保持不确定回执并允许手动核对；提交前将焦点留在持久区域。
- 长中文通知默认摘要，原生展开保留全文和键盘操作。修复窄屏列 flex 导致的巨大空白，使用正常块流；不添加全局 CSS 覆盖。

## 实际证据

| 检查 | 结果 |
|---|---|
| 开发定向 | 4 文件 / 63 项，三套类型与改动 lint 通过 |
| 准确完整门 | 08:09:47 READY 9/9；636 文件 / 5,788 项；完整 lint、类型、构建通过 |
| 同版 HTTP | 33/33；3377 明确返回上述 SHA；64/64 无漂移 |
| 布局与键盘 | 1280/390/320 无页面横溢；全文展开、折叠、跨宽度保留展开状态 |
| 同一长文合成行 | 390px 旧空白布局约 3454px → 297px 折叠；320px 341px、1280px 146px；不是全站性能指标 |
| 读取竞态与失败 | 迟到严重度筛选不覆盖新筛选；503 撤旧、读数未知；手动重试与焦点正确 |
| 写入不确定性 | 服务端已提交后模拟丢回包，仅一次 POST，人工刷新确认已读 |
| PostgreSQL 并发 | 六个同通知请求仅一条新增阅读关系，marked 总和为 1 |
| 角色与业务边界 | 采购全部已读后自身未读 0、仓管仍 2；任务、告警、事件、流水与投递状态控制不变 |
| 回源 | 明确本人已读/高严重度筛选后，从通知进入 /alerts?id=1，手机可见对应源告警 |

浏览器 pageerror 为空。证据为分段验证：主写入脚本在全部写检查通过后，因裸 URL 恢复已保存筛选而找不到已读来源行；补验使用明确筛选，不重复写操作。补验还修正隐藏桌面重复元素和两个 main 导致的定位假设。原失败记录保留，不能称一次脚本全绿。

## 原件

原件目录（仓库外）：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

- `full-release-21d7012.log`；仓库 `.artifacts/verification/2026-09-09T00-09-47-734Z.json`：开始结束同 SHA、干净目录、9 项终态。
- `owner-21d7012.json` 与 `frozen-21d7012/`：隔离运行身份和构建。
- `notifications-21d7012-read-result.json`、`notifications-21d7012-{1280,390,320}.png`：响应式、读态与键盘。
- `notifications-21d7012-write-result.json`：写入与只读回源补验证据；`write-failure`、`source-failure`、`source2-failure` 原件保留。
- `notifications-dev-layout-390.png`、开发失败记录及 `notifications-dev-fixed2-*`：真实移动端空白反例与修复。

## 尚未完成

正式 3100 复查仍为镜像 `9ec914e49423…`、61/61，启动时间不变；原目录 173 个未提交路径未触碰。64 迁移准确候选的 Linux 交付、目标环境备份恢复及真实角色 UAT 仍独立待验。

G04 来源告警/情景证据/负责人/结果到既有待办的关联没有因此完成；G03 真实协议与应付口径、G05 平台状态、总监 12 组 101 条、三方授权/身份/对账/消费者/真实验收继续保持范围。

浏览器发现 AppShell 的 AntD Content 与内部 main 形成两个嵌套 main 地标，是后续可访问性问题。本候选未改，不能把测试定位修正写成产品语义已修复。
