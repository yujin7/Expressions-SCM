# D61 待办独立跟进与历史

## 交付状态

已验应用为`6b0f038da49815215a2e60a3eeb6278027e0d608`，隔离生产构建3375、合成PostgreSQL63540、迁移64/64无漂移。07:19:26完整门READY9/9：636测试文件、5780测试、生产构建、隔离HTTP33/33通过，起止同一干净SHA。07:24:39补齐分段浏览器证据。

**未部署。** 正式3100再次检查仍旧镜像`9ec914e49423…`、61/61无漂移。此结果不证明当前admin密码、真实数据、供应商协议、业务UAT或完整G04已完成。

为避免旧候选常驻耗用资源，核对owner、实际cwd、监听PID和健康版本后停止3372/3373/3374三个本会话验证进程。冻结包、数据库和失败证据均保留；3375继续健康运行，正式3100不受影响。[回收记录](</Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/retire-before-6b0f038.json>)。

## 完整操作链

有读取/修改范围的人员在待办进入“跟进记录”，阅读受控历史、追加至少5字的进展或结果依据，收到持久记录ID，再返回原事项。桌面/手机切换不会卸载父层抽屉草稿。记录为只追加审计，不修改任务状态、完成时间、updatedAt、派工策略或来源告警；关闭业务仍须走原有明确操作。

服务锁定work_items行后检查可见范围，再核对请求UUID与追加审计。同一作者、同一工作项、同一UUID与相同内容重放返回原eventId；内容/作者冲突拒绝。权限检查先于重放。GET只返回明确DTO字段，不开放任意审计payload/IP；按审计ID降序游标20条分页。没有新任务平台或新迁移。

提交30秒超时或网络回执不确定时保留内容/UUID，禁止自动重试，只有“确认同一提交”复用原标识。提交中同步锁防双击，禁用输入和关闭；成功显示准确记录ID。这样既能防止丢回包造成重复记录，也不把客户端取消误认为服务端回滚。

## 缺陷与修复证据

| 路径 | 原反例 | 修复与证据 |
|---|---|---|
| 新API登记 | f8df739未登记history路由，完整门失败 | 6e184b6补A708、284API及708控制一致性；未放宽原检查 |
| 翻到末页/读错误重试 | 触发按钮消失后焦点落BODY，Escape失效 | 操作前将焦点留在持久抽屉内容；旧组件反例失败，浏览器验证通过 |
| 保存及显式确认 | 6e184b6禁用提交按钮/输入时失焦，保存成功后Escape失效 | 6b0f038提交前复用持久焦点；旧代码新增反例1失败6通过，修后组件7条、服务7条通过 |

定向检查之外，三套TypeScript与改动lint通过；准确全量门结果独立列于上方，旧候选绿色不能覆盖后续候选。

## 同版浏览器与数据核对

- 320→390→1280→320，草稿保留，抽屉无水平溢出；320正常保存、未确认、已保存及读取重试截图已目视核对。
- 人工暂扣真实服务器回执，确认提交中焦点仍在抽屉、Escape不能中断提交；释放回执后输入清空并显示记录，Escape关闭。
- 实际数据库已提交后模拟丢回包：首次201/eventId34；人工确认同一UUID后200/replayed=true/eventId34。只一条记录，确认后Escape关闭。
- 末页按钮消失后的Escape通过。独立Chromium上下文再次验证真实503读取错误→重试→历史返回→焦点保留→Escape关闭。
- 新建仓管浏览器上下文登录后，其他人员待办history GET/POST均404，pageerror为空。
- 合成库跟进总数27→29，仅新增本轮正常保存与丢回包两条；重试不增加第三条。工作项仍open、completed_at为空、updated_at仍最初值；ledger0、alert_events6（open3/refresh3），三条告警无变动、notifications0。

### 自动化中断不改写成应用结论

浏览器原始失败均保留：原生AX快照/等待曾35秒超时，随后同会话目标观察为about:blank，不能推断是应用导航；按钮离场loading图标影响精确可访问名称；纯文本定位命中响应式隐藏重复节点。改用可见按钮完成原请求确认，并以随宿主提供的Playwright建立独立Chrome上下文补验读取重试和角色拒绝，不安装项目依赖、不改业务以适配脚本。

这是分段证据，不宣称单脚本一次全绿。完整门与浏览器采用不同验证通道，均绑定同一应用SHA。

## 原始回执

- [准确完整门](../../.artifacts/verification/2026-09-08T23-19-26-269Z.json)
- [全量日志](</Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/full-release-6b0f038.log>)
- [分段浏览器最终记录](</Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/browser-todo-submit-6b0f038-result.json>)
- [独立读取重试脚本](</Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/todo-submit-independent-read-6b0f038.mjs>)
- [前序真实PG六路同请求回执](</Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/todo-history-pg-f8df739.json>)（服务代码未在两次焦点修复中改变；不能当作本轮重新执行PG并发）

## G04下一步：仍须完成的连接

当前CapacityTarget只传SKU/显示名/返回地址，产能结果未绑定来源预警、责任人或待办。通用跟进备注不是情景身份、真实承诺或关案结果，不能据此销掉G04。

后续动作应从“核对结果”明确交给负责人：保存经过服务端核对的SKU、厂、拟交日期、人工数量/单位、比较截止时间、申报依据与未知原因；来源告警必须服务端验证类型/身份/可见范围，不接受前端伪造。读模型行没有正式告警时需明确“人工核对来源”，不能造告警ID。

优先复用现有、可见的同源未结待办，避免再发一份工作；没有适合承接项时才明确新建并选择有效负责人/截止日。新建重试需要准确幂等和回执，不能沿用普通manual不去重行为而宣称安全；现有source_kind限制alert/manual/review，不能偷偷新增类别或占用告警指纹。备注与情景应保留独立历史，结果回查必须区分已联系、待确认、有凭据承诺、未达成和业务完成。具体契约与反例应先登记，再实现。

这个后续工作仍需实现、同版验证、正式交付和真人验收。D55观察不定量、D69删除签认、D70派工、D71抑制天数、总监12组101条、G03/G05及三方集成范围均不改变。
