# 采购行履约BI与迁移验收 · 2026-09-11

## 结论与边界

候选 `dbc44d8672060298b3a0c06583b91338f3cf633c` 完整工程门 READY；不是正式部署或采购收货全流程验收。
修复源自 `b94b04bc` 的采购行写入，`09f76379` 接通报表，`dbc44d86` 补台账/说明/窄屏。
主责依次为 integrate-supply-chain-data、design-supply-chain-flows、release-sweep；并行安全规则仅用于保护独立分支、数据库和验证进程。

## 实现

- 承诺兑现和供应商记分卡共用只读归属索引，复用收货写端身份校验。显式行必须属于同PO且SKU一致；历史唯一PO/SKU兼容，多行未知归属排除相关行，不借用首行或部分已知事实。
- 真正无收货且已收控制量0的到期行仍为逾期未齐；控制量不一致、历史承诺不可信仍排除。数量继续十进制、基础单位，退货按准确采购行扣减。
- 原始/当前承诺、首批/足量、录单日/检验确认日不混为一谈。外部三方仍为独立观察边，未取得事实资格不参与评分，不自动改供应商等级。
- 展开评分明细固定800表宽和430说明列，窄屏内部滚动；不改全局CSS、不隐藏说明。缺数据文案不再断言“PO未填交期”，准时率计数标签明确为采购行。

## 反例与验证

修前4个业务反例红：准确采购行仍被排除；无收货0被当歧义；数据库加载不带行；旧歧义评分得到100%。另一次合成夹具缺SPU初始化失败已修正，不冒充业务反例。
定向9文件41项、三套类型、改动lint通过；新PG外键注册护栏先红后补，目录测试还会逐一植入缺失/错表/错定义/未验证反例。

首轮 `09f76379` 完整门 NOT READY：681文件通过、1失败，6167项通过、1失败。新增0065漏在审计台账而非Drizzle journal；补A711/分类/正文/索引后通过。失败回执 `.artifacts/verification/2026-09-10T23-57-19-723Z.json` 保留。

最终完整回执 `.artifacts/verification/2026-09-11T00-05-25-097Z.json`：起止同干净SHA，682文件6169项，构建/类型/lint、9个门、HTTP34/34零跳过通过。

## 数据库隔离与迁移

容器 `exp-scm-capacity-20260909` ID `eb10a79197a28a3a6822f2b00d0373098757061ea1b250cfd1e71d5ab1537b56`。
从合成库 `qc_boundary_20260911` 复制到独立 `purchase_line_20260911`，后者65→66迁移，完整journal hash/时间匹配；外键实际为 `FOREIGN KEY (po_line_id) REFERENCES po_lines(id)`，已加入只读PG验证器。
原52条SH行旧字段摘要迁移前后同为 `312b214d472704fb3d46cb43af57b59b`，新增行号全null，无自动历史回填。旧库/3424未迁移。

后续专用只读BI夹具：供应商8/9，PO19/20，PO行19–22，SH53–56。直接种子是读端测试资料，不代表审批/过账成功。无正式数据写入。

正式数据库只读事务检查：重复PO/SKU组0、受影响SH行0、未结束受影响SH行0。只能证明这类歧义当前未出现，不能推导其他历史质量全通过。

## 同版真实浏览器

冻结3426，同SHA、66迁移，角色warehouse01；独立浏览器登录进入工作台。API与实际页面均为已核对供应商50%（1/2行）、历史歧义null（0可评估行）；展开理由明确2行归属待核对。
到货日历API只返回正确迟到行20，原始/当前两个口径，承诺日前0、截止日10；歧义2行排除。页面新粒度和标签已显示，旧“重复PO×SKU行排除”不存在。
实际点击导出得到 `promise-dbc44d86.csv`，两条口径行与API数量/日期相同。但CSV未携带采购行ID，仍是未闭环缺口，不能宣称完整身份贯穿导出。

首轮390宽展开页高5188，说明列极窄；修后同夹具390宽页面2218，展开区约325高；320宽展开区约347高。两种窄宽以及首轮1280均无页面横向溢出，说明未删。
实际容器滚动命令将内部scrollLeft移到560而页面为0，截图目视确认说明列可达。鼠标wheel尝试未观察到位移，不能声称触摸/滚轮手势已通过；触摸和键盘滚动体验可继续验证。浏览器errors无输出、无错误覆盖层。
退出第一次只到了确认页，API仍200；随后关闭测试浏览器，新会话匿名401。因此不能用它证明已验证“已登录→确认退出→401”的完整退出链；会话已关闭。

## 证据位置与剩余

证据根目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。
关键文件：`run-release-purchase-line-09f76379.mjs`（参数支持新SHA）、`postgres-dbc44d86.log`、`full-release-dbc44d86.log`、`formal-observation-dbc44d86.json`、`owner-dbc44d86.json`、`fixture-purchase-line-bi-09f76379.json`、`bi-dbc44d86-mobile.png`、`bi-dbc44d86-320-note.png`、`promise-dbc44d86.csv`。早期失败截图保留。

正式3100原容器/镜像/启动时点未变，未发布。后续必须补导出和可见明细行ID/回链、准确采购行跨SH并发与回滚、历史歧义人工核对闭环；不以首批指标替代全收OTIF。登录HTTP部署、异地备份、101条真实UAT、三方资格、D69–D71及旧浏览器异常继续，完整目标未完成。
