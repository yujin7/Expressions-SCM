# 结算读取与处理入口一致性验收

2026-09-13（上海）。精确应用候选 `0b78ed399d97c0c4421d42e2a4d6205770959978`，分支 `agent/scm-settlement-read-20260913`，工作树 `worktrees/material-source-20260913`。本报告只证明隔离候选；正式部署及当前协作状态看 [NOW](../NOW.md)。

## 发现与修复

前轮实际仓管驳回发现：JS详情可读、关联链却404。继续核对《01》§6和D62后，确认不只是链路显示问题：PMC缺结算菜单，JS列表/详情原先对普通已登录角色过宽，配置后的合格审批人又被搜索/待办/关联链的静态菜单资格挡住。金额脱敏不能替代读取授权。

| 场景 | 本轮一致契约 |
|---|---|
| 采购、PMC、财务、管理员 | 按原规格读取JS；补齐PMC菜单，菜单基础资格仍由路由注册表派生 |
| 当前JS配置角色且当前账号有审批人标记 | 可读结算历史并从待办、搜索、准确单据链接及关联链进入处理；不伪装成财务角色，不自动授予金额权限。静态菜单不因此扩到所有仓管/运营/质检 |
| 普通运营、仓管、质检 | 未取得上述配置资格时，JS详情/列表403、关联链404，搜索和待办不暴露JS；不能用“金额已隐藏”代替这层拒绝 |
| 显式渠道范围（包括空数组） | JS没有可靠渠道粒度，非管理员拒读拒写；既有管理员例外保留。JS新建、费用更新、提交、批准、驳回在当前账号锁内回查数据库范围，调用方传入无限范围不能绕过 |
| 结算预览 | 仍只对原采购/PMC/财务/admin角色开放，并核对渠道范围；不因配置为隐藏金额审批人而开放完整预览 |
| 非法JG筛选 | `jgId`为空、0、负数、小数或非法字符串返回400，不悄悄返回未筛选整表 |

JS读取资格收口于 `src/server/modules/settlement/read-access.ts`；列表、详情、导航读取复用它。搜索/关联链每请求只加载一次JS配置；待办复用本来已加载的配置。HTTP使用当前数据库会话身份。JS写端范围在事务内再核对，读取提示不能替代审批配置、职责分离、金额、版本和过账保护。

## 已完成审批的安全重试

PR51审查意见指出PC金额资格检查放在幂等识别之前，会让已成功审批者在失去金额查看权限后无法安全重试。已独立修复并回复/解决该审查线程；PR51补丁 `34e478a3` 与本分支 `5c0231bf` 等效。本轮同时修正JS同类次序。

先由原 `approveDoc` 核对当前审批资格、准确单据和已完成轮次；已完成的同轮请求直接返回，不重复审批、改价、审计或过账。真正的新批准仍须可见金额；资格不足抛错使外围事务连同内部审批整体回滚。错误轮次、撤销审批人标记及不符合当前范围仍拒绝。未放宽盲审，不把重放当新财务行为；JS已完成轮次也不再被后来的业务依据重新计算。

## 精确候选验证

| 检查 | 结果 |
|---|---|
| 完整发布门 | **READY 9/9**，709文件 / **6583项**，HTTP **34/34、零跳过**；全量lint、全部类型、真实PostgreSQL迁移契约、正式构建和live smoke均通过 |
| 锚定 | 2026-09-12 17:56:01.961Z → 18:00:07.562Z，起止同 `0b78ed39`、工作树干净；上海为09-13 |
| 回执 | `.artifacts/verification/2026-09-12T18-00-07-562Z.json`；日志 `.artifacts/settlement-read-20260913/release-0b78ed39.log` |
| 新边界回归 | `search-scope.test.ts` 28项覆盖真实路由、角色/配置/撤权、范围及非法筛选；`fee-scope.test.ts` 22项覆盖五类写端范围拒绝与零副作用、JS重放；PC对应57项定向测试通过 |
| 失败透明性 | 初次回归中“普通仓管仍可读脱敏JS”的旧预期失败，依据规格改为拒绝，另以三类当前配置审批人验证合法脱敏路径；不是删除访问测试换绿灯 |

## 实际浏览器 → API → 数据库

精确冻结包 `.artifacts/runtime-0b78ed39` 在本机3453运行，使用隔离合成库 `scm_contract_quickwins_20260911`（65411、66迁移），关闭后台作业。未使用正式3100或外部平台数据。

1. PMC实际表单登录，展开财务菜单出现“结算单”，打开列表及JS12。点击关联JG后准确到 `outsource/jg?docId=46`，显示JG-MSRC-20260913-A；再点结算链接回到JS12，而非仅按列表搜索猜单据。
2. 在隔离库把JS审批配置临时从finance改为warehouse（该合成仓管账号原已有审批人标记）；PMC实际提交JS12：draft v3 → pending v4。
3. 仓管实际表单登录，点击“待我审批”，看到JS-20260913-0006并点击“去处理”。详情/链路均200；链路准确包含当前JS12、WO13、JG46。金额字段在响应中缺省，页面显示“—”；`approve=false/reject=true`，无批准按钮。
4. 桌面及390×844窄屏截图逐张查看：关联节点正常换行，当前身份常驻，驳回按钮可操作；页面宽度和scrollWidth均390，关联区宽342，无横向溢出。首次切换尺寸时截到动画中间帧，等待稳定后重新截图，不以该中间帧作为验收结果。
5. 手机宽度实际点击驳回、填意见并确认：JS12变draft v5，新增且仅新增cycle4、仓管6的一条reject记录；结算金额仍0.00，JS12库存流水0条。驳回后配置仍有效时历史与链路200。
6. 临时配置恢复finance并复核；同一仓管旧会话立即得到详情/列表403、链路404、搜索空结果。刷新页面明确提示无权限且不再显示旧详情，没有未捕获浏览器异常。

证据目录 `.artifacts/settlement-read-20260913/`：`pmc-js-chain-desktop.png`、`warehouse-js-chain-desktop.png`、`warehouse-js-chain-mobile.png`、`warehouse-access-revoked-mobile.png`。本次JS12是零金额读/处理路径夹具，不冒称非零扣款、库存核销或真实财务UAT的浏览器证明。首次数据库核对误用了不存在的ledger表名，仅该只读查询失败；已按实际 `stock_ledger` 复核，未据失败查询写入数据。

## 仍须继续

- 物料数量、退料、扣款依据变化后的完整结算恢复，已冻结财务差额处置与真实角色全链验收。本轮不静默重算已保存/冻结结算，不改变物理退料生命周期。
- 其他财务报表/导出/写入权限需按各自契约另验；不能把JS这一组入口收口宣称为全财务域审计完成。
- 正式DB+附件异地备份授权、发布窗口及实际Safari HTTP/HTTPS验证未完成；本机合成账号登录不等于用户正式登录已修复。
- 外部平台授权/映射、D69/D70/D71以及总监101条真实业务验收仍保留。未改正式账号、密码、配置、库存或平台权限。

本轮没有新增平行技能或知识权威；沿用工作流技能补齐真实角色入口、写路径技能守住范围/幂等/原子性、发布验收技能区分精确候选和正式上线。应用全门之后的文档提交不冒称另一个SHA重跑全门。

收尾已核对3453的实际PID/cwd/监听端口及隔离PG的owner/端口，停止本轮两个测试浏览器、候选进程及PG容器；库、截图、冻结包与所有验收证据保留，未删除业务数据或共享依赖。PR51含修正补丁已合并 `30c29eca`，不是本轮新JS读取修复的部署证明。

主线协调：先确认PR51合并树与`34e478a3`全树一致，再合并；四份自有文档因squash历史发生旧新状态冲突，逐块保留本轮已验状态，未覆盖他人代码。合并`03d4c6e5`全树与合并前`b76345f1`一致，与应用全门锚仅五份文档不同。合并后全部类型和正式构建再次通过（`post-merge-types.log`、`post-merge-build.log`）；文档治理与能力台账2文件37项通过，不把历史714个台账条目另算为新增测试数量。
