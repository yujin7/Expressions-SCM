# 批次流水全量与来源回链验收 · 2026-09-11

## 准确版本与结论

- 候选 `4f9066e812cd0e14c8471970825448ff29a72799`，分支 `agent/scm-workbench-evidence-20260907`。
- 完整门起止同SHA、工作树干净：689文件 / 6258项通过，HTTP34/34、失败0、跳过0；READY9/9。回执 `.artifacts/verification/2026-09-11T07-09-06-430Z.json`。
- 冻结运行于3433，PID3071，66迁移；独立容器 `exp-scm-capacity-20260909`、数据库 `purchase_line_20260911`。这不是正式3100部署，也不是业务签认。

## 修复与反例

原实现只取最近200条，既未明示截断，又用这200条推断整个批次是否出库；同一单据同一天多行还共用表格key。203条合成流水的9个初始反例全红。后续另用期初+盘亏证明负数不等于物理出库（1项红）。

现用同一SQL快照取得全部匹配流水总数、已知出库类型证据和当前页；按时间/不可变流水ID倒序，SKU与批次同时过滤。默认30条、最多100条，空页仍返回总数，非法参数明确400。保留数量字符串、上海日期、基础单位、流水ID和来源行标识；盘亏/冲销/核销不冒充发货。有出库记录不声明历史全覆盖、净出库未反冲或销售终点已知。

来源单号复用库存流水解析器，逐来源表批量读取；存在单号才给精确docId链接，未解析保留待核对。首次登记来源单独标注。翻页撤旧并关闭原操作窗口，响应页码不匹配或重复流水ID拒绝显示，库位作业等待期间禁止翻页。

## 数据库与浏览器实证

证据目录：`交付证据/2026-09-09-申报产能/`（工作树外的项目根目录）。

- `seed-batch-ledger-4f9066e8.mts` / `fixture-batch-ledger-4f9066e8.json`：新合成SKU11、批次3、仓29；所有库存经注册post写入。203条全量逐页核对、203唯一ID、合计9.0201；页面服务单次约3.9–7.1ms，仅这个小型合成样本，不外推正式性能。
- Chrome仓管实际登录，查 `QA-TRACE-4F9066E8 / QA-LOT-4F9066E8`：首屏30/203；实际点击第7页23/203，较早销售出库可见；0.0001与上海9月4日显示准确。
- 实际点击 `QA-OUT-4F9066E8` → `/inventory/docs?docId=2`，正确单号详情打开；浏览器后退保留查询及第7页。没有测试刷新后保存/跨浏览器分享；当前查询仍为组件状态。
- 合成单头只用于读取/导航验证，未走真实审批。该复用测试库在目标详情显示了早前合成审批记录，不能算来源单据审批/追溯业务闭环通过；后续整条业务UAT应采用全新隔离库并核对单据、行与审批事件身份。
- 1280/390/320无整页横向溢出；390下三个表容器320宽，内部420/660/830。截图 `batch-ledger-4f9066e8-390.png`（全页）、`batch-ledger-4f9066e8-390-top.png` 均已目视。全页截图因滚动位置将固定导航捕在中段，回顶部的视口截图确认导航正常，不判成业务内容重叠。
- 离线点击追溯：旧表全部撤掉，错误可见；恢复网络并点重试：3表及30/203恢复。页面JS错误列表为空。
- 已认证HTTP：页20返回200、0行但total203/出库证据true；page=0、pageSize=101、重复page参数均400。首次浏览器评估脚本语法错误，修正后重测上述HTTP，不将脚本失败归为产品缺陷。
- 实际确认退出前目标API200，退出后401；测试浏览器已关闭，没有保存认证状态文件。

## 未完成与发布边界

正式容器在本轮前后均 `af36cd6581f6...`，镜像 `9ec914e...`，启动时间 `2026-09-11T02:43:18.62257297Z`，本轮未改。正式61迁移与候选66迁移分别核验，不混为已发布。

本地HTTP登录修复发布、异地成对备份、三方事实资格、101条真实UAT与D69–D71仍待；批次过账闸门未开启。完整FEFO、历史无批次归属、冻结召回清单/全量导出、跨浏览器查询恢复，以及供应商/客户真实流向不是本次读取修复可以证明的结果。
