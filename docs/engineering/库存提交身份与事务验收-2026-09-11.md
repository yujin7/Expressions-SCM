# 库存提交：身份、事务与真实交互验收

日期：2026-09-11。应用候选 `e7c38e7654ffae6ae0fe719ad1e01118af3d8b8f`；正式未发布。

## 发现与修复

原库存提交先更新状态/版本，再独立写审计。注入审计失败后，调用失败但单据已由 draft/v1 变成 pending/v2。服务还信任传入身份；停用、撤销会话或移除仓管/admin 后可能沿用旧资格，异常历史草稿 CA 也可进入不能独立审批的流程。

现复用 `currentWriteActor`，在同一事务中保持当前身份、锁定单据后核对状态/版本，再原子更新状态/版本/时间和审计。沿用 HTTP 仓管/admin 资格及制单人/admin 限制；CA 拒绝独立提交并提示来源盘点。提交不改行数量、不预占或过账，不改审批、红字规则，不修写历史数据。

## 验证分层

| 证据 | 结果 | 边界 |
|---|---|---|
| 修前反例 | 新增12项，7红5绿 | 审计失败留下已提交状态、旧身份错误放行均被复现 |
| 修后定向 | 5文件47项；应用/测试/脚本类型检查与改动 lint 通过 | 不替代真实PG并发 |
| 全新PG | `stock_submit_20260911`，空库66迁移，13个检查点通过 | 含迁移和源码稳定性，不是13条业务链；运行时为提交前97335275+明确dirty，不冒充准确候选门 |
| 准确候选全门 | READY 9/9；692文件/6280项；HTTP34/34零跳过 | 08:06:44.306Z→08:10:56.299Z；起止干净同SHA；不等于正式部署 |
| 真实浏览器 | Codex浏览器1280×720、3438，仓管建单→提交失败→刷新核对→重试成功→退出 | 截图/页面树在会话中；没有另存截图文件，不声称Safari/手机/完整过账已验 |

## 真实PG并发与回滚

1. 两个独立请求先被实际单据锁阻塞，观察 `pg_stat_activity` 等锁后放开：200/409，仅一次版本增加与审计。
2. 并发停用账号先持用户锁，提交等待后403，草稿不变。
3. 并发作废夹具先持单据锁，提交等待后409，不覆盖作废状态；这不是作废服务的完整验收。
4. 数据库审计拒绝触发器让提交失败，状态/版本/时间精确回滚；移除该测试触发器后原v1可成功重试。
5. 全新库库存流水/余额均0。合成记录与库保留，只移除了本测试故障触发器/函数。

## 浏览器→HTTP→数据库

在另一既有合成库 `purchase_line_20260911`，warehouse01通过实际表单建立 `RK-20260911-0001`（id4），数量0.1250，备注 `QA-STOCK-SUBMIT-e7c38e76 合成提交验收，不审批不过账`。

仅针对该单提交审计安装测试触发器，点击提交及确认后显示系统错误134492ea及先核对提示。数据库核对仍 draft/v1、审计0；移除故障触发器/函数后刷新同一详情并人工再提交，显示待审批，数据库 pending/v2、提交审计1、该单库存流水0。未审批或过账。退出后受保护页面回登录；直接访问API被浏览器工具阻止，不能当成401证据。

## 未完成项与证据位置

- 异常 pending CA 待办仍有错误审批域映射，须提供准确来源核对而不是直接隐藏异常。
- 制单人详情仍见不适用审批按钮；1280宽页面/720宽抽屉标签竖排，均未在此修复。
- 其他库存创建/撤回/作废/短结/审批/红字写路径仍需逐项审计；正式登录、发布、批次整链和真实UAT另验。
- 门回执：仓内 `.artifacts/verification/2026-09-11T08-10-56-299Z.json`。
- Git外证据：项目外层 `交付证据/2026-09-09-申报产能/` 下 `verify-stock-submit-20260911.mts`、`stock-submit-20260911.json`、`build-e7c38e76.log`、`full-release-e7c38e76.log`、`owner-e7c38e76.json`、`formal-observation-e7c38e76.json`。不搬走日志或冻结包破坏回执路径。
- 正式镜像仍 `sha256:9ec914e49423cd4c61b546e70fccaeb6baa081af8df0ef0bf99cb9f7a338bbf8`，启动02:43:18.62257297Z，61迁移；本验收未改正式运行。
