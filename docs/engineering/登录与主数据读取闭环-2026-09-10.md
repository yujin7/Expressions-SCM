# 登录与主数据读取闭环

## 16:33准确候选已验（未发布）

bc42a1e8a3d8f3c9a7753fc731ac2424476a1ea8完整门于16:32:43 READY9/9，645文件5845项、生产构建、同版HTTP33/33通过；起止同一干净SHA，回执2026-09-10T08-32-43-746Z.json。之前e1c8a95失败回执保留；两条商品来源补唯一消费者登记后不再误标孤岛，整仓引用一致性门未放宽。

冻结3386准确同版64/64无漂移。实际Chrome默认242ms/callback233ms（隔离局部测值）；三主数据迟到拒绝/503撤旧与重试、供应商慢A快B详情及Escape通过。G05同版正常审计82、已提交丢回包显式同ID核对83、六并发同ID仅84、旧指纹409、仓管读取200/提交403，320/390/1280保留草稿，pageerror空、业务控制计数不变。登录截图及320来源表单已目视；原生浏览器首命令因工具PATH缺node未启动，补运行PATH后通过，不能将工具环境错误记为应用故障。

正式3100复查仍61/61、镜像9ec914…、容器937799a92aac…；本轮没有部署/重启正式服务、重置密码或变更正式数据。原173路径仍保留。新完整门解决本候选发布验证缺口，不代表全站/真实来源/业务UAT完成；供应商页内排序、仓库360失败/迟到、64迁移交付及101条仍待。

## 16:23准确候选续验

e1c8a95完整门NOT READY：645文件中644通过/1失败、5844通过/1失败。失败是G05新增消费`jdy:jst-item-master-mirror-observation`及`jst:item-master`未写入唯一消费者登记表，运维页面会误标无消费者。补真实模块/中文名，并把过期item-master孤岛断言改为精确消费者断言；不放宽全仓引用一致性检查。失败回执`2026-09-10T08-23-10-701Z.json`保留，完整门内生产构建与HTTP因前门失败跳过。

门前独立生产构建成功，冻结3385准确e1c8a95/64迁移；浏览器冷登录默认192ms、callback247ms（仅隔离测值）、三主数据迟到/503恢复、供应商详情竞态和Escape均通过。G05同包真实Chrome＋63540：正常审计79、丢回包同请求重放80、六路同ID仅81，旧指纹409、仓管GET可读/POST403，320/390/1280保持草稿，无pageerror，业务控制计数不变；320截图目视。新增合成SKU不代表真实平台商品确认。

正式容器再查变为937799a92aac…，启动时间2026-09-10T08:09:05Z；镜像仍9ec914…、61迁移。它相对前次观察已被外部重建，不是本轮部署，不声称启动时间未变。原工作目录继续保留。开发3384已停止，sample显示异常处理栈高占用，但没有可解码的业务错误，根因仍未最终确认。

最新实践复核：采用[React官方的清理/忽略旧读取原则](https://react.dev/learn/synchronizing-with-effects#fetching-data)；[Auth.js当前React接口](https://authjs.dev/reference/nextjs/react)及已安装next-auth/react.js都提供明确ok/error返回。本批保留现有认证平台与依赖，不将升级版本视作修复证明。

## 范围与背景

承接 G05 商品状态确认后的实际浏览器发现：热更新后首次认证成功但根路径空白，以及共享主数据表格旧查询迟到后覆盖新查询。保留现有 NextAuth、本地账号、权限、业务规则、数据库和发布路径；不更换平台，不重置正式密码。

此前的全景研究仍适用于架构方向，但其状态数字只对应记录时点。最新已通过完整门的应用是 a5a8d62；G05 的 c0033f2 有开发验证但不能继承前版完整门。本批修复也必须绑定自己的准确候选。

## 修复内容

- 登录只在明确 `ok` 且无错误时执行一次新文档导航，去除同时 router.replace/router.refresh 的竞争；保留同源 callback 校验。即时 ref 防连击，导航期间不重新解锁，失败后允许人工重试。
- CrudTable 复用 useDocumentRead：查询身份绑定、取消旧请求、迟到结果拒绝、超时、明确错误与重试。下一查询读取时撤下旧事实，不把失败或错误响应格式显示成“0条”。
- 列表业务筛选变化先用第一页发请求；刷新继续读取当前查询。编辑详情请求互相取消，旧记录的详情不能覆盖新记录的编辑表单；新建和卸载取消尚未完成的详情请求。
- SKU、供应商、仓库共享修复；不改变服务端授权、排序或写入契约，也不为页面新增独立缓存。

## 反例与定向证据

仓库外证据目录：`交付证据/2026-09-09-申报产能/`。

1. 原登录组件反例 7失败/1通过；原列表反例3失败。真实Chrome baseline 实测先慢OLD再快NEW，OLD仍覆盖NEW，见 `access-read-baseline-failure.json`。
2. 修后 `access-read-fixed1-result.json`：SKU/供应商/仓库均通过迟到结果拒绝、503撤下旧数据及当前条件重试；默认/带callback真实登录成功，无pageerror。只读合成响应，不代表正式业务数据或全站性能。
3. 390/1280截图保留。本次目视SKU390及供应商1280，工具栏与表格不贴叠、操作可见；这是局部界面证据，不代表全部97页。供应商合成响应的active并非正常合格枚举，不据此改生产状态标签。
4. 2026-09-10补连击/失败重试和详情竞态组件检查，5文件53项通过；三套类型、改动lint通过。包含existing useDocumentRead 26项和callback12项，不能报成53条新增能力。
5. 实际详情补验首次错误截图也超时，未保存原失败JSON；工具退出记录仍保留。脚本调整为先保存错误再尝试截图，`detail-read-dev-retry1-failure.json`确认停在login导航，尚未执行详情断言。3384登录/health再次curl分别10/5秒无响应，进程13888高CPU；sample记录保留。不能将其宣布为密码错误或已找到唯一根因。
6. 已核对owner、cwd、父子PID及端口，停止本会话3384开发进程；仅撤本次Next生成的next-env/tsconfig差异，未删文件、失败证据或数据库。后续以生产构建冷启动补验，不把开发成功自动外推到生产包。

## 全景总账校正

2026-09-10 16:14上海，正式PostgreSQL只读事务（5秒查询限时）取得：open告警69/resolved3；acked0；待办open147/cancelled1，账号5共140、账号1共8；事件open7/refresh451/close1。该close无actor、reason_code为auto_hysteresis，即自动迟滞关闭，不能计人工采用。通知飞书sent108、站内sent562/pending3，逐收件人reads0；墓碑0。近七天integration_runs只有jdy：83成功、2失败。

这些数字取代旧总账的当前值，但不能说明线下无人工作，也不能证明JST/用友当前凭据必然失效。逐流授权、身份、截止与对账仍需实际证据。发送状态不证明人已读；pending需对照调度，不能仅凭数量判故障。

历史156次提交、843变更路径在本轮重新复算成立；原总账509文件/3452项和旧镜像属于历史记录。当前正式3100只读健康61/61无漂移、镜像9ec914…；隔离3382为a5a8d62/64迁移。原目录0a56c4b且173变更路径继续保留。已提交c0033树为97认证页、285API、64迁移、643测试文件，数量不等于上线/业务完成率。

## 后续验收与不缩减范围

- 本批准确提交完整门、生产包冷登录/详情竞态/列表失败恢复；G05生产包保存与权限仍应补验。
- 原工作目录173路径归属和合并、64迁移正式备份/恢复与受控交付、正式账号与关键页面UAT。
- 供应商列表当前仍用前端分页内sorter/filter，不能标为全结果排序完成；仓库360仍有失败保持Skeleton及无迟到保护。另保留AppShell嵌套main语义、无节点NPD默认入口等已发现UX项。
- 总监12组101条逐项验收，G03真实账期协议、G04真实产能/责任/结果、G05回源资格；简道云逐流治理、聚水潭/用友授权与对账、飞书真人触达及最小权限。
- D69删除确认、D70真实派工、D71七/八天业务决定不变；化妆品及其他消费品的监管分类、目的地市场、质量/批次/效期、海外仓、币税单位不能因UI修复被删减。

来源：本仓NOW/CURRENT、总监需求验收续接、相关源码/测试；仓库外上述原始浏览器回执；`2026-09-09-当前工作全景复核.md`及本次Git、health、docker与只读SQL输出。Quick Tunnel仍仅适用于测试开发、无SLA保证，见[Cloudflare官方说明](https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/do-more-with-tunnels/trycloudflare/)；本次不变更公网入口。
