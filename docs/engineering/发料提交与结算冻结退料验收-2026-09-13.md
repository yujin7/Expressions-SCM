# 发料提交、窄屏明细与结算冻结退料验收

2026-09-13（上海）。最终应用候选 `0bd4e463e842193af7748517665ac2f02da2368b`，分支 `agent/scm-issue-submit-20260913`，工作树 `worktrees/material-source-20260913`。**候选验证，不是正式部署或真实业务UAT**。当前状态只看 [NOW](../NOW.md)，业务意图只看 [CURRENT](../spec/CURRENT.md)。

## 修复与边界

| 问题 | 修复 |
|---|---|
| FL创建使用调用方身份，来源验证在写事务外 | 事务内读取当前账号，复核启用/会话/仓管权限，先锁JG再核对当前状态、仓库和物料；取号、主表、明细、审计同成同败 |
| FL提交状态更新和审计分离，未复核当前来源 | 当前身份→JG锁→重新读取并锁定FL；复核来源、制单人/仓管/admin资格、草稿状态、版本；状态和审计原子提交。旧版本/重复提交409，不重复递增版本或写审计 |
| FL/TL非法JG筛选退化为未筛选整表 | 显式传入空串、非数、0、负数、小数、Infinity或不安全整数均400；未传仍表示不过滤。共享parseId拒绝不安全整数，避免舍入误命中。不是所有列表参数的全仓审计 |
| 手机明细数量拆行、物料身份挤成竖排 | FL/TL明细和创建表设置固定列宽与表内横向滚动；保留完整物料名、数量精度和中文标签。复用DocumentDrawer，不新建布局系统、不裁掉宽表 |
| **结算核销后仍可退料并消耗混合库存（PR53 P1）** | 所有退料创建、提交、新批准在JG锁内查冻结结算；已有JS且不是draft/pending时409，要求财务/仓管核对纠错。复用JG费用冻结状态集合；退料选择器在分页、总数及准确回读前排除冻结来源。普通历史查询不隐藏JG |

最后一项纠正上一轮“冻结后只是不自动改财务金额”的不充分边界：库存按仓库/SKU/批次混合，TL≤历史FL不能证明这笔JG核销后仍拥有可退库存；因此不能仅靠余额守卫放行。冻结财务/库存纠错流程尚未实现，不提供绕过开关。合法驳回与已完成准确轮次重试在新批准检查之前返回，仍受当前权限/职责分离约束。

FL提交保留既有当前有效制单人例外；非制单人不能凭旧仓管角色提交。不修改审批配置、金额权限或原有超发规则，不在创建/提交时过账。无新迁移或依赖。

## 证据

| 验证 | 结果 |
|---|---|
| 最终完整发布门 | **READY 9/9**，**712文件/6632项测试**；HTTP **34/34、零跳过**，全量lint、应用/测试/脚本类型、生产构建、迁移契约通过 |
| 精确回执 | `.artifacts/verification/2026-09-12T19-19-13-139Z.json`；起止同`0bd4e463`且干净；`release-0bd4e463.log` |
| 真实PostgreSQL竞争 | **10/10**，多连接和真实锁等待；`postgres-final.log`，夹具`ab6ca4b1`。前4项为退料/结算依据及累计超退超发；5–7为FL创建/提交与JG状态变化、重复提交；8–10为真正JS批准核销与TL创建/提交/批准竞争。5–6的JG变化是直接合成状态更新，不冒称完整关闭流程 |
| 原子性与权限测试 | 新FL边界19项、两列表参数16项；审计失败回滚、停用/会话变化/当前角色、来源状态变化、重复提交，另有4项布局结构契约 |
| 核销后混合库存回归 | 测试先真正批准JS核销20，留下1000混合库存；TL创建/提交/批准均拒绝且主表、明细、审批、审计、余额、流水不变。冻结来源准确选择为空，普通查询仍可见；驳回及既有已完成TL重放可用 |
| 反向验证 | 临时停用冻结拒绝条件，新增回归测试实际失败（本应拒绝的创建成功）；恢复后最终全门通过。`frozen-return-injected-red.log`保留，临时变更未提交 |

以上日志、截图与查询结果均位于`.artifacts/issue-submit-20260913/`。前两个已验候选也保留：`8e0affd9` READY9/9、711文件6627项；`13edcc8a` READY9/9、712文件6631项。最终结论只以加入冻结保护后的`0bd4e463`为准。

过程中的测试错误如实保留：首次列表测试暴露不安全整数被接受后修正parseId；冻结回归首次误把数字ID转成字符串（字符串只匹配单号）导致准确查询断言失败，改为数字ID；并发脚本混合返回类型推断错误修正后全类型检查通过。不是把失败删除后宣称首轮全绿。

## 实际浏览器 → API → 数据库

合成账号`warehouse01`实际表单登录；隔离端口3455、库`scm_contract_quickwins_20260911`、66迁移、后台作业关闭。没有修改正式账号密码、3100运行环境或生产数据。

- 在`8e0affd9`浏览器新建FL22（`FL-20260913-0010`），JG124，输入`1.2345`；页面点击提交后pending v2。旧v1再次提交409；数据库create/submit各1条，数量仍`1.2345`，`fl_issue`流水0。首次只读查询使用错误的`FL`来源字符串，随后按实际`fl_issue`及全部source_doc_id复核，未据此写数据。
- `13edcc8a`的390×844实际截图：FL详情750px、TL详情660px、两创建表540/580px分别在342/326px容器内滚动，页面scrollWidth390；物料名称可读，滚到右侧`1.2345`保持完整。创建表只读取/取消，没有额外提交。截图：`browser-fl-mobile-after.png`、`browser-fl-mobile-quantity.png`、`browser-tl-mobile-after.png`、`browser-fl-create-mobile.png`、`browser-tl-create-mobile.png`。
- 最初缩放立即截图有临时裁切；等待布局稳定后抽屉实际宽390、左边界0。真实缺陷是表格压缩，不是抽屉缺响应式。保留稳定的修复前图`browser-fl-mobile-before.png`，不把过渡画面当根因。
- 最终`0bd4e463`，JG133的JS19已完成核销。浏览器对TL19点击批准得到明确“结算已冻结，不可继续退料”提示，单据仍pending v2、流水0、无批准审计；新建API同样409。带returnEligible的准确JG查询total0，不带该筛选仍total1。
- 同一浏览器填写意见并驳回TL19成功：draft v3，reject审计1、流水仍0。截图`browser-frozen-return-refused.png`；查询`browser-frozen-return-database.txt`及`browser-frozen-return-rejected.txt`。浏览器错误缓冲无未捕获异常；健康响应编译标识为`0bd4e463`、66/66无漂移。

## 未完成与交接

- 此轮当时遗留的**FL/TL前端操作资格写死角色**，已由后续[当前资格与独立驳回验收](发退料操作资格与恢复入口验收-2026-09-13.md)续接；本报告原`0bd4e463`证据不据此改写为包含后续功能。
- 冻结后库存/财务差额纠错、代理成本资格、工单外物料、历史审批资格、歧义采购行与完整真实角色链仍待续接；极长名称/极限数量及其他页面不由四张表的窄屏证据推算全覆盖。
- 正式异地DB+附件备份授权、发布窗口、Safari HTTP/HTTPS真实登录、外部平台授权/数据资格、D69/D70/D71及总监101条真实验收仍未完成。
- PR53全绿后仍被策略阻止，实际原因是未解决的P1审查讨论，不是需要绕过分支保护。修复提交进入原PR并回复证据后再按正常检查合并；GitHub状态以实际查询为准。

沿用写路径技能约束原子性/锁顺序，体验技能要求等待稳定布局并比较真实窄屏，发布技能绑定精确候选。只更新NOW/CURRENT、领域报告、脚本导航及历史报告纠正注，不复制技能或创建第二份SSOT。清理只停止本任务已核对PID/cwd/端口的3455进程、浏览器和owner明确的隔离PG；数据库、冻结包、截图、失败日志与共享依赖均保留。
