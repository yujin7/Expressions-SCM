# 物料数量校验短路与错误恢复验收 · 2026-09-14

本报告续接[采购退货实物批次验收](采购退货实物批次与来源资格验收-2026-09-14.md)中真实`qty=abc`返回500的问题。物料链本项已修复并验收；不是全系统无缺陷或正式部署批准。当前发布状态只看[NOW](../NOW.md)，业务意图只看[CURRENT](../spec/CURRENT.md)。

## 精确范围与实现

- 应用候选`5b8e1bf0654382b4c46affab78f0ef82c5bc24e0`，分支`agent/scm-ct-physical-lots-20260914`；[PR99](https://github.com/yujin7/Expressions-SCM/pull/99)基于PR98。前一文档提交`1bd5c821`保留修复前的真实失败证据。
- 根因：Zod连续`refine`在十进制格式失败后仍执行`dCmp`，普通异常落成500。改为`pipe`，只有格式合法才进入正数/非负比较。TL/CT的10位整数、4位小数校验也通过共享管道接续，避免同一格式错误再报一次精度错误。
- 覆盖FL创建/修改、TL创建/修改、SH实收及可选预计数量、QC合格/不合格/让步数量、CT创建。保持字符串精度、数字输入兼容、首尾空白归一、原零/负数政策和可选字段含义。
- 不改全局错误响应、十进制算法、权限、来源、审批、期间锁或过账；真正的数据库/程序错误仍为500。无新迁移、依赖、历史回填或生产写入。

## 自动化与反向验证

新增`tests/matflow/quantity-validation.test.ts`22项、`quantity-validation-route.test.ts`21项。实际路由调用实际服务，仅认证/数据库解析/错误日志边界替身；10个字段入口分别验证错误路径在业务数据库解析前结束，合法输入仍能到达数据库边界，数据库故障仍为500，未登录仍401。

临时植回旧比较写法，新检查出现**21失败/22通过**；恢复修复后43项全部通过。包含非数字、空串、指数形式、逗号、异常数值/类型等；测试初稿错误地读取TL包裹schema的内部类型、且未考虑重复精度提示，均先失败后修正，未删除相应断言。

完整回执`.artifacts/verification/2026-09-13T19-16-41-324Z.json`：

- 起止干净同SHA；2026-09-13 19:12:00.721Z → 19:16:41.324Z。
- 自动READY9/9；**783文件、7729项测试通过**；HTTP **34/34**，失败0、跳过0；72迁移。
- 操作契约、PG迁移契约、完整lint、应用/测试/脚本类型检查、生产构建通过。文档后续提交不冒称重跑应用门。

## 真实浏览器 → HTTP → PostgreSQL → 页面

仅独立3489冻结运行包`.artifacts/runtime-quantity-validation-5b8e1bf0`，使用已有合成测试库/账号；未修改正式3100、真实密码或业务数据。

1. 实际表单登录后，浏览器认证请求覆盖7条写路由/10个字段组合，每组6个非法值，共**60请求全部400**且含具体`lines.0.*`路径，无错误编号。请求脚本不是各页面原生表单逐一验收。
2. 在完整回归启动前，60请求前后分别读17张表的行数及完整行摘要：FL/TL/SH/QC/CT头行、取号、PO明细、库存流水/余额、审计、批次、错误日志**全部不变**。不是只数单据条数，也不是以HTTP400猜测零写入。
3. 原生CT表单选PO262/采购行269、自有仓110、实际旧批次20、数量`0.0001`。测试仅将第一次出站数量故意改为`abc`，不伪造服务器响应；页面实际收到400并显示字段错误，表单和原数量保留。
4. 第二次原生点击发送未篡改数据，201创建CT4（`CT-20260914-0004`）。数据库只有该笔合法草稿，原行/SKU/批次准确、数量`0.0001`，本单库存流水0，PO已收仍`4.8766`。不提交/审批该合成草稿；草稿不是库存扣减证据。
5. 桌面详情与390×844手机详情实看，页面宽390、无整页横溢，明细在自身表格内横滚；正常导航/错误恢复后无捕获到的浏览器异常或框架错误遮罩。实际DOM捕获了短时错误消息；错误截图时间点已不含toast，不把截图冒称消息像素证据。

证据在`.artifacts/quantity-validation-20260914/`：`http-results.json`、`db-before.json`/`db-after.json`、`ui-recovery.json`、`recovery-db.json`、`injected-old-refinement.log`、`release.log`。复用浏览器助手的本轮图片在`.artifacts/ct-physical-lots-20260914/quantity-*.png`。一次自动等待误用了不存在的按钮文字而超时，重新读取真实按钮后继续；这不是产品加载失败。

## 下一修复与发布边界

- 同类扫描已在`inventory/schemas.ts`数量/期初单价与`inventory/count.ts`实盘数量**纯schema实测复现**`invalid decimal: abc`抛错，尚未修复；本轮未对这三个入口做真实HTTP复验。证据`inventory-followup.txt`。下一轮先补这些入口的400/零写入回归，不说全仓同类问题已清零。
- CT采购批次来源证明、原单纠正、完整审批配置资格和真实业务UAT继续按NOW；仓库批次余额不证明来源于本PO。
- [GitHub运行34777077783](https://github.com/yujin7/Expressions-SCM/actions/runs/34777077783)8个job均0步骤；Static checks检查103777045048的注释明确账户付款/额度不足而未启动。未获得云端通过，不能把本地通过替代它。
- 按`release-sweep`区分结论：**本物料错误路径候选验收通过；全系统正式发布仍NOT READY**。已知库存校验缺陷、依赖PR链、异地配对备份/发布窗口和实际Safari/公网/真实角色验收均未由此消除。
- 核对PID36285、cwd和3489监听后停止本轮进程；独立浏览器关闭；确认owner与127.0.0.1:65411绑定后停止隔离PG。测试库、冻结包、截图和回执保留，未删除任何业务数据或备份。

文档收尾：19项知识治理检查通过，四份相关文档111个本地链接无缺失；NOW为84行/14171字节。沿用现有canonical技能与双权威入口，不另建平行状态/技能副本。
