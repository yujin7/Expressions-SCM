# 告警人工处置：重放、并发与重新知悉

2026-09-08，隔离合成环境。对应101条中的预警/爆单处置与待办结果证据；不代签真实业务验收。

## 反例

- 重复知悉：一条ack事件却写入两条审计，时间和操作者可被覆盖。
- 并发关闭：两个调用均先读到open，第二个更新零行仍返回成功并写审计。
- 人工其他原因：前端要求备注，服务/API却可接受空白。
- 同日升级：引擎真实重置知悉后再次知悉，按日事件键吞掉第二条新事实。21:49新增行为反例为1失败/7通过，实际2次处置仅1条ack事件。

## 修复边界

人工知悉/关闭在同一事务内锁定目标告警；权限先于状态与重放检查。已知悉且仍open的重试返回原回执，不改变时间/人/备注、不追加审计。关闭仅首个操作者成功，其余409。manual必须有去空白后的解释。

已被引擎重置后的知悉属于新一代事实：首条仍保留历史日期键，后续键关联上一条不可变ack事件ID；同日升级不再丢失证据。普通重放在生成键之前返回。没有修改/删除旧事件及审计，不伪造历史缺失事件，无schema迁移。

D67升级/七天重置条件、D71放弃抑制的七/八天行为、角色边界、正式库存/销量/建议量均不改。

旧alert-engine-guards将“清空知悉后重新知悉”误当普通重试的断言改为两个明确行为：不清空的重试仍一条；清空后的新代为两条。新增测试用真实upsertAlerts升级，而不是仅手改状态证明升级链。

## 开发证据

- 告警及知悉规则：12文件86项通过；三套类型与改动lint通过。
- `交付证据/2026-09-08-爆单正命中/manual-action-postgres-generation.json`：专用PG5组通过，真实持有行锁并确认两个请求都在等待后放行；验证首次知悉、升级后知悉、并发关闭、知悉/关闭竞争与空备注拒绝。
- 旧red/green记录保留，generation记录标明dirty来源，不能冒充准确提交的全门。

## 2f224ab准确验收（21:58–22:02）

- 全门READY9/9，622文件5602项，HTTP33/33，lint/三套类型/生产构建通过；起止同一干净SHA。回执`.artifacts/verification/2026-09-08T13-58-50-093Z.json`；原始日志在外层交付证据同目录`full-release-2f224ab.log`。
- 同版冻结包3358，build revision完整匹配2f224abc3f68861d5fe7a136798510c793d13bcb；62/62零漂移、jobs关闭。`manual-action-postgres-exact.json`记录干净提交的5组真实PG竞争检查。
- `browser-manual-2f224ab.json`：78条合成日销经真实计算/看门狗生成的507未映射正命中，服务空manual说明400，UI知悉及HTTP重试回执不变。
- `browser-manual-close-2f224ab.json`：真实UI选择其他原因/填写明确QA说明/关闭，320→390→1920→320备注不丢，计数器距离footer15px；重复关闭409，原说明未覆盖。errors为空。
- `browser-manual-role-2f224ab.json`：仓管无处置按钮，对505 open知悉/关闭与507已关闭目标均403，不把状态暴露优先于角色拒绝。
- `manual-readback-2f224ab.json`：507的ack和close各恰一事件/一审计、操作者7、时间/原因/备注一致；505原有两条ack审计保留，未被拒绝请求追加或清洗。
- 320/390/1920弹窗截图已目视核对；关闭卡片展开显示来源、判定、上海知悉时间、关闭人/原因和完整QA备注。`2f224ab-close-reason-320.png`留存可见说明；不是仅靠API字段证明UI存在。

## 继续事项

本次证明上述合成处置切片，不等于真实风险被解决、全部爆单场景或全站UAT完成。正式3100仍61/61未部署，原工作区173路径未动。真实负责人、数据/UAT、G03–G05及101条仍继续；来源正命中的历史计算证据与本次人工处置证据分别留档，不改日期冒充同次全链计算。
