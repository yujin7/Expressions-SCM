# 退料原单纠正与实物批次保真验收

日期：2026-09-14。应用候选 `b6dcdda825eea6e4610e08cc670d28387f6f1877`，分支 `agent/scm-tl-draft-recovery-20260914`；[PR97](https://github.com/yujin7/Expressions-SCM/pull/97) 基于待合并 PR96。本文是候选证据，不是正式部署或真实业务签认；当前状态只看 [NOW](../NOW.md)。

## 缺口与修复

退料被驳回后回到草稿，却没有原单编辑入口；数量或原因错误无法沿原审批历史修正。已补现有详情路由 PATCH 和“修改退料草稿”：改退回自有仓、原行数量/原因、备注，可明确删除部分原行但至少保留一行。原单号、制单人、JG、来源委外仓、SKU、实物批次和保留行ID不换绑。退料不是新领料，不以FEFO替换实际退回的旧批次。

- 当前仓管原制单人或管理员才可改；重新验证账号启用、角色及会话版本。待批/完成/作废和已有退料流水的伪草稿不能编辑。
- 同事务锁JG、TL及有序仓库，重验来源、结算冻结、仓库实时记账与加工厂身份；版本冲突409，不覆盖别人。原批次须属于原SKU，受控退回允许已到期原批次，不扩大普通出库资格。
- 明细参数严格校验原行ID、重复/外来行、正数及最多4位小数；保存不提交、不审批、不过账，版本和前后审计原子更新。
- 编辑器同步防重复保存；网络结果不明、冲突或畸形成功响应必须先重读。关闭后的迟到响应不关闭新单据；提交在编辑期间禁用。
- FL/TL复用 `canEditMaterialDraft`，原FL行为不变；无新增路由文件、迁移、依赖、技能或平行状态文件。A098更新现有路由契约，台账当时为727项（逐项套件额外5项汇总断言不计控制条目）。

## 精确验证

| 证据 | 结果与边界 |
|---|---|
| 完整发布门 | READY 9/9；777文件、7641测试；HTTP 34/34、零跳过；类型、lint、生产构建及72迁移契约通过 |
| 候选稳定性 | 2026-09-13 17:47:27Z→17:52:26Z，起止均干净且同一 `b6dcdda8`；后续文档提交不冒称新应用重跑全门 |
| 真实PG双写 | TL179两个写者同版本保存，后者实际等待JG行锁；先写者2.1234成功，后者3.25返回409。版本2、原行ID179、仅1条编辑审计、零退料流水 |
| 实际浏览器 | 隔离3487、冻结候选，合成管理员与不同合成制单人。TL178申请6而本JG只已发5，不能批准；独立驳回后原单改4.1234和“不合格料退换”，再提交、批准成功 |
| 页面→请求→数据库 | 四次页面写请求均200：reject(version2)、PATCH(version3)、submit(version4)、approve(version5)。最终completed/version7；制单人284、JG488、原行178、SKU131、批次16保持不变 |
| 审批与库存 | 原驳回cycle2与批准cycle5都保留，审批人均非制单人。仅两条TL178流水：自有仓106 +4.1234、委外仓107 −4.1234；原批次余额分别9.1234、5.8766。草稿TL179仍零流水 |
| 桌面/手机 | 编辑器实际截图已看；390×844弹窗358宽，表格内部横滚，不撑宽页面。数量输入真实字符串4.1234；完成后只读提示和关联单据可见。浏览器错误列表为空；非全站/Safari验收 |
| 失败保护测试 | 新TL服务16项、编辑器7项、HTTP新增2项；覆盖冻结、失效身份/仓库、外来行/批次、精度、审计失败回滚、双击、冲突及迟到回调。编辑器断网/畸形响应为回调测试，不冒称实际浏览器故障注入 |

PG实际锁等待与提交后新版本复查为实测；锁行为背景见 [PostgreSQL 官方行锁说明](https://www.postgresql.org/docs/current/explicit-locking.html)。

## 验证中纠正的假设

1. 测试把实时仓只改为snapshot记账时触发已有仓库分类CHECK；改为合法的kind+accountingMode组合后验证业务拒绝。不是应用故障。
2. 数据库核对最初假设入/出库腿都是正sourceLineId。实际既有TL契约是委外出腿178、自有入腿−178；按服务实现修正断言后通过，未改流水。
3. 浏览器等待误用“批 准”文案，实际按钮“审批通过”；请求200且实际状态正确，修正观察条件，不重复提交。
4. CLI对长单号链接的原生点击未打开抽屉，观察到真实链接后用DOM点击进入；后续提交/驳回/保存/批准用原生按钮。此项不算原生指针导航通过，留作后续复核。AntD选项通过可见文本选择；辅助树浮点显示不替代input.value和请求字符串精度。

## 证据位置及资源

工作树内 `.artifacts/tl-recovery-20260914/` 保留 `pg-result.json`、`browser-writes.json`、`browser-db-result.json`、构建/全门/服务日志、桌面与手机截图及复现脚本。全门回执 `.artifacts/verification/2026-09-13T17-52-26-586Z.json`。冻结包 `.artifacts/runtime-tl-recovery-b6dcdda8` 保留。

按PID90022、cwd、127.0.0.1:3487核对后停止本轮应用；关闭专用浏览器，按owner与65411绑定核对后停止 `expscm-quickwins-pg-20260911`。合成库、证据、冻结包均保留，未删除业务文件；正式3100未修改、未部署、未重置密码。

## 尚未完成与发布边界

- [GitHub运行34772622184](https://github.com/yujin7/Expressions-SCM/actions/runs/34772622184) 8个job均未执行步骤；Static checks注释明确付款失败/额度不足。不是云端测试通过，也不是代码测试失败；所有者处理后须重验。
- main本轮仍 `ee1c977e45b1c4e30961d2d6d6a4ea2068fefde2`；PR97→96→…→55依赖链尚未合并，依赖合并后重接主线再验。
- 原来源/SKU/批次本身错误的作废替换、冻结结算后的差额纠错、跨工单真实物料占用与生产全角色试点未完成；此编辑器有意不提供改实物身份的绕行入口。
- 正式部署仍需获授权的异地DB+附件备份位置、访问方式与窗口；当前合成验证不替代正式Safari HTTP/HTTPS登录或真实业务UAT。
