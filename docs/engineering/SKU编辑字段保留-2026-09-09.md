# SKU 编辑字段保留 · 工程证据

## 问题与修改

G05商品状态接入前核对发现：updateSku 未传 lifecycle 时写 on_sale，创建schema的active默认值也用于更新；多个可选字段以空值覆盖。nearExpiryDays还被batch-trace的收货批次必填判断消费，因而不是单纯显示问题。

更新schema不再套用active创建默认值。SKU行锁内读取当前值，未提交保留、nullable字段明确null或空字符串才清空；必填字段、稳定主码、S1类型、维度校验不变。主档与周期参数、审计在原事务提交。表单只发送已变可选字段，旧表单不重发别人的状态/规格；显式改同字段仍沿用原语义，不声称新增乐观版本冲突机制。

没有新表、迁移、依赖、身份认领或平台状态自动映射，没有更改审批、数量、库存账、通知发送或D69–D71。

## 开发验收

- 初始3项测试2红1绿，修复后6文件21项通过。覆盖未传保留、显式清空、非法值拒绝、审计异常整笔回滚、创建/标准名称/S1/周期回归，且保留效期后无批次收货仍拒绝。
- 3381开发服务与隔离63540合成PostgreSQL，实际Chrome：仅改名称保留停售/停用/样品/效期；清空版本和阈值后JSON为null，重新打开仍为空。
- 旧表单打开后通过另一请求把状态改为trial、阈值300、规格50ml，再仅改名称保存，保留这三个新值。三组并发不同可选字段均保留；10条更新审计before/after连续，周期35/7保留。新仓管上下文PUT403。
- dev-retry1审计41–48，dev-stale审计49–58，均仅合成SKU；库存/JG/告警/待办/通知计数不变。320/390/1280跨宽度草稿保持、保存按钮可见，320截图已目视。
- 首次浏览器脚本以精确“货品名称/保存”定位，但可访问名称含必填标记和空格，超时；原failure.json/png保留。按真实AX名称修正后成功，不称一次全绿。原生浏览器登录后过早导航导致回到login，等待回调后正常；没有重置账号或放宽认证。
- 开发服务owner/cwd/监听进程核对后停止，Next生成的两个配置仅撤本次差异。正式3100和原工作目录未变。

以上为09:12开发阶段记录；准确候选验收见下节。正式交付仍待，不代表G05状态字典/人工映射或真实业务UAT完成。

## 准确应用验收（09:20）

- 应用a5a8d621d8211177585e59866cc2d662bc08a71a，分支agent/scm-workbench-evidence-20260907，完整门起01:14:42Z、止01:19:19Z，起止同一干净SHA。
- 回执`.artifacts/verification/2026-09-09T01-19-19-005Z.json`为READY9/9；640文件5807项、全仓lint、三套类型、运维/PG迁移契约、生产构建、同版HTTP33/33，无跳过。
- 冻结3382真实health为同SHA/64迁移/无漂移，jobs=0，隔离63540合成数据库。Chrome真实表单仅改名称、显式清空/重开、打开旧表单后其他请求修改状态/阈值/规格再保存、三组并发不同字段、连续审计59–68及新仓管PUT403全部通过。320/390/1280保存按钮可见、输入保持；320/390截图目视，pageerror为空。库存/JG/告警/任务/通知控制计数不变。
- 同版原件`sku-preservation-a5a8d621d8211177585e59866cc2d662bc08a71a-result.json`、三个宽度截图、`full-release-a5a8d62.log`、`owner-a5a8d62.json`，均在上述仓库外证据目录。
- 09:18复查正式3100仍镜像9ec914e49423…、61/61、启动2026-09-08T20:03:01.2246885Z，未部署。旧3380以owner89516、cwd frozen-d346ea7和监听版本三者核对后停止；未删文件/数据，保留新3382。
- 本次无新增迁移，回退代码不需倒转数据库；但旧更新代码会恢复本次数据保留风险。最新64迁移的目标环境交付/新增交易保护仍是独立未完项，不因本次PG契约检查通过而宣称完成。

## G05后续来源依据（只读，不是已实现）

正式镜像最近两次run357/334失败；最近成功run297/job524业务截止2026-08-31，6448行pending staging、`payload.data.itemStatus`全部字符串启用，6445个商品码，3个重复组/3条超额、缺码0。最初顶层字段查询全空，检查payload结构与摄取实现后改用data路径取证，未误报字段缺失。来源启用不等于内部在售；需展示旧批次截止与新尝试失败、处理重复/身份资格，再做人工状态确认。详见仓库外`交付证据/2026-09-09-0917-G05状态来源续核.md`。

## 证据位置

仓库外 `交付证据/2026-09-09-申报产能/`：`browser-sku-preservation.mjs`、`sku-preservation-dev-failure.*`、`sku-preservation-dev-retry1-result.json`、`sku-preservation-dev-stale-result.json`以及对应320/390/1280截图。失败与合成fixture均保留。

仓库测试：`tests/master/sku-update-preservation.test.ts`、`tests/components/sku-form-payload.test.ts`；实现：`master/sku.ts`、`master/schemas.ts`、`components/sku-form-payload.ts`、SKU页面transformSubmit。
