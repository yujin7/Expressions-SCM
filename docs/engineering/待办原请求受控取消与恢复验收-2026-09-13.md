# 待办原请求受控取消与恢复验收 · 2026-09-13

## 范围与裁决

本轮补齐状态/改派原请求的安全放弃入口，不是“取消待办”、撤销已保存操作或清空浏览器记录。只对明确保留的原UUID、所见版本和原意操作，不产生新请求编号，不改D70派工政策。

- 提交先获得请求锁并保存：取消只返回原保存回执，不回滚任务。
- 取消先获得请求锁：同事务追加`mutation_cancelled`审计，复用0071原请求唯一索引；后到同键PATCH返回409。
- 当前版本已前进且无原回执：原请求已经过期，返回事实，不伪造取消回执。
- 当前身份、会话和可见范围、原保存人及任务身份仍须通过；即使目标责任人已停用，也能阻止自己的未执行改派。
- 取消审计失败整笔回滚；重复取消只返回同一回执。任务状态、版本、时间、责任人、通知和库存不因取消改变。

## 界面与协议

原恢复侧栏增加“阻止原操作执行”与明确的第二次确认。确认前再次GET核对，再按同原键POST；取消回复丢失时保留本机记录。GET区分保存回执、取消回执与任务现状；阅读并再次核对后才能清理本机匹配记录。回执无效、异意、存储变化、卸载/换账号时不自动清理或重发。历史清楚写作“阻止原操作（待办未改变）”。

没有新增依赖、迁移、表或通用流程引擎；复用既有服务端当前身份、请求锁、事务、审计、单调版本、Web Lock与焦点返回。

## 已取证

应用候选`366b8c7235a88dbe367015eff3cd4245218093c5`，测试端口3477，冻结目录`.artifacts/runtime-todo-cancel-366b8c72`，72迁移隔离合成PostgreSQL，后台任务关闭。

- 待办相关回归：24文件/254项通过；app/test/scripts类型检查及本次文件lint通过。
- PostgreSQL实锁：11项检查、10次真实锁等待；精确候选复验夹具`5548acf1`、操作者265、回执2112/2115/2117/2119/2121/2123；保存/取消两个先后方向、重复取消、停用人员竞争均通过。原控制量库存414、告警4、通知55不变。
- 实际Chromium、测试admin、1280×800与390×844：隔离任务27从“开始”按钮产生原请求，人工故障使原PATCH未到服务端；二次确认取消已提交后丢弃回复；再次GET找回回执2110；手机端确认清理后回可见恢复区域；随后同键实际PATCH得到409。任务仍open/v2、责任人9，数据库只有一条取消审计。
- 桌面二次确认/回执、手机回执/清理/历史截图已检查，无横向越界，浏览器console/error记录为空。未把测试浏览器说成实际Safari验收。
- 浏览器脚本首次预期原始故障文字，实际共享网络层正确转为中文；第二处误预期返回工作区，实际恢复成功提示仍可见，回该区域符合焦点契约。均为测试断言修正，不是应用修复；没有重复执行原业务操作。

证据：`.artifacts/todo-mutation-cancel-20260913/`，含构建/门禁日志、browser-result.json、data-result.json、五张已检查截图、隔离运行与浏览器复验脚本。合成库、审计、冻结包与证据保留。

## 仍未闭合

仅覆盖待办状态/改派原请求；手工创建、普通跟进等其他写端不因此获得服务端取消。跨设备/损坏记录自助对账、其他页面交互、齐套来源/占用资格、冻结纠错、真实角色全链和总监需求完整验收仍在。候选不等于正式部署；3100与外部平台本轮未修改或刷新成功结论。

正式发布仍须授权异地DB+附件配对备份、发布窗口与真实Safari HTTP/HTTPS及角色UAT。本地门不能代替GitHub执行。

## 交审与收尾

[PR87](https://github.com/yujin7/Expressions-SCM/pull/87)基于未合并PR86；刷新后父分支仍为`ac204f3992319317f85d4e64f0c4fc23d2576851`，main仍为`ee1c977e45b1c4e30961d2d6d6a4ea2068fefde2`，未执行合并。应用CI[34757497471](https://github.com/yujin7/Expressions-SCM/actions/runs/34757497471)8个job均0步骤；Static checks注释明确最近付款失败或支出限额不足，未进入代码执行。须账户所有者处理后再验，不能报告GitHub CI通过。

已核对PID19983的3477监听和冻结目录归属后停止，只关闭本轮浏览器与隔离PG；正式3100未停。没有删除源数据、审计、依赖、构建或验收证据。下一工程优先级回到齐套来源与占用资格，不重复建设已完成的待办选择器/回执/本页焦点。

## 精确候选完整门

`366b8c7235a88dbe367015eff3cd4245218093c5`：2026-09-13 12:29:05.199Z→12:33:56.816Z，起止干净同SHA；READY 9/9，761文件/7452项，HTTP34/34零跳过。回执`.artifacts/verification/2026-09-13T12-33-56-816Z.json`。全量测试247738ms、构建30461ms、HTTP冒烟540ms；此为隔离机器一次测量，不泛化为生产性能承诺。后续文档提交不冒称新SHA重跑了应用门。
