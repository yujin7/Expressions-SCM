# 盘点读取与单号点击验收 · 2026-09-11

## 结论与边界

06:14续验：同一5f2c10dd/3420/PD2经独立CDP诊断与逐步浏览器操作，立即409显示正常、0.4000保留、GET恢复保持原版本；撤掉合成响应后实际点击保存，PD2为draft/version2/0.4000，create与update_counts审计各1，库存仍0.1000、无调整单。实际退出后GET401。成功截图`count-save-success-5f2c10dd-final.png`已目视。此前两次自动化超时仍未定因，不把它们改为PASS；独立CDP首轮因输入焦点未选中、次轮因点击未触发POST而终止，诊断文件保留。已补齐分段人工浏览器尾部，不宣称原整脚本无故障或产品/工具根因已排除。

准确应用候选 `5f2c10dd5fa6496868898c1937d86d14e520f371`，分支 `agent/scm-workbench-evidence-20260907`。
该候选首次自动完整门为 READY 9/9；首次报告中盘点保存失败恢复整条浏览器流程为 NOT READY / 未完成。上方06:14记录补齐了分段尾部，不改写原失败回执。后续服务写边界e411cdc6的验收见[当前权限与提交恢复](盘点当前权限与提交恢复验收-2026-09-11.md)，不可把测试门替代真实交互证据。
未部署正式 3100，未迁移或修改正式业务数据。正式容器、镜像与启动时间在本轮前后相同。

## 已证实的缺陷与修复

- 87b8a8c3 的 PD2 单号是两段 inline 文本，第一段 y=352.84–369.84，第二段 y=374.84–391.84。
  外接矩形中心落在中间空隙，`elementFromPoint` 返回 td，点击后 history 调用为零。
  同一页面点击第一行文字 (305,360)，立即触发 pushState，打开 `?docId=2` 抽屉。
  这是不连续点击区域，不是已证明的路由/水合故障。
- PD 和 SH 列表改用 `DocumentTargetLink`：inline-block 连续矩形，最小高24px；原生 href 保留当前列表参数并替换 docId。
  普通点击仍走既有抽屉选择，修饰键保留原生行为。未增加请求、导航状态层或授权规则。
- 新候选实测：PD2 的链接矩形128×44、SH52为130.47×24，中心均命中链接；两处实际点击与聚焦后 Enter 均打开正确抽屉。
  新标签/修饰键仅有组件行为检查，本轮没有完成真实多标签操作，不扩大结论。

## 准确版本检查

- 回执 `.artifacts/verification/2026-09-10T21-59-39-929Z.json`：起止干净且同 SHA；675测试文件/6112测试通过，三套类型、全量lint、生产构建、运维/迁移契约及同版HTTP34/34零跳过通过。
- 冻结服务 `http://127.0.0.1:3420`，65迁移无漂移。独立数据库 `qc_boundary_20260911`，容器 `exp-scm-capacity-20260909`。
- 9项链接组件检查与11项盘点读取/等待检查通过。将显示方式改回 inline 的植入使连续点击区域检查失败，恢复后20项通过。
- 1280/390/320px：盘点摘要650px、明细1016px在各自表内滚动；末列可达。截图已目视。
  截图刻意滚到最右侧，不代表物料身份被删除；不据此宣称所有页面或所有字段布局完成。

## 浏览器失败与续验，均保留

证据根目录：项目外 `交付证据/2026-09-09-申报产能/`。

1. `browser-count-workflow-87b8a8c3.json`：创建PD2成功后，单号中心点击失败；未重复创建。该单备注为“合成盘点读取与录入等待87b8a8c3”。
2. `browser-count-link-5f2c10dd.json`：15项通过，包含两个真实链接、键盘、三个宽度、未保存禁止提交；之后浏览器命令超时，重读时为about:blank，原结果为FAIL。
3. 同一PD2人工续验：保存等待锁住输入、扫码和关闭；30秒后明确“操作可能已完成”，保留0.4000，不自动重发。重新读取后原版本和本地输入仍在。
4. `browser-count-save-5f2c10dd-r2.json`：准确候选、原版本保留、等待锁定3项通过；释放合成409响应后浏览器命令再次超时，仍为FAIL。
   无证据足以判定是产品脚本、焦点/布局、浏览器自身还是自动化工具问题；不能静默归因或删除失败。
5. 最后独立SQL核对：PD2仍draft/version1，实盘0.1000，只有一条create审计、无update_counts。
   所以不能声称人工重试已经成功落库0.4，也没有为方便验收直接改库。

## 后续优先项

1. 定位保存失败反馈后浏览器超时，继续同一PD2，不再创建；完成409恢复、人工保存、唯一审计、库存不变和实际退出401。
2. PD服务提交当前是状态更新后事务外审计；创建/保存/审批也须补当前身份和并发边界核验。不能用客户端防双击代替。
3. 单号组件先覆盖本轮PD/SH；其余相同链接需按真实消费者与URL契约逐项核查，不做全局CSS替换。
4. 正式Safari本地HTTP会话问题、异地备份与发布前置、101条真实UAT、三方数据资格和D69–D71均仍待。

## 本轮技能如何约束工作

`design-supply-chain-flows` 要求验证真实点击、键盘及窄屏而不是只看回调测试；`parallel-sessions` 保持独立候选/冻结服务和正式环境分离；`release-sweep` 明确自动门通过不覆盖失败的浏览器故事；React检查未引入额外请求或重复状态。
