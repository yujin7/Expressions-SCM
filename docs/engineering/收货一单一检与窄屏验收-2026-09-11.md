# 收货一单一检与窄屏验收

日期：2026-09-11。应用候选：`3eb379b13a9597f5e94286831184bd176b24094c`。

## 业务边界与改动

仓管/admin在已审批SH上全行检验；合格、不合格、让步数量精确相加等于实收。检验提交后不修改，一单只有一检；确认入库仍经过账引擎，不直接改库存。事务内读取当前账号并持共享锁，再锁SH核状态和重复，QC头/行/审计原子。0064仅增加两条唯一约束，不清理或猜修历史。

三张详情表（收货行、检验结果、检验录入）设置明确表内横滚和物料列宽，不隐藏字段或将中文表头挤成竖排。320px下需要在表内横滑核对数量；未新增全站样式、业务规则或审批步骤。

## 证据分层

外部证据目录：`交付证据/2026-09-09-申报产能/`（项目容器根目录，非Git工作树内）。

| 层级 | 已验证范围 | 证据 |
|---|---|---|
| 开发反例 | 合法夹具4红1绿；重复QC、过期状态、旧身份、数据库重复约束 | `tests/matflow/qc-write-boundary.test.ts` |
| UI契约 | 修前4红，修后4绿；只是静态约束，不代替浏览器 | `tests/components/sh-detail-layout.test.ts` |
| 准确候选全门 | 663文件6027项，9/9、HTTP34/34、65迁移零漂移，起止同一干净SHA | `.artifacts/verification/2026-09-10T17-23-52-680Z.json`；外部`full-release-3eb379b.log` |
| 真实PG竞争 | 六请求确实等SH锁；一QC/一审计/五409；四次入库一过账/三409；锁内新closed拒绝；直接重复插入23505 | 外部`verify-qc-boundary-3eb379b.json`，准确SHA且dirty为空 |
| 浏览器 | 仓管真实表单登录，合成SH6实际检验和入库；重复409、运营403、退出401；1280/390/320编辑/结果态表头高度、内部横滚、末列可达与无整页溢出，36项 | 外部`browser-qc-3eb379b-r2.json`；`qc-*-3eb379b-r2.png` |
| 正式状态 | 容器/镜像/启动时间不变，3100仍61/61；未应用本候选迁移/代码 | 本轮只读检查；正式缺`.env.backup` |

## 失败记录与限制

- 最初夹具缺SPU导致失败，补齐后才算有效反例；未把夹具问题当业务缺陷。
- 65d4e41完整门6021通过/1失败：新迁移未登记台账。补A710及65迁移控制总数后重新完整运行，不降低目录双向核对。
- 准确PG脚本首次从项目容器根启动，tsconfig别名未解析，尚未执行测试；改从正确工作树启动后7项通过，没有重放已执行的写入。
- 浏览器首轮`browser-qc-3eb379b.json`保留FAIL：resize后横滚读取时宽度由357变342，首次最大scrollLeft少15px。复验等待实际滚动尺寸稳定，仍要求最后表头全部进入可视范围。最终截图已目视，第一轮不是PASS。
- 合成数据库`qc_boundary_20260911`独立于正式库，来源为种子PO及显式合成SH；不是业务人员UAT，不说明真实数据已补齐。未上传检验照片，不宣称附件流程本轮验收。
- SH锁只串行同SH；**不同SH同时入同PO的已收累计仍待独立并发核验**。原PO同SKU多行归首行规则本次未改，不能称该分摊已完善。
- 正式发布、异地备份、101条逐条业务验收和三方集成资格仍独立待办。HTTPS实际账号登录成功与本地HTTP正式修复未发布也须分别披露。
