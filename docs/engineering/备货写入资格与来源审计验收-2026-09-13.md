# 备货写入资格与来源审计验收 · 2026-09-13

## 结论与候选

应用`39f41457d0e136e12f9084104f75863a1d9b8b16`，分支`agent/scm-bh-write-authority-20260913`，[PR71](https://github.com/yujin7/Expressions-SCM/pull/71)，基于PR70文档锚`d6dde08d403aa9c569c44775531824c5ff311f32`。**隔离READY9/9，未正式部署、未签真实业务UAT。** 当前状态只看[NOW](../NOW.md)，业务意图只看[CURRENT](../spec/CURRENT.md)。

上一批已闭合原BH明细生成回执；本批续接其上游交易资格和来源审计。使用写路径技能落实同事务身份、来源锁、审计边界，按发布技能验证准确候选。现有CLAUDE/AGENTS及7技能已有这些约束，本批补实际执行与行为证据，不新增第二套规则或状态权威。无新增迁移、依赖、页面或API，迁移仍68。

## 已修复

| 原缺口 | 当前行为 |
|---|---|
| BH创建/编辑/提交/审批/撤回/收口直接信任调用者角色或旧会话 | 每次在拥有写入的事务内回查并锁定当前账号；停用403、已提供会话版本失效401；不借调用者伪造admin/ops权限 |
| 创建前在事务外检查SKU启用 | 事务内按SKU ID顺序共享锁定并校验启用，再取号、建单和写审计，避免停用期间凭旧结果建单 |
| 操作目标与生成/编辑等竞争缺少共同锁 | 同BH写端锁定准确头单，复用当前DB本人/共享渠道范围后定位；范围外404；锁后执行原状态、版本、制单人及审批规则 |
| NPD/实时补货/冻结S&OP靠给调用用户补ops角色调用直录建单 | 显式内部`createDerivedBh`来源：NPD允许当前PMC/ops/admin，补货及S&OP允许当前PMC/admin；直录仍仅ops/admin。来源入口要求事务内钩子，身份仍是真实账号 |
| 实时补货草稿先提交，再在事务外写来源审计 | BH头、行、取号、创建审计、补货来源审计同成同败；来源审计失败不再遗留无来源草稿 |

提交/编辑仍由制单人或admin执行；审批配置、审批人标记、禁止自审（含admin）、审批轮次幂等保持原规则。withdraw/void/reopen/complete/short_close继续复用原状态机与身份规则，不自动关关联单、不写库存。数量、基础单位、日期、业务时间以及计划冻结口径不变；自动生成仍只产草稿。

使用现有`currentWriteActor`与`bhReadScope`，没有另建权限框架。[PostgreSQL官方锁文档](https://www.postgresql.org/docs/17/explicit-locking.html)说明冲突行锁等待后读取更新行；本项目并发证据实际观察等待，而非只引用理论。当前scope记录读取不是所有跨用户范围变更的全局冻结协议，不能扩大证明范围。

## 自动化证据

- 完整门`.artifacts/verification/2026-09-13T04-06-50-409Z.json`：起止干净同39f，**READY9/9、740文件/7011项、HTTP34/34零跳过**；全量lint、所有类型、构建、运维与PG迁移契约通过。
- 定向5文件**51项**：新增BH资格17，原草稿编辑17、来源事务2、补货7、冻结计划共识8。覆盖六类写入停用/会话失效、伪造角色、派生/直录权限区别、来源审计回滚、审批/撤回/收口审计回滚、重放撤权及当前范围。
- 新夹具首次遗漏范围记录`createdBy`必填值，定向16通过/1失败、类型检查也报同一夹具缺字段；补齐后51项及全部类型通过。没有修改schema或放松断言；初始失败日志保留。
- 真实PG `scripts/verify-postgres-bh-write-authority.ts` **5/5**；精确候选夹具`e8b9c2b2`（初次开发夹具`f2f0669c`也5/5）。观察第二连接确实处于Lock等待：编辑先提交→旧版本提交409；审批先提交→撤回409；两位审批人→仅一条审批/审计并重放成功；撤销审批标记先提交→403；SKU停用先提交→建单400。库存流水414不变。
- 冻结`.artifacts/runtime-39f41457`在自有3464运行，后台任务关闭；健康68/68无漂移、build SHA匹配。完整门另用`.next`；本次构建生成的next-env/tsconfig差异逐项撤回，未覆盖未知修改。

## 实际浏览器流程

隔离合成账号运营226、审批PMC227，SKU90（`BHAUTH-46357f94-FG`）。没有操作正式账号或真实库存。

1. 运营实际登录，在BH新建表单搜索并选择SKU、输入数量`12.3456`和期望日期`2026-10-12`，点击保存；生成BH113（`BH-20260913-0009`），准确打开详情。
2. 点“修改草稿”，填写核对原因，将数量改为`23.4567`，保存后version2；原SKU/日期保持。点提交及二次确认，成为pending/version3；制单人没有自审按钮，仍可撤回。
3. 第二个独立浏览器会话用PMC227登录并打开准确BH113；具备审批/驳回入口。直接请求直录创建BH返回403“需要运营角色”，没有因内部派生兼容而放宽直录。
4. 实际点击审批通过并确认，成为approved/version4；有一条PMC227的approve/cycle3记录。数据库精确核对四条审计分别为create/update_draft/submit（226）和approve（227），只有一行SKU90、数量`23.4567`、交期`2026-10-12`。
5. 桌面截图及390×844手机详情已目视核对；手机整页390、表窗口342、内容628内部滚动。角色说明、单号/状态、长名称、日期、审批记录均可读；未新增UI样式，不能据此宣称全站体验已验。
6. 浏览器错误列表为空，开页有内容、无框架错误覆盖层。库存流水**414→414**。这是隔离双角色故事，不是正式Safari/真实业务试点。

`.artifacts/bh-write-authority-20260913/`保留PG、构建、门禁、服务、夹具、DB断言及桌面/手机截图。原始截图、密码、数据库及冻结运行包不提交GitHub。

## 未闭合边界与后续

- 实时补货及手工BH创建尚未具备与手工WO同等的持久请求键/丢响应恢复；本批只保证单次建单与来源审计原子，不宣称重复点击一定同单。
- NPD首单与冻结S&OP的既有回执重放在上游服务，仍需复核重放时当前身份、来源资格及跨账号范围；本批派生入口保障实际新建事务，不用它冒称上游所有路径已加固。冻结共识/当前实时开单模式的跨事务切换也未整体冻结。
- 历史BH→WO未分配明细人工认领/纠错、真实齐套到厂/质量/占用、冻结财务库存差额及全链真实角色验收仍待续接。外部连接器本批未重新探测，不把mock/历史token等同业务可用。
- [CI 34737112914](https://github.com/yujin7/Expressions-SCM/actions/runs/34737112914)的Static checks及Tests(1/4)注释已实查为账户付款/额度使job未启动。不是代码断言失败，也不是CI通过；需所有者处理后重跑。
- PR71基于PR70，70→69→…→55依赖未合并。不自动合并到功能分支；上游合并后重接main、重新核对候选。正式3100未修改/未刷新探针，仍需授权异地DB+附件配对备份、访问方式和发布窗口，之后验证实际Safari HTTP/HTTPS、首次改密及真实角色试点。

## 收尾

- 技能治理19项、钩子安全13项及系统审计726项，共758项通过；本轮4份文件79个本地Markdown目标均存在，差异格式检查通过。NOW为83行/14,087字节；历史证据和未完成范围未删除。
- 自有3464进程PID59102、父进程59077及冻结目录核对后停止，两个专用浏览器会话关闭；owner及65411端口确认后的隔离PG停止，3464已无监听。正式3100未动。
- 保留证据和合成库，不删除共享源文件、依赖、附件或备份。文档收尾提交单独核验，不冒称文档SHA重跑应用全门。
