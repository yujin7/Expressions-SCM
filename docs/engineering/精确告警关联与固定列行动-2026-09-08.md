# 精确告警关联与固定列行动

2026-09-08；续接101条需求的库存/爆单预警行动链与G02。基线6779fea，本批不改库存、数量策略、告警写权限或D69–D71。

## 问题与修复

1. 两张决策列表原先下载最新500条告警后在浏览器找关联。超过500条时，真实旧告警可能被误报为没有告警，未知悉计数也被截断。
2. 复用`GET /api/alerts`新增精确keys模式，仅开放inventory_cover/sales_spike未关闭类别；按授权范围、类别、精确键查询，不向浏览器扫描无关历史。
3. JSON键保留店铺逗号/竖线；每批最多100键且编码后6000字符，客户端最多4并发。返回完整性检查不接受部分成功；类别未知悉计数单独统计，不把多批计数相加。
4. 两张页面读取绑定当前身份集合；切换/刷新撤下旧状态与动作，取消旧请求，15秒超时后显式重试。未知、成功无可见告警和真实0分开。爆单旧查询行在effect前撤下，CSV不会把未读取状态写成无告警。
5. 实际1920浏览器发现，独立知悉列在未横向滚到底时被右侧固定跳转列盖住，普通点击会命中链接。两表合并为170px的“知悉与行动”固定列，状态与跳转垂直排列；不靠强制点击绕过遮挡。库存表少130px，爆单表少100px。

## 已取得证据

- keys接口旧实现12条反例失败：超过500、空键、范围/金额脱敏、畸形及歧义请求。
- 固定列两个组件反例旧实现失败，修后保留同一动作单元的状态回调与右侧固定属性。该测试不声称证明浏览器几何。
- 14:49定向10文件151项通过：接口、范围、分批、读取生命周期、卡片、深链/HTTP和客户端边界；改动lint零问题，三套类型通过。
- 新PG16合成实例`exp-scm-lookup-20260908`，loopback65373，62迁移；2条旧库存告警+502条新填充=504条。来源为已有合成窗口库的复制，不是实际业务库。
- 开发3352、基线6779fea加本批未提交改动，三段共13组记录：主脚本3组、续验7组、仓管3组。不得称为13条全站用例或单脚本全绿。
- 正常旧列表返回500条且无id=1；新keys返回该旧告警，类别未知悉总数504。PMC正常按钮知悉后503，id=1仍open；PG读回只有1条ack事件和1条ack审计。id=2未知悉，仓管直接调用403，没有额外事件。
- 慢响应、刷新失败、15秒超时、重试和忽略已取消请求的迟到响应均验证；成功无关联显示“无可见告警”，空筛选仍有授权类别总数。
- 1920桌面、390/320手机展开证据与行动取得截图，窄屏无页面水平溢出；仓管amount已被API移除而safeCount=7仍可读，按钮不展示。浏览器page errors为空；控制台保留预期403网络记录。

## 失败保留与范围

外层`交付证据/2026-09-08-精确告警关联/`：

- `browser-dev.json`：等待表达式换行转义错误；无通过组。
- `browser-dev-attempt2.json`：已取得>500关联证据后遇到真实按钮遮挡，截图保留。
- `browser-dev-attempt3.json`：修复遮挡后3组通过，止于测试按“重试”查找，而实际AntD名称是“重 试”。
- `browser-followthrough.json`：续验7组通过，止于原生Auth.js退出页按钮名“Sign out”，不是中文“退出登录”。实际点击确认退出后继续仓管验证。
- `browser-warehouse.json`：3组通过。`warehouse-320.png`为滚动位置的整页拼接，固定头显示在拼接中部是截图方式；另以scrollY=0取得`warehouse-top-320.png`并人工检查，不把拼接图当页面顶端几何证明。
- `qa-owner.json`、`fixture.json`、准备脚本、开发脚本与各截图均保留。未改写失败回执为绿灯，未重置合成知悉以假装首次操作。

## 尚待

本批准确提交的生产构建、全量发布门及冻结包浏览器复验；爆单真实合成源数据到关联的业务浏览器；大导出的取消、失败恢复及>5000异步边界。现有分批接口/组件测试包含sales_spike，但不替代它的业务端到端验证。

实际3100服务与真实数据、账号、外部连接、派工均未修改。原工作区173个路径差异保留。本批开发证据不能继承451df6b的发布READY，也不是实际部署/UAT完成。

## 15:03生产包复验续修

2ba6bd8准确发布回执为`.artifacts/verification/2026-09-08T07-01-28-180Z.json`：READY9/9、613文件5493项、HTTP33/33；开始结束相同干净SHA。冻结包版本与62迁移匹配。

`browser-frozen.json`三组后failed：版本、旧id=1关联以及正常按钮知悉id=2均已验证，仍保持open且类别未知悉502。下一个刷新按钮的可访问名称带上了透明loading图标。DOM证据：按钮不带ant-btn-loading类，图标容器width=0/opacity=0/scale(0)，但内层role=img、aria-label=loading仍被辅助工具计入按钮名称。这不是接口仍在加载，更不能用强制定位掩盖。

续修为两表刷新/重算按钮增加稳定aria-label与aria-busy，共享错误组件重试也按subject稳定命名，惠及它的其他调用者。新增5个失败反例，修后2文件64项通过，三套类型与改动lint通过。没有新增依赖/缓存平台/权限规则；React检查侧重稳定名称、显式忙碌状态及复用共享错误组件。新候选必须重新取得准确发布与生产包交互证据，旧门不覆盖此增量。
