# 爆单正命中与窄屏证据

2026-09-08。范围：D56/G02正命中验收中发现的展示缺陷；不改阈值、身份、授权、数量或D69–D71。

## 实测发现

独立PG数据库`spike_chain_qa`由已有纯合成QA库复制，正式3100及原QA库未写。冻结8492a20运行3355。
新增78条日销量/7条对照/6个SKU及一条促销计划，使用计算服务→真实看门狗→统一告警引擎，未直接插入告警。
普通/促销/未映射共3条告警（505–507），重放只刷新3条；缺日、低基数、平稳、跨店缺日不能互补不命中。
源→API正确，1920/390/320无页面级溢出；但320实际截图证明：固定行动列挤住长SKU，参数Tag长值被表容器截断。
页面点击知悉505产生状态反馈，重复POST保留open；后续关闭操作在发现布局缺陷后暂停，不冒称全部整链已过。

## 修复

- 爆单同一个分页Table在窄屏用完整事实卡列，桌面保留宽表；两种都可按涨幅排序，不另挂全量卡片或新增数据请求。
- 卡片保留完整编码/名称/店铺、各日日期及数量、基线/阈值、最新涨幅、截止、知悉与动作；促销/未映射明确标注。
- 共用AlertEvidence在/alerts及库存/爆单使用：关键触发依据常显，技术参数快照用原生details按需展开、键值换行，不裁掉长值。
- 不扩大权限，不改写历史事件或关闭原因；排序仍使用已加载的完整命中集合。

## 开发证据与失败留痕

新4项测试旧实现全失败（其中两项因新卡片尚不存在），修复后通过；106项相关测试、三套类型、改动lint通过。
新增Grid断点使旧无DOM mock缺导出，首轮6项失败；只补xl桌面mock，原加载/权限/固定列断言未删除。
PG夹具第一次在写入前误把roles当jsonb，42883；改按真实text[]选择，失败原件保留。
原浏览器脚本5组后因scoped snapshot的refs仍含全局元素而失败；改按局部snapshot文本选择后续验，不覆盖原失败回执。
首次关闭弹窗截图截在动画中，不能据此判透明样式故障；正式验收必须等待动画结束再截图。

外层证据：`交付证据/2026-09-08-爆单正命中/`。准确候选发布门、冻结包布局/关闭/拒绝/结果回读尚待；不能继承8492a20绿灯。真实用户/UAT、101条、G03–G05及受控部署继续。

## f426cd7准确候选与第二个实测缺陷

21:11完成准确完整门READY9/9，621文件5592项；冻结3356健康返回同SHA、62/62无漂移。
浏览器首轮4组后误认为补货深链使用数据库ID，实际契约是SKU码；查源码与真实href后修正验证器，原失败JSON留存。
续验10组覆盖源/API/精确关联、1920/390/320完整身份和日量、真实506知悉与重放、技术参数展开与无浏览器错误。
截图目视及DOM测量发现共享关闭弹窗的count下沿497、footer上沿488（320px），重叠9px；不是动画问题。
因此停止实际关闭，新增备注计数器流空间契约测试：旧实现1失败、其余8项通过。
修复在共享AlertCloseModal文本域预留24px，保护22px绝对定位计数器；两个消费者为/alerts和/inventory/alerts，不改全局Textarea或任何写入语义。
新候选仍需准确全门、关闭弹窗几何/截图、关闭原因持久化和权限拒绝；f426cd7的READY不继承。

## 0313d2f断点重建丢失弹窗

21:20准确完整门READY9/9，621文件5593项，HTTP33/33。320/390计数器与footer正间距已验；但从390切1920，已展开行仍在，局部AlertCloseModal被重建，弹窗消失。原browser-0313d2f-open.json失败保留。
不将它归咎于验证器：真实弹窗状态原在Table的展开单元格内部，响应式列重建使其丢失。
修复将库存/爆单每个Tab的关闭目标与唯一弹窗提升到表外；行内只请求选中目标，避免一行一个Modal，也避免桌面/手机重复实例。提交保护、原因备注及服务端权限仍用原共享组件。
新增父层目标保持测试旧实现1失败，修复后4文件108项、三套类型和改动lint通过；真实带备注跨断点、结果回读和新准确全门仍待。

续查写路径还应单列验证：人工manual备注要求在服务端是否同样强制，以及并发关闭CAS未命中时的审计去重；本批尚未重现或修改，不能由顺序重放通过推断并发安全。
