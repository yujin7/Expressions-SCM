# 在这台笔记本上实跑系统

> 2026-08-04 实测跑通。生产版（Next standalone + PostgreSQL + pg-boss 调度）已在本机运行。

## 现在的状态

| 项 | 值 |
|---|---|
| 访问地址 | http://localhost:3100 |
| 数据库 | PostgreSQL 16（容器），本机可连 `127.0.0.1:15432` |
| 调度器 | pg-boss，18 个任务已注册，时区 Asia/Shanghai |
| 同步时段 | 10:00 / 16:00（午餐前、下班前），告警 11:00 / 17:00 |
| 容器健康 | healthy |

**与开发服务器并存**：开发服务器占 3000，本机实跑用 3100，互不影响，
数据库也各自独立（dev 是 PGlite 文件，实跑是 PostgreSQL 容器）。

## 日常操作

```bash
# 启动
docker compose -f docker-compose.prod.yml -f docker-compose.local.yml --env-file .env.prod up -d

# 停止（数据保留在 docker volume 里）
docker compose -f docker-compose.prod.yml -f docker-compose.local.yml --env-file .env.prod down

# 看日志
docker compose -f docker-compose.prod.yml -f docker-compose.local.yml --env-file .env.prod logs -f app

# 改了代码后重新部署
docker compose -f docker-compose.prod.yml -f docker-compose.local.yml --env-file .env.prod up -d --build app

# 健康检查
curl -s http://localhost:3100/api/health
```

关机重启后 Docker 会自动拉起（`restart: unless-stopped`），只要 Docker Desktop 是开机自启的。

## 让同事也能访问（局域网）

默认只绑 `127.0.0.1`，**只有这台笔记本能打开**。要让仓库、采购在别的电脑上用：

1. 把 `docker-compose.local.yml` 里 app 的端口改成 `"0.0.0.0:3100:3000"`；
2. 把 `.env.prod` 里 `AUTH_URL` 改成 `http://<本机局域网IP>:3100`
   （**必须改**，否则登录后回跳指向 localhost 会失败）；
3. 重新 `up -d`。

⚠ **开放前请想清楚**：这是明文 HTTP，同网段任何人都能访问，也能抓包看到登录信息。
内部小范围试用可以接受；长期使用应放到有 HTTPS 的服务器上。
另外笔记本合盖/断网时服务就没了——这也是它只适合试用、不适合当正式生产的原因。

## 账号

`npm run db:seed` 已建好 admin / ops01 / purchasing01 / warehouse01 / warehouse02 /
quality01 / pmc01 等账号，密码是播种时通过 `SEED_ADMIN_PASSWORD` 传入的那一个。

**首次登录后请立即改密**（系统内「修改密码」）。播种脚本拒绝弱口令与默认密码，
密码不写在任何文件里。

## 三方集成在这台机器上的状态

| 系统 | 状态 |
|---|---|
| 飞书 | ✅ 可用（群机器人 + 签名） |
| 简道云 | ✅ 可用，14 条契约，10:00/16:00 自动拉数 |
| 聚水潭 | ⏳ 出库单权限已开通，等 IP 白名单（本机出口 IP `183.6.68.35`）+ 另 3 个接口权限 |
| 用友 | ⏳ 等 8 条 API 授权 |

> 聚水潭的 IP 白名单填的是**这台笔记本当前的出口 IP**。换网络（换 WiFi、开 VPN、
> 去别的办公室）出口 IP 会变，届时会重新报 `110`，需要在聚水潭后台更新。
> 这也是长期应放到固定 IP 服务器上的另一个理由。

## 排查时用得上的两个坑（都已修，记下来免得再踩）

1. **容器健康检查一直失败、但宿主机访问正常**
   Next.js standalone 默认绑到容器主机名对应的 IP，不是 `0.0.0.0`；
   healthcheck 用 `127.0.0.1:3000` 因此永远连不上。
   已在 compose 里设 `HOSTNAME: "0.0.0.0"`，并写进 `npm run check:ops` 防回归。
2. **数据库密码认证失败**
   PostgreSQL 的 `POSTGRES_PASSWORD` **只在首次初始化数据卷时生效**。
   卷已存在时改环境变量没用，要么进容器 `ALTER USER scm WITH PASSWORD '…'`，
   要么删卷重来（会丢数据，慎用）。
