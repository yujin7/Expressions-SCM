# 平台身份写入边界与条码竞争复核

日期：2026-09-06；基线 `89dd4101ca4597d63d5792b2d44ca7d8bdcbdcce`。
独立候选：`agent/scm-studio-compact-20260906`。不修改原工作区、真实业务库或外部身份。

## 修复与业务效果

1. 三个 POST（单条认领、批量认领、批量补条码）统一委托新鲜身份守卫。
   服务层也检查既有 admin/pmc/purchasing/warehouse 角色及 D62 的全局身份治理范围。
   GET 与写端复用相同范围策略，展示能力与写角色复用一处定义；保留 admin 的既有范围豁免。
2. 条码补齐复用已有物理条码归属锁和所有权检查，不再只检查 `skus.barcode`。
   GTIN、历史标识（含停用但仍保留归属的记录）占用时拒绝，不把空旧字段误当成无人占用。
3. 每行仍独立事务、只补空字段、不覆盖已有不同值；写入与审计同事务，审计失败则条码回滚。
   不自动认领、不改库存、不清理真实重复条码，也不为兼容字段新增全表 UNIQUE。
4. PostgreSQL 身份并发脚本扩展为 GTIN/GTIN、补条码/补条码、补条码/GTIN 三种竞争。
   修正旧脚本尝试删除 append-only 审计后忽略失败的清理方式：现在保留合成记录与审计。
   复用既有安全准入，只允许显式授权、loopback、`scm_contract_*` 库，拒绝 URL 覆盖。

## 证据

- 修复前的有效反例：5 个服务权限用例及 2 个跨标识条码归属用例失败；不是从注释推断。
- 新增 HTTP 边界 36 项：三条写路由的会话失效、角色拒绝、渠道/部门范围拒绝、授权角色正向及 admin 例外。
- 服务边界 10 项：角色/范围拒绝发生在任何事务前；启用/停用 GTIN 与 legacy 归属均保留；审计注入失败后无条码/审计残留。
- 脚本安全准入 12 项：三种模式均在创建数据库客户端前拒绝未授权、非一次性库、远程和 URL 覆盖。
- 实际 PG16：原 QA tmpfs 容器（无主机目录挂载），独立新库 `scm_contract_identity_20260906_1820`，端口65364。
  61 条迁移应用后目录契约通过；未装入任何业务导入（import_jobs=0）。
- 三种真实竞争都观察到 2 个 session 的 advisory lock 等待；释放后只有一方成功，另一方为解释明确的409归属冲突。
  跨旧字段/标识表合计只含一个 SKU 所有者；各竞争只产生一条成功审计。最终执行无 pg 查询排队警告。
- 三套 TypeScript、全仓无缓存 lint、生产构建274/274及 standalone 安全检查通过。
- 首轮全量有1项失败：新增架构门未注册进审计台账；已将 A701 与正文/分类/总量/测试期望同步，不放宽目录双向匹配。
- 最终全量：561 文件 / 4,849 项全部通过，失败与 pending 均0。
  `/tmp/expscm-identity-write-final-20260906.json`；首轮 JSON 保留在
  `/tmp/expscm-identity-write-full-20260906.json`，不覆写为通过。

## 验证边界与剩余事项

- 真实 PG 证明针对上述三个竞争形态，不代表所有身份生命周期写入、历史重复条码或其他导入路径都已穷尽。
- HTTP 专项执行真实路由与策略，但新鲜身份读取/下游写服务被模拟；实际服务另由 PGlite/PG 验证。
  本轮未重新进行真实浏览器登录或外部人工认领 UAT。
- 内部服务的 actor 来自调用者；HTTP 路由负责回查当前会话与范围。本轮不声称批次运行中途的撤权可回滚已经提交的行。
- 批量服务仍沿用逐行结果契约；部分失败的持续展示、重试定位与错误分类仍需单独完善。
- 未运行本提交完整 `check:release`、未推送/部署；不可继承旧 `070463e` 的 READY。
- D69 删除真实性、D70 责任归属、D71 抑制天数及三方授权均未变动。

本轮 write-path 技能驱动接口/服务双层权限、真实并发和审计回滚验证；parallel-sessions 保持隔离候选、端口和数据库边界。
