# 库存预警完整导出与下载权限绑定

状态：开发验证通过，准确候选完整门/冻结包验收待续；**未部署3100**。
原需求：G02 / SSA-4，兼顾CP-4所有导出出口的权限与可解释失败；不宣称上述需求完整验收。

## 用户流程与边界

计划员在库存预警按当前筛选/排序导出，不只导出当前页。5000行以内直接CSV；超过5000行返回持久任务回执，进入导出任务页下载。异步任务按保存的筛选/排序读取执行时数据，不声称是点击时冻结快照。44列数量/覆盖/日期证据由显式列映射生成，不扩散任意金额字段；未知、零、负值区分，排序先作用于全结果。

沿用既有数量列表读取范围、CSV行上限和截断说明，不改补货定量、库存、审批、外部身份或D69–D71。新接口`GET /api/export/inventory-alerts`复用注册表与worker。

## 修复

- 同步/转后台的共享导出按钮：同步双击防重、30秒超时、停止等待、筛选变化/卸载取消、迟到响应无效；202回执持续显示，200必须为CSV，禁止把登录HTML下载为文件。不自动重试可能已建任务的请求。
- 现有文件下载复用该生命周期，区分“重新下载”和“重新创建任务”，失败留在原页，不开JSON标签页。
- 导出任务页复用15秒有界读取；上次成功读取有排队/生成任务时，五秒后才再读，不重叠轮询。后台刷新保留已完成行且标明旧快照，避免取消正在下载的文件；手动刷新或读取失败撤下旧动作，失败停止自动轮询、显式重试恢复。最新100条范围公开说明。
- 任务页由八列收为五列；手机同源卡片；完整失败原因可触摸/直接阅读，不靠hover。时间显示上海时间。知悉时间另复用`formatAsOf`，避免UTC截字误作上海时间。
- 任务创建与审计同事务，创建/执行复核实际账户及导出角色。下载与任务列表使用新鲜会话。
- worker在已有params JSON保存服务端权限指纹与文件SHA256；客户端保留键被剔除。下载复核生成者当前权限、账户状态及实际文件hash；降权/改范围/旧文件缺绑定/文件损坏要求重新导出。没有迁移，没有自动回填旧文件权限或删除历史文件。

## 反例及失败保留

1. 原任务页刷新保留旧行、轮询无可见读取状态、卸载无AbortSignal；已用实际组件回调测试钉住。下载组件使用与失败文本可读性另有结构/渲染断言，不冒充浏览器证明。
2. 浏览器1829轮发现真实响应为`{error}`，按钮误读`message`而只显示409。修复前针对实际响应形状的测试失败；复用`fetchJson.serverErrorMessage`，不回显HTML/对象/过长错误；保留原失败JSON和截图。
3. 1834轮后台任务#2仍由热更新前global单例worker执行，缺权限绑定，下载409。确认DB里两任务均done、无运行中任务后，仅重启已核对归属的3353进程。没有篡改任务#1/#2或为它们补签权限，原失败回执保留。涉及worker变更的开发验证必须重启后重验，不能只依赖HMR。
4. 原下载安全反例覆盖降权、停用、旧文件、其他用户/管理员等；渠道反例的首次运行有夹具字段错误，不把它算作有效旧实现反例。修正夹具后行为测试通过。

## 当前证据（隔离合成，不是业务UAT）

外层证据目录：`交付证据/2026-09-08-库存预警完整导出/`。

- `browser-export-dev-1837.json`：八组通过；pmc01，3353，1920/390/320；旧文件中文拒绝、创建任务#3、实际5002行44列全排序CSV、读取失败/重试、15秒超时、窄屏、停止下载/重试相同文件、浏览器errors空。截图已目视检查。
- PostgreSQL隔离`exp-scm-lookup-20260908` / 65373：62迁移；5002个EXPORT-QA合成SKU，任务#3 done/5002/权限绑定存在；#1/#2无绑定保留；每个任务恰一条create审计。未写实际3100。
- `full-5002.csv`：SKU从EXPORT-QA-5002降序到EXPORT-QA-1；`mobile-retry-5002.csv`字节相同。
- 定向行为测试、三套类型、改动lint已执行；准确候选完整门的汇总随后单独记录，不继承e279f09的READY。

## 尚未完成

- 本批准确SHA完整门、生产冻结包及权限撤回后的真实浏览器复验、共享库存余额/流水导出回归。
- 旧`AsyncExportButton`（风险/分层/检测/库存分析主动转异步入口）仍用旧POST/toast/popup交互，未将本批防重取消覆盖宣称适用于该入口。
- G02正命中爆单源→命中→关联→行动全链；真实渠道×仓销售事实、当期覆盖与部署/UAT。
- G03账期谈判、G04申报弹性产能、G05聚水潭状态、原101条逐项验收仍继续。

## 发布兼容提示

新下载门拒绝无绑定的历史导出文件。文件保留可追溯，用户需回原列表按当前权限重新导出；不能批量把旧文件贴上当前权限指纹。发布应将生成worker与下载路由作为同一版本部署；回退旧代码会丢掉本批下载安全门，不能只以“无schema变更”认定回退无风险。

## 19:00续修：四报表共享入口与登记

699462a准确门结果NOT READY，5529项通过/1项失败：新API漏登台账。新增A704并同步分类/正文/索引/总数，不删除或放松双向扫描，709项专项通过。该候选的冻结生产包8组浏览器通过，任务#4实际5002行/44列；续验320px主库存页任务#5回执在x=27..293、宽266内完整显示，重算/刷新另行换行，无页面横溢；截图已目视检查。不是全站视觉结论。

随后补四个既有AsyncExportButton消费者（risk、segmentation、detectors、inventory-analytics），不改API、队列或业务权限。统一由ExportButton提交POST，201仅接受正整数job.id；与200 CSV/202任务分流。请求身份包含序列化筛选，等值内联对象重建不取消，真正改筛选或离开取消等待并屏蔽迟到回执。保留30秒界限、同步防连点、停止等待/核对任务提示和持续可读的错误，不弹新窗口、不自动重试、不声称取消后台任务。错误JSON/HTML回执只给安全说明。

8条新反例在旧实现全部失败，旧postJson特意委托同一fetch以避免只因mock不同判红，其中连点实际观察两次POST。另加卸载与HTML回执检查。复跑导出/共享网络3文件145项通过；台账专项另计709项。该续修的准确完整门、四报表浏览器、权限撤回以及余额/流水共享入口仍须续验。上方“旧AsyncExportButton未修”是699462a时点历史，不适用于本节代码，但不能据此宣布生产交付或101条完成。

## 19:27续修：源总量与文件必须核对

c22feb4已于19:10准确完整门READY9/9（618文件5541项、HTTP33/33），冻结包10组交互通过。但该浏览器脚本只比CSV与任务rowCount，未比源总量：分层/库存分析5009对象各只导出500行，风险/检测器空样本未暴露同类问题。原回执保留，不能当完整数据证明。

四个生产器取全筛选结果再slice到独立cap，列表HTTP仍保留500上限，不从请求透传allRows。服务已先计算全体再分页，故不做每页重复全量计算，也不改业务口径/权限/排序。worker写文件前要求total为非负安全整数且rows.length等于min(total,50000)，否则任务失败、没有新文件、无自动重试；合法空集保留列头，超过50000仍有明确截断行。旧文件不删除或伪造新绑定。

四个502条正样本均在旧代码报500≠502（此前夹具缺渠道kind的失败已修正，不算业务反例）；修后验证全身份集合、17行cap稳定前缀和尾部SKU筛选。worker六种少/多行或非法总量反例旧实现全部误报done，修后failed且目录空；新增空集及50001→50000明确截断的成功测试。定向6文件45项、三套类型、改动lint通过。

同时修DecisionVisual卡片自身换行：标题260px弹性基准，工具不足时下一行，标题和说明不裁切；不按整个屏幕宽度猜嵌套卡片。已检查库存分析、驾驶舱和ProjectionDrawer调用。新准确候选完整门、生产包源→任务→CSV核对、320/390/桌面以及共享消费者体验仍待；实际3100、原173路径、101条其余需求不受这批局部通过覆盖。
