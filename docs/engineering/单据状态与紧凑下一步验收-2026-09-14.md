# 单据状态与紧凑下一步验收 · 2026-09-14

应用候选 `61c85791701185f183c2136784d696d05f188033`，分支 `agent/scm-document-outcome-guidance-20260914`；[PR113](https://github.com/yujin7/Expressions-SCM/pull/113) 依赖 [PR112](https://github.com/yujin7/Expressions-SCM/pull/112)，未合并、未部署。续接[CT替代关系](采购退货错单替代关系与恢复验收-2026-09-14.md)和[库存替代关系](库存错单替代关系与恢复验收-2026-09-14.md)，解决这些流程自身的重复提示、模糊入口和窄屏信息密度，不宣称全站美学或性能已经完成。

## 变更与保留边界

- 库存与CT详情共用一个具名“单据状态与下一步”区域：保存的作废/短关原因、当前服务端操作限制、准确原单/后继链接、当前替代资格与下一步集中排列。缺原因明确历史未登记，缺资格不展示创建入口。
- 库存“作废/短关不冲销已过账库存”、CT“作废不代表已退货或已冲销”等关键边界始终显示。只把较长的替代流程说明放进原生 `details/summary`；按单据ID重置展开状态，不让上张单据的帮助展开拖长下一张。
- CT作废详情移除可创建独立新单的重复“新建正确退货单”页头按钮，只保留明确关联原单的替代入口；普通新退货仍从列表创建。不复制错误来源/实物/数量，不自动保存或退货。**此项替代PR112报告中仍保留独立新单页头入口的旧描述。**
- CT无可用页头操作时不渲染空操作容器；详情标签保持完整横排，长内容可换行。样式只作用于新提示组件，不新增全局CSS补丁、依赖、路由、数据库迁移或权限规则。候选迁移仍78，能力台账控制仍740。

工作流技能使“原因→限制→准确下一步”集中而不藏关键警示；发布技能要求同一候选的浏览器及数据库证据。展开交互参考 [W3C disclosure键盘约定](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/)，标签/内容分别使用已安装AntD支持的 [Descriptions样式接口](https://ant.design/components/descriptions/)。React检查复核纯展示边界、调用者拥有导航/创建、稳定key及无额外取数；没有新建技能或第二份状态权威。

## 同数据、同视口测量

基线 `85959eb1` 与本候选分别运行精确冻结包，使用同一隔离库CT57、库存80。数值为侧栏body顶到首个事实明细区的距离，包含CT原有链路条；不是单个提示组件高度，更不是API速度。帮助默认收起，所有关键警示保留。

| 页面/视口 | 基线 | 候选 | 减少 |
|---|---:|---:|---:|
| CT · 1280×633 | 451px | 366px | 85px |
| CT · 390×844 | 613px | 462px | 151px |
| 库存 · 1280×633 | 272px | 212px | 60px |
| 库存 · 390×844 | 309px | 233px | 76px |

手机CT还消除了页头额外高度，首个事实区绝对位置719→520px，提前199px。两页两视口document.scrollWidth均等于视口宽，桌面/手机截图均已查看。表格自身仍允许横向滚动，不把不可见列或较短合成名称当作所有长名称都已验。

本机证据 `.artifacts/document-outcome-20260914/`：`before.json`、`after.json`及同名前缀截图；生成脚本保留。冻结包 `.artifacts/runtime-document-outcome-61c85791`。这些忽略目录中的原始截图/日志不是随Git自动分发的交接附件，本文保存可审查的结果与复核方法。

## 真实页面交互及控制量

独立3489、合成admin账号，后台任务关闭；未操作正式3100、真实口令或外部平台。原生浏览器完成：

1. 原生 `summary` 聚焦后Enter展开、Space收起，两类详情均通过；不是通过修改 `open` 属性伪造键盘结果。
2. 库存80→81保留 `status=void` 和准确docId。CT57打开帮助后跳58，故意让一次详情GET拒绝：旧原因/关联/事实撤下，显示读取失败；点重试GET成功到58，显示它自己的前驱，帮助恢复收起。`navigation-retry.json`保留3个GET及结果，无业务写请求。
3. 从当前真实GET允许替代的库存75、CT51点击唯一替代入口，打开重新填写的空表单，正确标识原单；库存空保存触发4个字段错误而不POST，CT空创建按钮禁用。两者所有捕获请求仅GET，document宽390无外溢；`interactions.json`及空表单截图保留。
4. 浏览器错误收集为空，无框架错误层。自动化首次误把新建窗口当侧栏、随后误把CT非Form容器当Form而等待失败；改为实际Modal结构再完整重跑通过。此为验证选择器错误，不作为应用失败或业务成功证据。
5. 前后逐字段SHA256比较8张业务表完全相同：stock_balances132、stock_ledger509、stock_docs80、ct_docs57、bh_docs154、stock_create_requests18、ct_create_requests17、bh_create_requests12。未新增/改动业务单据或请求回执。登录/冒烟产生的认证审计不在这8表声明内。

## 精确候选与交付状态

完整本地门 **READY9/9**：806文件/8199测试，HTTP34/34零跳过，迁移78/78，全部类型范围、全量lint、运维契约、生产构建通过。回执 `.artifacts/verification/2026-09-14T02-22-45-999Z.json`，UTC02:18:08.027→02:22:45.999，起止同SHA且工作树干净。针对性4文件72项已通过，其中新组件21项覆盖资格矛盾/缺失、非void拒绝创建、现有后继、忙/恢复阻断及关键警示不折叠；CT29项含唯一替代入口与空白创建。后续文档提交不冒称重跑应用门。

应用[云端运行34799068306](https://github.com/yujin7/Expressions-SCM/actions/runs/34799068306)8任务均零步骤，Static checks 103837865024注释明确近期账户付款失败或额度需提高；云端检查未启动，本地通过不能代签。PR113→112→…→55仍为依赖链，远端main本轮核对仍`ee1c977e45b1c4e30961d2d6d6a4ea2068fefde2`，仓库保持私有。

核对3489 PID62282、冻结目录及实际监听后已停止；独立浏览器、有owner的隔离PG也已停。保留合成库、冻结包及原始证据，没有删除业务文件、备份或共享依赖。CLAUDE/AGENTS及7个canonical技能无需复制本次状态；NOW更新当前交付锚，CURRENT记录意图变化，旧报告保留时点含义。

文档收尾：这三份入口/报告124处本地链接均可解析；NOW84行/14178字节；能力台账与技能治理2文件37项通过，`git diff --check`无错误。不是重新计算全项目完成率，也不复制旧轮测试计数。

**尚缺**：正式获授权DB+附件异地配对备份与发布窗口、云端计费恢复后的检查、正式Safari HTTP/HTTPS及真实角色UAT；其他来源单替代/退出、跨设备/损坏请求恢复、已过账纠错、真实批次来源/工单占用、外部事实资格及总监101条全量签认继续。此次视觉收敛不改变这些边界。当前状态只看[NOW](../NOW.md)，业务意图只看[CURRENT](../spec/CURRENT.md)。
