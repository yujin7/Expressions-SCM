# 外部数据融合 · 决策增强计划（2026-09-02）

目标：把简道云、聚水潭、用友的数据真正变成决策的输入，而不是停在「已同步」。
本文先说实测到的事实，再说瓶颈在哪，最后给分阶段的执行清单——第一阶段已经落地。

---

## 1. 事实：三方数据今天到底进来了什么

生产 PostgreSQL 只读实测（2026-09-02）：

| 系统 | 状态 | 已进入系统的数据 |
|---|---|---|
| **简道云** | ✅ 15 条流每日自动同步（10:00 / 16:00） | 天猫 SKU **日销量** 191 万行、退款 132 万行、平台费用 1.3 万行、天猫/拼多多/唯品会 SKU 对照 34 万行、采购需求/采购单/收货/供应商/仓库/盘点/调拨/样品 各数十~数百行（2024-12 停更的旧应用） |
| **聚水潭** | ⛔ 6/6 接口被权限/出口 IP 阻断 | 0 行业务数据；连接器、对账、可信仓白名单、日出库对比器都已就绪 |
| **用友** | ⛔ token 通、8 条 API 授权 0/8（HTTP 403） | 0 行；运行时客户端、契约白名单、证据留痕已就绪 |

**结论一**：现在唯一活着的外部数据源是简道云，而且最有价值的是**天猫日级 SKU 销量**：
最新批次覆盖 2026-05-01 ～ 2026-09-01，3 家店、2,076 个平台 SKU、支付金额 ¥5,751 万。

**结论二**：系统内部的销量事实（`sales_monthly`，人工上传）**停在 2026-06**。
销速、可销天数、滞销、临期风险、ABC、补货建议全部基于它——比外部观察晚了 3 个月。

---

## 2. 瓶颈：不是数据不够，是身份对不上

外部需求信号、退款驱动、履约缺口、渠道贡献……所有分析都只能作用于**已映射到系统 SKU** 的那部分。实测：

| | 平台 SKU 数 | 占支付金额 |
|---|---:|---:|
| 经对照表 → 商家编码 精确映射 | 573 | **46.7%** |
| 在对照表里、有条码但无编码（条码认领队列 291 条待处理） | 282 | ~4.7% |
| **根本不在对照表里** | **1,217** | **44.2%** |

销量最大的品全在最后一行：NING 补水睡眠面膜（¥161 万 + ¥139 万 + ¥69 万）、清洁泥膜（¥108 万）、
DEVIANCE 氨基酸洗面奶（¥76 万）…… 既有的身份控制塔只能看见对照表里的行，对这一半营收是盲区。

**所以第一阶段不是再接更多数据，而是先让现有的一半营收能归到 SKU。**

---

## 3. 第一阶段（已落地）：营收排序的身份缺口 + 第二条身份桥

### 做了什么

1. **读模型 `platform-sku-identity-gap`**（`/api/report/platform-sku-identity-gap`）
   - 每个平台 SKU：4 个月支付金额、件数、退款、最近售出日、身份状态
     （已映射 / 已认领 / 对照表无编码 / 条码待认领 / 不在对照表），**按金额倒序**；
   - **建议候选**：按店铺推断品牌，用规格（`净含量:220g` → `220g`）与名称词元和系统成品比对打分，
     最多 3 个，只建议不裁决；
   - 给出「若采纳全部建议，金额覆盖率会到多少」，让业务看到投入产出。
2. **第二条身份桥**：业务在决策工作室「平台身份覆盖」标签一键认领 →
   `sku_identifiers(kind=external, scope=JIANDAOYUN:TMALL, value=店铺|平台SKU)`，
   写守卫回查、同事务审计、幂等、冲突 409（`/api/master/sku/platform-claim`）。
3. **外部需求信号**在对照表给不出唯一归属时改走这条桥——认领一个，覆盖率立刻上升，
   滚动需求简报、退款驱动、履约对比全部受益。
4. 读模型随连接器同步重建，并在认领后主动刷新；绑定精确批次 + 认领版本。

### 纪律（与既有裁决一致）
- 一切仍是 `observation_only`：不写 sales_monthly、库存、销速、补货；
- 候选分数只表示相似度，永远由人确认；同一平台 SKU 已属于别的 SKU 时拒绝抢占；
- 测试：`tests/report/platform-sku-identity-gap.test.ts`（排序、状态、候选、认领→双读模型联动、冲突）。

### 意外收获：279 个平台 SKU（金额 28.8%）可以一键认领
最新对照表批次里有 **829 行 / 279 个平台 SKU**（支付金额占 28.8%）的「商家编码」与系统 SKU 编码逐字相等，却从未被认领——
治理规定外部码即使同码也不自动认领（`tests/integrations/jiandaoyun-identity-boundary`），
而对照表契约只解析条码，这些行连认领队列都进不去。读模型把它们攒成 `exactHits`，
页面提供「一键认领精确命中」（人复核后批量确认，每行独立事务与审计，单行冲突不拖累整批）。
上线时实测：按金额覆盖 22.6% → 点一次即 51.4%；再采纳全部相似候选可到 57.4%。

**2026-09-02 已执行**：以 admin 批量认领 279 行精确命中（279 条 `sku_identifiers` external/JIANDAOYUN:TMALL，
逐行审计 `create_from_alias_claim`）。按金额覆盖 22.6% → **51.4%**，驾驶舱可用外部影子列的 SKU 74 → 129，
「内部无动销但外部在售」从 4 → 5 个被识别。依据是公司自己对照表里商家编码与系统编码逐字相等；
如需回滚：`update sku_identifiers set active=false where scope='JIANDAOYUN:TMALL'`（审计保留）。
剩余缺口需要人判断：有相似候选的可再推到 60.5%，其余是主档里根本没有的品（睡眠面膜 220g 等）。

### 业务要做的
在决策工作室 → 平台身份覆盖 → 「天猫平台 SKU 身份缺口」按金额从上往下认领。
前 60 个就覆盖了未映射金额的大头；有候选的一行只需核对后点「认领为此」。

---

## 4. 第二阶段：让最新销量进决策（观察叠加，不改事实）——第一片已落地

已上线（2026-09-02）：读模型 `external-velocity`（系统 SKU × 近 30/90 天净需求，锚定批次最大业务日，
两条身份桥都算，未映射 = null 不是 0）；经营驾驶舱滞销榜与临期/滞销风险页新增「外部近 30 天净需求」影子列；
驾驶舱顶部显示内部事实截止月、外部观察截止日、时点差，以及**内部判无动销但外部近 30 天仍在售**的 SKU 数。
`tests/report/external-velocity.test.ts`。覆盖率越高这列越完整——所以第一阶段仍是前提。

| 决策面 | 现在 | 加什么 |
|---|---|---|
| 滞销 / 临期风险 / 可销天数 | 只看 `sales_monthly`（到 6 月） | 并排显示「外部观察近 30/90 天净需求」与覆盖标记；内部说无动销、外部在卖的 SKU 单列——**这是最直接的少报废、少错打折** |
| 补货建议 | 内部 3 月销速 | 「外部观察销速」作为影子列 + 差异，不自动采用；识别外部已断货（连续 N 天 0 销）而内部库存充足的 SKU |
| 经营驾驶舱 | 全渠道销量 2026-06 | 数据龄告警：内部事实晚于外部观察 N 天；按外部观察补最近月份的**观察值**（明确标注） |
| ABC / 帕累托 | 近 6 月内部 | 外部近 90 天的影子 ABC，对比分层漂移 |

实现路径：一个 `external-velocity` 读模型（SCM SKU × 业务日净需求，已由外部需求信号产出 `demandBySkuDay`），
供各页面按 skuId 取 30/90 天窗口；每处都带 `authority / coverage / asOf`。

---

## 5. 第三阶段：把「全渠道」补齐（拼多多、唯品会销量）——契约已落地并同步

2026-09-02 用 `/app/entry/widget/list` 实核字段、按统计日期探针时效后，新增 4 条字段最小化契约
（已写入 `JIANDAOYUN_SYNC_CONTRACTS`，随 10:00/16:00 自动同步）：

| 契约 | 表单 | 用途 | 时效 |
|---|---|---|---|
| `tmall-unit-daily-observation` | Tmall_0.03 单品日汇总 | **子货品编码 = 系统编码**，平台 SKU ID → 系统编码的第三条身份线索（进入 exactHits 一键认领） | 2026-01 有值，已停更；平台 SKU ID 不变故仍可用 |
| `pdd-order-observation` | Pdd_C.01 订单查询列表 | 拼多多订单级需求（商家编码-规格维度；不取消费者资料/买家留言/商家备注） | 最新 2026-08-20 |
| `vip-shop-trading-observation` | Vip_A.01 店铺交易数据 | 唯品会 店铺×品牌×日 销售额/量/转化率 | 最新 2026-08-03 |
| `tmall-product-pnl-observation` | Tmall_0.02 宝贝日汇总 | 天猫 商品×日 真实成交、销售费用、预估毛利/净利 | 最新 2026-08-01 |

拼多多订单实测每天 3,000~6,000 行明细，全量与 120/60 天窗口都超过 1,000 页安全上限。处置：契约加**服务端时间窗**
（`window: { field, days: 3 }`，简道云 data/list filter），每次只拉最近 3 天（约 7,000 行）；快照守卫对时间窗契约
不再套用"全量行数不得下降 / 旧记录必须仍在"（那两条只对全量快照成立）；`external-velocity` 把最近 90 天内所有批次
按业务键（订单号+商品+商家编码）去重后累加，滚动快照随每日同步自然累积成 90 天窗口。
拼多多的身份：对照表 1,555 个商家编码里 274 个与系统编码逐字相等但 0 个已解析（治理不自动认领），
身份缺口页新增「拼多多精确命中」一键认领（scope `JIANDAOYUN:PDD`，值 `店铺|商品ID|商家编码`），认领后拼多多分量才有数。

`external-velocity` 已把拼多多有效订单件数（剔除已取消/退款成功）并入近 30/90 天净需求，并分平台可拆
（`tmallNet30 / pddNet30`）；身份走拼多多对照表的已解析身份。唯品会（品牌级）与天猫宝贝损益（产品级）
的读模型是下一步：前者进渠道结构，后者进毛利视角。

原始普查见 `docs/integrations/简道云表单时效普查-2026-08-04.md`：

| 表单 | 价值 | 已知 entryId | 前置 |
|---|---|---|---|
| `Pdd_C.01_dd_订单_订单查询列表` | 订单级、**带商家编码**，能直接对上主档 | `69b2195752dfffff3fcd5da1` | 拼多多商家编码命名空间需业务确认（NOW 已记） |
| `Vip_A.01_店铺交易数据` | 店铺×品牌销售额/量、转化率 | 待用 widget 列表核对 | 品牌级，不到 SKU |
| `Tmall_0.01/0.02 店铺/宝贝汇总` | 店铺日汇总，可做控制总量对账 | `69af9342…` / `69aa70f3…` | — |

做法与天猫完全同构：新增契约（appId `699ebeac318154b4f6d3dda6`）→ 观察 staging → 身份桥 → 读模型。
每接一条流都要先跑 `npm run jdy:survey` 核字段，再定 businessKey 与 freshness。

---

## 6. 第四阶段：聚水潭与用友解锁后自动点亮的能力

代码已就绪、只等授权（`docs/integrations/待办-控制台动作清单.md`）：

- **聚水潭**：日出库 → 需求脉搏「简道云需求 vs 聚水潭出库」对比器立即有数；全仓库存 → 库存分析多一条外部口径；
  逐 SKU 控制总量 + 7 天恢复 UAT 后才可作为运营事实。
- **用友**：供应商/组织/采购/入库/成本/凭证 8 条只读流 → 供应商记分卡、价格偏差、渠道贡献的
  财务侧校验（关账、币种、折让）。

---

## 7. 治理与度量

- 每个外部读模型都必须暴露：`authority`、`sourceAsOf`、`coverage`、`limitations`、`gate`；缺失不补零。
- 身份是所有融合的前提，度量口径固定为**按金额的覆盖率**（不是按行数）：
  今天 46.7% → 第一阶段目标 ≥ 80% → 第二阶段解锁条件。
- 每条新契约都走：普查核字段 → 契约 → 观察 staging → 身份桥 → 读模型 → 页面，
  只在最后一步才出现在决策面，且永远标注观察口径。
