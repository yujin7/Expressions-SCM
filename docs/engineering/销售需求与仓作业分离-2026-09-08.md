# 销售需求与仓作业分离（G02）

## 目标与边界

关联SSA-4及D35/D55。PMC在库存预警看见销售日均及独立作业量，调拨/发料/盘点等不能在无销售证据时成为补货预警的需求代理。告警回验不能再把同一错误学成准确预测。仅改读模型与未来回验证据，不写实际库存，不改建议量、阈值或历史核验。

## 事实契约

- 系统权威是追加的stock_ledger，粒度为SKU×仓×批次×业务事件；仅实时仓进入系统销售需求，停用仓的合法历史仍保留。
- `sales_out/post`与`stock_doc/reverse:sales_out#正整数ID`是销售及其红字共同边界，见`core/sales-ledger.ts`；聚水潭每日系统侧对账复用同一谓词。来源或动作仅相似不算销售。
- 销售净出库=销售负向出库−销售正向红字；红字计在其occurred_at所属业务窗口。零、负数、无销售事件null分别保留，不将无记录补零。
- 库存预警窗口为上海最近30个已结束业务日[start,end)，排除今天及未来；在一次聚合中另取非销售负向作业量（未扣正向冲销），不参与需求。最小四位数量按六位中间日均保留正值。
- 不把“有部分销售记录”声称为“全渠道窗口完整”。外部观察、内部月销和系统销售不相加，原有正值优先顺序保留；G01外部完整窗口另验。
- 新v7缓存绑定仓模式和计算业务日；跨日不让今天部分记录被提前算入30个完整日。日期从现有business-day模块提供，不另造时区格式化器。
- 未来告警回验v3以观察窗连同前30天销售净额判断需求。无正净额弃权，库存余额回放仍包含全部合法流水；覆盖、快照与有入库的弃权条件保留。旧v2证据不改写、不重算，只作为历史计数。

## 反例及证据

- `tests/report/inventory-alerts-ledger-demand.test.ts`：纯调拨/发料/采购退货/盘亏/入库冲销、S级无销售不开户；300销售−30红字+600调拨只得9/日；零/负/未知；起止午夜/闰日/未来/快照仓；微量销售；跨日缓存、仓模式及停用历史；伪装动作。
- 修复前同一反例得到30/日，純作业300得到10/日，最小数量被舍成0。首轮分层夹具漏必填字段，补齐后五个业务反例均在旧实现失败；不把夹具错误当缺陷证明。
- `tests/jobs/alert-outcome-sales-demand.test.ts`：6个场景中应1真/5弃权，旧实现为5真/1误/0弃权。包含销售在前窗、红字在后窗净额归零的反例。
- 既有回验夹具中的虚构`test`负向事件改为实际`sales_out`类型，原覆盖/快照/幂等断言保留；非销售行为由新反例明确约束，不以改预期放行。
- 定向15文件101项与三套类型通过；准确提交完整发布门、真实隔离PostgreSQL/浏览器页面、导出及角色检查待本批回执。不得继承f914742 READY。

## 尚未证明

本批不是全站/全101条完成。外部7/15日净量、正式当期数据及覆盖、渠道×仓销售事实、真实业务处置、实际发布/双入口登录与UAT仍待。系统出库只是已登记仓记录，不替代外部退款或财务收入；不利用本批自动签上游删除、改派待办或变更抑制天数。

## 冻结包发现与窄屏续修

6f8e393于05:34（上海）通过准确完整门READY9/9、608文件/5,423项、HTTP33/33。合成PG62条迁移；计划员浏览器及实际下载CSV核对销售净额270、日均9、作业600，纯作业300没有主日销，微量0.0001保留正日均，零净额与未知不同。

但390px冻结包截图证明左右固定列覆盖中间可视区域，手机只能看到身份和动作；口径说明仅hover也不满足键盘/触控契约。这是浏览器验收失败，不能被机器READY覆盖。原图及CSV保存在外层交付证据/2026-09-08-销售需求口径。

后续改动复用待办窄容器卡片模式和ContextHelp，不引入新平台、断点运行时或计算口径：
- 窄屏首层保留完整SKU/品名、等级/主预警、在库、主日销来源、可销天数、含缺省提示的阈值及销售/作业量；辅助口径和告警证据可展开。
- 宽表与窄卡共用服务端页、过滤条件、分页和动作/权限组件；分页单一出口，不复制查询或另算需求。
- 销售帮助包含明确窗口、纠正日、作业未扣正冲销、未知及覆盖边界；共享原生按钮支持键盘/触控。大表仍局部横向滚动。
- 组件反例6项，连同需求性质和深链共3文件22项通过，三套类型及改动lint通过。SSR断言不代替浏览器焦点和布局；新准确提交完整门/冻结包仍待。

本次不称排序已全域收口：旧宽表部分列仍仅对当前服务端页排序，后续应接服务器全量排序及移动端入口；告警索引失败/超过500条的完整状态、导出取消和G01窗口继续按独立行为证据处理，不能以本次卡片修复标为完成。

## 6e736e9终态与共享帮助越界反例

该候选05:45完整门READY9/9、609文件/5,429项、HTTP33/33。05:53真实冻结包390×844键盘打开销售帮助后，焦点正确进入dialog，但实际面板x=-65、right=293、width=358，屏幕左侧说明被裁。不是仅动画开始的零宽度：结束后及截图仍越界。机器门通过不覆盖这个失败。

已读共享组件、CaliberNote和DataSourceBadge消费者及本机antd/rc-trigger实现。边缘对齐默认可以翻转，却没有启用shift；触发器在屏幕中间偏右且弹层宽358时，两种边对齐都可能越界。修复仅给共享Dropdown的align.overflow显式启用adjustX/adjustY/shiftX/shiftY，保留已有自适应宽高、内容滚动、焦点与非模态语义；不引入第二套浮层、强行fixed定位或全站截断CSS。

新增context-help-placement配置回归，旧实现1失败/1通过，修后连同内容/卡片3文件12项通过。该测试只保护库配置，不声称模拟了DOM定位；新准确提交仍须真实浏览器验证销售帮助、页头口径及另一页面来源徽标的窄屏/桌面、Enter/Escape/Tab与指针关闭。原版失败截图保留为pmc-mobile-help-final.png（名称来自当时预期，不代表通过）。
