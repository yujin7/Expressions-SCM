# 改价与结算操作资格验收 · 2026-09-13

## 结论与边界

精确应用候选 `840876317ece949e06e37582787c5f9a7059ab4f`，分支 `agent/scm-pc-js-actions-20260913`，**READY 9/9**；707测试文件/6545项、HTTP34/34零跳过、真实PostgreSQL并发8/8。候选不是正式部署或真实角色UAT；正式运行、发布备份和平台资格仍看[NOW](../NOW.md)。

上一条费用闭环PR48已通过8项远端检查，合并主线为`28d0dddf3a04a9da3a6ff73c3072332396c71092`。本分支合入该主线时因squash历史出现3处文本冲突；逐处核对并保留新操作资格，合并结果与合并前`c120566b`应用树完全相同，再跑类型、构建和全门。没有覆盖原始工作仓、删除用户工作或改正式数据。

## 本次实际修复

- PC详情不再只因`pending`就给所有人显示通过/驳回；JS详情不再把审批资格硬编码成财务角色。两者复用`approvalRoleError`，读取当前审批配置、当前账号及制单人身份；管理员也不得自审/自行驳回，缺配置不放行。
- PC列表从当前身份下发发起资格，只有采购/admin显示加工费申请入口；列表读取失败时撤回该资格。详情读取同样使用新鲜身份做提示和金额脱敏。
- 加工费PC若关联JG缺失、申请原价过期或结算冻结，不显示通过，但合格审批人仍可驳回。冻结规则复用`assertJgFeeMutable`，不再复制一套状态判定。
- 驳回后的指引要求先核对结算未冻结及无待审申请，避免引导用户反复提交必然被拒绝的冻结单据。
- JS草稿保留制单人/PMC/admin提交权；费用更新仍仅PMC/admin。状态结束后不显示执行动作；审批提示明确只是资格，金额、数量、余料、版本仍在写事务内重新核验。
- PC申请及审批加同步防重复提交锁，提交中不允许关掉对应窗口。JS复用已有操作锁；提示合在一个信息区，不叠加两个大提示框。
- PC详情查询进入领域读取模块，时间线复用共享读取并以时间/ID稳定排序。没有新API、迁移、依赖，也没有修改库存或费用写入算法。

## 可复验的证据

工作树下 `.artifacts/pc-js-actions-20260913/` 为本轮证据，不是可随意删除的缓存。

| 检查 | 结果 |
|---|---|
| 角色/配置/制单人纯函数 | 22项：自审、非审批人、缺配置、动态角色、终态、拒绝出口、草稿权限 |
| HTTP当前身份及脱敏 | 10项；无当前身份401、服务未调用，仓管金额剥离，PC发起资格逐角色验证 |
| 真实服务与旧详情导航 | 最后针对性4文件50项；配置变更立即反映到详情，冻结金额不改，旧导航完整保留 |
| 全量发布门 | `.artifacts/verification/2026-09-12T16-41-59-584Z.json`；起止干净同SHA，09-12 16:37:55Z→16:41:59Z（上海09-13） |
| 并发数据库 | `postgres-final.log`，夹具`6fc56b53`，8项全部通过 |

首轮候选`03eadf8b`的全门为**NOT READY**：旧PC导航测试只模拟旧会话读取，迁为新鲜身份后两个断言401；原脱敏测试甚至未检查成功响应。修正mock以明确拒绝旧会话读取，并增加200断言，未放宽应用鉴权。失败回执`2026-09-12T16-35-24-974Z.json`、冲突状态下的失败构建日志及后续通过日志都保留，不当作成功证据。

## 实际浏览器流程（全部为隔离合成数据）

环境：自有PG容器`expscm-quickwins-pg-20260911`，数据库`scm_contract_quickwins_20260911`；旧开发候选3448、最终冻结候选3449。绝不把此处登录证明用于正式3100或公网账号。

1. 管理员经实际登录表单进入，合成JG40上建立PC41和JS9。JS9由草稿提交到pending版本2，制单人不能看到审批/驳回按钮。最终候选重新登录核对PC41同样无自审按钮，并验证拒绝后的新文案。
2. 采购审批人实际打开PC39：只显示驳回，提示关联JS8已冻结。点击驳回、填写说明、确认后PC39为draft版本2，有一条reject记录；JS8仍completed版本5、加工费35.00。该实际操作发生于`03eadf8b`，最终候选仅续改驳回后提示及旧测试，相关执行代码不变；不冒称在最终版本重复写入了同一轮审批。
3. 最终`84087631`财务表单登录，JS9有通过/驳回，无PMC费用更新。实际驳回后JS9为draft版本3、一条reject记录、金额仍0.00（该合成单无收货，用于角色与状态验证，不用于证明费用口径）。
4. 最终仓管表单登录：PC41只读，原价/新价/偏差显示“—”，页面不显示发起或审批按钮；PC/JS响应没有相应金额键。直接尝试审批PC41返回403，仍pending版本1。
5. 桌面1280×633及窄屏390×844截图逐张检查。最终JS窄屏抽屉x=0/宽390，页面宽390，按钮正常换行，正文未裁切；未发现页面脚本错误或框架错误遮罩。仅代表本次PC/JS场景，不宣称全站布局全部验收。

最终截图：`pc-maker-desktop-final.png`、`pc-reader-masked-final.png`、`js-finance-desktop-final.png`、`js-finance-mobile-final.png`。前序截图另保留，无覆盖；测试进程和浏览器验收后停止，冻结包、PG数据与证据保留。

## 仍需继续

物料数量/退料/扣款变化后的结算恢复路径仍需建立并按真实来源核验；当前费用更新明确不替代整单重算。已冻结财务差额单未实现。正式备份/发布、Safari HTTP/HTTPS、真实业务角色UAT及平台资格不能由本轮候选替代。

09-13后续：本报告原候选不包含所有后续修复。PR49已合并`9a54ec18`，含JS不可见金额角色禁止批准；实际浏览器随后发现独立驳回按钮缺失，已在新候选`edb782fc`修复并实际驳回验证。PC选择JG的999上限与失败恢复也已续修，详见[加工单选择与物料读取验收](加工单选择与物料读取验收-2026-09-13.md)。保留本报告历史候选与计数，不将旧证据重标为最新。
