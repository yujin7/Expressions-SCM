# 待办全量排序与读取身份复核

日期：2026-09-06；基线 `73c193ce38535ff4dd21ff3ba3d9c348baf1dbbb`。
独立分支 `agent/scm-studio-compact-20260906`；未发布候选。

## 问题与实现

- 原有优先级、状态、截止列只对已取回的一页排序；“状态”比较器实际上比较逾期标志。
  现支持优先级、真实工作流状态、截止日期、创建时间四列服务端排序，先权限/筛选，再排序，最后分页。
- 字段和方向有固定白名单，非法请求返回中文 400；不把请求字符串当 SQL 标识符。
  无截止日期在双向排序中均排最后；原默认状态→优先级→截止日期→ID 的稳定次序保留。
- 排序进入各列表独立 URL 参数，改变排序回第一页；清除排序回默认次序，保留兄弟标签页参数。
  新读取开始撤下旧行，继续保留取消请求、持续错误及显式重试机制。
- 待办列表、详情、统计/摘要、可选责任人 GET 统一回查新鲜会话身份，不能借旧 JWT 的管理员角色
  扩大当前部门范围或继续读取失效会话。未改变既有可见范围规则、写权限、派工或完成语义。
- 浏览器发现优先级列的排序图标挤压表头，列宽由 80 调至 100，桌面恢复单行。

## 验证证据

- 旧服务全量排序 7 个反例失败；旧 GET 新鲜身份回归 8 失败/1 通过。
  初次合成 fixture 漏填已完成时间触发数据库约束，修正后才计入有效反例，不把 setup 错误算负控。
- 当前专项 5 文件/36 项全部通过：全量分页、双向空值、状态/优先级、非法排序、路由权限、
  URL 独立性，加既有 PATCH 原子性与可见性。路由测试使用真实服务/PGlite，只 mock 会话入口；
  不是生产 JWT 撤销的浏览器验收。
- 最新生产构建 274/274 与 standalone 安全检查通过。精确提交完整发布门另行执行，
  不继承基线 READY；最终结果绑定 `.artifacts/verification/` 中同提交报告。
- 隔离 PostgreSQL：本机 65364 的一次性 QA 库，无真实数据卷，后台任务关闭。
  25 条带 `QA-SORT-20260906-` 前缀的合成待办经实际 create/status 服务建立并保留审计；
  自我指派不发送通知。未写真实 15432 库、未改派或关闭业务事项。
- 最新 standalone 运行于独立 3317 端口。计划员真实登录后：
  - 带第 2 页/截止升序的登录回跳仍保持 URL，末页为 22、23、24、25、1（1 无截止日）。
  - 在第 2 页点击截止列改降序，真实 GET 为 page=1，首项 25；再翻页为 5、4、3、2、1。
  - 清除排序回 page=1 与原默认首项 12；状态升序按 open/in_progress/done/cancelled 分组。
  - 我的待办优先级排序保留全部待办的筛选/状态排序参数，真实结果高→中→低。
  - 只延迟一次真实 GET，不替换返回数据：等待期间旧行数为 0、加载状态可见；释放后恢复。
  - 浏览器写请求被拒绝，排序响应只记录不替换。无浏览器错误/框架错误页。
- 1280px 桌面、390px 手机均无页面级横溢出；桌面优先级标题为单行。
  手机宽明细表仍需内部横滑，不宣称手机整页信息密度或全站 UI 已穷尽验收。
  截图目录：`/tmp/expscm-todo-sort-browser-20260906.PqKllN/`，
  包含 `deadline-page2.png`、`desktop-final.png`、`mobile-final.png`。

## 边界与下一步

无新迁移、外部授权、生产部署、历史数据修复或业务 UAT。D69–D71 均未裁决/变更。
宽表手机阅读、详情展开和完成来源事项后的持续反馈仍可继续改善；实际交付需受控发布。
工作流技能要求排序与权限覆盖完整队列，隔离技能保护业务库，浏览器技能验证真实交互，
发布技能要求对精确提交重新验收。
