# 改价金额批准权限验收

2026-09-13（上海）。精确应用候选`7ba1adab68eebd22b13c3b3fb8b358294d416621`，分支`agent/scm-pc-money-review-20260913`，工作树`worktrees/material-source-20260913`。基于PR50候选继续；GitHub合并与正式部署分别核对，不从本报告推断已上线。

## 从静态风险到实际复现

前轮记录的PC金额资格风险已用真实服务及PGlite复现：动态审批配置改为ops/warehouse/quality时，不可见金额的合格审批人原本能批准加工费PC。修复前测试明确得到`status: approved`，而非预期拒绝；日志保留于`.artifacts/pc-money-review-20260913/before-fix.log`。

## 修复契约

- `approvePc`在事务中加载当前账号并锁PC后、写审批之前，沿用`canSeePrices`唯一权威；不可见金额角色的approve返回403，不能靠旧采购会话或调用方伪造finance绕过。
- 覆盖JG加工费与PO采购价两种PC，不更改审批配置、不自动授予财务权限、不改变价格生效/结算冻结规则。
- 原审批配置、审批人标记、禁止自审、版本、幂等及审计继续执行；合格审批人的reject不要求查看金额。拒绝批准时单据、审批、现价、价格分段、审计均不变。
- `pcTaskActions`同步返回`approve=false/reject=true`及可执行说明。PC界面原已有独立驳回条件，本轮不改布局或另造权限系统。

## 验证

| 证据 | 范围/结果 |
|---|---|
| 定向3文件57项 | 三个隐藏金额角色、两类PC、旧会话/伪造财务角色、脱敏、驳回、现价/分段/PO行不变；账号后来获得finance兼任后按原配置允许审批。原自审/撤权/审计失败/过期原价/重放测试继续通过 |
| 完整发布门 | `7ba1adab`起止干净同SHA，READY 9/9；709文件6566项、HTTP34/34零跳过；包含全量lint、全部类型、迁移契约及正式构建 |
| 回执 | `.artifacts/verification/2026-09-12T17-32-43-737Z.json`；完整日志`.artifacts/pc-money-review-20260913/release-7ba1adab.log` |
| 实际页面→API→DB | 精确冻结候选3452；管理员表单登录后创建合成PC49；仓管表单登录打开准确链接，价格显示“—”、仅驳回可用、无框架错误遮罩。详情金额键缺省、approve接口403；真实点击驳回并确认后draft版本2，仅一条仓管驳回记录，JG47现价仍2.00、价格分段0条 |

浏览器截图逐张查看：`.artifacts/pc-money-review-20260913/pc-masked-rejection-desktop.png`；DB核对`browser-db.log`。临时配置只在隔离合成库`scm_contract_quickwins_20260911`将PC设为warehouse，实际驳回后已恢复purchasing并复核。未改正式账号、密码、配置或业务数据；本机测试登录不证明正式3100登录已恢复。测试夹具初版使用错误枚举/缺SPU失败，已按真实schema修正，失败不计通过。

收尾已核对进程归属并停止3452候选、测试浏览器及隔离PG容器；冻结包、数据库与证据全部保留。文档更新后治理/台账2文件738项通过。前序PR50已合并`2a8c9c8b`；先确认其全树与原候选`bd725c48`相同，再协调其squash历史造成的三处自有文档冲突。合并`57cb10bd`全树与合并前`9c10fee4`相同；与已验`7ba1adab`仅四份文档不同，应用/测试未变。合并后重新通过全部类型与正式构建（`post-merge-types.log`、`post-merge-build.log`），不冒称合并SHA另跑过完整门。

## 仍未完成

- 后续续接：PR51审查确认金额资格检查次序会误拒已完成轮次重放，独立补丁`34e478a3`已修正。上文`7ba1adab`为当时精确候选，不是该补丁的全门证据；新候选及JS详情/关联链读取一致性已验见[结算读取与重试](结算读取与处理入口一致性验收-2026-09-13.md)，未靠扩大金额权限解决。
- 退料、物料扣款与完整结算依据恢复，已冻结财务差额、真实角色全链UAT仍待完成。
- 正式DB+附件异地备份授权、发布窗口及正式Safari HTTP/HTTPS验证仍是部署前置；外部平台授权/映射及D69/D70/D71未改变。
- 本轮仅关闭已验证的PC金额批准缺口，不称所有财务审批、全部UI或全项目已完成。最新状态以[NOW](../NOW.md)为准。
