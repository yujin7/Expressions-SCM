# 批次追溯：读取身份、隔离处置与紧凑布局验收

日期：2026-09-11。结论仅针对隔离候选，不是正式3100发布或真实业务UAT。

## 准确版本与环境

- 行为修复：`6337882a377098534350f576eb0d0d316b9e0a67`。
- 窄屏续修／最终应用候选：`43cd7e5a8cf562a412507b28033bc88a9741d054`。
- 分支：`agent/scm-workbench-evidence-20260907`；完整门开始、结束均为同一干净候选。
- 最终冻结环境：127.0.0.1:3432；独立PostgreSQL `purchase_line_20260911`，66/66迁移、无漂移，后台作业关闭。
- 完整门：687文件、6232项测试通过，HTTP 34/34、零跳过，9项检查通过。
- 回执：`.artifacts/verification/2026-09-11T02-37-05-393Z.json`。
- 第一轮633候选也通过完整门，但390截图显示流水仓库名竖排，因此不能用第一次门禁替代最终布局验收。

## 修复与边界

追溯和库位分布复用`useDocumentRead`的身份绑定、撤旧与15秒超时；查询编码变化、同条件刷新、迟到成功／失败均不能恢复过时操作对象。追溯响应需匹配查询SKU、批次及有效ID，结构不完整明确拒绝。

隔离／放行窗口保存打开时的批次和库位快照。异步表单校验后再次核对快照和角色，数量保持十进制字符串；提交开始即防双击、锁查询及窗口关闭。30秒停止等待只表示客户端结果未确认，不表示服务端取消；错误持续展示，核对入口只读回，不自动重复写入。服务端现有权限、库位作业事务审计及库存唯一过账规则不变。

查询只有SKU、批次号两个明确字段及一个追溯按钮；中文组词确认不提交。小屏登记单列，参考库存／库位／流水分别420／660／650内部横滚，长说明移入可换行正文。没有新增库表、迁移、外部数据资格或改变批次迁移开关。

## 测试证据

新增`tests/components/batch-trace-lifecycle.test.ts`15项行为测试：切换即撤旧、双层迟到请求、同批刷新、分布失败及GET重试、错SKU／错批／不完整结构、旧窗口回调、双确认单次写入及十进制载荷、校验期间角色撤回、响应丢失、读取／写入超时、两字段布局与内部横滚、中文组词保护。

首轮13通过／1失败是测试替身把`Object.assign("input", …)`当作原始字符串比较的错误，修正后通过；不把它声称为修前产品反例。定向原批次、隔离、收货读取等4文件44项通过，后补组词用例至45项；最终完整门涵盖15个新增用例。回调测试不等同真实AntD或浏览器验收。

## 实际Chrome → HTTP → PostgreSQL

角色：warehouse01。隔离合成夹具SKU10 `QA-ACTION-6337882A`、仓28、批次2 `QA-LOT-6337882A`，普通库位1、隔离库位2；初始总量1.0001。夹具只在独立数据库通过注册`opening`来源的`post`建立，不直接写账本／余额；来源999633是合成夹具，不是期初审批UAT。脚本及夹具保存在交付证据目录。

1. 633版实际填表隔离0.0001、放行0.0001，作业1／2与对应审计各一条。
2. 最终43cd版从普通库位0.0001尝试隔离1：明确“来源库位 NORMAL 库存不足”，确认按钮禁用，作业数仍2；点击“核对当前分布”恢复，未重复写入。
3. 43cd版重新打开并按默认精确数量0.0001隔离，作业4；390宽实际填写原因并放行0.0001，作业5。作业ID3由拒绝事务消耗序列但没有业务行，不能把ID缺口当作丢单。
4. 最终数据库：总账1.0001；普通库位0.0001、隔离库位0、未定位1；四条成功作业各有一条匹配审计。未增加仓库总量或新增流水来伪装隔离。
5. 不存在的批次明确报错，三张旧表全部撤下；切回原批次可查。断网重查撤旧、持久错误与重试，恢复网络后点击重试恢复三张表。
6. 单独阻断库位GET时追溯仍可读，库位区显示“尚未加载，不能判断可作业库存”，无隔离入口。移除网络拦截时测试浏览器转到about:blank且会话丢失，未将该轮库位重试冒称通过；重新登录后实际操作拒绝的读回恢复与整页断网重试均另行通过。
7. 1280桌面、390小屏以及320宽布局观测：页面scrollWidth不超出viewport；320下三表容器250，内表420／660／650。390最终全页与操作窗口截图已目视，无竖排挤压或窗口横溢出。未遍历所有设备、触屏及缩放组合。
8. 浏览器未捕获页面脚本异常，无框架错误覆盖层。快照曾保留退场loading图标；DOM宽0、opacity0且按钮非loading类，不当作持续加载故障。输入框实际value为0.0001，辅助树浮点表示不当作业务数量精度证据，数据库为准确0.0001。
9. 首次退出只点顶部入口停留确认页，探测仍200且等待超时，不能算退出完成。重新登录后受保护接口200，实际点击确认页“退出登录”，跳转login且同接口401；测试浏览器均关闭。

## 证据位置

目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

- `full-release-43cd7e5a.log`、`owner-43cd7e5a.json`、`formal-observation-43cd7e5a.json`。
- `seed-batch-action-6337882a.mts`、`fixture-batch-action-6337882a.json`。
- `batch-action-6337882a-390.png`：修前窄屏缺陷证据，保留。
- `batch-action-43cd7e5a-390.png`、`batch-action-43cd7e5a-modal-390.png`：最终页面及窗口。
- 数据库只读核验和实际浏览器命令输出见本任务记录；未保存浏览器凭据状态。

## 仍未完成

正式镜像仍`9ec914e49423…`、61迁移、启动时间2026-09-10T19:53:56.547232837Z，本次未部署或修改正式库存。正式发布前异地配对备份位置未确认，不绕过；本地Safari登录修复也尚未进入正式镜像。

批次／FEFO闸门仍关闭；到期日、历史批次归属、出库覆盖、召回重建及实际业务资格不能靠此UI验收宣称完成。流水全量分页／唯一行身份、上下游单据回链、完整批次化工作流还需继续核验。三方连接器资格、101条需求UAT、D69–D71和真实用户关闭待办仍独立跟进。
