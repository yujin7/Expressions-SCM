# 采购退货：当前权限与提交失败恢复

## 结论与范围

准确应用候选 `87c2128e8734a5dadab8bfd02799a0d9f92d75e3` 在独立环境通过完整工程门和下述数据库/浏览器流程。不是正式发布，也不是101项需求或真实业务UAT全部完成。

修复CT创建/提交/审批对当前用户资格的核验，以及提交状态/版本/审计事务原子性。审批的职责分离、按PO已收上限、库存唯一过账入口、十进制计算和幂等保持不变。活跃制单人本人提交规则保留；草稿不预占数量，创建请求未新增幂等键，不自动修历史数据。

## 反例与修复

- 修前8个定向测试7失败：审计失败仍变待审批；停用/降权/撤销审批资格的旧身份仍能执行部分写入；已提供的旧会话版本未被服务核验。原有制单人/重复提交约束是1个通过项，不把它冒充新缺陷。
- 当前用户行共享锁贯穿CT事务；提交再锁CT核对状态和版本，与审计一起提交。审批传当前角色与审批资格，而非旧页面提供的身份。
- 修后3文件19项；完整门665文件6040项、全量lint、三套类型、生产构建、HTTP34/34零跳过，READY9/9。门回执 `.artifacts/verification/2026-09-10T18-10-20-715Z.json`，起止同一干净SHA。

## 真实PostgreSQL并发证据

环境为既有独立合成容器 `exp-scm-capacity-20260909`（ID `eb10a79197a28a3a6822f2b00d0373098757061ea1b250cfd1e71d5ab1537b56`），数据库 `qc_boundary_20260911`；未接触正式用户和业务库。

`ct-authority-exact-87c2128-r2.json`：准确干净SHA，9项通过。实际观测四个提交连接等待CT行锁，放锁后一成功/三409，一条审计；指定合成CT审计触发器失败后草稿与版本不变，恢复后原版本可重试；审批实际等待另一个事务撤销审批资格，撤销提交后403、单据仍待审批且无审批事件；旧停用制单人不能新建。

先前 `ct-authority-exact-87c2128.json` 也通过9项，但与构建并行时捕获 `next-env.d.ts` 临时生成差异，因此不作干净SHA证据；构建自带规范化结束后重新验证，未手工覆盖其他改动。开发回执也单独保留。

## 浏览器与失败恢复

冻结同版服务3411，65迁移无漂移。实际Chrome：仓管01表单登录，受控接口建立CT17草稿；指定该单审计失败，实际点击提交得到500和安全错误码提示，草稿/版本/审计均无副作用。

首段 `browser-ct-87c2128.json` 保留FAIL及6项已通过：选择器期望按钮完整名“提 交”，而退场动画残留零宽loading图标使可访问名称带前缀。实际DOM按钮未禁用且无加载状态class，不是请求仍在执行。未改应用或重新造单抹掉失败。

续段 `browser-ct-recovery-87c2128.json` 对同一CT17进行只读核对，再人工点击重试→待审批→仓管01退出401→仓管02表单登录→实际审批→已完成→同版本重放幂等→退出401，14项通过。重试前已移除仅该合成CT生效的失败触发器；脚本finally清理，末检无残余触发器。

独立SQL核对CT17的create/submit/approve/post_and_complete各1条、审批事件1条；PO14已收从0.2扣至0.1，重放不再扣。源类型按实际登记的ct_return核对CT17为唯一−0.1000流水；仓18/SKU2四笔流水净额与余额、PO已收均0.1000。截图 `ct-submit-failed-87c2128.png`、`ct-completed-87c2128.png` 保留；已目视完成态。没有把桌面流程验证当手机布局或全站UI验收。测试浏览器已关闭。

以上外部证据均在项目的 `交付证据/2026-09-09-申报产能/`；脚本、失败/续验回执和冻结运行目录保留，不包含正式口令。

## 未完成与下一步

- 正式3100保持原容器/镜像/启动时点，61迁移；异地备份配置仍缺，不绕过受控发布。候选65迁移与正式61迁移分别记录。
- CT列表与建单选PO后异步读取的迟到/失败恢复需要继续行为审计，尤其不能用旧PO可退行创建新PO退货。当前服务已查行归属，但UI不应让人猜测失败原因。
- SH等相邻单据的提交/权限边界不能由本次CT结果代替；同SKU多PO行分摊、创建回包未知、历史异常处置仍需明确设计与验证。
- 101条真实业务验收、简道云/聚水潭/用友真实授权和消费资格、D69–D71决策继续，不以本修复宣称闭环。
