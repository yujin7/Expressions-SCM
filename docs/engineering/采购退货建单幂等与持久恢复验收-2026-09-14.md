# 采购退货建单幂等与持久恢复验收 · 2026-09-14

应用候选：`0f6f6e8a1c3f82016b1609f1acd7a8567e192308`。分支`agent/scm-ct-create-recovery-20260914`，[PR104](https://github.com/yujin7/Expressions-SCM/pull/104)基于[PR103](https://github.com/yujin7/Expressions-SCM/pull/103)。候选、正式部署和真实业务验收分开；本报告不签认正式3100或外部集成。

## 本次补齐

采购退货页面此前每次POST都是独立建单，网络丢回复后无法按原意找回。现公开创建强制UUID和显式实物批次（`null`仅表示明确历史无批次），以当前账号+原键串行；规范化采购单、出仓、逐行采购身份/物料/精确数量/批次/原因/备注形成原意。相同键相同内容返回原单，异参409，不以相同SKU/数量推定不同新键是重复业务。

草稿、取号、审计及回执同事务；回执不可修改/删除/清表，账号+请求唯一、原单唯一。GET只读等待进行中的原创建、核对当前身份/角色/归属，返回原单当前状态，来源后来关闭不再妨碍恢复。新增0073、第74项迁移；A733/A734登记，台账734控制、298API。无新依赖。

页面发送前保存有限原请求，按账号隔离、跨标签锁；存储失败/损坏不发送或自动遗忘。刷新/换页不自动重发，超时保留待核对入口。修正先GET，已保存则打开原单；未保存才同键改正并提交。来源行缺失或换SKU明确阻止，不静默丢掉原行；同一弹窗内修正也撤下旧余额并重新读取，未读完不可创建。确认原单后再GET，仅清匹配的本机键，才准备下一笔。离开账号的迟到结果不导航/报成功。

公开POST不带requestKey、未显式给batchId的旧调用现在400；仓库内已查唯一浏览器消费者并同步更新数量路由测试。内部createCt的现有调用契约不变。旧草稿没有回执不会被自动补认领，不能从同内容倒推原键。

## 证据

- 针对性81项通过，浏览器发现同弹窗余额未刷新后增加第82项；父页面现26项。最终完整门READY9/9，793文件/7922项，HTTP34/34零跳过。回执`.artifacts/verification/2026-09-13T22-07-42-988Z.json`：09-13 22:02:56.375Z→22:07:42.988Z，起止干净且同SHA；文档后续提交不冒称重跑应用门。
- 真实PG四连接9项：同键重放等待、GET等待、未提交不可见、单草稿/回执/审计、不可变回执、异参409、原单作废且出仓停用后回读、会话撤销拒绝、CT零库存流水。最终夹具`ct-create-recovery-7c81adc1`，CT47；原1条期初流水保留。测试中的作废/停仓仅为合成状态夹具，不冒称原生作废操作验收。
- 原生页面选择合成PO273/行280、SKU173、出仓173及明确历史无批次，填1；合成已收由10调整为0.5后真实POST409，0草稿/回执/审计。点击修正触发实际PO GET，显示0.5并在客户端阻止旧数量提交。改0.4后同UUID真实201，等待服务器回应后故意抛网络错误，不伪造成功。
- 刷新恢复实际原请求，GET找回CT48，打开原明细；确认清理前再GET。恢复段4个GET、没有POST。1张CT/1行/1回执/1条create审计；数量0.4000、batch=null，库存10.0000、已收0.5000、仅原期初流水不变。
- 最终git-clean冻结包再打开CT48；为检验原键重放，在独立合成账号恢复刚才真实请求夹具，点击页面“重试原请求”实际POST201仍返回CT48，再GET/明确确认。数据库仍1单1审计。不是自动恢复或新建成功的伪响应。
- 390×844手机页面宽390；长失败提示下底部操作top772/bottom808可见，表格独立滚动；原生浏览器错误捕获为空。不是全站设备/真实Safari或全部角色UAT。

## 未通过过程与保留

`ce071a99`首轮检查在发现同弹窗余额显示问题后中止，不计通过。`0f6f6e8a`第一次完整门静态部分通过，但旧冻结运行包标记git-dirty，冒烟正确拒绝，回执`2026-09-13T22-00-55-186Z.json`为NOT READY。改用同SHA完整门产出的git-clean包重验，不修改健康响应或放宽门。

证据位于`.artifacts/ct-create-recovery-20260914/`：build/server/release多轮日志、fixture、DB前后、请求/PO重读/恢复流量、PG结果；截图在`.artifacts/ct-physical-lots-20260914/ct-create-*.png`。旧标记包、初轮CT46、最终CT48、合成PG和所有证据均保留，不清掉失败证据。正式3100、真实账号/数据未动。

## 仍未完成

原错误草稿与新单的替代关系、未执行请求服务端取消、跨设备意图去重/恢复、损坏本机记录自助对账、已过账纠错、其他单据受控退出和真实批次采购来源证明仍缺。历史无批次不可追溯，出仓余额不证明货属于本采购单；此轮不改库存、不提交审批、不自动过账，不改变这些资格。

正式发布仍需获授权异地DB+附件备份位置/访问方式与窗口、迁移后恢复/回退计划、实际Safari HTTP/公网HTTPS及真实角色试点。云端CI单独核验，不由本地绿代签；聚水潭/用友授权、D69/D70/D71和原总监需求继续由NOW/CURRENT管理。

## 交审与清理

最终干净冻结包`.artifacts/runtime-ct-create-0f6f6e8a-verified`；已核对cwd/3489端口后停止PID43555，独立浏览器及owner匹配隔离PG已停。保留数据库、全部冻结包/失败与成功证据、备份和共享依赖；未动正式3100。

[应用CI运行34785695898](https://github.com/yujin7/Expressions-SCM/actions/runs/34785695898)8个job均0步骤。Static checks `103800646381`的GitHub注释明确账户付款/额度问题，云端没有通过证据。PR104交审，不自动合并/部署；正式主线本轮复核仍为`ee1c977e`，依赖103→102→…→55尚未合并。NOW/CURRENT更新的是候选事实和剩余边界，不另建技能/状态权威。
