# 库存单据退出边界与状态恢复验收 · 2026-09-14

应用候选 `e9940ef22f74aadba5e688197018fce8af967910`；分支 `agent/scm-stock-lifecycle-exit-20260914`，[PR109](https://github.com/yujin7/Expressions-SCM/pull/109) 基于 [PR108](https://github.com/yujin7/Expressions-SCM/pull/108)。未合并、未部署。续接[建单请求取消](库存建单请求取消与恢复验收-2026-09-14.md)，本轮处理已存在库存单的退出，不混同未执行请求取消。

## 确认的缺口与修复

- 原独立退出服务未排除盘点自动生成 CA，界面隐藏按钮不能保护直接 API 调用；现在撤回、作废、短关均拒绝脱离来源 PD 的 CA。
- draft/pending 若已存在同来源身份库存流水，继续流转会掩盖状态异常。提交、审批/驳回、撤回、作废统一拒绝；读取操作提示同步禁用并说明核对路径。正常库存事件按 subtype/id，红字载体按本单 stock_doc/id，不因同数字的其他来源误拦，也不误用被冲销原单 ID。
- 原详情遗漏已保存的 closedReason。现已作废/短关详情展示准确原因，历史缺值显示“未记录”，不伪造原因或推断库存已冲销。
- 原操作仅短暂报错、无超时上限。共享库存按钮现在同步防重复点击、20秒有界等待、核对响应目标和版本；错误保留在确认框。400允许修正，不确定回复/冲突等要求先GET核对原单，不重复POST。卸载后迟到成功/失败不通知或刷新另一个详情。
- 确认框携带原单号并限制视口；短关仍只关闭未执行剩余，不反过账。原审批重放与红字创建语义保留。

写路径继续使用事务内新鲜身份、行锁、所见版本、原子审计；库存只经既有过账注册入口。无迁移、依赖或路由增加，候选仍76迁移。没有修订业务裁决或新增平行技能。

## 精确候选证据

本地完整门 **READY9/9**，800文件/8092测试（新增31项），lint零错误/警告、应用/测试/脚本类型、运维、真实PG迁移合同、生产构建、HTTP34/34零跳过通过。回执 `.artifacts/verification/2026-09-14T00-34-44-010Z.json`，UTC00:30:03.978→00:34:44.010，起止干净同SHA。冻结包 `.artifacts/runtime-stock-exit-e9940ef2` 编译身份为同SHA/git-clean；后续文档提交不冒充重跑应用门。

新增测试覆盖CA三条退出、同身份流水五条拒绝、不同来源同ID不误拦、红字身份、精确回复、审批相反动作拒绝、超时/迟到、连续点击、原因保留和GET恢复。不依赖仅字符串扫描宣称正确。

真实PG脚本 `scripts/verify-postgres-stock-lifecycle-exit.ts`：显式opt-in、loopback scm_contract_*、三连接与实际 pg_blocking_pids。13项包括：作废先赢拒绝迟到提交、未提交状态不可见、原因保留；审批先过账拒绝撤回、精确单次0.0001及审批重放；真实审计触发器故障回滚和恢复后重试；CA三种状态拒绝；旧会话等待身份撤销后拒绝。

精确候选夹具 `stock-exit-983bf1f4`，制单314/审批315、SKU181/仓182，单69–75；脚本结束7单/9审计/1笔0.0001库存。开发前测另留 `stock-exit-45e5faa0`（用户312/313、单62–68）同结构；不是重复真实业务数据。唯一故障触发器/函数均已撤除。

## 原生页面与数据验证

证据目录 `.artifacts/stock-lifecycle-exit-20260914/`，独立3489、合成admin id3；没有正式密码修改。

1. 打开本轮合成草稿75（RK-20260914-0024），原生作废确认显示原单号和原因输入。
2. 实际POST作废200后在浏览器丢弃回复，页面显示“操作可能已在服务端完成”，保留原因、禁用重复提交，不显示成功。
3. 390px手机原生“核对原单状态”只GET同单，详情显示已作废、原原因与原明细；一次POST、一次GET，无第二次作废。GET是当前状态，不声称证明用户自己那次丢失请求成功。
4. 页面操作前后131条余额逐字段哈希相同，流水508不变；只追加单75的一条void审计，version1→2。该单是上述精确夹具中身份撤销测试后保留的草稿，浏览器由合成管理员明确作废，不新造业务单或改源数据。
5. CA72页面无独立退出按钮；直接HTTP作废409、中文提示回来源盘点；再次GET仍draft/version1，所有操作提示false。
6. 桌面/手机截图已查看，390px页面宽390、确认框358×306，无横向溢出；实际浏览器错误捕获为空，无框架覆盖层。一次等待使用未去空格的“作废”而超时，两次选择器先命中隐藏新建弹窗；改用实际可见快照完成验证，没有把观察工具失败当业务失败。

## 交审、保留与下一步

PR109→108→…→55按依赖交审；本轮远端main仍 `ee1c977e45b1c4e30961d2d6d6a4ea2068fefde2`。应用[云端运行34793197632](https://github.com/yujin7/Expressions-SCM/actions/runs/34793197632)8任务均零步骤，Static checks 103821199321注释明确账户付款失败或额度不足，未启动检查；不以本地绿代签云端。后续文档提交运行另验。核对PID14157、cwd及3489端口后已停独立服务，浏览器和owned隔离PG已关闭，保留冻结包/合成库/证据；正式3100未动，无业务文件、迁移、备份或共享依赖清理。

**不在本轮完成范围**：跨刷新命令原回执及跨设备恢复、错误单与替代单关联、所有生成单来源生命周期、真实批次来源资格/工单占用、正式Safari登录/部署及真实角色UAT。异常已有流水的历史单仅被识别并阻断，不自动改状态或修账；需业务核对合法纠错路径。正式发布仍需获授权DB+附件异地配对备份及窗口，不绕过保护。

当前状态只由[NOW](../NOW.md)管理，业务意图只由[CURRENT](../spec/CURRENT.md)导航；本报告是证据，不新增状态权威。
