# 待办紧凑体验与结果留存复核

日期：2026-09-07；基线 e0be4aa；分支 agent/scm-todo-experience-20260907。
独立待办布局WIP从a3a1f0a逐差异迁入，本域在两个基线间没有代码变化。原WIP及原项目173路径未覆盖。

## 人用契约

计划员从我的/全部待办，按全量优先级、状态、截止或创建时间排序，分页阅读责任人、来源和详细依据；
按既有权限开始/完成/重开/改派/取消。完成当前筛选中的事项后，仍能核对准确结果并进入来源。
待办与告警/复核是独立生命周期；本轮不改分派策略、截止日、写权限、原子事务或通知调度。

## 改动

- 桌面由分散字段改为五列，事项摘要合并标题/编号、责任人/角色及来源；完整明细、指派人、创建/完成时间以原生details展开。
- 容器宽度不足900px切卡片；我的/全部共享数据、权限判定、pending锁、写回调和一份分页。
  手机可通过显式排序控件操作四个已有服务端排序字段；桌面表头保持优先级/状态/截止排序。
- 成功或失败的结果保留在列表上方，精确待办链接在完成后仍可读；来源边界使用既有安全深链权威。
  同步ref防止快速重复PATCH；未知写结果不自动重试；移除与持久说明重复的8秒长浮层。
- 卡片按钮最小高度32px；长文字可换行，无纯hover才能读取的必要信息。
- 完成率按人/角色×月份呈窄屏卡片，保留总数、已完成、按时、逾期/不按时、取消、可疑和两个比率。
  null率显示“—”、真实0显示0%；始终使用服务器值，不本地重算。
- 统计刷新清除旧行/口径/导出；失败有持续错误及显式重试。没有改API、SQL或统计规则。

## 已验证

1. 组件8项测试：双布局一份读取/分页、四字段排序入口、完整明细和来源、快速双击只发一次PATCH、
   完成行消失后反馈仍在、未确认写不重试、只读用户无按钮、统计刷新/错误/恢复、窄屏完整计数及未知率。
   新增窄屏统计反例先1失败/7通过，修复后8通过；重复浮层反例亦先红后绿。
2. 22文件/221项待办相关测试、三套TypeScript、无缓存lint通过。生产构建274/274及standalone安全检查通过。
   本文提交后仍须精确提交发布门，不能继承e0be4aa基线的READY。
3. 实际隔离PostgreSQL16：65364/scm_spike_qa，容器无业务挂载、tmpfs存储、61迁移、后台任务关闭。
   通过createWorkItem及事务创建25条合成事项，包含24条manual和1条关联合成告警。首次合成截止日误跨9月30日，
   被真实日期守卫拒绝，整批回滚；修正夹具后才创建成功，未放宽产品校验。
4. 3320浏览器：合成计划员真实登录；390px卡片、无整页横溢出；升/降序更新URL并返回正确优先级，
   第二页实际5条；长明细完整展开；完成后行消失、提示保留；PG对应complete审计1条、来源告警仍open。
5. 完成率真实API显示1条系统事项，人工事项不计入，完整卡片可读；网络abort后旧卡片/导出消失，
   错误持久，显式重试恢复，按人/按角色切换正确。截图和DOM均核验，不以hook夹具冒充浏览器测试。
6. 去重复浮层及32px按钮的最终构建另起3321，重新打开合成#22再完成：PG累计complete2、reopen1，
   对应两次有意完成，各一条审计；来源告警保持open。浏览器持久反馈完整、长info浮层0、按钮实测32px。

## 限制

首轮3189909完整门：1失败/569文件通过、1失败/4,991项通过。失败为共享布局护栏要求桌面表保留横向容器；
补回按列宽设定的870/860px容器，正常桌面无需横滑，窄屏仍显示卡片。原护栏保持，修正提交必须完整重跑。

未推送或部署；未使用实际业务用户/数据做UAT，未触发真实通知、集成或库存。窄屏完成率卡片未做超大月份范围压力测试。
本轮没有解决其他页面全部宽表，也不证明D69–D71、外部授权或真实业务闭环完成。
代码候选、隔离结果和3100实际运行版本必须分开记录。证据归档仅含合成截图与发布回执，不含凭据或数据库副本。
