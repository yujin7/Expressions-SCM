# 平台身份批量结果与精确重试复核

日期：2026-09-06。基线 `29dc68fed8e80a4206672577387dd1cba4a96d5c`。
独立候选 `agent/scm-studio-compact-20260906`；真实3100服务、业务库和外部授权不变。

## 用户流程与边界

- 天猫认领、拼多多认领和补条码复用一个批量提交流程，保留原始人工确认和逐行事务。
- 逐行区分已保存、此前已一致、业务拒绝和结果未确认；部分失败不再弹全成功。
- 结果保留在本页面，可关闭后重开；重试先进入二次复核，只发送业务拒绝项，保留先前成功项。
- 网络中断、异常响应或未知服务器错误不能证明回滚：列为未确认，禁止盲目批量重试。
- POST结束后不再等待读模型刷新才释放窗口；读取中或读取失败仍禁止打开新的认领/重试。
- 派生统计待重建不再声称页面会立即重试成功，更不能为了更新统计重复写身份。
- 两个服务的未知错误只返回非秘密错误编号；日志保留操作、actor、SKU和SQLSTATE，不输出SQL、参数或原始异常信息。
- 桌面结果分四列，小屏合并为项目/下一步与结果两列；无需横滑就能看到拒绝原因。
  单页不显示多余分页器，未知占比不再显示成“—%”。

## 验证证据

- 前序实现反例证实全成功提示、网络未确认无结果窗口、原始SQL异常泄露三类缺陷。
- 本轮浏览器进一步发现690px结果表在390px手机上隐藏右侧状态/原因；新增回归在旧代码失败，再修复通过。
- 最终专项4文件47项通过；三套TypeScript、全仓无缓存lint、274/274生产构建和standalone安全检查通过。
- 最终全量563文件4,876项通过，失败/跳过均0：`/tmp/expscm-bulk-outcomes-final-20260906.json`。
  前一布局版本4,875项报告保留为历史，不混作最终报告。
- 真实PostgreSQL16：65364隔离tmpfs容器，无主机挂载；在既有一次性
  `scm_contract_identity_20260906_1820` 库重跑GTIN/GTIN、补条码/补条码、补条码/GTIN竞争。
  三组都观察到两个锁等待者，最终一个归属、一条成功审计；新增三条合成审计保留，import_jobs=0。
- 最终standalone在3317运行，`SCM_RUN_JOBS=0`，仅连接合成QA库。
  浏览器实际登录、导航和真实身份GET200通过；随后仅将身份GET/POST替换成合成响应，所有身份POST均未发往数据库。
  天猫两项部分成功→关窗重开→复核→只重发SKU99→保留SKU42成功结果，通过。
  拼多多网络失败→两项未确认→无批量重试入口，payload含platform=pdd，通过。
  补条码payload只有skuId/barcode及来源，部分成功与逐行拒绝原因可见，通过。
- 390px结果表实测326px、页面宽390px；桌面1280px布局已查看截图，无浏览器错误。
  证据目录：`/tmp/expscm-bulk-browser-20260906.EiEH0r/`；夹具`fixture.js`明确拦截写请求。

## 不应扩大解释的范围

- 浏览器合成响应证明交互，不替代真实外部身份认领UAT；真实数据库并发另由上述脚本验证。
- 只保留当前页面最后一批结果，不是持久化批次历史；离页/刷新后须查身份主档和审计。
- 结果未确认需要人工查证；不自动推断成功、不自动清理历史重复条码。
- 本提交需单独运行完整`check:release`，不能继承070463e的READY；未推送或部署。
- D69删除真实性、D70责任归属、D71抑制时长、外部权限与真实业务验收未改变。

流程技能要求二次确认与异常出口；写路径技能保留事务/审计，浏览器技能促使修正真实小屏可读性。
