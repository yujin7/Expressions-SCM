# 供应链总监需求 · 现状映射与实施计划（2026-09-03）

> 定位：本文是**实施映射**，不是决议。口径类决议已由业主授权代决并登记 `docs/spec/CURRENT.md` D50–D66（本文只引用，不再讨论）；
> 当前状态与下一优先级仍以 `docs/NOW.md` 为准。需求原文：`docs/requirements/2026-09-02-供应链总监会议纪要.md`、
> `docs/requirements/2026-09-03-四屏数据需求表.md`。线框：`docs/requirements/wireframes-四屏-2026-09.md`。
>
> 事实口径：现状文件路径均在 2026-09-03 仓库核对（`src/db/schema/director.ts`、`drizzle/0047_director_program.sql`、
> `src/server/modules/admin/params.ts`、`src/lib/transfer-types.ts`、`src/server/core/constants.ts`）。
> 标「待复核」的数字来自研究/映射阶段的只读观察，实施前须在当前 revision 重新取证。

---

## 1. 一页结论

**能复用什么（不重写）**

| 能力 | 现状文件（唯一权威） | 四屏/需求里的用法 |
|---|---|---|
| 全网在库 / 快照 | `src/server/core/stock-view.ts`（getOnHandBySku / getLatestSnapshotRows） | 屏 1 当月库存数量、屏 3 各仓明细 |
| 在途 / 未结供给 | `src/server/core/supply.ts`（getOpenSupplyLines） | 屏 2 订单「已下单未交付」、含在途可销 |
| 销速窗口 / 日均 | `src/server/core/velocity.ts`（lastMonths / dailyFromWindow） | 预警表日均、爆单基线并列对照 |
| ABC 分层 | `src/server/rules/abc.ts`（classifyAbc） | D58 在此参数化为 S/A/B/C 四档 |
| 补货引擎 v2 | `src/server/rules/timephased.ts`、`src/server/modules/replenish/service.ts`（shortageDate / orderByDate / belowLead） | 屏 2 库存预警表的「预警时间」 |
| 仓间调拨建议 | `src/server/modules/report/transfer-suggest.ts`、`src/server/rules/transfer.ts` | 屏 3「启动调拨计算」 |
| 周转纯函数 | `src/server/rules/inventory-metrics.ts`（turnover / fifoAging） | 屏 3 各仓周转 |
| 经营驾驶舱聚合 | `src/server/modules/report/dashboard.ts`（getDashboard，60s 缓存 + 角色键） | /cockpit 装配层的模板与部分数据块 |
| 控制塔异常 / 队列 | `src/server/modules/workbench/focus.ts`（computeExceptions / getWorkbenchFocus） | 屏 2 红卡、屏 4 队列计数 |
| 数据来源证据矩阵 / 运维健康 | `src/server/modules/report/data-source-readiness.ts`、`src/server/modules/admin/health.ts` | 屏 1 数据来源表 |
| 系统告警 + 通知 | `src/db/schema/system.ts` system_alerts、`src/jobs/freshness.ts` 看门狗范式、`src/jobs/system-alert-notify.ts`、`src/jobs/notify.ts`、`src/server/integrations/feishu.ts`、`src/server/core/notify-audience.ts` | 爆单 / 断货 / 调拨成本告警与飞书投递 |
| 复核清单 / 审批收件箱 | `src/server/modules/review/checklist.ts`、`src/server/modules/inbox/service.ts` | 屏 4 计数；work_items 的写路径模板 |
| 导入 staging→放行 | `src/server/import/staging.ts`、`src/server/modules/release/engine/*` | 周期主数据补录、历史采购额参考、运营提报 Excel |
| 读模型缓存 | `report_read_model_cache`（`src/db/schema/rollup.ts`；键 `/vN` + source_binding） | 所有新读模型 |
| 外部观察读模型 | `external-velocity.ts`（/v3）、`channel-observation.ts`（/v3）、`external-demand-signal.ts` | 日销序列、销售金额预填参考、爆单信号 |
| 图卡契约 / 指标注册表 | `src/components/DecisionVisual.tsx`、`src/components/metrics.ts`（架构门拒绝未登记 metricId） | 四屏每张卡的来源·时点·覆盖·限制 |
| 参数平台 | `src/server/modules/admin/params.ts` PARAM_DEFS + `core/params.ts` getNumParam | D54–D65 全部阈值已登记（见 §4 W1） |

**缺什么（W1 已建基座，W2 填实现）**

- 库存**金额**估值（无金额列；`core/valuation.ts` 待建，D51）；销售金额事实（`sales_amount_monthly` 已建表，服务待建，D53）；库存占比规则（D54）。
- **日粒度**销量序列（内部 `sales_monthly` 月度且停在 2026-06；仅简道云天猫日销到 2026-09-01）；爆单规则（D56）与库存预警阈值规则（D57）。
- S/A/B/C 四档与月度固化（`sku_planning_policy` 已建表，`rules/abc.ts` 参数化待做，D58）；备货权责规则（D59）。
- 调拨类型（`stock_docs.transfer_type` 已建列 + `src/lib/transfer-types.ts`）、调拨费用（`transfer_fees` 已建表）、线路成本规则（D60）。
- 通用待办（`work_items` 已建表）、部门目标（`department_goals` 已建表）、按人飞书私聊（D61）。
- 渠道范围（`user_data_scopes` 已建表）、路由注册表 `src/lib/route-access.ts`（待建，D62）。
- 订单期间指标 / 降本（D63）；账期结构化（`suppliers` 三列已建）与候选读模型（D64）。
- 数据质量准确率与周核对（`data_quality_reviews` 已建表，D65）。
- 四屏壳 `/cockpit`（D50）。

**真瓶颈是数据日更，不是代码**

| 数据前提 | 现状（待复核） | 阻断的需求 |
|---|---|---|
| 内部销量事实 | `sales_monthly` 月度，止于 2026-06；聚水潭 API 6/6 授权阻断，仅手工模板 | 分级窗口滞后 3 个月、内部日销为空、预测/试点评估 |
| 周期主数据 | `sku_params.normal_lead_days` 约 520/5,376、logistics 近 0（研究引用） | 30+15+5 大量回退默认值；试点候选名单极短 |
| 单位成本 | `sku_costs` 手工基准，覆盖率未实测 | 库存金额、占比、金额周转 |
| 销售金额 | 正式事实层无；用友 0/8 授权 | 屏 1 销售金额只能手工月录（可预填观察） |
| 平台身份 | 天猫按金额覆盖 71.2%，外部销速 1,703/2,076 平台 SKU | 爆单漏报、外部影子列缺口 |
| 调拨费用 | 全仓为空，从上线起累计 | 线路基线上线后 3–6 个月多为「样本不足」 |
| PO 真实流量 | 开发库 2 张 PO；承诺交期填报率低 | 订单指标、OTIF、降本基线 |
| 快照仓逐日快照 | snapshot-age 阈值 3 天，非逐日 | 快照仓日曲线、逐仓周转 |
| 供应商合作起始日 / 历史采购额 | `suppliers.createdAt` 是 2026 建档日；2024/2025 采购额需 Excel | 账期候选池可能为空 |
| 16 名渠道运营账号 | 仅 ops01 种子；店铺→渠道映射未建 | 渠道范围裁剪、运营提报 |

**波次与产出**

| 波 | 内容 | 产出 |
|---|---|---|
| W1 基座（已做/进行中） | 迁移 0047 + 参数 + 常量 + 敏感字段；共享规则/核心（估值、四档 abc、阈值、爆单、调拨成本、权责）；路由注册表与数据范围 | 其他域只消费、不重实现 |
| W2 各域并行 | A 库存日级/估值/销售金额/占比；B 预警；C 调拨；D 分级与权责；E 待办与目标；F 渠道范围；G 采购订单与账期；H 数据质量；I 主数据补录与 BI 小项 | 各域 service/读模型/页面/测试 + manifest |
| W3 装配 | `/cockpit` 四屏装配、菜单/命令面板、台账（A589+/C203+）、指标登记、全量门禁、部署与 UAT | 可演示的四屏 + 通过门禁的 revision |

---

## 2. 需求 ↔ 现状 ↔ 缺口 ↔ 提案 总表

约定：**现状**列写复用的权威文件；**提案**列写实现落点（新建标 `新`）；**波次** W1/W2-x/W3/L（L=单独立项，见 §8）；
**口径**列：已参数化的写「Dxx 默认值，可在 /admin/params 调整」。共 101 条需求。

### 2.1 驾驶舱四屏（CK，11 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| CK-R1 | 四屏架构 | `/report/dashboard`、`/admin/health`、`/alerts`、`/workbench`、`/inbox` 分散 | 无统一入口 | `新` `src/app/(app)/cockpit/page.tsx`(+Suspense) + `cockpit-client.tsx`(Tabs，paramPrefix ds_/al_/inv_/todo_) + `src/server/modules/report/cockpit.ts` 四个只读装配函数 + `src/app/api/report/cockpit/route.ts`；`/report/dashboard` 不动 | W3 | D50 决议：并列不替代 |
| CK-R2 | 每日拉数状态 | `data-source-readiness.ts`、`admin/health.ts` getOpsHealth、`src/jobs/interval-runner.ts`/`scheduler.ts` | 无业务视角「今日拉数 + 下次计划」 | cockpit.ts 屏 1 装配 loadDataSourceReadiness 安全摘要 + runSnapshotAgeAlert + salesWindow + 最近 import_jobs；`新` `src/server/rules/schedule-next-run.ts` 纯函数算下次运行 | W3 | 如实展示聚水潭/用友阻断（D50） |
| CK-R3 | 当日库存走向 | `stock_ledger`（occurredAt 索引）、`stock_snapshots`、`stock-view.ts` | 无逐日在库 | `新` `src/server/modules/report/inventory-daily-position.ts`（键 `inventory-daily-position/v1`，实时仓按上海日界倒推，快照仓只取实际 bizDate）；刷新挂 interval-runner/scheduler 11:35/17:35；页面只读缓存 | W2-A | D52 决议；缺日留空 |
| CK-R4 | 库存占比 + 环比 + 目标带 | `decision-studio.ts` momPct 模式 | 无占比、无目标 | `新` `src/server/rules/inventory-sales-ratio.ts`（月末版 + 月均版并列，任一缺失返回 null）+ `src/server/rules/period-compare.ts` | W2-A | D54 默认 45/47，可在 /admin/params 调整（inventory_sales_ratio_target_low/high） |
| CK-R5 | 各调拨线路批次与均价 | `stock_docs subtype=transfer`、`jiediao.ts` 线路矩阵模式 | 无类型/费用 | 消费 W2-C 的 `report/transfer-routes.ts`（线路键 from,to,transfer_type）；cockpit.ts 不自推线路 | W3 | D60 决议 |
| CK-R6 | 当月库存数量 + 逐日出入库 + 历史按月环比 | 同 CK-R3；`/inventory/ledger?from&to` 深链 | 无月末序列 | inventory-daily-position 输出月末在库序列；点击展开逐日表并深链 ledger；上线前月份显示「无逐日账」 | W2-A | D52 |
| CK-R7 | 当月库存金额 + 逐日出入库金额 + 历史 | `sku_costs`（14,4）、`report/margin.ts` costedSkus | 无估值 | 消费 W1 `src/server/core/valuation.ts`（sku_costs → 财务运营成本观察 → null，输出覆盖率）；金额 = qty × unitCost 用 decimal 工具；覆盖率 <80% 标「不完整」 | W2-A | D51 决议（计价基础待财务追认） |
| CK-R8 | 当月销售金额（手工改写、默认隐藏）+ 历史 | `sales_amount_monthly`（已建，append-only supersedes）、`channel-observation.ts` 观察金额 | 无服务/API | `新` `src/server/modules/report/sales-amount-monthly.ts`（finance/admin 写，getFreshSessionUser + writeAudit）+ `src/app/api/report/sales-amount-monthly/route.ts`；`新` 读模型 `platform-sales-amount-monthly/v1` 供预填（source=prefill_observation）；DTO 键 salesAmount 已入 SENSITIVE_FIELDS | W2-A | D53 决议（含税/退款待财务追认） |
| CK-R9 | 预警屏壳 | `focus.ts`、`report/risk.ts`、`report/detectors.ts`、system_alerts | 无统一屏 | cockpit.ts 屏 2 装配 W2-B 的预警读模型 + computeExceptions + getRiskWorklist 计数 | W3 | D57 主预警互斥 |
| CK-R10 | 库存管控屏壳 | `dashboard.ts` warehouseStock、`inventory-analytics.ts` | 无按仓/地区周转 | cockpit.ts 屏 3 装配 W2-C `report/warehouse-inventory.ts` + `transfer-routes.ts` | W3 | D60 |
| CK-R11 | 日常事务屏壳 | getInbox、focus.queues、countReviewItems | 无待办/目标 | cockpit.ts 屏 4 装配 W2-E `todo/stats.ts`、`goals/service.ts` 只读输出 + 调研结论常量 | W3 | D61 |

### 2.2 库存预警 / 交期 / 周转（IAL，8 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| IAL-01 | 导入线下预警公式，400+ 主 SKU/1000+ 子项 | `rules/safety-stock.ts`、`rules/timephased.ts`、`rules/bom-explode.ts`、`report/material-demand.ts` | 无预警表页面；子项层无可销/预警时间 | 不执行 Excel 表达式：公式逐项映射到现有参数，映射不了的写纯规则；`新` 页面 `src/app/(app)/inventory/alerts/`（useListState+Suspense）+ `src/app/api/inventory/alerts/route.ts` + `src/server/modules/inventory/alert-worklist.ts`；material-demand 增 coverDays/alert | W2-B | 待业务：公式原文（§7） |
| IAL-02 | 预警表列：等级、日销、7/15/30 日销 | `external-velocity.ts` net30/net90、`decision-studio.ts` loadDailyFacts、stock_ledger | 无 7/15 窗口、无逐日序列 | `新` 唯一日销读模型 `src/server/modules/report/demand-daily.ts`（键 `demand-daily/v1`，三源并列：平台净件数 / JST 出库 / 实时仓 ledger 出库，不相加）+ `core/velocity.ts` 新导出 dailyFromDays；external-velocity 一次升 /v4（net7/net15） | W2-B | D55：观察序列可预警不定量 |
| IAL-03 | 30+15+5 报警 | `sku_params` 三周期、`cover_alert_days`、belowLead | 无缓冲、无缺省 | `新` `src/server/rules/alert-threshold.ts`（唯一阈值规则，输出 thresholdDays/level/source）；replenish/service.ts、transfer-suggest.ts、dashboard coverBuckets 改消费 | W1→W2-B | D57 默认 30/15/5，可在 /admin/params 调整（default_production_lead_days / default_logistics_lead_days / alert_buffer_days） |
| IAL-04 | 报警后启动调拨计算 | `transfer-suggest.ts`（仅实时仓） | 阈值不同、无深链 | transfer-suggest 阈值改读 alert-threshold，新增 skuIds 过滤；预警行深链 `/report/transfer-suggest?skuIds=`；快照仓不参与并明示 | W2-C | D57「在库可销」 |
| IAL-05 | 各仓周转率 / 总周转 | `rules/inventory-metrics.turnover`、`inventory-analytics.ts` | 只有 SKU 级 | 唯一实现 `新` `src/server/modules/report/warehouse-inventory.ts` + `src/app/(app)/inventory/warehouses/`（平均在库 = (期初+期末)/2 由流水倒推；快照仓 null；总周转只汇总实时仓）| W2-C | 数量口径；金额仅 PRICE_VISIBLE_ROLES |
| IAL-06 | 周期主数据补全 | `sku_params`（新增 purchase_lead_days 已建列）、`report/data-health.ts`、`leadtime-learning.ts` | 无覆盖率 × 分层视图 | data-health 增「周期主数据完整度 × 分层」块；`新` 补录工作台 `src/app/(app)/master/supply-params/` + `PATCH /api/master/sku/supply-params`（只填空 + 逐行审计） | W2-I | 主数据事实，无口径参数 |
| IAL-07 | 产品等级 S/A/B/C | `rules/abc.ts`、`sku_planning_policy`（已建） | 无 S 级 | W1 `rules/abc.ts` 增 classifyTiers + tierToAbc（S→A）；`新` `src/server/modules/replenish/tier-policy.ts` 月度固化 + 覆写（writeAudit）| W1→W2-D | D58 默认 50/80/95，可在 /admin/params 调整（grade_s_pct / grade_a_pct / grade_b_pct） |
| IAL-08 | 红色预警框 / 主动推送 | system_alerts、`freshness.ts` 范式、system-alert-notify | 无 inventory_cover 类别 | `新` `src/jobs/inventory-cover-alert.ts`（category=inventory_cover，refKey=SKU，S/A/B 才开单，恢复自动关闭）；alerts-client CAT + CATEGORY_LABEL 补中文；红卡只做一处（见 §5 #13） | W2-B | D57：S/A/B 开告警，C 仅列表 |

### 2.3 销量波动 / 爆单（SSA，7 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| SSA-1 | 连续 3 天涨幅超 50% | `rules/detectors.ts`（月度）、`rules/spc.ts` | 无日级规则、无日序列 | W1 `新` `src/server/rules/sales-spike.ts`（最近 N 天每日 ≥ 前 7 日日均×(1+rise%)，基线 < min_base 不判定，缺日不判定）；序列来自 demand-daily/v1 | W1→W2-B | D56 默认 3 天/50%/10 件，可在 /admin/params 调整（spike_consecutive_days / spike_rise_pct / spike_min_base_qty） |
| SSA-2 | 自动红色预警框 | AppShell 无横幅；focus.computeExceptions | 无 sales_spike 类别 | `新` `src/jobs/sales-spike-watchdog.ts`（refKey=sku:锚点日，幂等；人工「已知悉」写路径 `PATCH /api/alerts/[id]/ack` 或连续 3 天不命中自动关闭）；computeExceptions 增 key=sales_spike；共享 AlertBanner 组件；runExceptionNotify 跳过该 key 防双发 | W2-B | D56 决议 |
| SSA-3 | 进第二屏 + 每日刷新 | dashboard 缓存模式 | 无列表/API | 列表落 `/inventory/alerts` 的「爆单」Tab（不建 `/report/sales-spike`）；cockpit 屏 2 消费同读模型 | W2-B/W3 | 粒度：已映射系统 SKU + 未映射平台 SKU 另列（D56） |
| SSA-4 | 日销来源：物理仓出库 + 渠道 | jst_daily_sales staging、stock_ledger、简道云天猫日销 | 未组织成序列 | 并入 demand-daily/v1 三源并列（主序列 = 平台支付−退款）；出库白名单排除调拨/盘点/冲销（D35） | W2-B | D55 |
| SSA-5 | 供应商产能弹性 | `rules/supplier-capacity.ts` P50/P90；suppliers 产能三列已建 | 无写路径/UI | `master/supplier.ts` update 增产能字段（writeAudit）；compareCapacity 增 declaredCap 参数并返回 basis；预警行动作抽屉展示主供应商余量 | W2-G | 主数据事实；粒度待业务（§7） |
| SSA-6 | 紧急订单流程 | BH orderType=urgent、WO/JG urgentFlag、`rules/next-action.ts` | 无联动/SLA | 本计划只注册 next-action `bh.urgent_review`（可选）；预警一键建 BH 草稿与 urgent 联动**延后**（触碰 outsource/bh.ts 写路径，见 §5 #15） | L | 待业务：线下 SOP（§7） |
| SSA-7 | 飞书/站内/告警台承接 | system-alert-notify、notify.ts、notify-audience.ts | alertAudience 只发 admin | notify-audience.ts 为 sales_spike/inventory_cover 指定 ops/pmc/purchasing（唯一谓词）；私聊走 W2-E 的 union_id 路径，同一告警不群发+私聊双发 | W2-B | D61 去重谓词唯一 |

### 2.4 调拨链路 / 成本 / 第 3 屏（TR，11 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| TR-1 | 固定调拨类型 | `stock_docs.transfer_type`（已建列 + CHECK）、`src/lib/transfer-types.ts` 六值 | 写路径未校验、存量未回填 | `inventory/schemas.ts` 对 subtype=transfer 必填；`stock-doc.ts` 按仓库 kind 推默认值并入审计 after；docs-client 筛选；`jiediao.ts` 取数改 `transferType='borrow' OR reason='借调'`；存量单一次性回填脚本（staging 路径，审计） | W2-C | D60 六值固定清单 |
| TR-2 | 链路费用统计维度 | `transfer_fees`（已建：fee_type 四类、红字负额） | 无服务 | `新` `src/server/modules/inventory/transfer-fee.ts`（add / reverse 红字，getFreshSessionUser + writeAudit）+ `src/app/api/inventory/stock-doc/[id]/fees/route.ts`；金额键 unitFee/amount 已在 SENSITIVE_FIELDS | W2-C | D60：单据粒度、freight/handling/customs/other |
| TR-3 | 成本实时校验 | `rules/spc.ts`、`supplier-price-variance.ts` 加权均价纪律 | 无基线/规则 | W1 `新` `src/server/rules/transfer-cost.ts`（基线=同线路近 180 天数量加权均价；n<8 只提醒；n≥8 spc 中位数+MAD）；录费时 service 返回 warning（不阻断，写审计）；`新` `src/jobs/transfer-cost-watchdog.ts` category=transfer_cost | W1→W2-C | D60 默认 180 天/20%，可在 /admin/params 调整（transfer_cost_window_days / transfer_cost_deviation_pct） |
| TR-4 | 驾驶舱线路批次与均价 | 读模型缓存模式 | 无线路读模型 | `新` `src/server/modules/report/transfer-routes.ts`（键 `transfer-routes/v1`，线路 = from,to,transfer_type；docCount/Σqty/Σfee/元/件/基线/n/样本不足态）+ `src/app/api/report/transfer-routes/route.ts`；金额按角色裁剪 | W2-C | D60 |
| TR-5 | 各地各仓库存明细、周转 | stock-view、warehouses.regionCode/parentId（D32） | 无地区分组 | 同 IAL-05 唯一实现 warehouse-inventory.ts；「各地」= regionCode + parentId 树 | W2-C | 数量口径 |
| TR-6 | 30+15+5 报警并启动调拨 | transfer-suggest 用 cover_alert_days | 阈值不一致 | 同 IAL-03/IAL-04：只消费 alert-threshold；告警只留 inventory_cover 一类，仓库维度进 detail | W2-C | D57（alert_buffer_days 唯一，不建 transfer_buffer_days） |
| TR-7 | 物流调拨明细 | listStockDocs（无 from/to/类型筛选） | 无页面 | `stock-doc.ts` listStockDocs 增 from/to/transferType/日期筛选；`新` `src/app/(app)/inventory/transfer-routes/`（Tab：正式明细 / 线路汇总 / 费用明细 / 简道云历史 observation_only，各自 paramPrefix）| W2-C | 简道云历史只出聚合（仓库身份 0/2 未认领） |
| TR-8 | 物流费用明细 | 同 TR-2 | 无列表 | transfer-fee.ts list + `src/app/api/inventory/transfer-fees/route.ts`；导出 stripMoneyColumns | W2-C | 金额仅 PRICE_VISIBLE_ROLES |
| TR-9 | 数量/费用异常报警 | detectors 不判定纪律 | 无规则 | rules/transfer-cost.ts 增数量异常（> 中位数 × x）与零散（30 天同线路 > N 单）；同一看门狗、refKey 前缀区分 | W2-C | D60 默认 3 倍 / 4 单，可在 /admin/params 调整（transfer_qty_deviation_x / transfer_batch_max_docs） |
| TR-10 | 限制仓库总数 | `master/warehouse.ts` 无上限 | 无守卫 | create/update(active=true) 事务内 count(实体仓 active) ≥ 上限 → 提醒（不阻断，D60 为提醒）；仓库页显示「已启用 N / 上限 M」 | W2-C | D60 默认 12，可在 /admin/params 调整（warehouse_max_active） |
| TR-11 | 零散→批次调拨 | transfer-suggest 逐 SKU 输出 | 无线路合并 | transfer-suggest 结果按 (from,to) 分组只读；零散度指标进 transfer-routes 读模型；「合并为一张调拨草稿」写路径延后到 `/api/inventory` 域（§8） | W2-C（只读）/L（成单） | D60 零散 = 30 天 > 4 单 |

### 2.5 待办 / 飞书 / 部门目标 / 第 4 屏（TODO，6 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| R1 | 待办指派/状态/完成率 | `work_items`（已建：assignee/assigner/ownerRole/priority/dueDate/status/source）、review/checklist.ts 写路径模板 | 无服务/页面 | `新` `src/server/modules/todo/service.ts`（create/assign/updateStatus/list 三视角/count，同事务 writeAudit）+ `src/server/modules/todo/stats.ts`（完成率/按时率只读）+ `src/app/api/todo/route.ts`、`[id]/route.ts`、`stats/route.ts` + `src/app/(app)/todo/`（Tabs 各自 paramPrefix）；focus.queues 加「指派给我 / 逾期」；`新` `src/jobs/todo-due-reminder.ts` | W2-E | D61：部门先=角色；/inbox 名称保留 |
| R2 | 关联绩效考评 | closed-loop.ts 比率模式、outcome 台账脱敏先例 | 无考评实体 | 绩效 = 「完成率」Tab 按人按月证据导出（admin 与本角色可见，DTO 层裁剪）；不建 performance_reviews | W2-E | D61：证据导出不打分 |
| R3 | 第 4 屏待办进度 | dashboard 单屏 | 无卡 | `新` `src/app/(app)/todo/TodoProgressCard.tsx`（只 fetch /api/todo/stats?scope=summary），cockpit 屏 4 装配 | W2-E→W3 | D61 |
| R4 | 链接飞书安排人员待办 | feishu.ts sendText(chat_id)、users.feishuUnionId、NOTIFICATION_SCOPE_ALLOWLIST | 无按人私聊 | `FeishuAppClient.sendText` 增 receiveIdType 'chat_id'\|'union_id'；dispatchNotifications 对有绑定用户优先私聊、无绑定回落群；零新增 scope；不做 Task API 双向 | W2-E | D61 单向通知 |
| R5 | 供应链目标按部门设置与查阅 | `department_goals`（已建：dept_key=角色、period 月/季、metric_key、target/actual）、D1 五项 KPI | 无服务/页面 | `新` `src/server/modules/goals/service.ts`（upsert / recordActual 必带证据 / list 按 resolveScope(user,'dept') 服务端过滤）+ `src/app/api/goals/` + `src/app/(app)/goals/`；auto 实际值从 dashboard kpi/metrics 只读装配 | W2-E | D61/D62：本部门=角色；admin 全见 |
| R6 | 调研数据/结论 | CURRENT.md 决策簿 | 结论无载体 | `新` 零依赖常量 `src/components/survey-conclusions.ts` + `SurveyConclusionCard.tsx`（四条结论 + 证据页链接）；如需采集用 work_items.source_kind=manual | W3 | 待业务：四条措辞（§7） |

### 2.6 渠道分级权限（CP，7 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| CP-1 | 按渠道配置数据权限 | `user_data_scopes`（已建 channel\|dept）、users.roles、session_version | 身份链未带范围 | SessionUser/JWT 增 channelScope（null=不受限）；`auth/config.ts`、`session-version.ts`、getFreshSessionUser 回查；`admin/users.ts` setUserDataScopes（guardAdmin + writeAudit + sessionVersion+1）+ `src/app/api/admin/users/[id]/data-scope/route.ts` + users-client 渠道多选；/api/me、useMe 透出 | W2-F | D62：受限用户沿用 ops，admin 不受限 |
| CP-2 | 各渠道只看自身销量 | dashboard/decision-studio scopeWhere（用户自选） | 非强制；28 处读 sales_monthly 无范围 | `新` `src/server/core/data-scope.ts`（resolveChannelScope / channelScopeWhere 唯一权威）；第一阶段接 dashboard.ts、decision-studio.ts、sales-bridge.ts、cockpit.ts，缓存键含 sorted(scopeCodes)；未接入页面对受限用户 NoAccess；`新` `tests/architecture/channel-scope-guard.test.ts` | W2-F | D62 |
| CP-3 | 指定公开内容 | MENU_ROLES（AppShell）、CommandPalette.roles、page.tsx 手写门 | 无第三态 | W1 `新` `src/lib/route-access.ts`（ROUTE_ACCESS: roles + scopedMode public\|channel_scoped\|denied；canAccessRoute）；AppShell/CommandPalette 改为从它取值（编排方统一改）；`新` `tests/architecture/route-access-registry.test.ts` | W1→W3 | D62：公开 = 库存总量/临期/到货日历 |
| CP-4 | 敏感数据严格隔离 | maskSensitive（含 salesAmount/unitFee）、export-worker runAs | 行级隔离、导出范围 | data-scope.filterRowsByChannelScope 在 maskSensitive 之前；export-worker runAs 透传范围 + stripScopedRows；「点开才可查看」只在服务端已下发时可点 | W2-F | D53/D62：销售金额对 ops 隐藏 |
| CP-5 | 16 名运营账号与过渡期 | createUser、set-initial-passwords、ops 可制 BH | 无账号/映射 | admin 建号 + 设范围（既有审计路径）；BH 写路径与制单角色本计划不动 | W2-F（建号）/L（权责切换） | 待业务：名单与映射（§7） |
| CP-6 | 部门维度目标查阅 | 同 CP-1 | — | 同一 resolveScope(user,'dept')；不建 users.dept | W2-F | D62 |
| CP-7 | 分渠道分仓销售口径复用 | dashboard channelMix、notAppliedTo | 无「受限视图」标注 | DashboardData.scope 增 forced/scopeLabel；分仓 = 实时仓出库（inventory-analytics 增「分仓出库」卡，标「出库≠销售、剔除调拨 D35」）| W2-F/W2-I | D35 调拨不计销售 |

### 2.7 SKU 分级 / 备货权责 / 过渡期（SKU，10 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| R1 | S/A/B/C 四级，聚焦 S/A/B，C 归运营 | `rules/abc.ts`、segmentation.ts、`sku_planning_policy` | 无 S、不落库、无路由 | W1 abc.ts classifyTiers 参数化；tier-policy.ts 月度固化 + 覆写审计；segmentation 增 tier 列与 4×3 矩阵；replenish 行增 tier/ownership 筛选，C 级默认折叠标「运营兜底」 | W1→W2-D | D58 默认 50/80/95，可在 /admin/params 调整 |
| R2 | 备货规则单一主体 | admin/params 写=admin；sop-cycle 三方全签 | 制度化双向确认 | params 写权限按键分组开放 pmc（admin 兜底，审计不变）；sop-cycle consensusReady 按行权责：仅 supply_chain_direct 行时 pmc 单签可冻结，含 joint_review 行才要 ops 签认；`/replenish/sop` 加只读「现行备货规则」区块（参数 + 最近变更来自 audit_logs） | W2-D | D59：主体 = pmc |
| R3 | 过渡期运营提报交叉验证 | `ops_demand_submissions`（已建 append-only）、import staging | 无服务/页面 | `新` `src/server/modules/replenish/ops-demand.ts`（ops 写、supersedes、writeAudit）+ `src/app/api/replenish/ops-demand/route.ts` + `src/app/(app)/replenish/reconcile/`（按渠道/按 SKU 两 Tab，并排系统建议、内部日均、外部影子、差异%）；Excel 走 staging 模板 | W2-D | D55：提报只对照不驱动 |
| R4 | 80/20 权责 | auto-replenish 五门、fusion、detectors | 无持久权责 | W1 `新` `src/server/rules/replenish-ownership.ts`（tier∈S/A/B ∧ xyz=X ∧ 无异动 → supply_chain_direct；S/A/B ∧ Y/Z 或异动 → joint_review；C → ops_fallback）；auto-replenish 五门收口为调用该规则；计划信封 inputs.demand 增 tier/ownership/pilot，引擎版本升 time-phased-v3（同步 tests/replenish/plan-versions.test.ts） | W1→W2-D | D59 决议 |
| R5 | 试点从稳定 SKU 起 | planning_versions、closed-loop.ts | 无试点名单/度量 | sku_planning_policy.pilot 标记（pmc 一键纳入，审计）；`新` 读模型 `report/replenish-pilot.ts`（键 `replenish-pilot/v1`：候选数、销量占比、建议触发/草稿/错过窗口）；closed-loop 页新 Tab | W2-D | D59；成功指标待业务（§7） |
| R6 | 一处统一出分渠道数据 | sales_monthly.channelId、dashboard scope | 补货/分层无分渠道拆列 | `core/sku-facts.ts` 增可选 byChannel 序列；replenish 行展开显示近 3 月分渠道 + 运营提报对照；分层仍全渠道（禁子集帕累托） | W2-D | 口径：渠道只拆销量不拆分层 |
| R7 | 基于日销量/波动系数/运营规划决策 | segmentation CV 内联；`ops_plan_events`（已建） | 无纯函数 CV；无计划事件服务 | W1 `新` `src/server/rules/volatility.ts`（cv / classifyXyz，样本 <6 返回 null）；`新` `src/server/modules/replenish/ops-plan-events.ts` + `src/app/api/replenish/ops-plan-events/route.ts`；建议页行标签「运营计划：促销 9/15–9/30」，scenarios 预填 uplift（人保存） | W1→W2-D | D55：事件只作展示与情景预填 |
| R8 | 周期主数据前提 | 同 IAL-06 | — | 同 IAL-06；分池前置只要求 normal_lead_days 非空 | W2-I | — |
| R9 | 预警表产品等级 + 7/15/30 日销 | 同 IAL-02 | — | demand-daily/v1 暴露 tier（来自 sku_planning_policy 最新期）；external-velocity /v4 一次升版 | W2-B | D58/D55 |
| R10 | 20% 创收利润单品 / 磨合备包材 | channel-observation SKU 级毛利、sku_costs | 分层只按件数 | segmentation 页加「近 90 天外部毛利（观察）」「单位成本有无」影子列（经 maskSensitive）；包材前置备货归 D36/L | W2-D（影子列）/L | 待业务：重点 SKU 是否含利润（§7） |

### 2.8 前置备货 / 库存占比 / 周期主数据 / 聚水潭状态（FS，9 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| FS-R1 | 前置备货放供应商 | transit_refs pkg_stock/oem_map 参考层、core/supply 四档 | 无受控事实 | 不新建 supplier_reserve_positions（L）；过渡用 transit_refs 参考层影子列 | L | 待业务：是否算供给（§7） |
| FS-R2 | 成品返单 10–20 天 | leadtime-learning（PO→SH）、rollup_supplier_lead | 无 WO/JG 样本 | leadtime-learning 增样本源 WO 审批→首批 SH（sample_kind 隔离 po\|wo，不混入安全库存交期项）；目标区间参数化 | W2-G | 目标 10–20 天：新参数键 fg_reorder_lead_target_days_low/high（建议默认 10/20，编排方登记） |
| FS-R3 | 库存占比 50%→45–47% | 同 CK-R4 | — | 同 CK-R4 唯一实现 | W2-A | D54 |
| FS-R4 | 当月库存金额 + 逐日 + 历史 | 同 CK-R7 | — | 同 CK-R3/CK-R7（读模型，不建 inventory_valuation_daily 物化表） | W2-A | D51/D52 |
| FS-R5 | 当月销售金额手工改写 | 同 CK-R8 | — | 同 CK-R8 唯一实现 | W2-A | D53 |
| FS-R6 | 补全三周期主数据 | `sku_params.purchase_lead_days`（已建列）、`master/sku-supply-params.ts` facade、leadtime 适配器 | facade/导入/表单未接新列 | facade 增 purchaseLeadDays；schemas.ts + updateSku 允许写 normal/purchase；leadtime 适配器按 skuType 落列；sku-client 按类型显示；data-health 增两维 | W2-I | 语义：成品 normal=加工、logistics=在途；部件 purchase=采购 |
| FS-R7 | 聚水潭商品状态标注不清 | jst-item-master-mirror 契约（已同步）、platform-sku-claim 模式 | 无对照读模型 | `新` `report/jst-item-status-gap.ts`（键 `jst-item-status-gap/v1`，维表档批次选择）+ `master/sku-lifecycle-fill.ts`（提议 + 一键确认，只改 lifecycle，逐行审计）；挂决策工作室身份页旁 | W2-I | 待业务：状态映射表（§7） |
| FS-R8 | 日销量 + 波动系数 + 运营规划同屏 | sku-panorama、segmentation | 分散 | sku-panorama 抽屉与 replenish 行加 CV（调 volatility）与外部日均影子；运营规划来自 ops_plan_events | W2-D | D55 |
| FS-R9 | 周期缺失时报警 | belowLeadtime 缺周期不报 | 无兜底 | alert-threshold 缺省兜底并在行上标 source='default'；data-health 仍计缺失 | W2-B | D57 |

### 2.9 采购订单系统与指标（PO，11 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| PO-R1 | 二期从零搭采购订单管理 | po_docs/po_lines/po_promise_revisions/pc/ct、docflow 状态机、po.ts 写路径 | 无独立下单入口（PO 只由 WO 派生） | 本计划不做独立 createPo；二期另立规格 `docs/spec/20-采购订单二期业务流与规则.md`（L） | L | 待业务：成品直采/PR/D29（§7） |
| PO-R2 | 节点规则可配置可解释 | transition.ts、approval.ts、doc-aging 硬编码 3/5 天 | 未成文/未参数化 | doc-aging 两阈值进 PARAM_DEFS（po_pending_aging_days / po_approved_aging_days，建议默认 3/5）；sh_lines.po_line_id 行级关联延后（L） | W2-G（参数）/L | 待登记参数键 |
| PO-R3 | 人工录入准确率 90–95%、周核对 | supply-commitment fail-closed、data-health | 无核对清单 | data-health 增「采购单据质量」段（缺交期 / 长期草稿 / receivedQty 与事件不符 / PC 超期）；周任务生成 review_items(category=po_quality) | W2-H | D65：manual_po_chain 类 |
| PO-R4 | 周期主数据 | 同 FS-R6 | — | 同 FS-R6 | W2-I | — |
| PO-R5 | 已下单 | dashboard 全量计数、core/supply 未结量 | 无期间读模型 | `新` `src/server/modules/report/purchase-order-metrics.ts`（键 `purchase-order-metrics/v1`：下单时点 = approvals(docType=po,action=approve)，按上海月聚合单数/行数/基础单位数量/未交付量）+ `src/app/api/report/purchase-order-metrics/route.ts` + `/report/purchase-orders/`（真报表）；jobs/rollup.ts 每日重建 | W2-G | D63：已下单 = PO 审批通过 |
| PO-R6 | 已下单金额 | po_lines price/taxIncluded/taxRatePct、maskSensitive | 无汇总 | 同读模型逐行 dMul：未税为主、含税并列、已交付金额、CT 回冲；金额键经 canSeePrices 剥离 + stripMoneyColumns | W2-G | D63：未税为主 |
| PO-R7 | 订单至交付周期 | leadtime-stats.ts、leadtime-learning、supply-commitment | 三套起点口径 | `新` `src/server/rules/po-cycle.ts`（审批→首批 SH 天数；全收并列；P50/P90 minSamples=3）；leadtime-learning/scorecard 保持 created_at 口径并在 caveat 写明 | W2-G | D63：审批→首批 SH |
| PO-R8 | 成本下降 | price_lists、supplier-price-variance、rules/price.normalizeToBaseNet | 无基线/降本 | `新` `src/server/rules/cost-saving.ts`（基线 = 上一年度已批 PO 数量加权未税均价，无则首个；降本 = Σ(基线−当前)×当年已批数量；只计降价，涨价另列不轧差）；并入 purchase-order-metrics | W2-G | D63（基线待采购追认） |
| PO-R9 | 第 2 屏每日刷新 | rollup.ts 日任务 | — | purchase-order-metrics 由 rollup 每日重建；cockpit 只消费 | W2-G→W3 | — |
| PO-R10 | 虚拟仓归采购 | warehouses.kind transit/outsource/snapshot | 无「虚拟仓」概念 | 不建表：页面展示「采购在途（未结供给）」+ transit 仓余额两类并注明 | W2-G | 待业务：精确指代（§7） |
| PO-R11 | 账期字段承载 | suppliers.payment_term_type/credit_days/effective_from（已建） | 写路径/UI 未接 | 同 SPT-2 | W2-G | D64 |

### 2.10 供应商账期（SPT，5 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| SPT-1 | 识别候选（≥2 年、排名上升、OA/包材分池） | suppliers.kinds、supplier-price-variance 归一化、settlement-summary | 无合作起始日、无年度排名 | `新` `src/server/rules/supplier-payment-term.ts` + `report/supplier-payment-term.ts`（键 `supplier-payment-term/v1`：供应商×年 PO 未税额 + JS 结算额，processor/packaging 分池排名）+ `/report/supplier-payment-term/`；合作起始日：suppliers 增 cooperation_since（允许 0048 小迁移，编排方定）缺省回退最早 PO/JG 并标「系统推算」；2024/2025 采购额经 staging 导入参考表标 observation_only | W2-G | D64 默认 2 年，可在 /admin/params 调整（payment_term_min_years） |
| SPT-2 | 逐家跟踪账期调整 | suppliers 三列已建；supplier_lifecycle_cases | 写路径/UI/工作项 | master/supplier.ts update 增三列（writeAudit）；`新` `src/server/rules/payment-term.ts` 解析旧文本回填（失败留 null）；supplier_lifecycle_cases.kind 增 payment_term（CHECK 改写，允许 0048）；关案时同事务写新账期 | W2-G | D64 |
| SPT-3 | 同级别品牌政策参考 | — | 无数据 | 不建功能；目标区间为参数，候选页标达标/未达标 | W2-G | D64 默认 45/60，可在 /admin/params 调整（payment_term_target_min/max_days） |
| SPT-4 | 账期类应付占比 | PO/JS 金额；用友无应付契约 | 无应付台账 | 代理指标 creditTermSpendShare = 月结类供应商采购额 ÷ 全部（血缘 partial，标「非应付余额」）；真应付等用友授权 | W2-G | D64 看板 |
| SPT-5 | 第 4 屏部门目标承载 | department_goals | — | 读模型返回「年底达成率 = 已关案达标 ÷ 候选数」，作为 department_goals(purchasing) auto 实际值来源 | W2-G→W3 | D61/D64 |

### 2.11 数据质量核对（DQ，6 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| DQ-1 | 来源分类 RPA / 人工 | import_jobs.template、integration_runs.stream 各自命名 | 无统一分类 | `新` `src/server/core/data-source-class.ts`（rpa_warehouse \| manual_po_chain \| external_platform \| reference_file 登记到模板/流/单据类型）；`新` `src/server/rules/snapshot-quality.ts`（相邻日跳变、新增/消失 SKU、负数、未解析比例）+ `src/jobs/snapshot-quality.ts` category=snapshot_quality | W2-H | D65 三类 |
| DQ-2 | 准确率 90–95% 可度量 | reconcile-jst matched/diff、staging okRows/failRows、jdy controlSummary | 无准确率指标 | `新` `src/server/rules/data-accuracy.ts`（SKU 日级一致率 + 数量加权一致率 + 行级通过率 + 人工「首次正确率」代理）；指标登记 dataAccuracyRate；低于目标开 system_alerts(category=data_quality) | W2-H | D65 默认容差 1%，可在 /admin/params 调整（dq_tolerance_pct）；目标 90/95 建议登记 dq_accuracy_target_pct/dq_accuracy_stretch_pct |
| DQ-3 | 周核对→月核对 | month_close_checks（月、6 key 钉死）、recon_diffs 无写接口 | 无周期签认 | `新` `src/server/modules/import-review/data-quality-review.ts`（照 month-close：实时证据/固化/例外原因/乐观锁/writeAudit）；`PATCH /api/jobs/recon/[id]`（explained/resolved，重跑保留人工状态）；`新` `src/jobs/data-quality-review.ts` 周一 09:00 生成待办 + 通知 | W2-H | D65：连续 4 周达标后月核对（切换由人在参数页改 dq_review_cadence，建议登记） |
| DQ-4 | 历史缺失不伪装成 0 | freshness 从未导入不告警、快照带数据龄 | 无覆盖起点 | data-source-readiness 增 coverageStart/End（键 `data-coverage/v1`）；豁免清单常量 HISTORY_BACKFILL_WAIVED（登记 CURRENT.md） | W2-H | D65；豁免清单待业务（§7） |
| DQ-5 | 每日自动校验总览 | recon、staging 拒收、controlSummary、detectors 四处 | 无总览页 | `新` `src/server/modules/report/data-quality.ts`（键 `data-quality/v1`）+ `src/app/api/report/data-quality/route.ts` + `src/app/(app)/import/data-quality/`（来源×维度表：及时/完整/唯一/准确，只算能算的）| W2-H | D65 |
| DQ-6 | 手工改写销售金额留痕 | sales_amount_monthly 审计 | 未纳入统计 | data-source-class 增 manual_override；数据质量页独立列「本期手工改写指标数」，不混入准确率分子分母 | W2-H | D53/D65 |

### 2.12 BI 复用与接口（BI，10 条）

| ID | 需求 | 现状 | 缺口 | 提案 | 波次 | 口径 |
|---|---|---|---|---|---|---|
| BI-R1 | 分品牌销量 | dashboard 品牌结构、decision-studio、channel-observation | 品牌靠店铺名猜；brand-master/shop-master 契约无消费 | channel-observation 升 /v4（与 W2-B 合并一次升版）用 shop-master/brand-master 出 byBrand×platform；驾驶舱并排影子序列不相加 | W2-I | 件数为主，金额按角色 |
| BI-R2 | 分渠道分仓销量 | channelMix、stock_ledger 出库 | 无分仓销售源 | inventory-analytics 增「分仓出库」卡（出库≠销售，剔除调拨 D35）；渠道×仓交叉留白 gate | W2-I | D35 |
| BI-R3 | 效期分布 | expiry-list.ts 七段位、EXPIRY_TIER_DAYS | 无品牌维 | listExpiryBatches 增 brand 参数与 bucket×brand 矩阵；驾驶舱临期从 notAppliedTo 移入 brand 适用 | W2-I | 数量口径 |
| BI-R4 | SKU 销量排名 | dashboard TOP10、studio TOP30、external-velocity.bySku | 无外部排名列表 | decision-studio external Tab 增「SKU 外部销量排名」表（品牌/平台筛选、CSV、显示身份覆盖率） | W2-I | 件数；组合装按 D47 拆解并注明 |
| BI-R5 | 天猫/拼多多接口对接 | RPA→简道云 33 条契约 10:30/16:30 自动同步；pdd-product-daily/pdd-shop-daily 已同步未消费 | 无直连；拼多多无金额 | 短期：pdd 两流接入 channel-observation /v4（补金额/退款/转化）；直连开放平台不做（§8） | W2-I | 现行镜像已满足「自动同步」 |
| BI-R6 | 海外仓打通 | channels overseas、warehouses.regionCode、快照模板 | 无连接器/清单 | 阶段 1：建 kind=snapshot 海外仓 + 快照模板导入（零代码）；阶段 2 连接器待系统确认（L） | W2-I（阶段 1）/L | 待业务：清单与系统（§7） |
| BI-R7 | 聚水潭打通 | jst.ts 六端点、可信仓纪律、探针 | 授权 110/190 阻断 | 不写代码：按 `docs/integrations/EXTERNAL-SYSTEMS.md` §1a 由业主在控制台授权 + 固定出口 IP，随后 7 天 sync/reconcile UAT | 持续项 | 待业主：授权（§7） |
| BI-R8 | 「水盘」打通 | 零命中 | 身份不明 | 不实施；先确认指代（可能为「货盘-PMC」表转写误差） | L | 待业务（§7） |
| BI-R9 | 先线框后开发 | DecisionVisual 契约 | 无线框 | `docs/requirements/wireframes-四屏-2026-09.md`（本波产出）+ Artifact 线框页 | W1 | D50 |
| BI-R10 | 开通账号、展示可用功能 | /admin/users、set-initial-passwords、`npm run access:link` | 运营看不到平台观察 | admin 建号按角色；channel-observation 路由对 ops 放行「无金额视图」（maskSensitive 剥金额/毛利）；演示路径固定（驾驶舱→工作室 focus/external/identity/readiness→效期→库存分析→数据源就绪） | W2-F/W3 | D62 |

---

## 3. 最佳实践采纳表（研究 synthesis.adoptionMap 收敛后）

| 模块 | 采纳实践 | MVP（本计划落点） | 缺数据 / 前提 |
|---|---|---|---|
| 驾驶舱 | 四屏各自「一屏成立」+ 统一顶栏（来源·时点·覆盖·口径版本）；例外优先、例外表 ≤20 行、瓦片点击 = 下钻列表 | `/cockpit` 四 Tab；每卡 DecisionVisual 契约；顶栏读 data-source-readiness | 月末库存读模型；库存/销售金额 |
| 驾驶舱 | KPI 树顶层：库存/销售金额占比 + 目标带，手工月输入为主、系统值为辅、带审计 | sales_amount_monthly + rules/inventory-sales-ratio + 参数 45/47 | 内部销售金额；sku_costs 覆盖率实测 |
| 驾驶舱 | 各仓周转先数量口径；快照仓「无流水不计算」 | report/warehouse-inventory.ts | 快照仓流水不可得（明示） |
| 驾驶舱 | 断货时点计数替代时间口径缺货率 | 屏 2 红卡：在库 ≤0 且近 6 月有销 SKU 数按分层 | 逐日位置表（不建） |
| 预警 | 逐 SKU 动态阈值「生产 + 物流 + 缓冲」，只用内部口径着色，外部作影子 | rules/alert-threshold + 参数三键 | sku_params 覆盖率（主数据补录，不是代码） |
| 预警 | 三口径可销天数并排（在库 / 全管道 / 前视断货日）复用既有引擎 | 预警表列：stock-view.coverDays、fusion.fuseCover、timephased.shortageDate/orderBy | 内部日销事实 |
| 预警 | 每 SKU 只出一个主预警，其余作标签；驾驶舱计数与表格同源 | `新` `src/server/rules/alert-priority.ts`（out_of_stock > low_stock > near_expiry > overstock） | 无 |
| 预警 | 例外引擎升级（ownerRole/actionHref/dedupeKey/sourceRule/paramsSnapshot；事实闭环自动关闭；按 owner 分组） | system_alerts 扩列允许 0048（D66）；本波先用 detail jsonb 承载 | 站会流程（暂不做 SLA） |
| 预警 | 爆单 = 唯一纯规则 + 观察口径 + 误报账；上线前回放 90 天 | rules/sales-spike + demand-daily/v1 + `scripts/backtest-sales-spike.ts`（可选） | 内部逐日事实；拼多多完整月 |
| 分级 | S/A/B/C 落 rules/abc.ts 参数化，不改 classifyAbc 签名；新品标「未分层」而非 C | classifyTiers + tierToAbc | S 边界业务确认 |
| 分级 | XYZ 抽纯函数，样本不足返回 null | rules/volatility.ts | ≥12 个月历史 |
| 分级 | 策略参数按四级分域解析 | cover_target_days_s 待登记（建议默认与 A 相同 60）；scoped-params segment 允许 SX/SY/SZ | 各级目标覆盖天数 |
| 分级 | 试点候选由既有信号纯计算 | replenish-ownership + pilot 标记 | 交期主数据覆盖率（名单会很短——这是要先看到的事实） |
| 分级 | 单一计划主体落现有 S&OP，RACI 只登记 | sop-cycle 按权责签认；`/replenish/sop` RACI 说明 | 16 名运营账号 |
| 分级 | 运营提报影子表 + 试点看板 | ops_demand_submissions + `/replenish/reconcile` + replenish-pilot/v1 | 7 月后实际销量 |
| 调拨 | 类型主数据化；运费凭证观察口径不进库存成本；线路基线 + 两档信号 + 样本护栏（spc） | transfer_type + transfer_fees + rules/transfer-cost（n<8 watch / n≥8 MAD） | 费用数据从零累计 |
| 采购 | 供应商 OTIF 行级 + 小样本护栏；PO 承诺交期填报率看板 | po-cycle.ts P50/P90（minSamples 3）；data-health 填报率 | PO 真实流量 |
| 供应商 | 账期主档结构化 + 谈判进度走 lifecycle case | suppliers 三列 + lifecycle kind=payment_term | 合作起始日、协议附件 |
| 数据质量 | IRA 轨 A（盘点命中率）诚实标注；快照批次放行仓级预检；周核对包→月对账；来源×维度表不算综合分；销量口径一致性带绝对量下限 | rules/data-accuracy + snapshot-quality + data-quality-review + `/import/data-quality` | pd_lines 逐行状态（不做）；店铺↔渠道映射 |
| 待办 | 待办表 + 4 态状态机作投影层；告警→任务自动立案去重；结构性防刷（<10 分钟关闭标 suspicious、7 天内同指纹 reopen）；飞书单向 + 月表 | work_items + todo/service + rules/task-triggers（可选 W3）| close_code 码表（不做） |
| 权限 | 按渠道行级裁剪在服务端；「无权限」是独立空态不是 0 | core/data-scope + route-access | 用户↔渠道、店铺↔渠道映射 |
| BI | 统一图卡契约四字段 + 五态 + 指标唯一注册 + 缓存键升版；覆盖不足显示徽标不补零 | 所有新卡登记 metricId（§9） | 无（纪律项） |

---

## 4. 波次计划（文件级）

### W1 基座（本波；「已落地」经 PGlite 迁移 + tsc 验证；「并行中」由编排方验收）

| 状态 | 交付 | 文件 |
|---|---|---|
| 已落地 | 单一迁移 + 9 张新表 | `drizzle/0047_director_program.sql`、`drizzle/meta/_journal.json`、`src/db/schema/director.ts`（transferFees / salesAmountMonthly / userDataScopes / workItems / departmentGoals / skuPlanningPolicy / dataQualityReviews / opsDemandSubmissions / opsPlanEvents）、`tests/schema/director-program.test.ts` |
| 已落地 | 扩列 | `src/db/schema/docs.ts` stock_docs.transfer_type；`src/db/schema/masters.ts` suppliers.payment_term_type/credit_days/payment_term_effective_from/declared_monthly_capacity/capacity_uom/surge_capacity_pct；`src/db/schema/npd.ts` sku_params.purchase_lead_days |
| 已落地 | 常量 | `src/lib/transfer-types.ts`（六值 + 标签 + isTransferType） |
| 已落地 | 参数 20 键 | `src/server/modules/admin/params.ts`：inventory_sales_ratio_target_low/high、default_production_lead_days、default_logistics_lead_days、alert_buffer_days、grade_s/a/b_pct、spike_consecutive_days/rise_pct/min_base_qty、transfer_cost_window_days/deviation_pct、transfer_qty_deviation_x、transfer_batch_max_docs、warehouse_max_active、payment_term_min_years/target_min_days/target_max_days、dq_tolerance_pct |
| 已落地 | 敏感字段 | `src/server/core/constants.ts` SENSITIVE_FIELDS + salesAmount、unitFee |
| 已落地 | 文档 | 本文；`docs/requirements/wireframes-四屏-2026-09.md` |
| 并行中 | 共享规则 | `src/server/rules/abc.ts`（classifyTiers/tierToAbc）、`src/server/rules/alert-threshold.ts`、`src/server/rules/sales-spike.ts`、`src/server/rules/transfer-cost.ts`、`src/server/rules/replenish-ownership.ts`、`src/server/rules/volatility.ts`、`src/server/rules/alert-priority.ts` + 各自 `tests/rules/*.test.ts` |
| 并行中 | 共享核心 | `src/server/core/valuation.ts`（D51）、`src/server/core/data-scope.ts`（D62）、`src/server/core/velocity.ts` 新导出 dailyFromDays |
| 并行中 | 注册表 | `src/lib/route-access.ts`（零依赖，客户端可值导入）+ `tests/architecture/route-access-registry.test.ts` |

### W2 各域并行（每域交付 service / 读模型 / 页面 / 测试 + manifest；不改台账、AppShell、CommandPalette）

| 域 | 交付文件 | 依赖 |
|---|---|---|
| **A 库存日级 / 估值 / 销售金额 / 占比** | `src/server/modules/report/inventory-daily-position.ts`、`src/server/rules/period-compare.ts`、`src/server/rules/inventory-sales-ratio.ts`、`src/server/modules/report/sales-amount-monthly.ts`、`src/server/modules/report/platform-sales-amount-monthly.ts`、`src/app/api/report/sales-amount-monthly/route.ts`、`src/jobs/interval-runner.ts` + `scheduler.ts`（cockpit-read-model 11:35/17:35）、`tests/report/inventory-daily-position.test.ts`、`tests/report/sales-amount-monthly.test.ts`、`tests/rules/inventory-sales-ratio.test.ts` | W1 valuation；measure-first 取 stock_ledger 基线 |
| **B 预警** | `src/server/modules/report/demand-daily.ts`、`src/server/modules/report/external-velocity.ts`（/v3→/v4 一次）、`src/server/modules/inventory/alert-worklist.ts`、`src/app/api/inventory/alerts/route.ts`、`src/app/(app)/inventory/alerts/page.tsx` + `alerts-client.tsx`（Tab：库存预警 / 爆单）、`src/jobs/inventory-cover-alert.ts`、`src/jobs/sales-spike-watchdog.ts`、`src/app/api/alerts/[id]/ack/route.ts`、`src/jobs/system-alert-notify.ts`（CATEGORY_LABEL/alertHref）、`src/server/core/notify-audience.ts`、`src/app/(app)/alerts/alerts-client.tsx`（CAT）、`src/server/modules/workbench/focus.ts`（sales_spike/inventory_cover 异常项）、`src/components/AlertBanner.tsx`、`src/server/modules/replenish/service.ts` + `report/transfer-suggest.ts` + `report/dashboard.ts`（改读 alert-threshold）、`tests/report/demand-daily.test.ts`、`tests/jobs/*.test.ts`、`tests/architecture/alert-category-labels.test.ts` | W1 规则；简道云批次三档（D49） |
| **C 调拨** | `src/server/modules/inventory/schemas.ts`、`stock-doc.ts`、`transfer-fee.ts`、`src/app/api/inventory/stock-doc/[id]/fees/route.ts`、`src/app/api/inventory/transfer-fees/route.ts`、`src/server/modules/report/transfer-routes.ts`、`src/app/api/report/transfer-routes/route.ts`、`src/server/modules/report/warehouse-inventory.ts`、`src/app/api/report/warehouse-inventory/route.ts`、`src/app/(app)/inventory/transfer-routes/`、`src/app/(app)/inventory/warehouses/`、`src/jobs/transfer-cost-watchdog.ts`、`src/server/modules/master/warehouse.ts`（上限提醒）、`src/server/modules/report/jiediao.ts`、`src/components/labels.ts`、`scripts/backfill-transfer-type.ts`、`tests/inventory/transfer-fee.test.ts`、`tests/report/transfer-routes.test.ts`、`tests/master/warehouse.test.ts` | W1 transfer-cost |
| **D 分级与权责** | `src/server/modules/replenish/tier-policy.ts`、`ops-demand.ts`、`ops-plan-events.ts`、`sop-cycle.ts`（按权责签认）、`plan-versions.ts`（time-phased-v3）、`src/server/modules/report/segmentation.ts`、`auto-replenish.ts`（收口）、`replenish-pilot.ts`、`src/server/core/sku-facts.ts`（byChannel）、`src/server/core/scoped-params.ts`（SX/SY/SZ）、`src/server/modules/admin/params.ts`（pmc 写权限分组 + cover_target_days_s）、`src/app/api/replenish/ops-demand/route.ts`、`ops-plan-events/route.ts`、`src/app/(app)/replenish/reconcile/`、`replenish-client.tsx`、`sop-client.tsx`、`tests/replenish/*.test.ts`、`tests/replenish/plan-versions.test.ts`（版本断言） | W1 abc 四档、ownership、volatility |
| **E 待办与目标** | `src/server/modules/todo/service.ts`、`stats.ts`、`src/app/api/todo/route.ts`、`[id]/route.ts`、`stats/route.ts`、`src/app/(app)/todo/page.tsx` + `todo-client.tsx` + `TodoProgressCard.tsx`、`src/server/modules/goals/service.ts`、`src/app/api/goals/route.ts`、`[id]/route.ts`、`src/app/(app)/goals/`、`GoalProgressCard.tsx`、`src/jobs/todo-due-reminder.ts`、`src/server/integrations/feishu.ts`（receiveIdType）、`src/jobs/notify.ts`、`src/server/core/event-taxonomy.ts`（assign）、`src/server/modules/workbench/focus.ts`（queues）、`tests/todo/*.test.ts`、`tests/integrations/feishu-app.test.ts` | W1 data-scope（dept） |
| **F 渠道范围** | `src/server/core/dto.ts`（SessionUser.channelScope）、`src/server/auth/config.ts`、`session-version.ts`、`src/server/modules/admin/users.ts`、`src/app/api/admin/users/[id]/data-scope/route.ts`、`src/app/(app)/admin/users/users-client.tsx`、`src/app/api/me/route.ts`、`src/components/useMe.tsx`、`src/server/modules/report/dashboard.ts`、`decision-studio.ts`、`sales-bridge.ts`、`channel-observation.ts`（sliceByChannelScope）+ route、`src/server/modules/report/export.ts`、`src/jobs/export-worker.ts`、`src/db/seed-dimensions.ts`（店铺别名种子，若业务给清单）、`tests/auth/session-version.test.ts`、`tests/report/channel-scope.test.ts`、`tests/architecture/channel-scope-guard.test.ts`、`tests/redteam/dto-mask.redteam.test.ts` | W1 data-scope、route-access |
| **G 采购订单与账期** | `src/server/rules/po-cycle.ts`、`cost-saving.ts`、`supplier-payment-term.ts`、`payment-term.ts`、`src/server/modules/report/purchase-order-metrics.ts`、`supplier-payment-term.ts`、`leadtime-learning.ts`（WO 样本）、`src/jobs/rollup.ts`、`src/jobs/doc-aging.ts`（参数化）、`src/server/modules/master/supplier.ts` + `schemas.ts` + `supplier-lifecycle.ts`（kind=payment_term）、`src/server/modules/report/supplier-capacity.ts` + `rules/supplier-capacity.ts`（declaredCap）、`src/app/api/report/purchase-order-metrics/route.ts`、`supplier-payment-term/route.ts`、`src/app/(app)/report/purchase-orders/`、`supplier-payment-term/`、`src/app/(app)/master/supplier/supplier-client.tsx`、`tests/rules/po-cycle.test.ts`、`cost-saving.test.ts`、`supplier-payment-term.test.ts`、`tests/report/purchase-order-metrics.test.ts` | 允许 0048 小迁移（cooperation_since、lifecycle CHECK）——由编排方决定 |
| **H 数据质量** | `src/server/core/data-source-class.ts`、`src/server/rules/data-accuracy.ts`、`snapshot-quality.ts`、`src/server/modules/import-review/data-quality-review.ts`、`src/server/modules/report/data-quality.ts`、`data-source-readiness.ts`（coverageStart/End）、`data-health.ts`（采购单据质量段）、`src/app/api/report/data-quality/route.ts`、`src/app/api/jobs/recon/[id]/route.ts`、`src/app/(app)/import/data-quality/`、`src/app/(app)/jobs/recon/recon-client.tsx`、`src/jobs/data-quality-review.ts`、`snapshot-quality.ts`、`reconcile-jst.ts`（onConflict 保留人工状态）、`tests/rules/data-accuracy.test.ts`、`snapshot-quality.test.ts`、`tests/architecture/data-source-class-registry.test.ts` | — |
| **I 主数据补录与 BI 小项** | `src/server/modules/master/sku-supply-params.ts`、`sku.ts`、`schemas.ts`、`src/server/import/adapters/leadtime.ts`、`src/server/modules/release/engine/sku-params.ts`、`src/app/(app)/master/supply-params/`、`src/app/api/master/sku/supply-params/route.ts`、`src/server/modules/report/data-health.ts`（周期 × 分层）、`jst-item-status-gap.ts`、`src/server/modules/master/sku-lifecycle-fill.ts`、`src/server/modules/inventory/expiry-list.ts`（brand）、`inventory-analytics.ts`（分仓出库）、`decision-studio.ts`（外部排名表）、`channel-observation.ts`（/v4：shop/brand master + pdd 日流，与 F 域合并一次升版）、`src/components/data-asset-decision-coverage.ts` | — |

### W3 装配 / 台账 / 验证 / 部署

| 交付 | 文件 |
|---|---|
| 四屏装配 | `src/server/modules/report/cockpit.ts`（getCockpitSources / getCockpitAlerts / getCockpitInventory / getCockpitOps，60s 进程缓存 + 角色 + scope 键）、`src/app/api/report/cockpit/route.ts`、`src/app/(app)/cockpit/page.tsx`（Suspense）+ `cockpit-client.tsx`、`src/server/rules/schedule-next-run.ts`、`src/components/survey-conclusions.ts` + `SurveyConclusionCard.tsx`、`tests/report/cockpit.test.ts` |
| 菜单/命令面板 | `src/components/AppShell.tsx`、`src/components/CommandPalette.tsx` 改为从 `src/lib/route-access.ts` 取值；条目见 §9 |
| 指标登记 | `src/components/metrics.ts`、`src/lib/data-product-metric-lineage.ts`（升版）；id 见 §9 |
| 台账 | `docs/spec/16-…台账.md` A589+（API_ROUTE / AUTH_PAGE / MIGRATION 0047 / ARCH_GATE）、`docs/spec/15-…台账.md` C203+、`tests/architecture/system-audit-500.test.ts` + `capability-ledger.test.ts` 计数、`docs/spec/CURRENT.md`（D50–D66 已登记；新参数键补注）、`docs/NOW.md` |
| 验证 | `npm run check:pr` 全量（编排方）；回放脚本 `scripts/backtest-sales-spike.ts`（近 90 天分布供总监目检）；UAT：16 名运营账号越权断言、金额 RSC/CSV 脱敏断言、读模型缓存键升版验证 |
| 部署 | 迁移后重启 dev/prod（PGlite 仅启动迁移）；`/api/health` 漂移检查；公网链接 `npm run access:link`；飞书通告 |

---

## 5. 冲突消解表（critique.conflicts 19 条 → 唯一实现）

| # | 冲突 | 采用的唯一实现 |
|---|---|---|
| 1 | 迁移序号 0047 六方争用 | D66：全部并入 `drizzle/0047_director_program.sql`（已落地）；预警引擎/供应商小扩列允许 0048，由编排方取号 |
| 2 | 调拨费用表两套（transfer_fee_refs vs transfer_fees） | `transfer_fees`（`director.ts`：单据粒度、fee_type 四类、红字负额 reversal_of_id）；cockpit 只消费 |
| 3 | 线路读模型三处 | 唯一 `report/transfer-routes.ts`，线路键 = (from, to, transfer_type)；cockpit.ts 与 `/inventory/transfer-routes` 页同读同缓存键 |
| 4 | 30+15+5 三套实现 / 两个缓冲参数 / 两类告警 | 唯一 `rules/alert-threshold.ts` + 参数 `alert_buffer_days`（不建 transfer_buffer_days）；告警只留 category=inventory_cover，refKey=SKU，仓库维度放 detail |
| 5 | 各仓周转三处 | 唯一 `report/warehouse-inventory.ts`（平均在库 = (期初+期末)/2，期初由流水倒推；快照仓 null）；页面 `/inventory/warehouses`；inventory-analytics 不改口径 |
| 6 | 日销序列两读模型 + external-velocity 三方升 /v4 | 唯一 `report/demand-daily.ts`（/v1，三源并列）+ `core/velocity.ts` dailyFromDays；external-velocity 只升一次 /v4（net7/net15/cvDaily30/tier 一起加），B 域负责，D/I 域只消费 |
| 7 | CV 两处 | 唯一 `rules/volatility.ts`；demand-daily 与 segmentation 均调用 |
| 8 | S/A/B/C 两套（sku-tier.ts vs abc.ts vs skus.grade） | D58：只在 `rules/abc.ts` 参数化（classifyTiers/tierToAbc）；不建 sku-tier.ts、不加 skus.grade；结果落 sku_planning_policy |
| 9 | 库存估值三处本地 | D51：唯一 `core/valuation.ts`；A/C 域只调用 |
| 10 | 占比规则 / 销售金额表重复 | 唯一 `rules/inventory-sales-ratio.ts` + `sales_amount_monthly`；FS-R3/R4/R5 并入 A 域 |
| 11 | 部门两套口径（deptKey=Role vs user_data_scopes） | D61/D62 并存但解析器唯一：department_goals.dept_key = 角色；读侧一律 `core/data-scope.resolveScope(user,'dept')`；不建 users.dept |
| 12 | 四屏落点（/cockpit vs dashboard-client） | D50：`/cockpit` + `cockpit.ts`；各域提供数据块函数（返回 plain object），**不改** `dashboard-client.tsx`/`dashboard.ts` 的卡片；dashboard.ts 仅在阈值改读 alert-threshold 时动 |
| 13 | 红色预警框两处 | 一处：`workbench/focus.ts` computeExceptions 增 key sales_spike / inventory_cover（severity critical/high）+ 共享 `src/components/AlertBanner.tsx` 用于工作台与 /cockpit 屏 2；投递只走 system-alert-notify，runExceptionNotify 跳过这两个 key |
| 14 | AppShell/CommandPalette 六方直改 vs route-access 重构 | route-access.ts 先行（W1）；各域只在 manifest 登记条目；W3 由编排方一次改 AppShell/CommandPalette |
| 15 | BH 写路径三方改动 | 本计划**不改** `outsource/bh.ts`：SSA-6 一键紧急草稿、CP-5 渠道过滤、C 级 ops 直录延后（§8）；C 级「运营兜底」暂 = 运营收告警 + 既有 ops 提 BH 入口 |
| 16 | C203 / D50 / A589 编号撞车 | D66：编排方集中分配；各域 manifest 只写「需登记」 |
| 17 | transfer-suggest 多方改 | 一次改：阈值读 alert-threshold + skuIds 过滤 + 按线路分组只读；「合并草稿」写路径延后到 `/api/inventory` 域 |
| 18 | sales_velocity 休眠表 | 保持休眠、不写入（观察数据不进智能层输入表）；日均落点 = demand-daily 读模型；标记「废弃候选」待 D 号 |
| 19 | PARAM_DEFS 集中争用 | W1 已一次登记 20 键；后续新增（cover_target_days_s、po_*_aging_days、dq_accuracy_target_pct、dq_review_cadence、fg_reorder_lead_target_*）由编排方追加 |

### 5.1 违规项修正（critique.violations 10 条）

| # | 违规 | 采用做法 |
|---|---|---|
| 1 | `/report/sales-spike` 带写动作 | 落 `/inventory/alerts` 爆单 Tab；/report/* 仅真报表 |
| 2 | `/api/report/transfer-suggest/draft` 写路由 | 延后，且必落 `/api/inventory/stock-doc` 域 |
| 3 | 报表内自算 d7/d15/d30 日均 | `core/velocity.ts` 新导出 dailyFromDays，读模型只消费 |
| 4 | 第二分层权威 | 只在 `rules/abc.ts`（#8） |
| 5 | 估值本地实现 | `core/valuation.ts`（#9） |
| 6 | 看门狗不写审计 / 录费 warning | system_alerts 属系统写入可豁免并在文件头写明；`transfer-fee.ts` 的录入是业务写路径，warning 与 writeAudit 同事务 |
| 7 | 页面缺缓存时同步重建 | 页面永远只读缓存（loadSafe 兜底为空态）；重建只在调度任务 + 手工触发路由 |
| 8 | 「点开才可查看」只前端折叠 | salesAmount 已入 SENSITIVE_FIELDS；个人完成率在 todo/stats DTO 层按角色裁剪；红队测试补用例（RSC + CSV） |
| 9 | 群发 + 私聊双发 | `core/notify-audience.ts` 唯一谓词决定收件人集合；dedupeKey 含目标（role 或 userId），同一告警只投一条 |
| 10 | 快照差分绕过 stock-view | 快照仓「日快照差分」若做，先在 `core/stock-view.ts` 收口为 getSnapshotDelta；本波不做 |

---

## 6. 决议与参数对照（已默认化，业务可改，不再讨论）

| 决议 | 参数键（/admin/params） | 默认 |
|---|---|---|
| D54 库存占比目标带 | inventory_sales_ratio_target_low / high | 45 / 47（基线 50） |
| D56 爆单 | spike_consecutive_days / spike_rise_pct / spike_min_base_qty | 3 / 50 / 10 |
| D57 库存预警阈值 | default_production_lead_days / default_logistics_lead_days / alert_buffer_days | 30 / 15 / 5 |
| D58 四档分层 | grade_s_pct / grade_a_pct / grade_b_pct | 50 / 80 / 95 |
| D60 调拨 | transfer_cost_window_days / transfer_cost_deviation_pct / transfer_qty_deviation_x / transfer_batch_max_docs / warehouse_max_active | 180 / 20 / 3 / 4 / 12 |
| D64 账期 | payment_term_min_years / payment_term_target_min_days / payment_term_target_max_days | 2 / 45 / 60 |
| D65 数据质量 | dq_tolerance_pct | 1 |

---

## 7. 仍需业务 / 业主亲自决定（已去掉已默认化项）

**数据与资料（阻断实施，不是口径）**

1. 线下「成熟预警公式」Excel 原文（变量、窗口、系数、是否含在途）——IAL-01 映射前提。
2. 400+ 主 SKU / 1000+ 子项是否等同当前 751 在售成品 + BOM 物料；预警范围是否沿用 participatesInNormalSalesMovement。
3. 财务追认：销售金额口径（含税 / 扣退款 / 扣平台费）与粒度（公司 / 品牌 / 渠道）；库存计价基础（D51 sku_costs 优先待追认）；当前「约 50%」基线数字来源。
4. 各供应商合作起始日、2024/2025 年度分供应商采购额（Excel，含税口径说明）；各家现行账期与目标。
5. RPA 采集数据交付形态（每仓每日 Excel / API / 简道云中转）、覆盖哪些仓、是否附控制总量行。
6. 16 名渠道运营名单 → 渠道 / 店铺映射；channels 主档是否扩种子；店铺→渠道别名由谁维护。
7. 调拨费用来源（货代账单 / 财务 Excel / 简道云表单）与录入责任人；存量调拨单类型回填规则。
8. 海外仓：系统、仓库清单、库存粒度、币种、编码体系；「水盘」指代对象。
9. 聚水潭控制台授权与固定出口 IP、可信仓范围、是否申请奇门；用友应付/付款接口授权。
10. 部门目标指标清单、期间粒度、目标值与方向；调研结论四条最终措辞。
11. 供应商申报产能粒度（供应商 / 供应商×SKU / 工序）、维护人、更新频率。
12. 聚水潭 itemStatus 取值字典与 SCM lifecycle 四态映射；权威方。
13. 历史豁免清单：哪些来源、哪个日期之前正式不补。

**流程与权责（需业主/总监拍板）**

14. S 边界 50%（D58 待业务确认）；「20% 重点 SKU」是否含利润维度（两份原文口径不一致）。
15. 「共同评估」流程形态（SOP 签认拆分 / 建议页逐行确认 / 线下记录）；finance 是否退出签认。
16. 备货权切换时间表：3 个月过渡期后 BH 制单角色是否由 ops 改 pmc；过渡期内运营是否继续提 BH。
17. 紧急订单线下 SOP（发起人、加急审批人、SLA、是否跳过 BH）；预警是否允许一键建草稿。
18. 「工厂到仓」作调拨单还是 SH 收货附带物流段（重复计量风险）；保税仓调拨是否真实过账（L 项决议）。
19. 二期采购：是否覆盖成品直采 / PR 前置 / D29 商务字段；成本下降基线（D63 待采购追认）；「虚拟仓」精确指代。
20. 飞书：是否要求全员完成 union_id 绑定；应用可用范围是否覆盖相关人员。
21. 渠道运营是否可看外部平台观察件数（无金额视图）；受限用户是否允许导出。
22. 天猫/拼多多是否接受继续走 RPA→简道云镜像（当前已自动同步）还是必须直连开放平台（需注册开发者应用，与业主「不注册」偏好冲突）。
23. 试点首批范围与成功指标、评估周期（系统无正式缺货事实，只能代理）。
24. 数据质量：周核对签认角色；人工录入「首次正确率」代理口径是否接受。

---

## 8. 不做 / 推迟项及原因

**研究 synthesis.deferred（26 项，按模块归并）**

| 项 | 原因 / 触发条件 |
|---|---|
| 每日站会 Tier 1/2/3 页面 | 等运营报备路径、SLA 字段与固定站会真实存在 |
| 缺货率（时间口径）与售罄率 | 等内部日出库事实（JST 转正）或 sales_monthly 追到当月 |
| DDMRP 红黄绿缓冲 / 净流量优先级、多级拆解各自缓冲、VMI 持有量视图、DDMRP-lite 直接接补货 | 等渠道逐日落库、供应商级 MOQ、周期覆盖率 >90%、min/max 协议样例；observation_only 红线 |
| 参数回测（服务水平 vs 实际缺货） | 等缺货事实来源；先做一次性只读 SQL 诊断 |
| 星期归一 + 稳健 z + EWMA 并行爆单检测；促销日历与断货删失；流量先行预警 | 等 D56 规则第一份月度误报账；等 ≥20 条人工确认真爆单；促销日历先留参数位 |
| 实时仓两步调拨过账 + 线路周期 P50/P90；批次调拨 / 最小调拨量 / 装载率；物流费用三方审计；仓库合并候选分析 | 只加 shipped_at/arrived_at 再谈；等店铺↔仓库映射与快照仓 ADU；等承运商费率卡；平台云仓无可裁对象 |
| FVA 阶梯台账、lag-1 WAPE/Bias 考核；价值加权 ABC；分级版本化 + 覆盖审批（超出 sku_planning_policy 现有覆写）；长尾 C 类清理队列；按分级循环盘点 | 等 sales_monthly 追到当月、运营成为系统用户；unit_cost 覆盖 <95% 不讨论金额分级；月差异行 ≥30 再评估 |
| 供应商账期自动候选名单以外的 WAPT / DPO / 应付到期日历；记分卡四维扩展与账期资格挂钩；PO 三方匹配例外队列 | 等 PO 流跑满 ≥6 个月、用友应付契约授权、D7 上会改判；二期采购后在 supplier-price-variance 同域做 |
| 飞书交互卡片回调 / 任务 v2 镜像 / 应用机器人个人消息以外的能力 | 等公网地址稳定与 scope 授权；触碰 NOTIFICATION_SCOPE_ALLOWLIST 护栏 |
| 优先级矩阵 + 自动升级链；绩效评分 | 等 users.manager_user_id、SKU→责任人映射、每人 ≥30 条任务且 ≥3 个月 |
| 成本下降 KPI 以外的年初冻结基准表 | D63 已定基线 = 上一年度加权均价；冻结表等采购追认 |

**映射阶段 L 项（单独决议后再排）**

| 项 | 原因 |
|---|---|
| 保税仓调拨真实过账（realtime −/transit +、transit_writeoff 核销） | 触碰 posting 引擎与红字语义，最高风险边界；先出设计稿进 CURRENT.md |
| 飞书任务 Task API 双向同步 | 需 task/contact scope，触发最小权限护栏，且与「无需跨系统跳转」相悖 |
| performance_reviews 打分表 | D61：证据导出不打分；等 HR 口径 |
| supplier_reserve_positions 供应商前置备货参考层 | 等备货协议与维护人；参考数据不得减少记账真相 |
| 独立 PO 创建入口 / 成品直采枚举 / sh_lines.po_line_id / D29 商务字段 / `docs/spec/20` 二期规格 | 二期采购系统范围，需先定节点矩阵与决议 |
| 天猫 / 拼多多直连开放平台连接器；海外仓连接器；RPA API 连接器；「水盘」 | 需注册与授权 / 系统身份未知；现行镜像与快照模板先顶替 |
| inventory_valuation_daily 物化表 | 改用读模型 inventory-daily-position（可重建、缓存键升版）；量级实测后再议物化 |
| 预警一键生成紧急 BH 草稿 + urgent 联动 WO/JG；调拨建议「合并为一张草稿」；C 级 ops 直录路由与 BH 渠道过滤 | 三方同时改 outsource/bh.ts 与 stock-doc 写路径，与 D33 自动链边界交叉；W2 后单独一波 |
| system_alerts 扩五列（ownerRole/actionHref/dedupeKey/sourceRule/paramsSnapshot） | D66 允许 0048；本波先用 detail jsonb 承载，避免与 0047 同波撞迁移 |
| 「分渠道 × 分仓」交叉销售 | 需聚水潭出库单授权或平台发货仓字段 |

---

## 9. 需编排方登记的清单（各域 manifest 汇总口径）

**菜单 / 命令面板条目（经 `src/lib/route-access.ts`）**

| key | label | 组 | roles | scopedMode |
|---|---|---|---|---|
| /cockpit | 驾驶舱四屏 | 经营分析 | 全员 | channel_scoped（ops） |
| /inventory/alerts | 库存预警 | 库存 | pmc/purchasing/ops/warehouse/admin | public |
| /inventory/warehouses | 各仓库存与周转 | 库存 | 全员 | public |
| /inventory/transfer-routes | 调拨线路与费用 | 库存 | warehouse/pmc/finance/admin | denied（ops） |
| /todo | 待办任务 | 顶层（/inbox 旁） | 全员 | public |
| /goals | 供应链目标 | 工作台旁 | 全员 | channel_scoped（dept） |
| /replenish/reconcile | 运营提报核对 | 计划与补货 | pmc/ops/admin | channel_scoped |
| /report/purchase-orders | 采购订单指标 | 采购 | purchasing/pmc/finance/admin | denied |
| /report/supplier-payment-term | 供应商账期 | 供应商 | purchasing/finance/admin | denied |
| /import/data-quality | 数据质量 | 数据中心 | finance/pmc/warehouse/admin | denied |
| /master/supply-params | 周期主数据补录 | 主数据 | pmc/purchasing/admin | denied |

**指标 id（新增，命名沿用 metrics.ts camelCase）**：onHandMonthEnd、inOutDaily、inventoryValue、inventoryValueCoverage、salesAmountMonthly、inventorySalesRatio、salesSpikeCount、inventoryCoverAlertCount、orderedPoCount、orderedQty、orderedAmount、orderToDeliveryDays、costSavingYtd、warehouseTurns、transferLaneAvgFee、transferDocCount、transferAnomalyCount、workItemCompletionRate、workItemOverdueCount、goalAttainment、dataAccuracyRate、dataSourceFreshness、paymentTermAttainment、creditTermSpendShare、pilotSkuShare（详见线框 §6）。

**告警类别（alerts-client CAT + system-alert-notify CATEGORY_LABEL）**：sales_spike 爆单预警、inventory_cover 断货预警、transfer_cost 调拨成本异常、snapshot_quality 快照跳变、data_quality 数据质量未达标。

**读模型缓存键（首版 /v1）**：inventory-daily-position、platform-sales-amount-monthly、demand-daily、transfer-routes、warehouse-inventory、purchase-order-metrics、supplier-payment-term、replenish-pilot、jst-item-status-gap、data-quality、data-coverage；升版：jiandaoyun-external-velocity /v3→/v4、jiandaoyun-channel-observation /v3→/v4（各一次）。

**待追加参数键**：cover_target_days_s、po_pending_aging_days、po_approved_aging_days、dq_accuracy_target_pct、dq_accuracy_stretch_pct、dq_review_cadence、fg_reorder_lead_target_days_low/high。

**台账**：API_ROUTE / AUTH_PAGE 按上表逐条 +1；MIGRATION +1（0047）；ARCH_GATE：route-access-registry、channel-scope-guard、data-source-class-registry、director-program schema 测试；能力 C203+ 按域一条（驾驶舱四屏 / 库存预警表与爆单 / 调拨类型与费用 / S-A-B-C 与权责 / 待办与目标 / 渠道范围 / 采购订单指标 / 账期 / 数据质量核对 / 周期主数据补录）。

## W2 合并后待办（审阅非阻断项，2026-09-04 登记）——已全部处理（2026-09-04 凌晨第二轮）

- 采购指标：按月桶只取当年月份；「全收」按 OTIF 足量容差；purchase-order-metrics / supplier-payment-term 绑定含 sys_params；PO 行去税/补税收口到 rules/price.normalizeLineNetGross；`type AnyDb = any` 改用 core/svc（台账退役 2 条 LINT_EXCEPTION，重编号至 A642）。
- 数据质量：销量一致性只比较两侧都完整的月份（外部起始月不完整不比；内部缺月跳过不记不一致）→ `sales-consistency/v2`、`data-quality/v2`；核对节奏「连续 4 周达标→月核对」粘滞与退回规则 + 端到端测试；快照放行预检读 dq_snapshot_* 参数；准确率来源文案纠正为「自有实时仓出库 vs 聚水潭日销」，外部平台仅天猫；/api/dq/reviews 经 maskSensitive。
- 分层与权责：replenish-pilot 绑定含 sku_params 与 grade_/default_/detector_ 参数；分层固化输出「直出为 0」阻塞分布（leadDaysUnknown / xyzNull / xyzNotX / detectorHit）并在试点页显示、直达周期补录；受限运营提报/计划事件 channelId 必填且限范围内；semi 半成品周期口径前后端统一（同成品：加工+在途）；补建议页 tier/ownership/hideTierC 与 detectorHit 用例；getSegmentation 生产实测 46–75 ms，性能项关闭。
- 待办与目标：goals auto 实际值改为按真实读模型键与 payload 路径取值（inventory-sales-ratio/v1 rows[yearMonth].ratioMonthEndPct、warehouse-inventory/v1 summary.turns/dio、supplier-payment-term/v1 summary.attainmentRate（0–1 ×100）、purchase-order-metrics/v1 summary.otif.rate）；todo 可见性谓词统一（list/stats/第 4 屏/[id] 路由）。
- 调拨：statusReason/feeReason/qtyReason 改为无数值文案（数值只走结构化字段，价格可见角色前端拼接）→ `transfer-routes/v2`；transfer-fees POST warning 对非价格角色同样无数值。
- 保税订单：探针实核源表为一次性归档（507 行，统计日期全 2026-03-01）；契约改全量观察，507 行入库、496 行映射系统 SKU；不能当作当前出库，是否续推待业务确认。

仍待业务/业主：周期主数据补录（直出 0 的根因）、RPA 日快照交付、聚水潭/用友授权、四屏线框签认、D50–D66 默认值确认、保税订单表是否续推、货币口径追认（销售金额含税/退款）、待办绩效口径。
