# 加工单选择、物料读取与结算驳回验收

日期：2026-09-13（上海）。本报告记录工程候选，不证明正式3100已部署或真实业务UAT完成。

## 精确交付

- 应用候选：`edb782fcade5f65b62317d7c2c68e78be94a56da`；分支 `agent/scm-material-source-main-20260913`，工作树 `worktrees/material-source-20260913`。
- 基于已合并[PR49](https://github.com/yujin7/Expressions-SCM/pull/49)的主线 `9a54ec18284f7111a09d1f8dbef7ae403a822374`。PR49包含JS金额可见性批准保护，但不包含随后发现的前端驳回按钮修复；后者在本候选。
- `c0220ed4`：三处JG选择器与FL/TL物料读取；`edb782fc`：JS驳回按钮独立资格与回归守卫。随后文档提交不代表重新验证了应用。
- 原分支、工作树、失败记录及共享依赖均保留；未安装依赖、修改迁移、重置密码或改正式数据。

## 改动与保护

| 范围 | 修复 | 保持不变 |
|---|---|---|
| PC加工费申请 | 用既有RemoteSelect服务端搜索、50条分页、准确已选项回读；加载失败可见并可重试，不再只取前999条 | PC现有写时资格及结算冻结保护，不新增状态授权 |
| FL发料、TL退料 | JG选择复用服务端`receiptEligible=1`，先筛approved/in_progress再分页，不再前999条后本地筛选 | 现行业务状态及库存过账入口 |
| 物料读取 | 共享`useJgMaterialLines`：切换立即清空旧行、取消旧请求、旧成功/失败不得污染新来源；失败可重试 | 沿用工单物料依据；TL数量默认0及退料原因不变 |
| 创建保护 | 读取中/失败时禁止创建；同步重复点击只发一次创建请求；保存中不可切换来源或关闭 | 草稿不等于已审批或库存过账 |
| JS审批与驳回 | 后端当前角色须可见金额才可批准；不能用调用方伪造财务角色绕过。已配置合格审批人仍可驳回；前端驳回按钮独立读取`actions.reject` | 金额仍由唯一DTO脱敏，审批配置、禁止自审、版本、事务审计保持 |

## 最终全量门

`edb782fc`起止干净同SHA，2026-09-12 17:12:13Z→17:16:34Z：**READY 9/9**；709测试文件、6560项通过；HTTP 34/34，零跳过。覆盖运维契约、PostgreSQL迁移契约、全量lint、全部类型范围、全量测试、正式构建及实际HTTP冒烟。

本工作树证据：

- `.artifacts/verification/2026-09-12T17-16-34-485Z.json`
- `.artifacts/material-source-20260913/release-edb782fc.log`
- `.artifacts/runtime-edb782fc/`：精确冻结运行包，隔离端口3451，不是正式3100。
- `tests/components/jg-material-lines.test.ts`：7项，覆盖旧JG/WO成功与失败迟到、取消及重试。
- `tests/outsource/receipt-source-options.test.ts`：6项，含1005条较新草稿、先筛选再分页、搜索/已选项/第21页可达。
- `tests/components/js-rejection-qualification.test.ts`：独立驳回条件守卫，并注入错误条件验证守卫会拒绝；角色及真实服务行为另由操作资格、费用范围测试覆盖。

## 实际浏览器与数据库核对

全部使用隔离合成库`scm_contract_quickwins_20260911`，不是生产数据或正式用户登录证据。

1. 开发候选`900017ea`：PC真实页面请求失败后显示重试，恢复后可加载50→100条，并搜索找到远端JG；不是只检查组件代码。
2. 同候选：FL工单读取失败显示错误、禁止创建，重试恢复。故意延迟A工单响应并忽略取消，再切换B，迟到的A未覆盖B物料。
3. 同候选：实际同步点击创建两次，仅一次POST、一个FL草稿及一行物料；未批准、未过账。数据库凭据或密码不写报告。
4. 最终`edb782fc`：仓管经真实表单登录；隔离JS审批配置临时设为仓管。JS详情金额键仍缺省，`approve=false/reject=true`；强行批准返回403。实际页面点击驳回，JS12从pending版本2变draft版本3，仅一条驳回记录、金额保持0、无损耗核销流水。配置已恢复finance并复核。该零金额夹具仅证明角色/流转，金额计算另有服务测试。
5. 最终候选390×844：TL选择B后展示正确包材、数量0；页面宽390、弹窗宽374，按钮与表单未横向溢出。仅覆盖本次代表性页面，不能称全站布局已验。

最终截图：`.artifacts/material-source-20260913/js-reject-only-final.png`、`tl-mobile-final.png`；数据库核对：`browser-db.log`。开发过程截图、夹具及日志在同目录`development/`，明确属于前序候选，不能当作最终版本重复执行的证明。

收尾：核对3451进程PID/cwd/端口和PG容器owner后停止自有浏览器、候选进程及隔离容器；保留冻结包、数据库和全部证据，未动正式3100。文档更新后知识治理与台账测试2文件738项通过。

**测试不足也留档：** 前序`900017ea`已READY，但实际浏览器发现驳回按钮仍嵌在批准资格条件中；本轮拆开条件、加入回归并在最终版本真实驳回成功。全量绿不代替完整交互验收。

## 未完成及下一步

1. **物料数量/退料/扣款后的完整结算恢复**：当前`assertCurrentFee`检查收货数量及费用，不完整比较保存的物料扣款依据；`refreshJsFee`有意只更新费用。TL创建资格与JS关闭后恢复说明也有生命周期缺口。需设计明确差异预览、人工草稿更新、待审驳回及共享锁；不自动重算已冻结JS，不用简单禁止实物退料掩盖问题。
2. **JS关联链可见性不一致（已实际复现）**：合格仓管可读JS详情并驳回，但`/api/outsource/chain?docType=js&id=12`返回404。链读取沿用菜单可见性，详情/审批资格不同；需对齐读取授权与提示，不能仅隐藏错误或扩大金额权限。
3. **PC同类金额批准资格（后续已续修）**：本报告时仅静态发现；后续真实服务复现并由`7ba1adab`修复，详见[改价金额批准权限验收](改价金额批准权限验收-2026-09-13.md)。本报告原候选不含该修复，不将后续证据倒填为前轮完成。
4. 已冻结财务差额、真实来源/角色全链UAT仍未完成。当前候选没有发布到正式3100；仍需获授权的异地DB+附件备份、发布窗口，以及正式Safari HTTP/HTTPS验证。外部平台、D69/D70/D71状态未被本轮测试改变。

当前入口、部署状态及下一优先级只维护在[NOW](../NOW.md)，业务边界只维护在[CURRENT](../spec/CURRENT.md)，本报告不另建平行状态权威。
