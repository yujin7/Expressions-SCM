# 外部窗口跨模块与帮助边界 · 09-08

## 准确反例

e68abb0冻结生产包、3350、隔离PostgreSQL62迁移，六类合成SKU（完整、七天、零、负、无记录、非法日期），没有真实平台凭据或业务数据。

1. 完整门611文件/5450项通过，但补货建议与工作台HTTP500。日志为invalid decimal: null；`replenish/service.ts`把合法未知net30经String转为"null"再dDiv。真实Tmall生产者→补货消费者反例失败，不能用只覆盖排名/库存预警的测试声称全部消费者已支持nullable数量。
2. 1920/1280的分窗/帮助检查通过，390px长说明y=-4。已安装rc-trigger的useAlign在移入可见区后重新叠加popupOffsetY，rc-dropdown默认±4，因此只开shiftY不保证面板完整可见。对照[上游实现](https://github.com/react-component/trigger/blob/master/src/hooks/useAlign.ts)，修法以已安装代码和实际浏览器为准，不升级依赖。

失败原件：外层`交付证据/2026-09-08-外部需求分窗/acceptance-e68abb0/browser.json`、`failure.png`、`full-release.log`；准确完整门`.artifacts/verification/2026-09-08T00-25-14-291Z.json`为NOT READY，保留不改写。

## 修复契约

- 仅非null且通过既有PDD覆盖门的net30进入参考日均除法；null保留为null并解释30日窗口覆盖不足。合法零/负数不当未知。
- 不改变R11内部日均、建议数量或审批边界；新增实际staging→外部读模型→补货服务的四种反例，正外部日销也不得凭空产生建议量。
- ContextHelp沿用AntD浮层与双轴移位，offset明确[0,0]；浮层padding8让文字、边框和焦点有边缘空间。不新增定位引擎、不删减说明、不隐藏溢出。
- CaliberNote、DataSourceBadge、库存帮助等共享消费者继承修复；仍需实际跨消费者浏览器检查，不以引用关系声称全站已验。

## 验证状态

- 旧代码：窗口用例invalid decimal: null，定位配置反例undefined≠[0,0]，两项失败。
- 修后：定向6文件31项通过；三套类型、全量lint零警告；padding追加后定位3项再次通过。
- 准确451df6b完整门READY9/9（08:49上海），611文件5455项、类型/lint/生产构建、HTTP33/33；回执`.artifacts/verification/2026-09-08T00-49-10-443Z.json`。
- 冻结包3351沿用受控合成PG62迁移，实际6类窗口及补货/工作台200；1920/1280/390/320页面无横溢出，窗口帮助可见且Enter/Tab/Escape焦点正确。390/320另验CaliberNote、DataSourceBadge，截图已人工查看。
- 实际CSV44列6行与API一致，未知空白、零和负量保留；首脚本split(',')错误拆解带引号逗号列名，失败保留，续验改为有引号/换行解析且不放宽44列。续验在GET退出页等待自动退出又失败：应用按安全契约要求显式按钮确认，手动确认后完成仓管单独验证，未修改退出实现。
- 仓管真实合成账号读取6行、不能重算且服务端403，320px无页面溢出、无浏览器异常。回执分别为外层acceptance-451df6b/browser.json（失败前已通过范围）、followthrough.json（CSV/跨消费者通过，随后退出等待失败）、warehouse.json（通过），失败原件不改绿。

未部署，未修改实际3100、真实数据、原173路径、外部授权或D69–D71；本修补不代替G02–G05或101条需求完成。
