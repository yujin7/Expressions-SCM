# 采购承诺全量导出与行定位验收

日期：2026-09-11。范围是仓管/采购从承诺例外预览交接证据并核对原采购行，不代表整套SCM、正式部署或三方UAT已完成。

## 准确版本

- 应用：`5c92d4a12f07f47643d59b57fb39f43b4983f3af`，分支 `agent/scm-workbench-evidence-20260907`。
- 隔离冻结端口3428，独立合成库 `purchase_line_20260911`，66/66迁移，无漂移；没有本轮schema变更。
- `check:release` READY9/9，684测试文件/6187项，三套类型、全量lint、生产构建、同版HTTP34/34零跳过；开始/结束均同一干净SHA。回执 `.artifacts/verification/2026-09-11T00-38-04-691Z.json`。
- 正式3100镜像、容器、启动时间在验收前后相同；未写正式数据库。候选检查通过不等于正式放行。

## 改动与边界

1. 页面保留30条轻量预览，但显示预览/完整例外数。两种承诺口径分别计数；表头排序明确只作用于预览，不冒充全量排序。
2. 新导出种类与同步入口复用同一承诺加载器。5000以内直接CSV，超过5000交现有后台任务；worker的50000上限继续显式说明截断，数量控制与列级权限继续沿原入口。
3. 文件含采购单ID、采购行ID、系统内链接、源总数、文件例外数、观察窗及三方资格。按执行时最新事实重算，不声称固定页面快照或跨查询事务快照。
4. 同PO同SKU多行不合并；PO详情按真实行ID标记，错单行号与格式错误明确说明，不猜相同SKU。关闭或换单去掉原行选择；保存列表视图不保存临时目标。
5. 当前报表GET复用useDocumentRead：换查询/刷新撤旧、取消迟到回执、15秒超时、持久错误与人工重试；失败时无旧导出入口。未新增写入自动重试。

## 反例与自动化证据

- 修前3个反例红：CSV两条同SKU缺行ID/回链；空行缺身份列；完整计数和大于200条的读取缺失。修后7文件813项定向检查（717项为台账结构门）通过。
- 独立PGlite插入5001个不同采购行：预览30，生产器全量5001，cap17保留相同源总数；HTTP202创建一任务一审计，真实worker落盘5001行文件。另验CSV公式前缀中和、不输出银行字段、空窗口说明行、非法/重复参数与匿名拒绝。
- 首次新测试夹具漏汇总字段、SessionUser字段与字面量类型不完整，修正后通过；不计作产品缺陷。
- 首个候选 `d500cc3a` 全门也绿，但真实点击未跳转。DOM显示inline链接有多个文字矩形，中心在两行之间。新增布局反例先红，改用与DocumentTargetLink相同的inline-block连续命中区域后在本版重验通过，不能抹掉前次失败。

## 实际Chrome → API → 文件/原单

仓管 `warehouse01`，仅合成账号/数据；会话已实际退出并关闭。

- 初始2条异常导出保留PO19/行20的原始/当前两条口径、10kg实收、0短缺及三个awaiting_uat边界。
- 390px普通点击原单链接实际导航至 `/outsource/po?docId=19&poLineId=20`；加载后仅行20标为“已定位”，行19不标记，数量价格脱敏不变。
- 改为PO19/行22（实际属于另一PO）：显示“不在本单内”，无已定位行；关闭详情后URL撤掉docId与poLineId。
- 先成功加载，再拦截当前报表GET为网络失败：旧数据和导出入口撤回，有重试；解除拦截后实际点击重试恢复。
- 全门结束后向独立库增加专用PO21及61条行23–83（总行数22→83），无库存或流水写入。它是读取/导出夹具，不是采购审批流程证明。
- 实际刷新与下载：预览30/63，浏览器下载CSV为63条（新夹具61+原有2），包含最后的行83及原有行20。没有将30条预览误当全量。
- 390px和320px文档宽分别390/320；320表内容宽1480、容器250，横滚留在表内。390截图已目视检查原单行标记与错误态；不宣称全部页面或所有缩放场景通过。
- 浏览器错误收集为空、无框架错误覆盖层。退出确认页接口仍200；实际点击退出按钮后401，随后关闭会话。

证据目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

- `full-release-5c92d4a1.log` / `owner-5c92d4a1.json` / `formal-observation-5c92d4a1.json`
- `promise-full-5c92d4a1.csv` / `fixture-promise-export-5c92d4a1.json` / `seed-promise-export-5c92d4a1.mjs`
- `po-line-5c92d4a1-mobile.png` / `promise-error-5c92d4a1.png`
- 前次失败：`promise-export-d500cc3a-visible.png`；首个全页截图在视窗变化中记录10719高，稳定后实际DOM3857高，不把它误判成永久空白。

## 取舍与后续

遵循项目工作流、数据集成、写路径、并行安全和发布技能；React/Next.js与浏览器验证规范用于读取/导航/实际交互。沿用[Ant Design 5表格](https://5x.ant.design/components/table/)的rowKey、onRow及内部滚动，不为本修复升级UI框架；官网默认已指向6.x，因此核对5.x与本地安装类型后实施。

仍未完成：全量例外的交互分页/全局排序；固定快照导出（目前明确为执行时最新事实）；历史歧义认领与采购行真实PG并发；本地Safari登录修复正式发布、异地备份/恢复前置条件、101条真实业务验收；简道云/聚水潭/用友各自资格、D69–D71。应继续按准确需求和事实推进，不以本切片改写总目标。
