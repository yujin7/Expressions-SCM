# 采购行并发收退货验收 · 2026-09-11

## 结论与边界

候选 `9921b304547ba9f8eb6c20ab2fded28f28697535`（开始时工作区干净；应用代码同 `fa415f75`）在独立 PostgreSQL 合成库验证通过。第二轮记录 **52 个检查点**，其中 **7 组真实锁等待**；52 不是 52 个独立业务场景，也不是新增单元测试数。

本轮没有改应用代码、真实数据、正式镜像或迁移。它补齐此前“采购行真实 PostgreSQL 并发待验”的证据，不等于正式发布、全量历史数据修复、浏览器工作流或业务 UAT 完成。本轮不重复运行应用完整门，也不将此前的 6215 项测试改称本轮结果。

## 环境与方法

- 时间：北京时间 2026-09-11 09:30:56–09:30:57（第二轮脚本执行区间）。
- PostgreSQL 容器：`exp-scm-capacity-20260909`，完整 ID `eb10a79197a28a3a6822f2b00d0373098757061ea1b250cfd1e71d5ab1537b56`，运行中。
- 新建隔离库 `purchase_race_20260911`：从原合成库 `purchase_line_20260911` 导出恢复，66 条迁移；不复用正式库，也不改冻结 3429 使用的原合成库。
- 新建专用供应商、SKU、实时仓、同 SKU 双采购行，采购前置状态直接构造为执行中。**PO 建单/审批本身不在本轮证明范围**。
- SH 创建、提交、审批、QC 和入库均调用真实业务服务；CT 创建、提交和审批也调用真实业务服务。制单人与审批人是两个不同的有效仓管账号，审批资格从当前数据库读取；没有直接把 SH/CT 状态改成已批或已完成。
- 控制连接锁住目标 PO/SH/CT 行，两个业务连接同时进入。查询 `pg_stat_activity` 与 `pg_blocking_pids`，确认两个不同 PID 真实等待目标 `FOR UPDATE` 后才放锁；不是靠固定睡眠声称并发，也不在控制事务内复用活动快照。
- 每次核对按采购行排序的已收数、PO 状态/版本、库存流水合计/条数与库存余额；失败场景还核对业务单状态/版本、审批数、审计数。
- 临时审计故障触发器只在新合成库、精确单号 ID 与最终审计动作上生效；执行结束已核对临时触发器与函数均为 0。原库存/审计不可变事实触发器保持启用，未被关闭或绕过。

## 已证明的业务结果

| 场景 | 实际结果 |
| --- | --- |
| 相同 SKU 两采购行同时收货，目标基础数量 0.1 / 0.3，第二行全部让步接收 | 分别累计 0.1000 / 0.3000；流水与余额 0.4000；PO 只完成一次、完成审计一条 |
| 两张 SH 同时收第二采购行，数量 0.1 与 0.2 | 第一行仍 0.0000，第二行 0.3000；未误判整单已收齐 |
| 同 SKU 第一行收货 + 第二行退货同时执行 | 从 0.2 / 0.3 变为 0.3 / 0.1；库存净额 0.4000，无串行 |
| 第二行已收 0.2，两张 CT 各退 0.15；第一行另有 0.4，仓库总量足够 | 一笔成功、一笔 409；第一行仍 0.4，第二行余 0.05；失败 CT 保持 pending/v2，无审批/最终审计新增，不借其他采购行数量 |
| 同一 SH 同时确认入库 | 一笔成功、一笔 409；只有一条收货流水 |
| 同一 CT 同时审批及之后重试 | 两次返回成功，其中一次标记幂等；最终一条审批、一次退货库存影响，没有重复审计 |
| 同一第二采购行同时收 0.1、退 0.1 | 原 0.2 最终仍 0.2，第一行不变；流水与余额相等 |
| SH 已完成过账和 PO 累计后，最后一条 SH 入库审计故意失败 | 整个事务回滚：两行已收 0、PO 执行中原版本、库存与流水 0、SH 保持已批原版本、无 PO 完成审计；人工重试正常且只过账一次 |
| CT 在过账及 PO 扣减后，最后一条完成审计故意失败 | 审批、库存、采购行扣减和 CT 状态一起回滚；去掉故障后人工重试正常；已完成 PO 不自动重开 |
| 同 SKU 多行未传来源行 / 传其他 PO 行 | 分别 409 / 400，未生成 SH 草稿 |
| 历史 SH 缺采购行且同 PO/SKU 多行歧义 | 实际 PostgreSQL 入库拒绝 409，数量/库存/状态/审计均不变；不猜第一行 |

历史歧义场景仅在本轮专用、尚未入库的合成 SH 行上将 `po_line_id` 置空，以模拟迁移前事实；没有编辑真实数据或任何不可变流水/审计。

## 可复核证据

证据根目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

- 执行脚本：`verify-purchase-line-race-20260911.mts`，只允许本机专用库、核对容器完整 ID、迁移数量和干净候选；每次使用新标签，不覆盖已有回执。
- 第二轮回执：`purchase-line-race-9921b304-r2.json`，`PASS`，52/52 检查点；记录每组锁等待 PID、SQL、阻塞 PID、夹具 ID、数量与文档状态。
- 脚本 SHA-256：`45f29ee7bb2599687cc6fd29a83ef86fa3b5a5ee17002338687bb23f17c5ec3f`。
- 第一轮回执 `purchase-line-race-9921b304-r1.json` 保留。第一轮通过基础场景，之后追加同单并发、同采购行混合收退与历史歧义验证；不把第一轮与第二轮检查数叠加。
- 第二轮专用 PO 27–33、采购行 94–107，SH 65–75；这些 ID 只属于隔离库，不能拿去正式系统找同号或操作。

## 仍未解决 / 下一步

1. 正式服务仍为旧镜像 `sha256:9ec914e49423cd4c61b546e70fccaeb6baa081af8df0ef0bf99cb9f7a338bbf8`，启动时间 `2026-09-10T19:53:56.547232837Z`；本轮结束复查未变，健康接口为 61/61、无漂移，不是本候选 66 迁移。
2. 本地 Safari HTTP 登录修复尚未发布；上一轮实际 HTTPS 表单登录成功后出现初始密码修改要求。不能用本轮库存并发成功掩盖用户仍遇到的入口问题。正式发布前的数据库与附件异地备份位置仍待确认，不绕过该前置。
3. 历史歧义的可审计人工归属纠正仍需设计/实现与真实数据核对；本轮证明“错误输入被拒绝”，没有证明历史修复工作流已完成。
4. 收货、退货的真实浏览器操作验收与正式上线后回归、生产权限/配置和效期批次维度覆盖仍须独立证明；本轮使用无效期的原料 SKU、null 批次，未验证效期 SKU 或批次分配并发。
5. 简道云/聚水潭/用友事实资格、101 条需求业务验收、D69–D71、告警真实关闭与负责人认领继续跟踪；本轮没有外部平台写入，也没有改变其资格。

本轮 `write-path` 技能把验证收口到同事务、精确行身份、并发与人工重试；`parallel-sessions` 技能要求复制合成库、保留旧回执和正式运行状态。
