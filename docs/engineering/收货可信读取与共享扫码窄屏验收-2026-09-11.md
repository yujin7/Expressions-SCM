# 收货可信读取与共享扫码窄屏验收

## 范围与结论

仓管选可收的 PO/JG → 读取当前物料 → 录入实收 → 创建草稿 → 核对详情；失败时撤旧并提供只读重试，不自动重复建单。共享扫码组件另在盘点页验证首次计数与后续累加。本批只改变候选代码与隔离合成数据，未部署正式 3100、未改正式密码/业务库存、未变更来源分摊或审批规则。

主责 `design-supply-chain-flows`，准确候选由 `release-sweep` 验证，共享状态遵守 `parallel-sessions`。源码候选 **0cbc70ec7306c209eb565d0f6ec6e44acef5ebe2**，分支 `agent/scm-workbench-evidence-20260907`；冻结运行 3418，合成 PostgreSQL `qc_boundary_20260911`，65 迁移零漂移，后台任务关闭。

## 已交付的工程变化

- SH 列表和来源详情按当前查询/身份绑定：切换撤旧、取消迟到、15 秒超时、失败持久提示和显式 GET 重试；错误不伪装成 0 条。
- PO/JG 候选先筛已审批/执行中再分页，远程搜索每页 50 条，支持已选 ID 精确补名；非法参数 400。没有扩大读权限或把前 500/999 条当全集。
- 来源 ID/状态/数量结构异常不能创建；等待创建时锁表单与关闭。PO 同 SKU 多行用 PO 行 ID 分别编辑；扫码多行命中仍拒绝歧义。
- 收货行宽表内滚保留单位、数量、批号、日期；提交/审批拒绝按单据 ID 持久显示，重读不自动重复写入。
- ScannerEntry 去掉固定宽输入所在的 Space 包裹，改为可收缩输入、数量标签与数量控件成组。收货和盘点共用，不改变扫码数量算法。

## 准确证据

外部证据目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

| 证据 | 本次实际证明 |
| --- | --- |
| `.artifacts/verification/2026-09-10T21-21-14-150Z.json` | 0cbc70ec 起止同一干净 SHA，完整门 READY 9/9；673 文件 / 6,092 项，三套类型、全量 lint、生产构建、同版 HTTP 34/34 零跳过 |
| `browser-sh-read-0cbc70ec-verified.json` | 真实 Chrome 仓管 23 项：列表失败撤旧/重试，PO13 迟到不得覆盖 PO17 失败，重试恢复当前物料，三宽度弹窗/宽表/扫码控件，实际创建 SH52，草稿详情、单创建审计、零过账、PO 已收不变、实际退出后 401 |
| `browser-count-scanner-0cbc70ec-r2.json` | 真实 Chrome 仓管 10 项：通过原服务生成合成 PD1，1280/390/320 扫码控件不越界，扫码不自动保存，两次 0.1 保存成 0.2，仍为草稿、退出后读取 401；无未捕获浏览器错误 |
| 独立 SQL 复核 PD1 | draft/version2；create 和 update_counts 审计各 1；bookQty=0.1000，countedQty=0.2000，adjustDocId 为空；仓18/SKU2 余额仍为 0.1000 |
| `formal-observation-0cbc70ec.json` | 发布验证前后正式容器、镜像和启动时点相同；runner 不迁移/重建正式系统 |

截图已目视：`sh-create-{1280,320}-0cbc70ec-verified.png`、`count-scanner-{1280,320}-0cbc70ec.png`、补滚动后的 `count-scanner-320-visible-0cbc70ec.png`。没有把仅测框宽当作美学通过，也没有宣称宽表无需横滚。

## 失败记录与界限

1. 5c3c9fd9 完整门 6,089 通过/1 失败：旧日期检查写死变量名 `params`。8e2ba0f3 改为变量一致性加实际请求日期/清除行为，完整门 672 文件 / 6,091 项通过，回执 `2026-09-10T21-03-07-832Z.json`。不是降低业务要求。
2. 初轮浏览器先后因按钮图标/空格、弹窗切换尚未完成、warehouse 返回字段形状、创建后“提 交”字符串匹配中断，各失败回执保留。SH50 已成功创建，尾部只读续验，没有为修脚本再创建它；确认一草稿/一审计/零库存流水，PO17 仍 0，退出 401。
3. SH50 尾部第一份 SQL 写错表名 `sh`，实际为 `sh_docs`；改正后继续，失败留档。旧浏览器累计有 React185（堆栈含输入按键），因此 `browser-sh-read-tail-5c3c9fd9-r3.json` 为 PARTIAL，不声称无错误。新会话 8e2/0cbc 逐检查点无该错误，但旧触发条件尚未定因。
4. 8e2 浏览器首次等待 workbench，但应用正确返回原 SH 页面；改为核对仓管会话，续验20项通过。ScannerEntry 的320px截图却显示输入越界：旧测量只覆盖弹窗/表宽。新增布局回归先因测试 JSX 全局未初始化失败，补测试环境后才真实因固定宽断言变红；修共享布局后通过，并新增真实子控件矩形检查。
5. 盘点浏览器首次用 label 定位登录框失败；改用实际占位符后通过。三宽度首张盘点截图中扫码控件在折叠区之外，另滚动到实际控件补图，不用该首图冒充完整可见证明。

## 下一步（未完成，不被本证据覆盖）

- **盘点摘要与明细的窄屏布局**：320px 下摘要行偏高、表头/名称竖向挤压；需要固定合理列宽、内部横滚和紧凑数值格式，保留类别/单位/批次。扫码控件已修不代表这两张表已修。
- **盘点读取与保存/提交边界**：列表仍是直接 fetch 加 toast；需要核查迟到、失败撤旧、保存期间编辑/关闭、当前角色/版本与审计事务。不要把本次一次正常保存当作这些边界通过。
- **SH 未完边界**：旧 React185、间歇列表点开问题继续定因；服务器同 SKU 多 PO 行仍归首行，UI 行身份修复不等于完整行级追溯；未知结果下创建恢复需另核查。
- 正式 Safari 本地 HTTP 登录仍受 Secure Cookie 协议不匹配影响；本次仅再次验证 HTTPS 可登录，没有部署修复。正式发布前的异地备份配置/恢复验证仍待，不绕过发布前置。
- 101 条总监需求逐项真实 UAT、简道云/聚水潭/用友当前授权及数据消费资格、D69–D71、真实行动与学习闭环继续，不能据本批工程门判定完成。

本轮结束已关闭验收浏览器，停止已核对 PID/cwd/端口归属的旧冻结3416、3417，仅保留3418供后续；证据包和合成数据保留。未删除用户资料或真实业务数据。
