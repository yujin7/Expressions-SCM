# G04 产能依据承接与历史范围 · 工程验收

核验截止：2026-09-09 08:49，Asia/Shanghai。应用候选 **d346ea7f3279121c5cbf8ef61c7268649fe9e150**，分支 `agent/scm-workbench-evidence-20260907`。本文件是工程证据，不是当前状态的第三份权威；后续以 NOW/CURRENT 为准。

## 已连接的工作流

库存预警／已映射爆单 → 人工选择工厂、交期和数量 → 核对申报与本系统JG负荷 → 明确选择该来源的现有待办和当前负责人 → 保存核对时点依据 → 回到对应待办阅读全文、来源与追加实际跟进。

- 外部观察销量不带入人工情景，不自动开单、锁产能、改派、重开、关闭告警或改变数量。
- 沿用已有 work_items 与 append-only audit_logs；没有新增表、迁移或第二套任务系统。
- 所见情景保存前由服务器重算，源事实、申报、负荷或归属变化须重新核对。明确确认的任务必须仍未结、仍属于该来源、负责人有效并具备来源读取资格。
- 待办行锁串行化请求标识查询及审计追加；重复点击／并发同请求只写一次。回包不确定不自动重试，显式确认重用原标识及原内容。
- 历史快照保留当时申报、单位、有效期、依据、历史窗口、计划负荷、人工数量、来源及负责人。前端只接收受控历史投影，不下发任意审计载荷。

## 红队发现与修复

b34b742 已有保存与当前来源授权，但发布门前复查发现：若源告警后来从甲乙两渠道缩成甲，单独核对当前来源会让仅有甲权限的账号看到更广的旧依据。店铺别名后续重归属也有同类问题。

d346ea7 冻结保存时的渠道ID集合；历史正文须同时满足待办、当前来源及历史范围。无法归属的旧快照对受限账号保持不可读，不能用后来的映射追认。当前权限允许的正常新快照仍可读。

- PGlite新增两个实际行为反例：源缩窄／映射改变、未知归属后来补齐。
- 临时关闭历史范围检查，两个反例都失败；恢复后通过。此为有意反例验证，不是未修复失败。
- 三个真实登录上下文＋隔离PostgreSQL补验：当前核对GET 200，旧历史正文受限；两渠道账号可读原event40；后续改归属仍不放行旧依据。

## 准确版本验证

| 项目 | 结果 | 边界 |
|---|---|---|
| 定向开发检查 | 8文件 / 790项、三套类型、改动lint通过 | 不替代完整门 |
| 完整发布门 | READY 9/9；638文件 / 5,801项；lint、类型、构建、PG迁移契约、HTTP33/33通过 | 起止同一干净d346ea7 |
| 冻结生产包 | 3380 health返回准确SHA、64/64、drift=false | 隔离63540合成库，jobs=0 |
| 真实浏览器保存 | event37正常保存；event38已提交丢回包后原请求确认；event39六路同ID只新增一次 | 不是供应商真实承诺 |
| 状态控制 | 任务、告警、通知、库存与JG未因保存而变化；历史全文可读 | 只追加审计依据 |
| 角色拒绝 | 新仓管登录POST403、历史404 | 使用真实会话，不只mock角色 |
| 响应式 | 320/1280/390草稿保留、稳定布局截图目视正常 | 原390图在宽度动画中截取，另存稳定图，未删除原图 |
| 历史范围HTTP | event40源缩窄／别名重归属仍隔离，两渠道账号正常读取 | 三个独立真实登录上下文 |

完整门回执：`.artifacts/verification/2026-09-09T00-46-27-931Z.json`；开始00:41:50Z、结束00:46:27Z；未跳过live。仓库外原件统一位于 `/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`：

- `full-release-d346ea7.log`
- `owner-d346ea7.json` 与 `frozen-d346ea7/`
- `capacity-handoff-d346ea7f3279121c5cbf8ef61c7268649fe9e150-result.json` 与对应宽度截图
- `capacity-handoff-d346ea7-stable-layout.json`、`capacity-handoff-d346ea7-stable-390-3.png`
- `capacity-handoff-d346ea7-scope-fixture.json`、`capacity-handoff-d346ea7-scope-http-result.json`
- `retire-before-d346ea7.json`

首轮合成夹具曾缺SPU、done的completedAt；历史HTTP夹具曾缺范围created_by。均修正夹具并保留失败记录，不删除/放宽业务约束。额外UI脚本首次shell未在第二条命令继承Node PATH，修正执行环境后取证。清理脚本首次遇lsof附带文件描述符，安全断言中止、未停止任何进程；改为只核对PID字段后成功。

## 正式环境与剩余工作

正式3100再次只读核对仍为镜像 `sha256:9ec914e49423cd4c61b546e70fccaeb6baa081af8df0ef0bf99cb9f7a338bbf8`，61/61、无漂移，启动时间2026-09-08T20:03:01.2246885Z；未发布本候选、未改密、未触发外部同步。原目录仍0a56c4b及173个脏路径，未修改。核对owner/cwd/监听PID/SHA后只停止本任务旧3375/3377/3379验证进程；没有删除文件或数据，保留新3380。

这不是完整G04或整个项目完成：没有可见待办时仍需实际派工决策；真实产能协议与有效期、工厂其他客户占用、实际履约结果和业务UAT仍待。正式64迁移发布及恢复回退、G03/G05、总监12组101条、化妆品/国际供应链与三方连接器全链资格仍独立保留。D69删除事实、D70责任人、D71抑制天数没有擅自改判。
