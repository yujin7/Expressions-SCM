# 库存预警全结果排序 · G02

## 修复契约

旧版库存、可销天数及外部30日净件在客户端比较，只能排序当前页50行。新实现由服务端对筛选命中的全部行排序后分页，桌面列头与手机选择框共用cover前缀URL。默认不排序，保留读模型的风险顺序；改变字段/方向回到第一页，保留兄弟Tab查询。

字段允许列表包括SKU、S/A/B/C等级、库存、可销天数、30日外部净件及优先分。数值复用十进制比较，未知在升降序都最后；相同值按SKU自然顺序、ID兜底。非法字段/方向在读取数据库或重算前400。没有改变阈值、告警生成、R11定量、权限或库存事实。

## 证据

- 旧实现新增8条纯函数反例失败；修后纯函数/HTTP/客户端边界3文件26项通过，包含53行跨分页、精确小数、零/负/未知、等级及非法查询。
- 三套类型及改动lint通过。
- 隔离开发3352、既有合成PG65372、任务关闭；PMC真实登录。最终8组浏览器检查通过：1920列头排序，第二页切方向回首页；390/320手机字段/方向、刷新；前进后退；实际44列CSV导出全部6行且与排序一致；默认恢复及兄弟参数保留；浏览器错误为0。
- 人工查看1920/320截图后将等级列宽64→90，避免排序箭头将两字表头挤成竖排；调整后完整重跑8组通过。
- 首五次脚本失败均保留：分页JSON引用名称缺失、透明combobox被自身展示层覆盖、键盘选择假设、虚拟选项/快照名称差异及未等待下拉渲染。修测试等待/定位，未强制点击、未修改应用来满足脚本；第六次和最终宽度修正后均8组通过。
- 证据在外层`交付证据/2026-09-08-库存排序/`：`browser-dev-final.json`、`browser-dev-final.mjs`、`desktop-1920.png`、`mobile-390.png`、`mobile-320.png`、`sorted-export.csv`。开发验证不是冻结生产包或全站UAT。

## 仍待

准确候选生产构建/完整发布门及交付；关联告警索引仍只读500条，失败/加载时会误示未开告警；导出仍有5000行上限及取消/失败边界。G02没有完成，不能继承451df6b的READY。实际3100与真实数据未改；本轮浏览器和3352开发进程已关闭，合成PG留给后续验证。
