I have enough evidence. Here is the audit.

Repo root: `/Users/yj/Downloads/供应链系统 PRD/supply-chain` (paths below are absolute; line numbers verified against current files).

# Prioritized findings — new "director program" screens

### 1. OTIF on the cockpit is wrong by 100× (0.83% instead of 83%)
**Evidence:** `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/cockpit/cockpit-client.tsx:248` renders `OTIF {pct(...otifRate)}` with the local `pct = v => \`${v}%\`` (line 41). But `otifRate` is a 0–1 ratio: `src/server/modules/report/purchase-order-metrics.ts:199` (`o.rate = hit/evaluable`, 4 decimals), and the report page correctly does `(v*100).toFixed(1)` (`/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/report/purchase-orders/purchase-orders-client.tsx:28`). A director reads "OTIF 0.83%" and concludes suppliers never deliver on time.
**Proposal:** delete the three local formatters in cockpit-client (29–43) and have the server emit display-ready values, or at minimum import a shared `pctFromRatio`. Add a unit test asserting the cockpit block's OTIF string. **Effort: S**

### 2. "调拨 / 补货" action links on every alert row are inert deep links
**Evidence:** `src/server/modules/report/inventory-alerts.ts:161` emits `actions.transfer = /report/transfer-suggest?skuIds=<id>` and `actions.replenish = /replenish?sku=<code>`. The transfer-suggest **API** implements that deep link (`src/app/api/report/transfer-suggest/route.ts:12-17`, comment "预警行深链 …（D57 IAL-04）"), but the client never reads it: `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/report/transfer-suggest/transfer-suggest-client.tsx:55` declares `defaults: { q: "" }` and line 67 builds the fetch from `q/page/pageSize` only. `/replenish` has no `sku` param at all (`replenish-client.tsx:236-248` defaults are `q/coverDays/minCover/sortBy/…`). Both links land the user on a full unfiltered list. Used in two places: `cockpit-client.tsx:226` and `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/inventory/alerts/alerts-client.tsx:64`.
**Proposal:** add `skuIds` to transfer-suggest's `useListState` defaults and forward it to the API; change the replenish link to `?q=<code>` (already supported) or add a `sku` default. Add a route-level test that every `actions.*` href produces a filtered result. **Effort: S**

### 3. The cockpit's spike counts are capped at 10 and presented as totals
**Evidence:** `src/server/modules/report/cockpit.ts:291` slices `hits.slice(0,10)` / `unmappedHits.slice(0,10)`; the client then prints those array lengths as the headline: `cockpit-client.tsx:233` `已映射 {hits.length} · 未映射 {unmappedHits.length}`. With 40 spikes the director sees "已映射 10 · 未映射 10". The read model itself is not truncated (`src/server/modules/report/sales-spike.ts:178`).
**Proposal:** return `hitCount`/`unmappedCount` alongside the truncated rows (same shape as `inventoryAlerts.totals`) and render "前 10 / 共 N". **Effort: S**

### 4. A failed sub-query renders as "待我审批 0" — violating the page's own "绝不显示 0" doctrine
**Evidence:** `cockpit.ts:397-404`: if `getInbox` rejects but `countReviewItems` succeeds, the block is still `state: "ready"` with `inboxPending: 0`, and the failure text is buried in `note`, which `BlockCard` renders as 12px grey source text (`cockpit-client.tsx:62-64`). The file header (`cockpit-client.tsx:5`) and `cockpit.ts:36` both promise "无数据/无权限/待接入/样本不足各有空态，绝不显示 0".
**Proposal:** make `queues.data` per-item nullable (`inboxPending: number | null`), render `—` + an inline error chip with a retry, or degrade the whole block to `error`. **Effort: S**

### 5. Cockpit load failure = blank page with four tabs and a toast that disappears
**Evidence:** `cockpit-client.tsx:76-82` — the only failure handling is `message.error(...)`; `data` stays `null`, so `s` is undefined (129) and every tab body short-circuits to `null` (149/202/256/309). The repo already has the fix and states the doctrine explicitly: `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/components/LoadErrorAlert.tsx` ("Toast 会消失，不能承担'当前数据未加载'的业务语义"), used by 14 mature pages. Same gap in `inventory/alerts-client.tsx:40`, `todo-client.tsx:94`, `goals-client.tsx:73`.
**Proposal:** add `const [error,setError]` + `<LoadErrorAlert error={error} onRetry={load} subject="驾驶舱" />` to the four screens; also render a `<Skeleton>`/per-block `loading` while `data === null` — today first paint is an empty tab strip while 17 parallel server loaders (including the full replenish engine, `cockpit.ts:176-194`) run. **Effort: S** (M if you add per-block streaming)

### 6. Alert → "已知悉" is a dead end: the redline tells you there are unacked spikes, the target page has no ack control
**Evidence:** `cockpit.ts:274` labels the chip `爆单预警（未知悉 N）` and links to `/inventory/alerts?tab=spike`. On that page the ack function exists but is deliberately disabled: `inventory/alerts-client.tsx:67-71` defines `ack`, line 81 does `void ack;`, and neither spike table (103-108) renders it. The only place to ack is `/alerts` (`/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/alerts/alerts-client.tsx:57`), a different menu group. Worse, ack does **not** change status (`src/app/api/alerts/[id]/ack/route.ts:7`, `src/server/modules/alerts/engine.ts:107-113` counts `status='open'`), so acked rows stay in the open list forever and `unacked` is the only signal.
**Proposal:** add an "已知悉/已处理" column + button to both spike tables (wire the existing `ack`), show `ackedAt/ackedBy` inline, and add an "隐藏已知悉" toggle to `/alerts`. **Effort: S**

### 7. The four-screen cockpit is orphaned — nothing in the product links to it, and it competes with two other "驾驶舱"
**Evidence:** grep across `src/app`, `src/server`, `src/components` finds `/cockpit` only in the registry (`/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/lib/route-access.ts:70`). Landing is `/workbench` (`src/app/page.tsx:4`), whose only cockpit link points at the *old* dashboard: `src/server/modules/workbench/focus.ts:234` `{ key: "dashboard", label: "经营驾驶舱", href: "/report/dashboard" }`. The sidebar therefore offers 驾驶舱四屏 / 经营驾驶舱 / 工作台控制塔 — three overlapping first screens with overlapping counts.
**Proposal:** decide one director home. Minimum: put a "驾驶舱四屏 →" link in the workbench header next to 每日经营摘要 (`workbench-client.tsx:241`), point `focus.ts:234` at `/cockpit`, and rename the two entries so they are distinguishable ("驾驶舱四屏（例外优先）" vs "经营分析总览"). **Effort: S** (L if you actually merge them)

### 8. `/inventory/alerts` filters are invisible to the URL, and its own `page.tsx` claims otherwise
**Evidence:** `inventory/alerts-client.tsx:28-30` keeps `q`, `showC`, `onlyAlert` in `useState`; line 28 *reads* `sp.get("cover_q")` but nothing ever writes it back, so the param is dead. Filtering is client-side over the full row set (47-51). Yet `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/inventory/alerts/page.tsx:7` says "Tab 与筛选写进 URL（useListState）", and CLAUDE.md mandates `useListState` + `ListToolbar` for new list pages. Consequence: a director can't send "断货 S 级这 12 个 SKU" to PMC as a link, refresh loses the view, and there is no 密度/已保存视图/复制链接/导出.
**Proposal:** convert to `useListState({key:"inventory-alerts", paramPrefix:"cover"|"spike", defaults:{q,tier,primary,onlyAlert:"1",showC:""}})` + `<ListToolbar onExport={...}>`. **Effort: M**

### 9. Zero export and zero sorting across the whole new wave
**Evidence:** `grep -c 'sorter'` = 0 in cockpit, inventory/alerts, transfer-routes, purchase-orders, todo, goals, data-quality — versus 17 mature pages that have sorters (replenish, inventory-analytics, supplier-scorecard…). Export exists only in `todo-client.tsx:275` (stats tab only, not the item lists) and `purchase-orders-client.tsx:152`. Six tables on the cockpit, three on transfer-routes, five on data-quality, one on goals: none exportable, despite CLAUDE.md "UI: … 列表可导出".
**Proposal:** add `sorter` on the decision columns (可销天数, 涨幅, 逾期, 偏差%, 30 天单数, 差异%) and `onExport` via the existing `exportCsv` helper on `/inventory/alerts`, `/goals`, `/inventory/transfer-routes`, `/import/data-quality`. Cockpit tables should instead carry "在完整页面打开" links (they already have `extra` links — make them carry the current filter). **Effort: M**

### 10. "重算" is offered to users who are not allowed to run it
**Evidence:** `inventory/alerts-client.tsx:85` renders 重算 unconditionally, but `src/app/api/report/inventory-alerts/route.ts:14` requires `pmc|admin` and `src/app/api/report/sales-spike/route.ts` requires `pmc|ops|admin`. A warehouse user clicks and gets a 403 toast. The correct pattern is two files away: `purchase-orders-client.tsx:52` `const canRefresh = hasAnyRole(me, "pmc", "purchasing")` gating line 258.
**Proposal:** gate with `hasAnyRole` (admin passes automatically, `src/components/useMe.tsx:47-51`); same review for `data-quality-client.tsx:279` and `transfer-routes-client.tsx:258/534`. **Effort: S**

### 11. Goals: the progress card goes stale after every edit, and the metric picker contradicts its own help text
**Evidence:** `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/goals/goals-client.tsx:156` passes `refreshKey={data?.rows.length ?? 0}` — editing a target or filling an actual leaves the row count unchanged, so `GoalProgressCard`'s effect (`GoalProgressCard.tsx:26-28`) never refires and the 达成率 card keeps the old number next to the updated table. Separately, line 207 tells the user "其他已登记指标可直接输入 metricKey（manual 填报）", but the Select at 197-206 only offers the 6 `autoMetrics` and has no free-entry mode, while the API accepts any key registered in `METRICS` (`src/server/modules/goals/service.ts:349`, ~60 keys).
**Proposal:** use a monotonic `tick` counter for `refreshKey` (the pattern already used in `todo-client.tsx:335`); make the metric Select a grouped list of *all* `METRICS` entries (auto vs manual groups) with the short description as help text, and drop the misleading paragraph. **Effort: S**

### 12. Raw internal identifiers and repo file names shown to end users
**Evidence:** `goals-client.tsx:110` prints `metricKey` (`inventorySalesRatio`) beside every metric label; `goals-client.tsx:115` shows tags literally reading `auto` / `manual` / `auto·待读模型`; `goals-client.tsx:141` button "回填 auto 实际值"; `/Users/yj/Downloads/供应链系统 PRD/supply-chain/src/app/(app)/todo/TodoProgressCard.tsx:33` renders the link label as `→ /todo`; `todo-client.tsx:369` writes "「我的待办」(/inbox)" in body copy; `cockpit-client.tsx:353` shows "措辞以 CURRENT.md 决议登记为准" (a repo file) to directors; `cockpit-client.tsx:110` renders source states as `operational/observation/blocked` translated ad hoc instead of via `src/components/dictionary.ts`.
**Proposal:** route all of this through `dictionary.ts` / `metrics.ts` (which exist precisely for this: "新代码的用户可见文案必须引用本字典"), replace `→ /todo` with "查看全部待办 →", and replace the metricKey column with a `metricTooltip` info icon. **Effort: S**

### 13. Cockpit block #4 leaks unreviewed meeting notes as if they were product content
**Evidence:** `src/server/modules/report/cockpit.ts:137-142` hardcodes `SURVEY_CONCLUSIONS`, e.g. "规则制定 = 备货负责；运营模式 = 数据支撑；20% 创收利润单品先磨合备包材", rendered as a numbered list at `cockpit-client.tsx:349-354` with the footnote "待业务确认". A first-time director cannot tell whether this is a system finding, a policy, or someone's notes — and it sits next to real measured blocks, which damages trust in the measured ones.
**Proposal:** either remove the block until the decisions are ratified, or move it behind a "调研结论（草案）" collapse with an explicit draft badge, author and date, sourced from a table rather than a constant. **Effort: S**

### 14. Counts everywhere, rows nowhere (and silent client-side truncation)
**Evidence:** `cockpit-client.tsx:261-262` 启用仓/实时仓/快照仓 counts, then `.slice(0,20)` of the rows with no "top 20 of N" label (264); `272` filters to `realtime` and slices to 8 client-side; `337` slices goals to 12; `296` shows 异常/告警/零散线路 counts while only 10 anomaly rows are returned (`cockpit.ts:348`); `321` prints per-role 未完成/逾期 as plain text with no link (the same data is linked in `TodoProgressCard.tsx:41`); `364` prints 快照跳变告警 N and 对账一致率 with no path to the offending warehouses; `data-quality-client.tsx:269-271` shows 告警/待核对/手工改写 counts where only "待核对" has a table; `workbench-client.tsx:286` 待认领别名 count. `focus.ts:233` "近效期批次（90天内）" links to `/report/dashboard` rather than the existing rows page `/inventory/expiry`.
**Proposal:** rule — every count is a link that lands on a *pre-filtered* row list; every truncated table shows "显示前 N / 共 M，查看全部 →" with the filter carried in the URL. Fix `focus.ts:233` to `/inventory/expiry`. **Effort: M**

### 15. Density, mobile and shell inconsistencies on the new screens
**Evidence:** `cockpit-client.tsx:316-319` and `328-330` use fixed `<Col span={6}>`/`span={12}` (no `xs/md`), so screen 4 crams 4 statistics into a phone width, while the rest of the same file is responsive (`xs={24} md={12} xl={6}`); tables use `scroll={{x:800..1300}}` with no `fixed:"left"` key column, so on a laptop the SKU column scrolls out of view (contrast `transfer-routes-client.tsx:223` and `purchase-orders-client.tsx:121` which pin the identity column). `cockpit-client.tsx:134` is the **only** `bodyStyle` left in the repo (deprecated in AntD 5; everywhere else uses `styles={{ body }}`) → console deprecation warning. `inventory/alerts-client.tsx:98` hardcodes `pagination={{pageSize:50, showSizeChanger:false}}`, whereas the platform default is `showSizeChanger:true` (`src/components/useListState.ts:399`). `cockpit-client.tsx:86` calls `router.replace` without `{scroll:false}`, so switching tabs jumps to top (useListState always passes `scroll:false`, `useListState.ts:269`). No page-level `CaliberNote` on any new screen (18 mature pages use it; `src/components/CaliberNote.tsx:5` — "终结每页开头 1-3 段 Alert 横幅墙"), so `/todo`, `/goals`, `/report/purchase-orders`, `/inventory/transfer-routes` all open with a full-width blue banner wall before any data.
**Proposal:** responsive `Col` props, `fixed:"left"` on identity columns, `styles={{body}}`, drop the hardcoded pagination, `{scroll:false}`, and convert the four leading `Alert` banners to `CaliberNote summary/detail`. **Effort: S each, M as a sweep**

---

## Also worth fixing (below the top 15, all cheap)

- **`/alerts` hides the data it already has.** `alerts-client.tsx:8-13` declares `ownerRole` on the row type and never renders it — the director cannot see whose alert it is. The list is `limit(200)` server-side (`src/app/api/alerts/route.ts:20`) with no pagination, no category/severity filter, and the UI hardcodes `status=open` although the API accepts `?status=`. **Effort: S**
- **`/todo` is missing from the landing screen.** `focus.ts:430-434` builds 5 queue cards (inbox/notify/alerts/review/mine) — none is 待办任务/逾期, even though 逾期 is the D61 headline metric. Two of the five point at the same `/inbox`. **Effort: S**
- **Name collision:** sidebar `/inbox` is labelled "我的待办" (`route-access.ts:61`) while `/todo` ("待办任务") has a tab also called "我的待办" (`todo-client.tsx:354`). The banner at `todo-client.tsx:369` exists only to apologise for it. Rename `/inbox` to "待我审批" (the term the cockpit already uses at `cockpit-client.tsx:329`). **Effort: S**
- **`TodoProgressCard` shows 0 before/on failure.** `TodoProgressCard.tsx:37-40` `value={data?.mine.open ?? 0}` — the same block that the cockpit renders under the "never show 0" rule. Use `loading` + `—`. **Effort: S**
- **Data quality hides all its "why" in native `title=` attributes** (`data-quality-client.tsx:141,146,159,168,178,190,220`) — no keyboard access, ~1s delay, invisible on touch; the file already imports `metricTooltip` and uses AntD `Tooltip` in the KPI cards. **Effort: S**
- **Waive without a reason fails only after a round trip.** `data-quality-client.tsx:356-371` leaves OK enabled; the server rejects with 400 (`src/server/modules/dq/reviews.ts:195`). Disable OK when `action==="waive" && !closeNote.trim()`. **Effort: S**
- **Transfer-routes anomaly rows have no action.** `transfer-routes-client.tsx:480-523` — a director sees "偏差 42%" and can only open the doc; no 登记费用 prefill, no 生成待办, no 已知悉, despite a watchdog writing `system_alerts category=transfer_cost` (543). Same for the "零散调拨，建议合并批次" tooltip at line 225 — advice with no button. **Effort: M**
- **Goals period is a free-text box** (`goals-client.tsx:160-167`, "期间 YYYY-MM / YYYY-Qn"), validated by regex only on create (194). Use the `DatePicker picker="month"|"quarter"` already used in `todo-client.tsx:299`. **Effort: S**
- **Purchase-order year list comes from the browser clock.** `purchase-orders-client.tsx:95-98` `[0,1,2].map(i => new Date().getFullYear()-i)` — years with data outside that window are unreachable, and the list disagrees with `data.baselineYear`. Return the available years from the read model. **Effort: S**

---

## Numbers computed or reformatted client-side that should come from the server

| Where | What | Why it matters |
|---|---|---|
| `cockpit-client.tsx:29-43` | local `yuan()` / `qty()` / `pct()`; `yuan` does `Number(v)` on decimal strings and `/10_000).toFixed(1)` for 万 | float on money strings; duplicates `src/components/format.ts`; **`pct` is the source of finding #1** |
| `cockpit-client.tsx:233` | `hits.length` / `unmappedHits.length` as headline counts over a server-truncated array (`cockpit.ts:291`) | wrong number (finding #3) |
| `cockpit-client.tsx:264` | `rows.slice(0,20)` warehouses | silent truncation, no total shown |
| `cockpit-client.tsx:272` | `rows.filter(r => r.accountingMode==="realtime").slice(0,8)` | business rule (which warehouses count for turnover) executed in the browser |
| `cockpit-client.tsx:337` | `goals.rows.slice(0,12)` | silent truncation |
| `cockpit-client.tsx:288` | 元/件 rendered as the raw string, while `transfer-routes-client.tsx:137` formats the same metric with 2–4 decimals | same number, two appearances |
| `inventory/alerts-client.tsx:47-51` | tier/status/text filtering over the entire read model in the browser | can't share a filtered link, can't paginate server-side, whole model shipped to the client |
| `inventory/alerts-client.tsx:91` | 爆单命中 = `hits.length + unmappedHits.length` | derived KPI with no server-side definition; will drift from `countOpenAlerts` |
| `transfer-routes-client.tsx:140-146` | `feeReasonText` / `qtyReasonText` string-assemble the deviation narrative in the client (deliberate, for masking) | the *explanation* of an anomaly differs by role and is not testable server-side; consider two server-rendered variants |
| `transfer-routes-client.tsx:217-220, 276` and `475-478, 551` | q filtering + pagination slicing over the full model | fine at today's size, but `total` and the "共 N 条" label are browser-derived |
| `data-quality-client.tsx:245, 253` | headline KPI cards via `report.sources.find(s => s.sourceClass === "rpa_warehouse")` | which source class is "the" accuracy number is a business decision living in JSX |
| `purchase-orders-client.tsx:95-98` | year options from `new Date()` | see above |
| `purchase-orders-client.tsx:141` | 可比行 rendered as `v / (v + nonComparableLines)` | denominator computed in the client |
| `goals-client.tsx:47,111-112` | `UNIT_SUFFIX` map turns `unit` codes into 单位 in the client | server already knows the unit; `metrics.ts` already defines `MetricUnit` |

## Blocks that show a count with no way to reach the rows

- `cockpit-client.tsx:206-213` redline chips — count-only for every exception key beyond the two that have dedicated pages; chips with `count === 0` are still rendered and clickable, sending the user to an empty page.
- `cockpit-client.tsx:261-262` 启用仓 / 实时仓 / 快照仓; `271` 仓库 / 实时仓 / 实体启用仓 (three counts, one table showing 8 rows).
- `cockpit-client.tsx:296` 异常 / 告警 / 零散线路 — only 10 anomaly rows returned; 零散线路 has no listing at all.
- `cockpit-client.tsx:321` per-role 未完成 / 逾期 / 完成率 as plain text (the identical data is linked in `TodoProgressCard.tsx:41`).
- `cockpit-client.tsx:361` 通过 / 拒收 / 重复 per source; `364` 对账一致率, 快照跳变告警, 销量一致性 — none clickable.
- `cockpit-client.tsx:246-249` 本月已下单 / 已下单金额 / 成本下降 YTD — one shared "采购订单指标 →" link, no per-metric drill.
- `data-quality-client.tsx:269-271` 快照跳变告警 N 仓 / 待核对 N 项 / 手工改写 N 项 — only 待核对 has rows on the page; 手工改写 has no listing anywhere on it.
- `data-quality-client.tsx:260-262` 比较 N 行 / 例外 N / 低于量下限 N — 低于量下限 rows are never listed.
- `workbench-client.tsx:286` 待认领别名; `focus.ts:233` 近效期批次 (links to a dashboard, not rows).
- `alerts-client.tsx` (system alerts) — the list *is* the rows, but there is no count, no total, and a silent 200-row server cap.