# 库存分析销售证据一致性

## 范围与缺陷

原101条需求继续，当前只读库存分析切片承接“未知不能当零、图表和导出须可核验”。
旧服务`dailyBySku.get(id) ?? 0`把无记录变零；`daily:r2(daily)`把最小四位数量/91的正负日销都舍零，
可销天数却用未舍入值。缺月仍按完整91天分母计算，未披露实际月份。

## 实施

- 复用`salesWindow`锚定正式月销最新月，向前3月；历史÷91不改，非自然月实际天数。
- SQL按SKU聚合原始decimal销量，distinct月份计数，多渠道不重复算月份。
- 无记录/部分月份保留原量与0/1/2月份数，daily/daysCover为null；3个月有记录才计算。
  已登记三月不证明全渠道完整，不将历史需求称为今天需求。
- 日均复用`dailyFromWindow`，移除两位展示舍入；天数使用同一个数值。负量和登记零保留。
- 图形排除未知但披露数量；数据表和两种CSV保留这些行。非正日均左上占位，不标无穷。
- 页面显示源月份/历史分母；日均六位有效数字不将微量舍零。API类型单一、仅type导入服务端。
- 同步/异步CSV共用证据列和值转换，保留原始量、月份、状态、窗口及分母；不改变上限、权限或过账。

## 开发证据

9项新增服务/导出反例在旧实现全部失败：无记录、零记录证据、微量正/负、缺月、窗口、跨渠道月份、导出、空源。
修后3文件26项通过；加展示契约后6文件44项通过，改动lint无错误/警告；三套类型通过后继续最终核对。
新增展示测试含数值、状态、共享导出消费者与未知表格保留，不把源码断言视为浏览器证明。

## 准确候选验收（20:31）

应用8492a201308c85f5ad6b069f9d36e8cba05ac2de，开始/结束均干净同SHA。
完整门READY9/9：620文件5588项、HTTP33/33、全量lint、三套类型、生产构建通过。
回执`.artifacts/verification/2026-09-08T12-31-46-626Z.json`；日志在外层
`交付证据/2026-09-08-库存预警完整导出/full-release-8492a20.log`。

冻结3354同版、62/62迁移。只在已核对ID/标签的专用window_qa库新增6个SKU、6条余额、13条月销，
脚本`seed-sales-evidence-qa.sql`拒绝错误库/用户、重复运行和错误月销锚点。没有修改原样本或实际3100。

真实合成PostgreSQL→API→页面→同步/异步CSV：

- 缺失/缺月daily=null，原登记量和月份可读；零/微量正/微量负/正常日均分别保留。
- 1920/390/320均无页面横向外溢；6个有在库SKU中4个有资格绘点，覆盖4/6、66.7%，未知2个明确说明。
  零/负共用非正占位，微量正独立；实际动画完成后截图，数据表保留6行。
- 同步实际CSV与异步任务18实际下载CSV各6行，逐SKU核对日均、可销天数、原量、月份、窗口和分母；
  不仅核对任务自报数量。未知写空数值、同时有明确证据状态。
- 只筛缺失样本显示不足而非零销量点，数据表仍可打开；周转表复用同一语义。
- `browser-sales-8492a20.json`原回执8组通过后failed：测试取了AntD隐藏的第一个tabpanel，
  可见周转页实际上已正常加载；不改写旧失败。`browser-sales-resume-8492a20.json`针对可见页和
  浏览器错误为空两组补验通过，无需修改应用或重建。没有新增第二个导出任务来伪造成功。
- 三张散点截图与窄屏表格/周转截图已目视检查；宽表使用内部横向滚动，不声称手机同屏展示全部列。

本批为计划员合成角色验收，不替代所有真实角色/渠道/数据UAT。真实3100仍61/61健康，未部署；
原工作区173路径保留。没有真实销售、库存过账、审批、分派或D69–D71变更。

## 继续的完整目标

原101条、正命中爆单整链、G03账期谈判/G04申报产能/G05平台状态、真实当期数据及受控交付仍未完成。
本报表仍有已声明的当前在库近似平均库存、历史三月÷91、库存量展示两位等边界；本次不将其改成
财务审计口径或声称全站所有数量精度/覆盖问题已解决。实际版本整合、镜像/迁移/回退与真实入口验收另做。
