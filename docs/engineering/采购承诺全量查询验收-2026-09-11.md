# 采购承诺全量查询验收 · 2026-09-11

## 准确范围

- 应用提交：`fa415f756e3a49ef6abd4ae8a56c0d1cce9ee235`，分支 `agent/scm-workbench-evidence-20260907`。
- 隔离运行：`127.0.0.1:3429`，冻结同版，专用 `purchase_line_20260911` 合成库，66/66迁移无漂移。
- 正式3100仍旧61迁移镜像 `9ec914e49423cd4c61b546e70fccaeb6baa081af8df0ef0bf99cb9f7a338bbf8`，本轮无正式部署、业务修改或密码重置。
- 角色/任务：仓管和采购从异常集合找到具体采购行，核对原始/当前承诺；不是新采购审批或外部事实晋级。

## 改动与证据

1. 搜索单号、采购行ID、供应商和SKU；筛选原始/当前口径与逾期未齐/迟到补齐。先在全量结果筛选/排序，再分页。默认保持30行、紧凑密度。
2. 表头排序改为服务端排序，另有行ID及方向控件；相同值按PO/行/口径保持稳定身份次序。URL使用`promise_`命名空间，改条件回第一页；越界空页提供回第一页。
3. 分开窗口全部异常数、筛选匹配总数、本页条数。图表和履约率仍按全观察窗计算，不被异常筛选缩小分母；页面明示这一点。
4. 导出读取同条件全部页，不接受页码或页大小。沿用同步5000/异步50000边界，记录条件、匹配总数、窗口总数和执行时最新事实，不冒称冻结快照。无匹配与整个窗口无异常分别表述。
5. 复用ListToolbar/useListState/SearchInput和useDocumentRead；补页面Suspense，不新增菜单、路由、迁移、服务缓存或并行查询平台。

修前3个反例失败。修后定向7文件82项通过，另读取生命周期33项通过；三套类型与改动lint通过。完整准确门：**READY，9/9，686测试文件/6215项，HTTP34/34零跳过**。门开始/结束均同一干净SHA；回执 `.artifacts/verification/2026-09-11T01-05-04-984Z.json`。

## 实际Chrome验收

使用合成仓管`warehouse01`，不是正式管理员数据。既有PO21有61个合成行，加旧口径异常构成63条；未再创建业务夹具。

- HTTP表单登录进入workbench，再打开到货日历并点击显示数据表。
- 默认第1页行23–52；真实点击第2页后URL含`promise_page=2`，行53–82。
- 从第2页搜索`83`并Enter，URL撤页码，显示1/63、本页1，准确链接PO21/行83。初次紧接点击观察到加载态，待本次读取完成后才验结果；旧行已撤回。
- 真实下载`promise-filtered-fa415f75.csv`，1条数据、36个表头，行83、PO21、窗口63/匹配1/文件1与搜索83均准确。
- 真实清除搜索按钮恢复空搜索；选择行ID降序，首行为83；切升序首行为20。打开分享URL`promise_sort=lineId&promise_order=asc&promise_page=2`，显示行51–80，排序控件回显正确。
- 320宽选择迟到补齐，自动回第一页，匹配2/63；两条分别是同一行20的当前/原始口径，不误删成一条。
- 已成功读取后拦截本报告GET，真实刷新显示持久错误，旧行0、导出按钮0；解除拦截并重试，恢复2/63及导出按钮。
- 搜索不存在值显示0/63与“当前条件没有匹配异常”。历史后退恢复上一URL，前进并重新打开数据表后，搜索框/URL/无匹配结果一致。
- 390宽：文档390，表内320/内容1480，工具条138高。320宽：文档320，工具条204高；内部宽表可滚动，非整页横溢出。390和320-r2截图已目视；首张320截图落在页面顶部，只作原始过程，不作工具条验收图。
- 自动化首次点击AntD隐藏combobox输入被selection-item覆盖；改点实际可见Select容器后选择成功，不归为产品点击失败。检查浏览器错误队列为空。
- 收尾点击Sign out后立即探测仍200，随即关闭浏览器，**未取得退出后401证据**；不把点击提交当作已验证会话撤销。本轮不改认证机制。

外部证据目录：`交付证据/2026-09-09-申报产能/`，包含 `full-release-fa415f75.log`、`build-fa415f75.log`、`postgres-fa415f75.log`、`formal-observation-fa415f75.json`、`owner-fa415f75.json`、`promise-filtered-fa415f75.csv`、`promise-query-390-fa415f75.png`、`promise-query-320-fa415f75-r2.png`。浏览器会话已关闭，不保存会话密钥。

## 剩余边界

- 翻页、导出是各次读取时的事实，业务变化可改变结果；没有跨请求冻结快照，也没有以原子事务声明日历与三方旁证同截面。
- 本次未做加载性能对比，不宣称全站提速。日历及旁证仍随整个报告请求加载，独立按需取数是否值得拆分须测量再决定。
- 原始版本覆盖、历史采购行歧义、三方身份/单位/状态/UAT资格不因筛选功能完成而改善；真实101条验收、D69–D71、正式发布仍待。
- 本地Safari HTTP Cookie问题尚未部署修复；HTTPS已实测可登录并要求修改初始密码。异地备份配置缺失，已询问现有备份位置，不绕过门禁。

实现使用项目工作流设计/数据口径技能，按并行安全和准确发布技能验收；React/Next技能约束客户端边界与Suspense，浏览器技能要求真实交互与截图。表头服务端排序与onChange按当前项目AntD5契约，不升级框架：[Ant Design 5 Table](https://5x.ant.design/components/table/)。
