# 手工待办创建恢复与准确回链验收

2026-09-13。仅候选工程验收，不是正式发布、真实角色UAT或全项目完成证明。

## 候选与边界

- 应用：`b4d221514a36f83109d5e9c2bb8cde6ac0fa205e`，分支`agent/scm-todo-create-recovery-20260913`。
- 评审：[PR82](https://github.com/yujin7/Expressions-SCM/pull/82)，基于PR81；82→81→…→55依次整合，依赖合并后重接main再验，不自动合并功能分支。
- 隔离运行：3472，冻结`runtime-b4d22151`；health精确SHA、71/71迁移、零漂移；后台作业关闭。
- GitHub：[34750028730](https://github.com/yujin7/Expressions-SCM/actions/runs/34750028730)的8个job均0步骤；平台注释明确账户付款/额度问题。远端CI未通过，不用本机结果替代。
- 证据目录：`.artifacts/todo-create-recovery-20260913/`；本机忽略目录保留，不声称随Git推送。

完整发布门：READY9/9，755文件/7,329项、HTTP34/34零失败零跳过；运维、71迁移契约、全lint、三套类型、生产构建均通过。2026-09-13 09:40:52.863Z→09:45:47.893Z起止同一干净SHA，回执`.artifacts/verification/2026-09-13T09-45-47-893Z.json`。后续文档提交不冒称新SHA重跑全门。

## 最小业务闭环

[上轮](普通待办跟进持久恢复验收-2026-09-13.md)已处理普通跟进恢复；本轮补手工“新建待办”，不将重复内容自动当作同一任务。

1. 每次有意新建分配UUID；同一请求断线重试只返回原任务。相同内容但新UUID允许另建。
2. 原始标题、明细、责任人、责任角色、优先级、截止日及引用写入不可变创建审计。后来改派、完成或修改任务，不会把当前内容冒充原创建依据。
3. 创建与只读核对共用事务请求锁，核对当前启用身份、会话版本、数据库范围与原保存人；等待正在提交的创建后再判断是否存在。同键异意409，他人回执不泄露。
4. 客户端发送前按站点/账号保存有限恢复副本；Web Lock串行跨标签，20秒等待上限。刷新、关闭或超时不自动POST，不拿“暂未查到”当取消证明。
5. 缺失结果允许用户明确修正原请求，仍用原UUID；已创建则展示服务器原始依据，经用户明确核对后清本机记录。损坏、额度不足或不支持安全锁时拒绝新发送，不静默遗忘。
6. 成功提示给准确任务链接，同时保留原列表筛选。打开任务后可后退返回；空列表不再把新任务入口藏起来。
7. 浏览器实测发现底部填写后错误提示在视野上方，已补结果出现后的焦点/滚动定位。不是仅添加一条错误文字。
8. 非管理员的合法手工引用现在保留；伪造系统来源仍归一为手工，不借此预占系统指纹。

公开手工POST要求UUID、严格拒绝非法/未知字段，返回最小回执；旧浏览器缺键会收到刷新指引而不创建。内部系统来源仍保留原指纹/重开语义，内部无UUID调用不被宣称全域创建幂等。任务、审计和通知outbox维持同一事务；恢复重放不重复发通知、不改状态、不关闭来源、不写库存。

## 数据迁移与防回归

`0070_todo_create_request.sql`只增加审计表上手工创建请求键的部分唯一索引，大小写归一；没有新增业务表或依赖，没有历史回填/猜测。生成快照与0069逐对象比较后，唯一变化为此索引；既有字段、约束和外键保留。隔离库从70升71，正式库未迁移。部分唯一索引的适用边界参考[PostgreSQL官方说明](https://www.postgresql.org/docs/current/indexes-partial.html)，实际SQL约束另用真实PG验证。

新测试覆盖原键重放、7字段逐一异意冲突、原任务后来变更/原负责人停用后的原意恢复、当前身份/原保存人权限、审计失败整体回滚、HTTP参数与引用保留、本机损坏/额度、跨标签、超时、迟到响应、刷新核对/明确确认、同键修正与可见错误定位。原17项抽屉生命周期测试保留，现21项；helper17项、真实服务/路由12项。新增A725迁移控制，台账正文、分类表、CURRENT引用和测试数量共同更新。

首次候选`82c77b5b`完整门NOT READY：754文件通过、1文件失败，7,326项通过/1项失败；原因是迁移虽已journal登记，执行审计台账漏了该对象。补台账时范围段旧70又被定向测试抓到并修正；未修改门以忽略缺失。该旧门回执为`2026-09-13T09-34-26-658Z.json`，不能当最终通过证据。

## 真实PG与浏览器

最终应用PG夹具`3a937e0a`，账号262，6场景/5次真实Lock等待：双创建单次落库；GET等待创建提交；相同内容新键生成独立任务；并发异意409；停用先完成后GET与重放均403；直接绕过服务插入同键大写审计被23505拒绝。保留任务17/18/19与创建审计2060/2061/2062，库存414、告警4、通知51不变。

实际浏览器在隔离合成管理员下从空筛选新建任务20。真实POST201返回后，测试观察器故意丢弃回包；并非伪造成功。UUID为`cfdda05d-4c7d-4890-becb-a1987a13a0fa`，本机仍保留原输入；错误框在1280×633视野内top57/bottom119，恢复按钮同时可见。

整页刷新回`/todo?tab=mine&mine_q=no-match-create-synthetic`，独立恢复入口仍在。打开后明确GET核对，再确认原任务；观察到仅结果GET、统计GET及原筛选列表GET，没有第二次POST。清本机副本后展示“打开准确任务 #20”；实际打开`/todo?tab=all&all_q=%2320`只定位该任务，后退仍为原空筛选。过快自动化点击曾未触发读取，重新观察当前DOM后点击成功；不声称首轮自动化每一步都成功。

任务20只有创建审计2064；原标题/引用一致、状态open、completed_at为空；库存/告警/通知控制数不变。旧候选真正创建的任务16也在新版只读恢复，原审计2059没有重复。

已查看桌面恢复入口、失败后提示和390×844原回执/成功链接截图，根级宽度1280/390均无溢出，浏览器errors/console为空。代表性实际AntD验证不等于全站UI、正式Safari或真实业务角色验收。

## 收尾与仍缺

核对自有PID32661的cwd及3472监听属于最终冻结包后停止；本轮浏览器关闭，已确认owner的隔离PG停止且无3472监听。合成库、追加审计、截图、回执和两个冻结包均保留，没有删除业务文件或清空恢复记录来绕过验证。

现有AGENTS/CLAUDE与七个canonical技能已覆盖写入原子性、迁移登记和精确发布边界；本轮沿用它们补不可变回执、真实竞争、实际浏览器及审计登记，没有另建平行技能/记忆权威。CURRENT只补本轮业务契约，NOW替换当前证据与下一步；脚本说明登记隔离PG准入。

仍需状态操作原请求回执、跨设备/损坏记录自助对账、未提交请求服务端取消、全部负责人/来源/范围竞争与真实角色全链。浏览器恢复按站点/账号隔离，不是加密存储或多设备同步；不要删除浏览器记录来假装请求未发生。

正式部署仍缺获授权异地DB+附件配对备份、发布窗口、依赖整合及GitHub账户限制解除；正式HTTP/HTTPS登录和业务试点待发布后实测。没有更改正式3100、口令、隧道、三方凭据或业务裁决D69–D71。当前入口见[NOW](../NOW.md)，意图见[CURRENT](../spec/CURRENT.md)，运行脚本见[说明](../../scripts/README.md)。

文档收尾：技能治理/审计台账2文件749项通过，4份文档89个相对目标存在（不宣称全仓锚点已验证），`git diff --check`通过；NOW保持83行/14,029字节。文档检查不累加到应用7,329项。
