# 盘点筛选身份与失败恢复验收

## 范围与结论

主责design-supply-chain-flows；输入完整性前序按write-path，准确候选按release-sweep，共享状态按parallel-sessions保护。仓管在盘点列表筛月份、找单据、识别失败、清除恢复；不改变数量、权限、审批、过账或迁移。

候选 `3a32694f403f51af7f699e99b9017eeab72b5e3d`，分支 `agent/scm-workbench-evidence-20260907`。仅隔离冻结 `http://127.0.0.1:3424`，DB `qc_boundary_20260911`，后台任务关闭。准确完整门READY9/9；本页列出的20项浏览器续验通过，**不代表全站、所有键盘路径、正式3100或业务UAT完成**。

## 真实缺陷与修复

- 前序d15fc67b的月份Input和SearchInput直接用各自值作兄弟key。初值均空，改月份后DOM留下两个月份输入框；相同非空值也碰撞。实际失败截图 `pd-invalid-period-d15f.png` 保留。
- 改为 `period:` / `query:` 语义前缀，不加新状态层，不改变现有URL与显式提交契约。
- 非法月份API已经400并展示持久错误，但表格同时显示“暂无数据”。仅失败状态改为“数据未加载，请先修正筛选条件或重试”；成功但确无匹配仍保留正常空态，合法数据没有补零。
- 新3项反例修前红；测试替身首版按组件类型过滤不准和旧key字面量断言均修正，保留原业务行为断言。定向2文件33项、三套类型、改动lint通过。

## 准确候选门

回执 `.artifacts/verification/2026-09-10T23-17-49-882Z.json`：开始/结束同一干净SHA，679测试文件/6151项、三套类型、全量lint、生产构建、同版HTTP34/34零跳过；冻结健康65/65迁移零漂移。

外部证据目录：`/Users/yj/Downloads/供应链系统 PRD/交付证据/2026-09-09-申报产能/`。

- `full-release-3a32694f.log`、`owner-3a32694f.json`、`formal-observation-3a32694f.json`
- `browser-count-filter-3a32694f-r2.json`：07:21:26–07:21:33，实际Chrome交互20项
- `pd-filter-error-1280-3a32694f.png` / `pd-filter-error-390-3a32694f.png` / `pd-filter-error-320-3a32694f.png`
- `pd-filter-recovered-320-3a32694f.png`；390错误与320恢复截图已目视，控件不重复/不出视窗，宽表留在内部滚动。未声称密度、全表所有列或其他页面全合格。

## 交互与数据核对

1. `2026-13`只留一个月份框和一个搜索框，持久失败，无“暂无数据”假空态。
2. 实际月份清除按钮恢复8行、URL移除period；合法2026-09仍8行。
3. 月份和搜索同时2026-09仍各一个；刷新恢复两条件。该搜索确实无匹配，是合法空结果。
4. 实际搜索清除只移除q，保留period并恢复8行。
5. 相同月份导出：2026-09返回200 CSV，2026-13返回400，未忽略非法筛选。
6. 导航工作台→后退→前进→再次后退，月份与8行恢复，没有重复输入。
7. 再次改坏月份；1280/390/320输入和页面外框未水平溢出；手机清除恢复8行。
8. 最后实际退出确认，盘点API401，测试浏览器关闭。
9. 独立只读SQL：PD2 `draft / v4`，实盘 `0.4000`，PD2审计4条，总PD8张，仓18/SKU2库存 `0.1000`。本轮不调用业务写API。

## 保留的失败与未证明事项

首轮脚本 `browser-count-filter-3a32694f.json` 在自动 `fill('')` 后超时：可见框空而URL仍旧，按Enter恢复。随后实际清除按钮立即同步并恢复8行。键盘尝试中Meta+A未全选，只删末位；再尝试Control+A/Backspace时工具报告重启浏览器至about:blank。不能推断产品键盘正常，也不把异常默认为自动化问题。续验r2只更换为实际清除按钮并完整通过上述20项，原失败证据不覆盖。

正式镜像前后均 `sha256:9ec914e49423cd4c61b546e70fccaeb6baa081af8df0ef0bf99cb9f7a338bbf8`，未发布。正式Safari HTTP登录问题、异地备份、101条真实UAT、简道云/聚水潭/用友资格、D69–D71、SH多PO行分摊及旧抽屉/React异常继续独立跟踪。
