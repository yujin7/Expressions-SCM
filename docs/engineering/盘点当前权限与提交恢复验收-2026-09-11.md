# 盘点当前权限与提交恢复验收 · 2026-09-11

## 候选与范围

准确应用提交 `e411cdc697fea8235aadf676f7057a21ec1361a4`，分支 `agent/scm-workbench-evidence-20260907`。
冻结服务3421，独立PostgreSQL数据库 `qc_boundary_20260911`，65迁移无漂移。
本轮只在已有合成PD2和独立并发夹具验证；没有修改正式业务数据、账号、库存或正式镜像。

自动完整门READY9/9，盘点提交失败恢复与财务驳回已分段完成真实浏览器验收。
这不是全站UX、101条真实UAT或正式发布完成；角色按钮误导和下述浏览器观测异常仍未消除。

## 准确检查

- `.artifacts/verification/2026-09-10T22-26-05-058Z.json`：起止同一干净SHA；676测试文件、6120测试通过，三套类型、全量lint、生产构建、运维/迁移契约通过；同版HTTP34/34，零失败零跳过。
- `pd-authority-exact-e411cdc6.json`：准确干净SHA下真实PG13项通过。四个提交实际等待PD锁，一成功三409且只有一条提交审计；指定审计故障回滚；保存/提交同版本竞态仅一个成功；审批等待账号资格撤销提交后403、没有审批事件；停用账号不能创建。
- `formal-observation-e411cdc6.json`：检查前后正式容器、镜像、启动时点相同，runner未执行正式变更。只对该观察窗口作此结论。

## 浏览器 → API → 数据库

使用真实Chrome、普通按钮和链接交互，没有用API写入代替页面操作。

1. 仓管warehouse01登录3421，列表单号打开已有PD2。起点draft/v2，实盘0.4000，账面和库存0.1000。
2. 在确认独立容器ID和PD2原状态后，安装仅针对 `pd_doc/2/submit` 审计插入的合成故障触发器。实际点击提交及确认。
3. 页面显示“操作未确认成功”、错误码fe45ec77、“重新读取单据”，输入保留0.4000；独立SQL确认仍draft/v2、提交审计0。不是先更新状态再报错。
4. 删除该触发器和函数；实际点击只读恢复，再次人工提交及确认。API和独立SQL均为pending/v3、实盘0.4000、提交审计恰1，无调整单。
5. 仓管点击退出链接到确认页，再点击“退出登录”；随后GET详情401。退出链接本身不是已退出，首次确认前GET200属正常流程，不能冒充退出验收。
6. 财务finance01登录，列表同一单号打开PD2；实际填写“合成验收：退回核对”并确认驳回。最终draft/v4，实盘0.4000；财务驳回审批1条，create/update_counts/submit/reject审计各1条，无调整单，库存仍0.1000。
7. 财务完成退出确认后GET详情401；本会话浏览器错误清单为空，随后关闭自建浏览器。

证据目录：`交付证据/2026-09-09-申报产能/`（项目根目录下、仓库外）。
已目视截图：`pd-submit-failure-e411.png`、`pd-submit-success-e411.png`、`pd-reject-success-e411.png`。
当前宽度截图保留表内横向滚动，不能据此宣称新一轮三宽度验收通过。

## 失败记录与下一项

- 财务点击驳回后，数次范围快照报“没有可访问节点”；随后DOM只读检查确认弹窗已存在并可见，再点底层按钮被弹窗遮挡而拒绝。之后按可见占位符输入和确认成功。保留这些失败，不判定为产品点击丢失或单纯工具故障，也不称为单脚本全绿。
- 初次只读API诊断错误地按 `data` 包装取值，接口实际直接返回对象；已按真实契约重读并以独立SQL校验，未作业务写入。
- **新确认UX缺陷**：仓管的待审批页也显示财务审批/驳回按钮；财务在驳回后的草稿页也显示录数/提交控件。服务器当前身份/审批域拒绝不等于良好体验。下一步应将建单、录数、提交、审批控件与当前单据/身份的可用操作对齐，保留服务器最终授权，不能凭前端隐藏代替后端守卫。
- 旧保存自动化超时、React185未定因记录继续保留；正式Safari HTTP登录修复、异地备份发布前置、101条业务UAT、三方连接和身份资格、D69–D71决策仍未完成。
