# 复制为 .env.backup；不要提交真实目标、用户名或凭据。
BACKUP_DIR=/var/backups/supply-chain
BACKUP_REMOTE=backup-user@backup-host:/srv/backups/supply-chain
REQUIRE_BACKUP_REMOTE=1
MAX_AGE_HOURS=25

# 与部署使用同一个 Compose 配置/生产环境文件
SCM_COMPOSE_FILE=docker-compose.prod.yml
SCM_ENV_FILE=.env.prod
