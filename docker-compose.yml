services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: scm
      POSTGRES_PASSWORD: scm_dev_password
      POSTGRES_DB: scm
    ports:
      - "127.0.0.1:5433:5432" # 仅本机；生产环境务必改密码并置于内网
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scm"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
